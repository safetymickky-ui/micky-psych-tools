---
description: "W0 smoke seed (S07-W0-1). Section 4.1's near-miss prompt verbatim; it joins the two requests of skills/empty-vault/evals/evals.json ids 1 and 3 ('Empty the vault.', '/empty-vault panic-disorder'). Near-miss negative: draining the vault into the Learn hub is the sibling empty-vault skill's job, so vault-keeper must not fire."
tags: [vault-keeper, negative, smoke]
allowed_tools: [Read, Glob, Grep, Skill]
max_turns: 8
---

Empty the vault — drain everything into the Learn hub, panic disorder only.
