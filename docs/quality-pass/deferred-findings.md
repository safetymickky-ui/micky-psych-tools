# Deferred findings — for a later pass

Findings from the 2026-09-26 diagnostic that this pass did NOT fix. The owner scoped this
pass to pubmed-research-note + clinical-infographic, high severity only (see
[plan.md](plan.md)). Rows below are the rest, grouped by plugin, highest severity first.

`Status` says how far each finding got: `confirmed` = survived an adversarial verify pass;
`owner_fork` = real, but the fix is a choice only the owner can make; `deduped, not verified`
= merged from raw probe output but never verified (pubmed-research-note medium/low, the
handoff rows, and the visuals plugins) — re-check before acting; `fixed` / `partly fixed`
= closed later, with the release named (see that plugin's CHANGELOG). Refuted findings (27) are
left out.
Full evidence lived in the session scratchpad; each row keeps the fix direction only.

## pubmed-research-note

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | PR-04 | deduped, not verified | No source-status check: retracted, corrected, expression-of-concern and superseded-preprint sources pass silently, although PubMed metadata already flags them | Add a mandatory integrity step to tool-catalog §1 for every study kept in Sources: read get_article_metadata.article_types; 'Retracted Publication' -> drop it and name the retraction in one line; 'Expression of Concern' / 'Published Erratum', or efetch Comm… |
| high | PR-06 | deduped, not verified | Open Library is assigned the textbook-content job it cannot do, so teaching reports either leave the textbook side [unverified] or launder memory under an edition citation | Limit Open Library's stated job to edition identity (search by ISBN, not title). The textbook position may come only from text actually read in this run (a user-supplied chapter or PDF, a vault note sourced from the book, a publisher/Google Books preview vi… |
| high | PR-11 | deduped, not verified | Eval baseline not yet frozen: pre-rewrite tag absent and W0 smoke baseline not taken | Have the owner tag an explicit SHA (`git tag pre-rewrite fd47fba`) before any quality commit reaches master. If S12-W0-8 runs later, run `bash scripts/eval.sh --smoke <p> --against pre-rewrite -- ...` for these two plugins and say so in '## W0 smoke'. Edit … |
| medium | PR-12 | deduped, not verified | No endpoint-level check: the skill never establishes which endpoint a claim rests on (primary / key secondary / post hoc) or whether the registered primary was met | For each load-bearing trial: find its registry ID (abstract, full text or CT.gov search); state the registered primary endpoint and whether it was met, flagging any mismatch with the published emphasis; state whether the claim's outcome was the primary, a k… |
| medium | PR-13 | deduped, not verified | No effect-size interpretation rules: NNT/NNH and absolute rates are demanded but never derived, SMDs are not back-translated, and magnitude words have no convention (SMD 0.88 called 'small') | Convert pooled relative effects to an absolute difference per 1,000 at a stated baseline (the trial control arm, plus a Thai/Asian or local baseline when available), with CI. Compute NNT/NNH = 1/ARD with CI and time horizon, labelled 'calculated' when deriv… |
| medium | PR-14 | deduped, not verified | ClinicalTrials.gov is confined to a forward-looking publication-bias role: posted results, adverse-event and participant-flow modules and safety/futility terminations never enter the current evidence, and get_trial_details exposes only a flag | Split the rule: ongoing trials go in 'what would change it'; completed-unpublished trials, registry-posted results and safety/futility terminations enter the argument now and adjust the verdict or its confidence explicitly. Add a 'numbers beyond the abstrac… |
| medium | PR-15 | deduped, not verified | Harm rates are taken from one paper's coding without reconciling to the label's adverse-reaction table; a suicidality-relevant adverse event was dropped | When a label is fetched, take harm rates from its adverse-reaction table (or show both codings and name which is which). For questions about a symptom (e.g. suicidality), explicitly report the related AEs from label and trials. |
| medium | PR-16 | deduped, not verified | The completed-unpublished check has no procedure or working query, although one CT.gov filter and one PubMed field tag do it | Replace the prose with a recipe: (1) search_trials(status=[COMPLETED], advanced_query="AREA[ResultsFirstPostDate]MISSING AND AREA[CompletionDate]RANGE[MIN,<today-2y>]", count_total=true), paging with next_page_token; (2) for each hit, PubMed `<NCT>[si]` (pl… |
| medium | PR-17 | deduped, not verified | No guideline-anchoring or evidence-hierarchy step: current guidelines (condition-level and adjacent-specialty) are never required, and SR-vs-RCT, NMA and observational-harms rules are absent | Make guideline anchoring step one for treatment and teaching decisions: search guidelines by condition/decision as well as by drug, for every specialty that owns part of the decision (WHO, NICE, APA, CANMAT, BAP/WFSBP, VA/DoD, Endocrine Society etc., and th… |
| medium | PR-18 | deduped, not verified | Backbone search is one publication-type-filtered query with no MeSH/text-word pairing; the filter silently drops recent RCTs (measured) and the query is never logged | Build each concept as MeSH OR text-word synonyms (including brand names); combine publication types OR text-word design terms (randomized[tiab], meta-analysis[tiab]); add a recent-records pass (edat, last 12-24 months, no publication-type filter); log date,… |
| medium | PR-19 | deduped, not verified | Appraisal guidance covers only treatment, service, contested-claim and teaching decisions; harm, diagnostic, prognosis and observational-only questions are unguided | Add harm, diagnostic and prognosis rows to decision-brief §3: the designs that settle each, the numbers to count (absolute risk and adjusted HR; sensitivity, specificity and LRs; incidence over time) and the key biases. Add a tool-catalog recipe for observa… |
| medium | PR-20 | deduped, not verified | No applicability check for Thai/Asian patients: trial-population origin, pharmacogenetics, local registration and monitoring feasibility are never assessed | Add an applicability slot to the brief and a short 'Does this apply to your patient?' check in the report: trial populations (country/ethnicity, age, comorbidity, setting); known pharmacogenetic or dose differences in Asian populations for the agent; Thai r… |
| medium | PR-21 | deduped, not verified | The dated count line never defines N or counts web documents, and the report carries no search or selection record | Define N as the number of ## Sources lines of each kind; add 'web N' (optionally 'labels N', 'screened N'); make the grader cross-check counts against Sources. Add a short Search footer after Sources (or one required methods line): engines and search date, … |
| medium | PR-22 | deduped, not verified | Study naming is unspecified and the topic->DOI Sources grammar cannot identify which study carries a number when several share a topic | Require each load-bearing study's first prose mention to carry a stable label (design + n; acronym in parentheses at most) and its Sources topic phrase to begin with the same label and carry the key number (e.g. 'MA of 39 studies, periodontitis -> dementia … |
| medium | PR-23 | deduped, not verified | Nothing checks that body and Sources match: studies used in the body go unlisted, listed contrary sources go unused, and header counts go unreconciled | Make it a two-way rule: every study or NCT used in the body has a Sources line, and every Sources line is used or adjudicated in the body. Derive the header counts from Sources. Script the NCT and count cross-checks. |
| medium | PR-24 | deduped, not verified | 'Adjudicate, don't list' forces a winner: the chosen explanation need not fit all cited data, there is no 'why good sources disagree' path or guideline-conflict rule, and the mandatory verdict overrides the owner's no-verdict rule for disputed reference writing | Owner fork on the verdict rule; a reconciling option: reword SKILL.md:246-248 and report-craft.md:49-53 to 'When good sources disagree, first say why (population, dose, endpoint, design, bias, funding); then say which applies to this reader's decision; decl… |
| medium | PR-25 | deduped, not verified | The verdict may come last, the Close does not restate it, and 2-3k-word reports are dumped inline with no length target | Keep shape freedom for the body, but require a 2-4 line marked bottom line directly under the preface in every report: verdict, decisive reason, main trade-off, what would change it. The Close restates that verdict sentence in chat. For time-pressured quest… |
| medium | PR-26 | deduped, not verified | No voice rule: output language, register and reader level are unspecified, and the skill's own idiomatic phrasing is copied into reports | Add a short 'Voice' rule to report-craft: report and chat in the language of the request (Thai, English or mixed), technical terms in English; state the language of any verbatim teaching sentence (or give both). Short active sentences (about 20-25 words or … |
| medium | PR-27 | deduped, not verified | Owner fork: the depth contract mandates a developed prose paragraph per load-bearing study, against the owner's bullets-first form | Owner decides. Option: keep every depth field but render each load-bearing study as a fixed-order labelled bullet block (design · population/setting · n · comparator · primary endpoint · effect with CI · harms/dropout · RoB · funding · why believe or doubt)… |
| medium | PR-28 | deduped, not verified | Opt-out path: conflicting one-line rules, a self-built brief (and its assumptions) that never reaches the reader, and a 'reading it beat' clause that invites invented rivals | Keep one rule for opt-out: an 'Assumed:' line listing the material defaults taken plus the question sentence; drop 'the reading it beat' when there is no genuine rival reading; optionally add a collapsed 'Brief' block (verdict shape, anti-goal, what must be… |
| medium | PR-29 | deduped, not verified | The 'intent-lock costs nothing on clear requests' premise is false against intent-lock 0.4.2, which excludes precise requests and never emits the GOAL UNIFIED lock pubmed expects | Replace the cost claim with the true contract: intent-lock runs silently, may ask one probe round, and hands over nothing but an optional 'Assumed:' line, so this skill builds the brief itself from the conversation after intent-lock returns. Align the trigg… |
| medium | PR-30 | deduped, not verified | The tool catalog hardcodes plugin-scoped MCP prefixes that do not exist in the session and forbids the ToolSearch step that actually loads the tools | Resolve tools at runtime: connector names mcp__PubMed__* / mcp__Clinical_Trials__* first, then plugin names, then ToolSearch by keyword, then the web fallback. List the full live tool set so a name missing from the catalog is not read as a missing capability. |
| medium | PR-31 | deduped, not verified | get_article_metadata silently truncates at 20 PMIDs and overflows the tool-result cap; the catalog gives no batch rule or count check | State the cap in the tool catalog: at most 20 PMIDs per call, practically about 5-10 to stay under the output limit (or titles first, abstracts only for the shortlist, or extract fields from the saved file). Require comparing the returned count with the req… |
| medium | PR-32 | deduped, not verified | Two ClinicalTrials.gov tools are given jobs they cannot do: search_by_eligibility for generalizability and analyze_endpoints for endpoint switching | For generalizability, read get_trial_details.eligibility_criteria of each load-bearing trial and name the exclusions that matter for the user's patient; keep search_by_eligibility only for 'is there a trial my patient could join', with status set explicitly… |
| medium | PR-33 | deduped, not verified | No forward-citation step although replication status is a required check; find_related_articles ranks by similarity, not citations | Add a snowball step for each load-bearing trial: forward citations via elink pubmed_pubmed_citedin or Europe PMC citations, filtered with search_articles to RCT and meta-analysis publication types, then get_article_metadata. Use find_related_articles(link_t… |
| medium | PR-34 | deduped, not verified | Fixed eval mocks hold only positive, published evidence, so adjudication and the publication-bias sweep cannot fail; the trigger case has no engines | Give each case its own mocks built from real records that plant a disagreement - for prazosin, the small positive crossover RCTs plus the large negative multisite RCT (n=304, CAPS-B2 non-significant) - and plant one completed NCT with has_results false and … |
| medium | PR-35 | deduped, not verified | The ClinicalTrials.gov eval mock is reshaped rather than recorded: it adds has_results and completion_date fields the live listing lacks | Regenerate mocks by recording live tool output and only removing fields, never renaming or adding them. Add a small mock-shape check comparing each mock's key set with a recorded live sample (a subset is allowed, extra keys fail), and apply it to the suite-… |
| medium | PR-36 | deduped, not verified | S03's planned graders and linter regress the seeds: a weaker citation regex, and an llm verdict grader that reads the two-line Close | Adopt the seed pattern at minimum, extended with [n], superscripts, narrative and multi-source citations, in both the §4.1 grader and sources_lint. Replace the llm verdict grader with a deterministic Verdict + Confidence regex on the report file at a litera… |
| medium | PR-37 | deduped, not verified | Nothing validates the eval suites offline, and no deterministic content checker gates real outputs | Put checkers in three places: (1) inside each skill as a required pre-delivery step (sources_lint plus number traceability for PRN; check-html plus traceability, font-size and contrast for CI) writing a JSON verdict at a literal path, graded by regex plus a… |
| medium | PR-38 | deduped, not verified | Trigger lock fails the gate on any removed or reworded description phrase; the plan's own lock table has two gaps | Do not remove locked phrases in this pass. If one must go, run `python3 scripts/rewrite_gate.py triggers remove --phrase "<p>" --skill <s> --reason "<r>" --write` in the same commit, then `triggers verify`. Add plan.md §8 items: a §5 row removing 'just sear… |
| medium | PR-39 | deduped, not verified | Description edits now hit the 1,024-char cap and would shift the W3 before-rewrite trigger measurement | Leave both descriptions untouched in this pass and put new routing nuance in the body or references. If the owner wants a description change now, record it in baseline.md Owner records so S12-W3-2 treats it as the baseline. |
| medium | PR-40 | deduped, not verified | Exact-count and exact-text done-when checks in the plan can be broken, or turned into no-ops, by quality edits | Name any new eval case dirs exactly as S03 §4.1 and S06 §4.1 do (negative-whole-disorder, sink-fallback-no-vault-keeper, output-contract-light-lock). Keep new clinical rules in references/, not the SKILL.md body. Keep the NCT form on one line. Whenever an e… |
| medium | PR-41 | deduped, not verified | W0 smoke seed graders pin today's report preface and banner title; S03-W2-1 itself breaks one | Do not change the dated count line or the banner title text in this pass. Add a plan.md §8 item: S03's W2 block updates `dated-count-line` to the report/1 frontmatter and re-baselines that case (OQ8-a). |
| medium | PR-42 | deduped, not verified | The CLAUDE.md bump rule and the plan's `## Unreleased` rule conflict, and bump.py would mis-file entries above Unreleased | The owner chooses one convention for the whole pass: either (a) bump.py once at the end (pubmed minor, CI minor) plus MEMORY.md, with no `## Unreleased`, or (b) `## Unreleased` per plan rule 8 with no bump, accepting no Windows delivery until W1. Never mix … |
| medium | PR-43 | deduped, not verified | Pre-commit gate not enabled in this clone | Set core.hooksPath (local git config, not a repo edit) before the first commit, and run the full `bash scripts/health.sh` before pushing. |
| medium | PR-44 | deduped, not verified | Report-format and filing changes must wait for the learn-hub readers and sink.py | In this pass, keep the report's machine-read shape (dated line, preface block, one-line Sources grammar) and both filing destinations unchanged; put clinical-quality rules in content guidance (references, depth and verdict rules) instead. |
| medium | PR-45 | deduped, not verified | No recording rule for steps that run early or partly, so W1-W3 executors will re-apply or collide with finished work | Record each item: an Owner-records bullet in baseline.md; per-step commits with step ids; plan.md 'Done ahead of wave' notes under W1/W2/W3 (no new §3 rows), §8 Q38+ for each gap found here, §5 lock rows; spec steps 'Partly done <sha>; remaining ...'; S06 §… |
| low | PR-46 | deduped, not verified | Three conflicting rules for resolving an ambiguous request, one of which overrides an explicit opt-out | State one rule, scoped to when intent-lock did not run: pick the reading whose wrong answer costs more (or the broadest reading - choose one wording for both skill and plan), declare it in the 'Assumed:' line, and never re-invoke intent-lock after an explic… |
| low | PR-47 | deduped, not verified | No check for a prior report on the same question, and vault-keeper's 'extend over duplicate' rule can merge a new dated verdict into an old one | Before searching, query the vault (vault-keeper's query job) for a report on the same question; if one exists, reuse its sources as seed and state what changed since its date. Pass a 'supersedes: <title>' field and tell vault-keeper to file a new dated arti… |
| low | PR-48 | deduped, not verified | Registry facts in the NCT lines carry no as-of date and drift after filing | Add the registry's last-update-posted date to each NCT line (or 'as of <date>'). |
| low | PR-49 | deduped, not verified | The firecrawl engine prescribes a bare `firecrawl` command that is not installed; the no-install npx form, a timeout, and the JS-page limits of the WebFetch fallback are not stated | State the working invocation directly in tool-catalog §5: `npx -y firecrawl-cli@latest search/scrape ...` (keyless tier when no key is set), with a timeout and a per-URL WebFetch fallback as last resort. Add one line naming the Thai FDA search pattern (see … |
| low | PR-50 | deduped, not verified | lookup_article_by_citation returns the PMID in `key` and leaves `pmid` empty or swapped; the catalog gives no argument shape | Document the argument shape and the quirk: read the PMID from `key` when `pmid` is empty or non-numeric, then confirm with get_article_metadata. Make it the required check for any study cited from memory before it enters Sources. Note `id_type: "doi"` for D… |
| low | PR-51 | deduped, not verified | The PubMed tool's injected attribution notice pushes inline 'According to PubMed' narration that the skill's clean-prose rule forbids; the catalog does not reconcile them | Add one line to tool-catalog §1: the DOI link per source in ## Sources plus the engine names in the dated count line meet the tool's attribution requirement, so no inline 'According to PubMed' narration is needed. Keep the planned voice rule and cite this n… |

## clinical-infographic

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| medium | CI-03 | confirmed | 'Condense to a directive' turns descriptive evidence into recommendations; evidence strength and dispute status are dropped | Allowed transforms must keep the source's modality (recommendation vs observation) and its strength. Cards carry an evidence tag (design, n, and the source's confidence or grade). Comprehensive-review content renders as 'evidence shows…' unless the source i… |
| medium | CI-06 | owner_fork | Sources sit in a collapsed <details> that never prints or shows in the PNG: no DOI or NCT reaches the printed, laminated or shared sheet | Render Sources as an always-visible compact numbered list in print and in the PNG (small, 2-column, ≥12px printed; short citations with design + n + DOI/NCT), keyed to tiles if a trace key is adopted (CI-01). If a collapsible version is kept for screen, add… |
| medium | CI-07 | confirmed | Output-eval graders are presence regexes on an idealised fixture: invented numbers, a laundered [unverified] figure, trimmed qualifiers, a banner item moved out, and even the unfilled template pass | Add a fixture shaped like a real pubmed-research-note decision report: no avoid vocabulary, a verdict with confidence, inversions, a non-significant outcome, 'my inference', [unverified] items, a kill list, one contraindication, and an item explicitly "not … |
| medium | CI-09 | confirmed | check-html.mjs `external-reference` rule would fail every compliant infographic's DOI footer | Before S05-W2-1, scope `external-reference` to resource-loading references (`src=`, `<link href>`, `@import`, CSS `url()`), matching the CI seed grader. Log it as a plan.md §8 item. The quality pass should keep DOI anchors as-is. |
| medium | CI-10 | confirmed | No verdict / confidence / 'what would change it' slot: the Step 1 skeleton, anatomy and template omit the decision report's core output | Add a mandatory bottom-line / Verdict band at the top of the anatomy, the template and the Step 1 skeleton: the verdict verbatim-condensed; the confidence on pubmed's scale with its one 'not higher because' clause, at a salient size; the main trade-off; and… |
| medium | CI-11 | confirmed | No selection or omission rule: the contract allows only condense/promote/regroup, so one-page renders drop counter-evidence, named harms, the trade-off and 'my inference' markers at will, and never disclose it | Add 'select' as an allowed transform with a keep-priority — decision reports: verdict + confidence > safety > dose/steps with qualifiers > effect sizes > inversions > mechanism > supporting detail; topic reports: safety → key recommendations → numbers that … |
| medium | CI-12 | confirmed | No form for comparative or measured data: tiles cannot carry arm, n, CI or p, the illustrative/measured binary has no third class, and real-data plots have no rules | Add a 'comparative outcome' form to design-system.md and a template scaffold: one row per outcome, both arms with n/N, the effect with CI or p, a text NS tag (neutral token, not crimson), trial design and size in the header. Give tiles a sub-line for compar… |
| medium | CI-13 | confirmed | Fidelity contract covers sentences, not structure: headers, badges, flow order and colour codes make untraced claims | - Add to source-contract.md: - every container header, badge, legend colour, arrow and ordering is a claim that must hold for every member and trace to a source line; - items the source explicitly excludes from a group ("not on this list", "exception") get … |
| medium | CI-14 | confirmed | Banner scope, cell cap and 'cut' rules contradict across files; template prompts for an unsourced 'safe alternative' | Define the scope once in source-contract.md and reference it everywhere else. State that banner content is never cut. For overflow: group cells by drug class with a continuation list, in a stated priority order (absolute > boxed > interaction > relative). F… |
| medium | CI-15 | owner_fork | The anatomy pins the do-not banner last: it sits 2–4 screens down and off printed page 1, and doses are not the emphasised element | Add fixed top-of-page elements to design-system.md and the template: under the bottom-line/verdict strip (CI-10), a compact do-not strip or the full safety band above the columns (a short top banner with details below is acceptable). When more than one page… |
| medium | CI-16 | confirmed | No workable page budget: the single-page promise, the 12px floor and 'content does not bend' conflict; formats have no size or CSS; there is no cut order; the planned 12px fix pushes the example to 4 pages | Specify each format concretely: page size and @page rule, allowed page count (e.g. A4 = 1 sheet, or 1–2 sides with front = verdict, signature visual, actions, banner and back = evidence and sources), minimum type, content budget (max cards/words/diagrams), … |
| medium | CI-17 | confirmed | The 12px floor ignores viewBox-scaled SVG text: the signature curve's sourced thresholds render at ~6.8px in print and ~3px on phones, and every current and planned check is blind to it | State the floor in rendered pixels, SVG included: user-unit font-size × (rendered SVG width / viewBox width) ≥ 12px at phone, desktop and A4 content width (~670–700px). Ways to meet it: ship scaffolds with a viewBox near the print content width (~680–700 un… |
| medium | CI-18 | confirmed | Column badges fail AA contrast on every accent even after S06's palette change, and design-system.md still lists the failing colours as 'AA-contrast' | Make the badge solid (white chip with accent-coloured text, a darker accent shade, or a dark overlay such as `rgba(0,0,0,.24)`), ≥4.5:1. Extend S06-W1-1 to the badge, `.seq`, number chips and SVG text pairs, and add design-system.md:39-40 to its Files (reco… |
| medium | CI-19 | confirmed | Nested comment in the journey-curve scaffold leaks an orphan 'Illustrative — not measured data' caption and a literal '-->' onto the page | Remove the inner comment: use a `{{…}}` token or an SVG `<desc>`/`<text>` placeholder so the scaffold is one well-formed comment, or keep unused scaffolds in `<template>` elements. Add a template self-test / post-render check: the unfilled template renders … |
| medium | CI-20 | confirmed | The schematic-honesty rules miss two cases: one curve plotted against two different quantities, and a phase-specific target drawn across all phases. The disclaimer is also the least visible text on the page | Extend the schematic rules: one plotted quantity per axis (or label the curve 'haemodynamic trend, schematic' with no threshold lines from different measures); draw each threshold or band only over the phase it applies to; put the 'Illustrative — not measur… |
| medium | CI-21 | confirmed | The evidence date is not shown: the footer carries the render date only, and Step 0.2 reuses any matching vault report with no age check | Footer carries 'evidence searched YYYY-MM-DD' (from the report's date line or `created`) next to the render date, the source filename or vault title, and counts in the upstream's own format (books, sections, web documents); reconcile SKILL.md:95/118 with so… |
| medium | CI-22 | confirmed | Step 0 accepts any pasted or vault document as 'sourced' — no admission check | Add an admission gate. The source must carry a Sources/References block with resolvable DOI, NCT or URL entries and a date. Otherwise route to pubmed-research-note or comprehensive-review. Render unsourced input only on an explicit user override, and then w… |
| medium | CI-23 | confirmed | Upstream Assumed: line has no surface in the rendered infographic | When the source report opens with an `Assumed:` line, carry it into the infographic as a visible one-line basis or assumption strip under the subtitle, in the source's wording, and name it in the Close. Add a fixture eval where the source carries an `Assume… |
| medium | CI-24 | confirmed | Thai language unaddressed: no output-language rule, translation is not an allowed transform, lang hard-coded 'en', no Thai face in the font stack | Add a language rule. Default to the source language; use Thai on request, keeping drug names, units and technical terms in English verbatim, matching the owner's Thai-English style. Add translation as an allowed transform with explicit constraints. Set lang… |
| medium | CI-25 | confirmed | Release threshold 0.8 on a weighted average lets a dropped contraindication, a dropped unit or an external resource pass | Put the safety-critical graders (banner items, units and qualifiers, self-contained, [unverified]) in their own case with only those graders, so that any single failure falls below 0.8. Or weight each one at 25% or more of its case. Or run output cases with… |
| medium | CI-26 | confirmed | The documented CI smoke run cannot run here, and refused runs still score absence graders as passes | Drop Bash from the CI smoke cases until a render grader exists, or move Bash-needing cases into a release-only tag. Make eval.sh parse the result JSON and exit non-zero when any run has a non-null `error`. Check `command -v bwrap socat` before any Bash gran… |
| medium | CI-27 | confirmed | Text under 12px and sub-AA contrast are ungraded, and the planned deterministic gate has no rule for either | Add `min-font-size` (computed, and at A4 print scale) and `contrast-aa` (computed foreground against blended background; 4.5:1, or 3:1 for large text) as render codes in check-html.mjs / audit:visual. The prototype is about 40 lines (qp/evals-audit/checkci.… |
| medium | CI-28 | confirmed | S06's planned eval cases regress the W0 seeds: a looser banner regex, cases 3–4 with no source fixture (rewarding render without a source), and the legacy evals.json never deleted | Amend S06 §4.1 and S06-W1-3 to 'extend the W0 seed directories; keep their scaffold.sh and graders'. Replace the 'CRITICAL SAFETY' regex with the seed's heading pattern plus the banner-scoped item check (CI-07). Require every output case to have a source fi… |
| medium | CI-29 | owner_fork | W1+ plan steps are gated by the wave-entry rule: running any now needs owner approval and a record, and only a named subset can run without learn-hub | Get explicit owner approval to run named steps ahead of their wave and record it as a bullet in docs/rewrite/baseline.md `## Owner records` (step ids, date, reason, deferred checks). Commit each step as `<type>(S0x): <step id> ...` (plan.md:21) and mark par… |
| low | CI-30 | confirmed | Template icons do not show what they stand for: the clock renders as a solid disc, the ban as a plain ring, and ⚠ marks both directives and hazards | Redraw both symbols as stroked or even-odd paths (a ring plus a thick diagonal with `fill:none; stroke:currentColor`; a clock with separate stroked hands), give each column an icon for its role (pill, pulse, bed, …), then re-render the template and the exam… |
| low | CI-31 | confirmed | Heading and relation semantics: the rules skip h2, column and safety headers are not headings, and mechanism relation words are hidden from assistive tech | Make column headers and the safety header `h2` and cards `h3`; change design-system.md:129 to 'h1 → h2 (column/section) → h3 (card)'. Put the relation word in `.sep` as visible text that is not aria-hidden; hide only the glyph. |
| low | CI-35 | confirmed | S06 §2.3's CI SKILL.md W1 edits have no step id | If the quality pass adds these Step 2 and Failure-condition lines, attach them to S06-W1-1 by amending the spec's Files, or commit them as a non-plan fix and add a note to S06 §2.3. Either way, record it so W3's S06-W3-2 does not duplicate it. |

## clinical-minimal

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | CP-01 | confirmed | forest_plot is ratio-only with "Favours lithium" defaults and a fixed 0.02-2 axis, yet the SKILL routes every effect size to it: SMD plots invert significance, ORs above 2 land off the axis, and negative values crash | - Make the measure a required argument: `measure="ratio"/"difference"`. Put the null at 1 on a log axis or at 0 on a linear axis, and base significance on that null. - Derive the axis range and ticks from the data, with padding. Clamp the marker as well as … |
| high | CP-02 | confirmed | Clinical evidence documents rest on a one-line abstract rule and bypass the evidence plugins and the owner's verification rules | - State the split explicitly: clinical-minimal owns the look, and clinical content comes from a sourced report (from the session, from the vault, or generated first by pubmed-research-note or comprehensive-review). This is the same contract clinical-infogra… |
| medium | CP-03 | owner_fork | Direction colours hard-wire "up = green", so clinical increases in harm are shown as improvements | - Separate direction from valence. Give the table helpers a per-column `higher_is_better` flag, or accept a `(text, "good"/"bad"/None)` cell form. - Colour a cell only when its valence is known; otherwise use ink. - Make delta_text(0) return a neutral "0.0%… |
| medium | CP-04 | confirmed | kpi_slide silently drops every KPI after the third, although SKILL maps 3-4 headline numbers to it | - Compute the tile width from len(kpis) and support 1-4 tiles, or raise ValueError above the supported count and never truncate silently. - Make SKILL.md:73, the docstring and the brand book's "max three" agree. |
| medium | CP-05 | confirmed | Template skeletons are quotas, not menus: they push the writer to fill PICO, search and history sections the source lacks, and the fidelity rule covers numbers only | - Add to Workflow step 2: "Skeletons are menus, not quotas. A section the source cannot fill is omitted or reads 'not reported in the source'; never infer PICO elements, search strings or history." - Extend the source-only rule to non-numeric clinical claim… |
| medium | CP-07 | owner_fork | The mandatory render and picture gates need Windows, PowerPoint and the desktop browser, and nothing says what to do when they cannot run: raw tracebacks, a misleading "search again", no disclosure to the user | - Add an environment check at step 0 (a Platform section). On Windows with Office, run the full workflow. Otherwise: - build with cm.py; - render with `soffice --convert-to pdf` and Read the PNGs, noting that font substitution changes line wraps; - use user… |
| medium | CP-08 | confirmed | slidecheck FAILs every wrapped Thai line as a split word, and a FAIL cannot be waived, so a Thai deck can never reach exit 0 | - Apply the word-split rule only when both sides are Latin letters or digits (e.g. both match [A-Za-z0-9]), or skip it for no-space scripts (Thai U+0E00-U+0E7F, CJK). Better, detect a real mid-word break by comparing against the paragraph's text or word bou… |
| medium | CP-09 | confirmed | The review gate keeps a "pass" after a picture swap, a chart-data change or a colour change, and it accepts the agent's own unproven pass claim | - Add to the hash the SHA1 of each picture's image.blob (via picture_info), each chart part's XML (series values, point colours), and shape fill and line colours. - Require a per-slide note on pass (what the visual proves), or proof that slide-NN.png was op… |
| medium | CP-11 | confirmed | SKILL.md's Helper API, cm.py and the references disagree: every documented helper call raises TypeError, and the ppi and title-size thresholds conflict | - Rename the parameters to the documented names and keep the old names as aliases, or generate the Helper API section from the cm.py signatures. - Fix the arrow line in SKILL.md and the animate docstring (use bullets_steps). - Add a doc-drift unit test that… |
| medium | CP-12 | confirmed | chart_slide/style_chart miss the SKILL's own chart rules: no data labels, hbar order reversed, auto-title on single-series charts | - In style_chart, turn on data labels with number_format for bar and hbar. - For hbar, set category_axis.reverse_order=True. - Set chart.has_title=False unless a title is passed. |
| medium | CP-15 | confirmed | No guidance for common hard cases: restyling an existing file, the paper's own figures, case-file de-identification, and the letter sign-off | Add short sections for: - restyling an existing file: open it with python-pptx or python-docx, apply styles and tokens, keep the content; - user-supplied or source figures: skip imgpick, credit the source, still check ppi and fit; - case files: "de-identify… |
| medium | CP-16 | confirmed | No evals and no script tests, though the check logic is pure Python and testable on Linux | Minimal eval suite, each case with a result grader plus a tool_used Skill grader: - trigger positives: a pptx request, "ทำสไลด์" (regex over the deck script for new_deck, finish_deck, animate); - negatives: an evidence question -> pubmed-research-note; a on… |
| low | CP-10 | confirmed | slidecheck never checks tables, though the SKILL says it measures real table heights | - Add rules: a table frame whose bottom is below the content area (y > 6.6 in) or off the slide is a FAIL, and a table that overlaps the source-line text box is a FAIL. - Optionally probe cell text bounds in slideprobe.ps1. - Correct the SKILL claim until t… |
| low | CP-13 | confirmed | Picture sourcing fails or blocks the brand book's preferred sources: a spoofed User-Agent is refused by Wikimedia, the BLOCK regex is too broad, and HTTP causes are thrown away | - Send a descriptive User-Agent with contact details, as Wikimedia's policy requires, or use the Wikimedia API thumbnails. - Record the HTTP status and reason. - Anchor BLOCK to domain labels and path segments, e.g. `(^/\.)x\.com$`. - Make choose() distingu… |
| low | CP-17 | owner_fork | The source-line rule "short author–year" conflicts with the owner's citation preference and with the pipeline's DOI-only reports | Change the rule to "design, n · short DOI/PMID (or the source's own label)". Keep author–year only when the source provides it. |
| low | CP-18 | confirmed | Helper default geometry collides or leaves the content area sparse, so default use fails the skill's own checks | - Place the kicker from the title's measured line count, or give the title a bottom-anchored box above a fixed kicker. - Let kpi_slide and forest_plot size their rows and tiles to fill the content area. |
| low | CP-19 | confirmed | docx table(widths_cm=...) writes only the cell tcW; gridCol stays equal, so gridCol-first renderers ignore the widths | When widths_cm is given, also set table.columns[i].width (gridCol) and a fixed table layout. |
| low | CP-20 | confirmed | Helper defaults put the python-pptx author's name in file metadata, write unsigned deltas and use a data colour below 3:1 contrast | - Set core_properties (title; blank author and comments) in new_deck, new_document and new_workbook. - Add signs to delta_text and to the FMT delta formats. - Darken data-3 (e.g. #B5781F, 3.70:1 computed). |
| low | CP-21 | confirmed | Packaging and portability gaps: the only worked example and DESIGN.md are on the author's PC, numpy is missing from the install line, the scripts sit at the skill root, 6 trigger phrases are unlocked, and PowerPoint Quit() may close the user's decks | - Ship a trimmed, drug-neutral example deck script under references/. - Add numpy to the SKILL.md install line. - Move the scripts to plugins/clinical-minimal/scripts/, reference them via ${CLAUDE_PLUGIN_ROOT}, and add tests. - Run `rewrite_gate.py triggers… |

## code-explainer

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | VI-34 | deduped, not verified | 3 of 6 evals give no code but expect a finished byte-for-byte explainer, so they reward inventing the source; the rewrite carries two of them forward | Ship real fixture files (e.g. files/ParseScores.cs with List<int>, an empty catch and mixed indentation; retry.js with Math.random()*ceiling and a fetch; a small parser) and reference them in the prompts. Add one negative case — 'explain this function' with… |
| medium | VI-35 | deduped, not verified | Byte-for-byte fidelity rests on the model retyping the whole source through escaping and hand-tokenising (~1k spans per 135 lines); no script loads the file, and the only guard is a post-hoc re-read | Add to SKILL.md Step 3: 'Generate the code pane from the source file with a script — never retype the code.' Ship a small build script (e.g. scripts/code-pane.py <source> <ranges.json>) that reads the source bytes (pasted code is first saved to a temp file)… |
| medium | VI-36 | deduped, not verified | 'Byte for byte' covers rendered text only: copying code out of the page drops every blank line | Contract rule: each code line element ends in a real newline text node (or blank lines contain a newline or zero-width placeholder), and gutter content stays user-select:none. Add a verify item: select the whole code pane, copy, and diff the clipboard (or S… |
| medium | VI-37 | deduped, not verified | Steps are locked 1:1 to ranges and no run scenario is declared, so the 'execution order' stepper cannot show call/return, loops or repeated calls and misorders real runs | Separate the step list from the range list: steps are an ordered list of {range, optional sub-line span / iteration, trace row} records that may revisit a range; card click jumps to the first visit; raise or drop the 12-step cap for revisits. Pick one nesti… |
| medium | VI-38 | deduped, not verified | Defects must be named but there is no method for finding them; the probe explainer missed bump.py's half-bump failure and presented the failure story as complete | Add a bounded defect sweep to Step 1, framed as part of reading, not reviewing: for each side effect (write, network, mutation) ask what state is left if the next statement throws; for each except/catch ask what it does not catch; check docstrings and comme… |
| medium | VI-39 | deduped, not verified | Defect naming has no evidence bar and is tested in one direction only, so false defect claims are unpunished | Require every defect card to state the concrete trigger (input or condition) and the consequence. Where intent is plausible or depends on unseen code, label it 'possible issue — depends on <X>' without .t-inv. Add a failure condition 'a defect was claimed t… |
| medium | VI-40 | deduped, not verified | 'Run it' gives no safety limit for executing the user's code | Run only self-contained, side-effect-free code (pure functions, local computation) in a temp directory with example inputs. Never run code that touches files outside the temp dir, the network, subprocesses or databases without the user's explicit OK; otherw… |
| medium | VI-41 | deduped, not verified | The mandated current-step yellow band over the whole range drops 6 of 13 syntax token classes (incl. the defect token .t-inv) below WCAG AA, contradicting the shell's contrast claim | Copy VS Code: put the yellow band on the current step's first line only (with the ▶ arrow) and mark the rest of the range with a non-fill cue (2–3 px gutter bar / left border / outline, at most a ≤0.02-alpha tint), or lower the alpha and measure. Rewrite th… |
| medium | VI-42 | deduped, not verified | Token-class guidance covers only JS/Python, has overlapping classes, never splits multi-line tokens per line, and sets no tab-size, so highlighting varies by run and silently breaks | Add a per-language mapping table (JS/TS, Python, R, SQL, shell, C#/Java, HTML/CSS, JSON/YAML) onto the existing 13 classes (e.g. tag → .t-dec, attribute/key → .t-var, logical keywords → .t-dec, interpolation braces → .t-dec with the inner expression tokenis… |
| medium | VI-43 | deduped, not verified | No interaction model for the stacked (<900 px) layout: on a phone one Next scrolls the stepper and the current range out of reach | Specify a narrow-screen model: keep the code pane and toolbar sticky (or show the current card inline under its range) so a step never scrolls the controls away; put the flow panel before or beside the rail, or behind a tab. Verify at 390×844: step 3 times … |
| medium | VI-44 | deduped, not verified | Step 3.5 verification is an eyes-only checklist that names no harness, skips rules the contract sets, has no no-browser fallback, and the plan keeps code-explainer's check static-only | Ship a drive script with the plugin (e.g. scripts/verify-explainer.mjs, or a browser mode of the family check-html.mjs) and make exit 0 a hand-off condition in Step 3.5. It loads the page with Playwright at 1366×768 and 390×844, JS on and off (javaScriptEna… |
| medium | VI-45 | deduped, not verified | Planned escaping grader fails correctly tokenised output, the planned byte check needs a source file no case has, and core failure conditions have no case | Grade escaping on the code pane's rendered text (textContent of the code lines contains `List<int>`, or strip tags before the regex). For pasted code, have the skill write the snippet to a temp source file so --source has something to diff. Add cases for as… |
| medium | VI-46 | deduped, not verified | A tiny snippet or a quick 'what does this code do?' has no valid output: the skill forces a ≥4-step, three-mode artifact | Add a floor to Step 0: below ≈8 lines or fewer than 4 real ranges, answer in chat and offer the explainer, or define an explicitly allowed reduced mode (linked cards only, no diagram or step-through). Add to the description's NOT-for list 'a quick question … |
| medium | VI-47 | deduped, not verified | Step 0 contradicts intent-lock's silence rule, sends missing code to an interview, and settles a 'depth' that nothing uses; the rewrite leaves it unchanged and drops its eval | Replace Step 0 with the I01 handoff sentence (gate OPTIONAL plus the `Assumed: … — say if wrong.` fallback) and drop the 'say why' line. Handle missing code with one direct question, not intent-lock. Make depth concrete: 'what it does' = cards state behavio… |
| medium | VI-48 | deduped, not verified | Explanation-card content is unspecified: no anatomy or length, no rule for card language despite a Thai trigger, and an uncertainty marker that does not name its basis | Add a card anatomy to explanation-contract.md: h3 'Step n · lines a–b · name', one claim sentence, at most 3–4 short bullets in active voice, and a labelled '⚠ Defect' block (text label, not colour alone). Add to SKILL.md Step 3: cards follow the language o… |
| low | VI-49 | deduped, not verified | Reference files contradict each other (warn treatment vs fixed palette, dead Play under reduced motion, close/dirty dot, hard-coded branch, clickable nodes inside role=img), forcing per-run improvisation; diagram nodes end up mouse-only | State the resolutions in the references: add `--vsc-warn: #cca700` (VS Code editorWarning) to the shell palette table; under reduced motion keep Play visible (user-initiated) stepping with cuts not eases, or disabled with a text reason; the tab shows file i… |
| low | VI-50 | deduped, not verified | Global Space/arrow key map clashes with focused buttons, ARIA tabs and scrollable panes, and the Play pace's 'reading-time' is undefined | Ignore global Space/arrow keys when focus is on a button, tab or scrollable pane, or scope them to the editor region. Define reading-time (e.g. 200 wpm of the card text). Add a keyboard drive check. |
| low | VI-51 | deduped, not verified | Hover mode (the 'core interaction') usually lights an off-screen partner, with a 1.12:1 code-side signal | On hover, show an off-screen indicator when the partner is out of view (edge chip 'card below ↓' / 'lines 44-55 ↑', or a scrollbar/minimap mark), and use a measured, visible code-side hover treatment (e.g. a gutter bar). |
| low | VI-52 | deduped, not verified | Document-level rules (doctype, lang, charset, color-scheme, dark scrollbars) are missing, and the planned family contract adds a 1152 px width cap that contradicts the full-window shell | Add doctype/lang/charset, `:root{color-scheme:dark}` and `::-webkit-scrollbar` / `scrollbar-color` rules using `--vsc-scrollbar` to vscode-shell.md now. Ask S05 to exempt code-explainer from the I07-B `.wrap` max-width rule. |
| low | VI-53 | deduped, not verified | 'Keep it in the vault' files an asset that empty-vault later deletes and that no receiver takes | When filing, tell the user that code explainers are not carried into the Learn hub and are removed on the next empty-vault, and offer to keep the file in the working directory or repo instead; or have empty-vault skip `producer: code-explainer` assets. Pass… |

## comprehensive-review

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | EA-01 | confirmed | Absence of evidence is licensed without a modality-specific search, producing a false 'no drug evidence' claim | Before any 'no controlled evidence' sentence, require a logged modality-specific search: PubMed '<topic> AND <modality> AND randomized' plus CT.gov with AREA[InterventionType] for DRUG/DEVICE/BEHAVIORAL, all statuses. The sentence must name the searched sco… |
| high | EA-03 | confirmed | No settled-vs-disputed verdict rule, no confidence labels and no duty to explain source disagreement, so verdicts land on disputed questions and a source's conclusion is inverted | Write the owner's rule into SKILL.md, replacing 'house style'. Label each conclusion Settled (state it, with pubmed-research-note's confidence scale) or Disputed (both sides with design, n and effect, plus the stated reason for the disagreement — population… |
| high | EA-04 | confirmed | Treatment coverage has no safety contract (harms, contraindications, monitoring, label warnings) and no dose/label verification rule | Add a harms-and-safety requirement to domain 7: adverse effects with absolute rates, discontinuation, contraindications and boxed warnings, key interactions, and monitoring; route pregnancy and lactation safety to domain 8. Add 'key harms and dropout' to th… |
| medium | EA-02 | confirmed | Registry recipe equates 'no results posted' with publication bias, with no literature cross-check, staleness rule, or consistency pass | Registry pass 2: before flagging, search PubMed by NCT ID and by acronym/title, and check the chapter's own Sources. Flag only if nothing is found, and state the check. Pass 3: a primary completion date before the review date moves the trial to the 'overdue… |
| medium | EA-05 | owner_fork | No claim-to-source traceability: anchors are banned, Sources are topic-only lines, and nothing reconciles numbers with sources (also breaks clinical-infographic's one-hop trace) | Owner fork: keep clean prose but allow a minimal anchor (footnote marker or per-section Sources block ordered by first use), or keep no anchors and add a mandatory reconciliation pass. Either way, before filing: every number maps to one listed source, every… |
| medium | EA-06 | confirmed | Depth contract omits risk of bias, certainty, replication, absolute effect and population match, so weak or mismatched evidence is presented as strong | Extend the depth contract: every load-bearing study also states risk of bias (or the review's certainty grade where one exists), replication status, the population it actually studied (flag mixed or different populations and constructs explicitly), and an a… |
| medium | EA-07 | confirmed | No retrieval procedure or sub-item checklist: recipes return 20 recent hits, landmark and contrary syntheses and required sub-items go silently missing, and per-study numbers need not be read from the record | Add a per-domain retrieval routine: [Publication Type] tags (guideline, meta-analysis, RCT); a relevance pass plus a pub_date pass for the last 2–3 years; retstart paging to a stated depth; seed-then-find_related_articles for landmark trials; the highest-ci… |
| medium | EA-08 | owner_fork | Output length is unbounded (66–77k-word chapter) with no summary or findability layer, and the planned defect-7 fix removes the only length lever | Keep length as a locked field with a mechanism: Step 0 sets a target band (bounded default for a whole-topic chapter) that decides how much supporting literature is grouped versus expanded, while load-bearing studies stay in full. Mandate a key-findings blo… |
| medium | EA-09 | owner_fork | Prose-only house style conflicts with the owner's second-language readability and form preferences (no tables for matrices, no language or study-naming rules), and skill jargon leaks into deliverables | Owner fork. Either keep prose but add limits (sentences ≤25 words on average, active voice, common words, no idioms, a bullet summary per section) and document why prose is kept; or switch to the owner's default of bullets (two levels) with prose only for c… |
| medium | EA-10 | confirmed | Evals grade format only and are leaky: coverage is ungraded, citation graders miss banned forms, the header grader matches the chat Close, and fabricated bibliographies or fidelity defects pass | Add file-target graders: an llm coverage grader on the written md; Sources DOIs ⊆ mock DOIs; regex bans for `\[\d+(?:[,–-]\d+)*\]`, `et al\.? \(\d{4}\)` and author/journal/year patterns inside ## Sources; a header regex anchored to the file's first lines; t… |
| low | EA-12 | confirmed | Preface and header conventions diverge from pubmed-research-note and intent-lock; the planned I04 single-line preface leaves CR no place to name exclusions | During the report/1 alignment, give CR (kind: topic) an explicit `Coverage:`/`Excluded:` line or frontmatter field for locked exclusions and merged or renamed domains. Put opt-out defaults in the Assumed line. Count web documents in the header, or drop the … |
| low | EA-14 | confirmed | The 'publication-grade' register cannot be delivered under the fixed citation contract | Remove the option, or define publication-grade as an explicit override that switches to a standard reference style (e.g. Vancouver) and says so in the preface. |

## concept-animation

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | VI-01 | deduped, not verified | Clinical lane is chosen by on-screen numbers, not by clinical subject, so a clinical causal mechanism (even one the source refutes) animates unsourced from 'established knowledge' | Define the clinical lane by the kind of claim, not by numbers: any disease mechanism, causal pathway between conditions, drug or treatment/device action on the brain, or outcome claim is clinical whether or not numbers appear, and needs a sourced report. Ev… |
| high | VI-02 | deduped, not verified | Prev/Next/dot stepping lands each scene at t=0 and freezes it, so stepped scenes show captions for content that is not drawn; the grammar never defines stepping, Verify captures by Play, and the rewrite starts from this example | State it in the grammar: Prev/Next/dot/arrow navigation lands on the scene's HOLD (the action's end state, key elements visible) — either run the action to the hold or jump to the end state (finish the animations, or a static end-state class like the reduce… |
| high | VI-03 | deduped, not verified | No floor or check for rendered SVG label size: the wide 'meet' viewBox gives 3.6–5.5 px labels on phones and 7.4–9 px on the laptop check viewport, and the S05 layout port does not change it | Add a rendered-label floor to the grammar (e.g. ≥ 11–12 px at every verify / I07-G frame), measured as computed font-size × getScreenCTM().a. Prescribe a phone layout: a portrait viewBox or SVG groups that reflow under a max-width query, or fewer, larger la… |
| medium | VI-04 | deduped, not verified | Grammar has no encoding for evidence status (association, proposed, disputed, tested-null, reverse causation) and no rule that the title and final frame carry the source's verdict, so contested mechanisms are drawn as settled causal chains | Add evidence-status rows to the grammar, each carrying its source tier: association = undirected dotted link with the effect size, not drawn along a path; proposed/hypothesis = dashed plus an on-screen 'proposed' / 'hypothesis — disputed' tag; supported = s… |
| medium | VI-05 | deduped, not verified | Caption rule is untestable (no reading rate, word cap or language rule), collides with the 90 s cap, and the shipped example fails it in all 7 scenes | Make the rule computable in the grammar: a reading rate suited to the owner (≈180–200 wpm) and a caption cap (≈15–18 words, short literal sentences; allow up to 2 short sentences per scene); hold = max(3 s, 2 × words ÷ rate) measured from the end of the act… |
| medium | VI-06 | deduped, not verified | Fidelity is checked only at the storyboard and Step 3.5 is layout-only, so text and numbers written during the build are never traced to the source, and the footer's provenance is weaker than the sibling infographic's | Require a fact table in the storyboard (each on-screen clinical value or claim → source sentence/citation). In Step 3.5 extract every visible or assistive string (SVG text, legend, <title>/<desc>, h1, captions, footer) and every number+unit from the final H… |
| medium | VI-07 | deduped, not verified | Verify cannot test the grammar's motion-level rules (entry order, a still hold, ≤2 simultaneous motions, label collisions); the reference example breaks all four and shipped | Turn the storyboard into data: a per-scene 'enters' list and 'motions' list. Add scripted checks reported as named fails: (a) per scene, the set of visible elements equals the storyboard's cumulative 'enters' (no effect before its cause); (b) running animat… |
| medium | VI-08 | deduped, not verified | Generate-first routing sends a single mechanism to a whole-chapter review and may double-run intent-lock; the handoff sentence contradicts itself | Route a single mechanism, drug action or device effect to pubmed-research-note (topic-shaped); keep comprehensive-review for truly whole-disorder requests. Say explicitly that CA passes its locked reading downstream as the opt-out ('intent already locked by… |
| medium | VI-09 | deduped, not verified | General-lane factual values are asserted from memory under a blanket 'illustrative' disclaimer; the example's own formula and caption disagree about sixfold | Split general-lane numbers into (a) schematic magnitudes, which carry the illustrative label and no specific values, and (b) factual values, which need a source or an explicit 'unverified' tag in the same label. Add a consistency pass: every displayed formu… |
| medium | VI-10 | deduped, not verified | Companion PNG scope is undefined; the example exports the bare stage and drops the title, the summary caption and every footer disclaimer | Define the PNG as the full page frame (title, stage, final caption, footer), or require the stage SVG itself to carry the title, source line and disclaimer, so any crop keeps them. Add a check that the PNG contains the footer text. |
| medium | VI-11 | deduped, not verified | Planned evals contradict each other on the clinical-source rule, and none grades the failures that matter | Make Case 3 general lane (e.g. the TMS physics chain or a statistics idea), or give it a fixture source report and grade that every on-screen number appears in it. Add graders: every scene's hold (reached by stepping) contains its storyboard elements; small… |
| low | VI-12 | deduped, not verified | Mandatory intent-lock gate has no headless path and conflicts with intent-lock's own 'not for already-precise requests' scope | Adopt H-2 (gate OPTIONAL plus the 'Assumed:' line), and also exempt already-precise requests, matching intent-lock's own scope. |
| low | VI-13 | deduped, not verified | 'Palette defaults from the grammar' points at nothing: the grammar has no palette, colour semantics or type scale | Add a short token block to the grammar: ground, ink, muted, 5–6 semantic hues with their meanings, and an SVG label type scale with a minimum size. Or remove 'palette' from SKILL.md:54 and point explicitly at the example's :root tokens. |
| low | VI-14 | deduped, not verified | Global 'Space toggles, arrows step' rule is not scoped, so Space on a focused Prev/Next/Restart/dot toggles play instead of pressing that button, and arrows stop working once a button has focus | Scope the rule: global Space/arrow shortcuts act only when focus is not on a button, input or link (check e.target); a focused button keeps its native Space/Enter action; arrows step from anywhere except text inputs. Fix the example handler and add a keyboa… |
| low | VI-15 | deduped, not verified | Vault handoff asks for a 'standalone asset' that vault-keeper does not support and never lists the payload | Until H-7 lands, list the payload: HTML and PNG paths, title '<Concept> — Animation', a suggested MOC topic (the source report's MOC, or the concept's domain in the general lane), and a link to the source artifact. Delete 'standalone asset'. |

## decision-interview

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| medium | AL-16 | confirmed | 'Resolutions generalize' lets one destructive answer authorize later destructive actions without asking | Add a carve-out to the Record/governance rules: resolutions generalize only to reversible items; a destructive, irreversible or outward-facing action needs an explicit choice for each target, and asking again for a new destructive target is not re-asking. S… |
| medium | AL-17 | confirmed | The destructive/outward-facing class has two tests that disagree (verb list vs 'undo cannot reach'), 'overwrite' is undefined, recoverability is never checked, and standing pre-authorization has no rule — so the fallback halt is unpredictable | Give one operational test, applied to the action not the verb, and make the list examples of it — e.g. 'destructive = removes or replaces state this session did not create and cannot restore from a copy it made first (uncommitted edits, user files, remote s… |
| medium | AL-18 | confirmed | 'Explicitly chose' has no instance-level rule: a task verb like 'dedupe' can authorize deleting specific files and their unique text | Add one sentence to Triage: a task-level instruction authorizes the operation, not its instances. Which items go, and any content lost, still earn a question (or, in the fallback, a written decision request) unless the targets are fully determined and nothi… |
| medium | AL-19 | confirmed | Fallback output has no template: three ledger labels, no 'settled' state, the skill's own examples file an already-answered item as an agent default with 'say if wrong', and defaults surface only after work resumes | Give the fallback one fixed template that replaces the Step-5 ledger: `Decided without you:` (agent defaults only, one-line reason each), `Settled:` (already-answered item — source, no 'say if wrong'), `Decision needed:` (one block per halted item: decision… |
| medium | AL-20 | confirmed | No evals exist, the planned cases never exercise the interview, and their line-anchored graders fail correct block output and pass a defaulted delete | Fix the fallback template first (AL-19). Then grade blocks, not label lines: capture from `Decided without you:` to the next blank line or label (e.g. `[\s\S]*?(?=\n\n/\n[A-Z][a-z]+ [a-z]+:)`) and assert no delet/overwrit/push/publish/send in that span (wit… |
| medium | AL-22 | confirmed | The admission threshold's only non-destructive ASK example fails its own test, and the test cannot see outcome trade-offs | Restate the threshold for execution decisions: ask when options lead to materially different work (as now) OR materially different outcomes the user bears (risk, data loss, cost, quality trade-offs, who is affected); anchor effort to 'the remaining task'. R… |
| medium | AL-25 | confirmed | Common user-control cases have no specified outcome: explicit stop, explicit ask with nothing above threshold, partly-answered interview | Add an 'Ending the interview' block. Explicit stop → reversible residue takes recommended defaults under 'Decided without you:', destructive residue gets a written decision request (as in the fallback, since no choice was made). Explicit invocation with an … |
| low | AL-21 | confirmed | No rule for out-of-scope additions the agent proposes: they can be defaulted to 'do' overnight, and the :88 intent-lock route is all-or-nothing | Add a standing rule to Triage or the fallback: an item the agent originates outside the locked goal is not an execution fork; its default is 'not done, offered in one line as a follow-up', including in the fallback. Make :88 apply per item: send only goal-r… |

## handoff

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| medium | PR-53 | deduped, not verified | The one-hop claim->source trace breaks across the handoff: topic-level Sources, no claim markers, unlisted studies rendered anyway, and the Sources list collapsed out of PNG and print | Agree an interface that keeps pubmed's deliberate clean prose: a hidden trace layer (a short key on each Sources line plus a non-rendering HTML-comment marker after each load-bearing number, dose or contraindication) or numbered Sources with an anchor list;… |
| medium | PR-54 | deduped, not verified | The infographic has no verdict/confidence slot and no mapping from the decision report's anatomy (trade-off, inversions, what would change, patient framing) | Add a required verdict band to the anatomy and template directly under the masthead: the verdict verbatim, the confidence plus its one clause, ≥12px. Give a mapping table for pubmed-research-note reports (verdict, confidence, trade-off, inversions, what wou… |
| medium | PR-55 | deduped, not verified | vault-keeper's runtime SKILL has no asset save branch; the asset interface exists only in its README, and query cannot see assets | Move the asset contract into vault-keeper SKILL.md's save job as an explicit branch: kebab filename from the title; HTML and PNG copied as files (cp), never given frontmatter; an `## Assets` line in the MOC; a back-link under a named heading in the source a… |
| medium | PR-56 | deduped, not verified | pubmed still prints intent-lock's retired Assumed/Reframed/Skipped block; once it collapses to one conditional line, a locked scope and population are written nowhere | Adopt the single conditional Assumed line (as S03-W2-1 plans) and also give the report a persistent scope/population statement independent of whether a default was taken, e.g. one 'Scope:' line under the title or a `scope:` / `population:` field in report/1… |
| medium | PR-57 | deduped, not verified | The approved report/1 contract drops the counts line the infographic footer requires, and the infographic is not listed as an I04 consumer | Add a plan.md §8 item and add S06 (clinical-infographic) as an I04 consumer. Either keep counts in report/1 (frontmatter `counts: {pubmed, trials, books/sections}`, or keep the dated line) or have S06 amend source-contract.md:72 to take title and `created` … |
| medium | PR-58 | deduped, not verified | After W2, clinical-infographic Step 0 would not find reports filed to the learn-hub inbox (inferred) | Add a plan.md §8 item: S06-W2-1 (or S07-W2-1's query job) should also look in `$LEARN_HUB_DIR/research-notes/`. Not actionable in this pass, because the inbox routing does not exist yet. |
| low | PR-59 | deduped, not verified | The firecrawl SKILL lists CLI commands that no longer exist; the live CLI settles S09's open ASSUMES (`doctor` exists, `ask`/`docs-search` do not) and adds a `research` command that crosses the biomedical boundary | For S09: resolve its to-verify item - the CLI has `doctor` and no `ask` (support/ask remains a REST endpoint only), so update SKILL.md:38; add `firecrawl research` to the Boundary section as out of scope for clinical evidence, which routes to the PubMed bac… |

## intent-lock

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | EA-17 | owner_fork | Prediction v0 and the survival probe draft a pre-search clinical verdict and put unsearched doses and effect sizes in front of the prescriber, and on a stop into the report's first line | When the work's content is evidence-dependent (clinical/research callers), Prediction v0 predicts form and scope only — the question, the verdict's shape (e.g. 'a dose + titration + what not to promise'), sections, audience, what is left out — with placehol… |
| medium | EA-18 | confirmed | Gate 1 forces a probe round on a request the skill judged unambiguous, breaking the callers' 'zero questions, costs nothing' contract that justifies the mandatory Step 0 | Add an explicit zero-question path: when Phase 0.2 finds no incompatible readings and nothing is above threshold, Gate 1 passes by construction after zero rounds, with no probe and no line. Otherwise carry the probe inside the last question round (or offer … |
| medium | EA-20 | confirmed | Stop rules contradict each other: 'a short or non-committal reply is departure' has no test, turns the owner's terse answers into stops, and collides with the verbatim-alone re-ask rule | Classify replies by content. Count as a stop only an explicit stop word (LOCK, stop, 'just do it', in any language) or a rejected, dismissed or errored picker. Any typed reply that answers or affirms ('ok', 'โอเค', 'ได้') is an answer; on the probe it means… |
| medium | EA-25 | confirmed | misread-capture's trigger phrases catch ordinary revision and bug feedback, which would fill the 7-slot prior cap with preference-level entries | Add explicit Not-for clauses: revision requests that state the change ('shorter', 'in Thai', 'fix X'), and correctness or bug reports on delivered work. Fire only when the complaint is about what was built (deliverable, audience, frame, scope), or when the … |
| medium | EA-26 | confirmed | Evals (current and planned) cannot catch the failures that matter: no ledger read-back case, no zero-question precise path, a leak grader that matches only literal tokens, and current expected outputs that contradict the skills | Add: (a) a case with a fixture ledger whose prior matches the prompt, graded on the run reading the ledger and asking about or assuming along that prior's axis; (b) a caller-shaped precise request graded on zero AskUserQuestion calls and no Assumed line; (c… |
| low | EA-21 | confirmed | misread-capture re-asks a want the complaint already states and never elicits Prior, so the captured lesson is never read back; its own eval expects the opposite | Add an already-answered rule: take the want and the tell from the complaint when present (quote it), and ask only what is missing; state that a single-question capture is valid. Make Q2 ask for the check to run next time, so `Prior` gets filled. Align eval … |
| low | EA-28 | confirmed | The unprompted entry has three incompatible behaviours: 'offer it' (a permission question), silent start, or a one-line reason, the last forbidden as narration | Pick one: the unprompted entry is the round-1 picker itself, with the reason folded into the question text, no separate offer and no narration line. Update code-explainer:51 to match. |
| low | EA-29 | confirmed | Pushback scope is stated two ways: 'exactly one case' (the instruction defeats the user's own stated outcome) vs 'contest any answer you believe is wrong' | Keep :37's narrow case only and reword :137 to 'Push once, only in the :37 case'. |

## ml-concept-lab

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | VI-16 | deduped, not verified | Self-check panel can be fully green while the lab is wrong: the contract never requires a chip to be falsifiable, independent of the code it checks, latched, or aimed at the numbers the verdict rests on; the example ships chips that cannot fail | In build-contract.md's self-check section: (1) each chip compares a running or displayed value against an independent computation — recompute the loss from the plotted points, run finite differences on the gradient actually used in the update (mini-batch in… |
| high | VI-17 | deduped, not verified | Worked example's divergence banner states a false cause (η above 2/λmax while the readout shows it below) and applies the full-batch stability bound to mini-batch runs | Compute the banner's cause from the regime: cite 2/λmax only when full-batch with μ = 0 (stableRegime); otherwise say it diverged although η < 2/λmax, that this bound covers only full-batch GD without momentum, and name the mini-batch or momentum cause. Sho… |
| medium | VI-18 | deduped, not verified | Step 3.5 driving (today's recipe and the planned audit:visual drive tier) touches only range sliders, never runs at the extremes, screenshots presets mid-run and asserts nothing, so broken selects, false presets, NaN leaks and inverted verdicts pass green | Drive every control type: each <select> option, each checkbox state, number inputs at min/max/invalid/empty, every button, canvas click and every documented keyboard shortcut. After each, run (Play, or Step until terminal or the cap) to a settled state — wa… |
| medium | VI-19 | deduped, not verified | The one-PRNG, one-seed rule couples data and algorithm randomness, which contradicts the k-means pattern where the seed is the whole lesson | Replace 'one PRNG' with 'one PRNG function, named streams, every stream seeded and shown': a data seed (the world) and a run seed (initialisation, batch order, exploration), both visible and editable; the data stream is re-created on a data rebuild, the run… |
| medium | VI-20 | deduped, not verified | The seed does not reproduce a run after Reset, despite the contract and the example's footer claim | Re-seed on every reset path, or derive the shuffle stream deterministically from (seed, run). Add a drive check: run the same preset twice and require identical readouts. Fix the example. |
| medium | VI-21 | deduped, not verified | 'A failing chip is a bug' contradicts failure regimes where the invariant is meant to fail; there is no expected-violation chip state | Define three chip states: pass; fail (a bug); expected-violation, labelled 'violated: this is the lesson — <precondition>' and gated on a precondition the page computes and shows (negative edge present, lock off, η ≥ 2/λmax). The drive step accepts expected… |
| medium | VI-22 | deduped, not verified | The contract's aria-live rule (leading-edge throttle, no trailing flush, banner not live) means screen-reader users never hear the terminal result or its diagnosis | Announce every status change immediately (preset loaded, converged, diverged, cap reached), including the banner text — e.g. give the banner role="status". Throttle only running updates, with a trailing flush so the last state is always spoken. Add a drive … |
| medium | VI-23 | deduped, not verified | The planned phone-layout fix passes a 3-view explorable whose trace and restart views are 24 px and 8 px tall | Explorable variant of I07-C: below 640 px the stage grid becomes one column (`grid-template-columns:minmax(0,1fr)`, rows auto); each view panel gets its own floor (e.g. `[data-view]{min-height:200px}`) and the stage grows to their sum (scrolling is already … |
| medium | VI-24 | deduped, not verified | The palette defines only 5 role colours, with no categorical set or sequential ramp, so class, cluster and heat colours are improvised and collide with role hues | Add two named scales to the palette section: (1) a validated categorical set of ~8 hues that meets AA on --panel, each ≥ ~20 ΔE2000 from every role hue and from each other (or explicitly allowed to overlap roles only inside data marks), always paired with a… |
| medium | VI-25 | deduped, not verified | concept-patterns has technical errors: a wrong causal-mask invariant, a saturation lesson the prescribed toy size cannot reach, and a theorem listed as a live check | Change to 'lower-triangular including the diagonal (strict upper triangle = 0)'. For the scaling lesson, add a d_k control reaching ≥ 64 or state the saturation expected at the chosen d_k. Recast XOR as an assertable check (best linear fit's accuracy on XOR… |
| medium | VI-26 | deduped, not verified | 'Clinical' boundary is undefined, contradicts the plugin's own content, and the plan resolves it by sending interactive computational-psychiatry requests to a watch-only skill | Define the split in testable terms: this skill owns any request where the learner drives a computable model, including clinical-adjacent models (TD/RPE, drift-diffusion, Bayesian updating, PPV); concept-animation owns clinical mechanisms with no computable … |
| medium | VI-27 | deduped, not verified | Step 0 expects intent-lock to settle design parameters (misconception, audience, depth) it is built not to elicit, with no fallback source and no headless path | Use intent-lock only to settle the reading of the request. Let the skill settle misconception and depth itself: take the family default from concept-patterns unless the user stated one, and print it in the single `Assumed:` line. Adopt the no-picker fallbac… |
| medium | VI-28 | deduped, not verified | Planned evals cannot detect the failures that define this plugin (Math.random, canned frames, vacuous chips, unreachable failure regime, false banner, non-reproducing seed) | Add static graders (no `Math.random(`, no http(s) src/href, ≥3 `[data-preset]`, a seed input, a `prefers-reduced-motion` block, ≥1 `[data-check]`); a drive grader (each preset runs to its terminal state and fails on a red chip, NaN on screen or a console er… |
| low | VI-29 | deduped, not verified | The unit of one Step is ambiguous for k-means (and other two-phase algorithms): the contract says a full iteration, but its invariant is checked at every half-step | Define the step unit as the smallest operation whose invariant the page checks, and say for k-means (and EM) that this is a half-step (or state that the invariant is checked inside each iteration). Require the page to label which phase the last step was. |
| low | VI-30 | deduped, not verified | Global 'Space = play/pause' takes over focused buttons in the example the skill says to copy | Say in the contract that global shortcuts never fire when focus is on a button, link, input, select or textarea (or choose non-colliding keys). Fix the example's keydown handler and add a 'Space on a focused button activates only that button' drive check. |
| low | VI-31 | deduped, not verified | No minimum type size; the fit-one-screen rule and the example push text to 9–11.5 px, smallest and faintest on the honesty caveat | Add a type floor to the contract: ≥ 12 px for all DOM text including chips; ≥ 13 px for the footer and caveat, in --muted not --faint; ≥ 11 px for canvas axis labels. When content does not fit, shrink the stage or the number of controls, never the text. |
| low | VI-32 | deduped, not verified | No rule for on-page language despite a Thai trigger, and the chat close crams 8 items into 'two lines' | Write on-page prose in the language of the request, technical terms in English, short literal sentences, and set <html lang> to match. Make the Close at most 6 bullets. |
| low | VI-33 | deduped, not verified | Several contract rules cannot be followed or tested (units, fixed decimals, render date, lang, frame budget), and the worked example breaks them | Rewrite as testable rules: units where the quantity has one, otherwise mark it unitless; fixed significant figures with a stated exponent threshold; drop the render date or make it conditional; sample frame time with performance.now() during the drive step.… |

## plan-critique

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| medium | AL-01 | confirmed | Autonomous fallback defaults the lens-9 alternative and goal-changing forks that the catalog says are 'offered, never imposed'; the verdict then judges the agent-chosen plan | State in both the SKILL.md fallback and lens 9 that a lens-9 alternative, any fork that changes plan shape or scope above the threshold, and any fork that would change a clause of the goal (including a deadline written into the goal) are never defaulted. De… |
| medium | AL-02 | owner_fork | Always-fork override (SKILL.md:42) is unscoped: it turns harm-removing, label-mandated repairs into open forks that keep the unsafe original, gives [OPEN] steps no hold state, and reaches every outward-facing step so the interview floods | Split and scope the override. (1) Apply it only to destructive/outward-facing steps that a finding's repair or fork would add, remove, move, reorder or change; untouched steps stay as written and raise no question (keep 'scheduling ≠ confirming' for touched… |
| medium | AL-03 | owner_fork | 'Flagged, not adjudicated' has no mechanics: no flag slot, collides with 'commentary is cut', bars even a primary-label lookup, and patient treatment plans are unscoped — so decisive safety facts ship unverified or get cut | Define the flag and exempt it from the commentary cut: add a `Flag:` line (or inline `[EVIDENCE — <claim>; not checked here — check with <skill>]`) to the finding format carrying the fact, why it is load-bearing, the source to check, the country's label, an… |
| medium | AL-05 | owner_fork | Goal-fit lens never tests the goal's own soundness (ungrounded target, proxy metric, harmful incentive) | Add a goal-soundness probe to lens 1: is the target's size grounded; is the metric a proxy that can improve while the real aim worsens; does chasing it create a harmful incentive? Report a hit as a finding with a fork (keep the goal as written vs a reframed… |
| medium | AL-06 | confirmed | Verdict has no decision rule, its object shifts between submitted and delivered plan, and assumptions sit after all findings | Add a verdict rule: goal-fit FATAL → wrong plan for the goal; any FATAL left open → needs rework; FATALs all repaired, or any COSTLY → sound with repairs; FRICTION only → sound. The verdict always judges the plan as submitted, with an optional 'after revisi… |
| medium | AL-07 | confirmed | Yardstick round has no fixed place in the step order, conflicts with the 'is the deadline movable' fork example, and has no exit path at a stop or the guard | Run the lenses first; round 1 asks only the missing yardstick items that change at least one finding. Classify deadline, budget and risk appetite as yardstick items (no Recommended) and rewrite :48's example to use a real fork. Give one residue rule for eve… |
| low | AL-08 | confirmed | 'Revised plan, whole' carries no change markers or IDs, so traceability cannot be audited; no rule for long or file-based plans | Number findings (F1…) and answers (A1…). Tag every changed or added step inline with `[F3]`/`[A2]`, and list removed steps under 'Removed:' with their IDs; untagged steps are unchanged, which makes the trace checkable. Above a size threshold, or when the in… |
| low | AL-10 | confirmed | Delivery head is undefined: a one-sentence verdict cannot name every never-defaulted fork, there is no 'Open for you' slot, 'biggest problem' has no precedence rule, ledger labels drift, and [OPEN] renders as a code span | Define one verdict block: `Verdict:` line, then `Open for you:` (one line per [OPEN] item with id, step and recommendation) when OPEN forks exist, then findings. Add a precedence rule: a finding with irreversible harm outranks goal-fit for 'single biggest p… |
| low | AL-11 | confirmed | Evals: both positive cases lack the plan they reference, expected outputs describe an interactive path the harness cannot run, and no case covers the known failure modes | Put plans inline. Add graders for: the verdict label; an Assumed/Decided-without-you line under the verdict; no alternative adopted in the fallback (an [OPEN marker instead); change tags `[F\d+]` in the revised plan; the flag format. Keep one Thai-phrase po… |
| low | AL-12 | confirmed | Two-empty-rounds guard has no operational test, spawned forks are not counted, there is no never-re-ask rule, and an absent or errored picker has no path | Define progress as the open-fork count strictly dropping and fire the guard on two rounds with no net drop. Add: 'An answered fork is never re-asked; a later conflict with it is named once (push-once), not reopened.' Add: 'Picker absent, errored or dismisse… |
| low | AL-13 | confirmed | Unprompted-offer rule cannot be reached: the description has no unprompted trigger | Add one clause to the description ('and unprompted, offer it in one line when the user shows a finished, expensive plan and asks anything short of executing it'), keeping within the ~600-char target. Otherwise delete :15. |
| low | AL-14 | confirmed | Verifiability lens has no attribution probe (trend, chance, regression to the mean, comparison) | Add a verifiability probe: 'Could the success signal appear without the plan (secular trend, chance with small counts, regression to the mean in a selected group)? What comparison would show the plan caused it?' |

## plugin-creator

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | CP-23 | fixed (plugin-creator 0.3.1) | Registration and release still follow the pre-W0 version contract: the scaffold writes a catalog version, refine-plugin reports a false parity BLOCKER on every plugin, and its bump step is a no-op dry run, so the first real release breaks health | Make an interim fix now instead of waiting for W3: - Remove `version` from plugin-creator step 4, templates/marketplace-entry.json and the authoring-rules parity section. Replace them with "version lives in plugin.json only; the entry carries none (R83)". -… |
| medium | CP-24 | owner_fork | Elicitation collects 5 slots, but the templates need about 15 fields: the skill body, command and hooks are invented by the model or left as placeholders, and either way the result is reported as done | - Add per-component elicitation slots: - skill and command names, argument-hint, agent tools; - hook event, matcher, and script path under `${CLAUDE_PLUGIN_ROOT}`; - a procedure outline (or an explicit "leave TODO"); - sources of truth; - standing rules, in… |
| medium | CP-25 | confirmed | refine-plugin's audit covers packaging only, but "review/improve my plugin" triggers it and the report hides that limit: it found 1 of 13 known defects and 0 of 2 HIGH | - Open the report with its scope: "packaging and triggering only; procedure, references and tool calls not checked". Say the same in the description. - Add a trigger-collision check that compares the name and quoted phrases against the session's installed a… |
| medium | CP-26 | confirmed | The "done" gate is validate.py only, but the repo's gate is health.sh: broken hooks and dropped locked trigger phrases pass as success, and new phrases never enter the lock | - Make the done gate `bash scripts/health.sh --fast` (it already runs validate.py, strict on every plugin, the ratchet and the trigger lock), falling back to validate.py when `claude` is absent. - For a new skill, run `python3 scripts/rewrite_gate.py trigge… |
| medium | CP-27 | partly fixed (plugin-creator 0.3.1: fixtures and version graders; the not-for and two-tiers keyword graders remain) | The W0 smoke evals encode the old contract: fixtures reject CLAUDE.md-compliant behaviour, graders reward the catalog-version violation, and keyword graders cannot tell a found defect from a denied one | - Re-seed the fixtures from the real W0 scripts, or from stubs of them: no catalog versions, `--write` required, plugin.json plus a CHANGELOG stub. - Replace the catalog-version graders with: `"version"` absent from the entry, and 0.1.1 in plugin.json. - An… |
| medium | CP-28 | partly fixed (plugin-creator 0.3.1: the entry-version claim; the floor and stdio claims remain) | authoring-rules.md, billed as the single source of truth for "what validate.py actually enforces", states false validator facts and has drifted from the rubric the rewrite applies | - Now: correct the three false validator claims (the floor is a WARN only, stdio is allowed, the entry has no version), drop the stale floor and stdio blockers from refine-plugin, and drop "single source of truth" from the README. - At W3: rewrite from rubr… |
| medium | CP-29 | confirmed | No skill owns "add a skill, command or agent to an existing plugin", a common case in this repo's history | - Give plugin-creator an "add a component to an existing plugin" branch: the guard allows an existing plugin when the operator names a new component, and only that component is scaffolded from the templates. Otherwise, make refine-plugin own the case explic… |
| medium | CP-30 | confirmed | Neither description is bounded against the account-synced anthropic-skills:skill-creator, which claims the same phrases | - Add "Not for skills outside the micky-psych-tools marketplace or eval/benchmark loops (use anthropic-skills:skill-creator)" to both descriptions. - Add the pair to architecture §2.8 and to the synced-names and sibling lists. - Add one near-miss trigger ca… |
| medium | CP-31 | confirmed | refine-plugin releases after every applied edit and writes no change note; under the plan's "## Unreleased" policy, bump.py puts the new version above the unreleased notes and leaves it empty | - refine-plugin writes the change note itself. - During the rewrite, it adds the note under `## Unreleased` and does not bump unless the operator asks. - When it does release, it moves the `## Unreleased` notes under the new version heading, or it waits for… |
| low | CP-32 | confirmed | The description recipe produces a usable description but misses the R3/R5/R8 details, and nothing handles sibling reciprocity | - Add R3 order and content, the R4 offset, R5's soft 600 and R8 reciprocity to authoring-rules.md. - Add a scaffold step that proposes the reciprocal Not-for edit to each named sibling and adds the pair to the sibling-pairs list. |
| low | CP-36 | confirmed | refine-plugin's suggested commit leaves out the catalog and lock files it edits, labels every change "refactor", and skips the MEMORY.md release step | - Generate the `git add` list from the files actually touched (`git status --short`). - Pick the commit type from the bump level (fix or feat). - Add the MEMORY.md step until W3 replaces the table with `validate.py --versions` (R88). |

## psych-paper-digest

| Sev | Id | Status | Finding | Fix direction |
|---|---|---|---|---|
| high | EA-30 | confirmed | Evidence floor filters on NLM Publication Types, which most new RCT, meta-analysis and consensus records lack at entry, so they are silently and permanently dropped | Add text-word arms next to the publication-type arms: randomized[tiab] OR randomised[tiab] OR "meta-analysis"[tiab] OR "systematic review"[tiab] OR guideline[ti] OR consensus[ti] (no wildcards, per the tool; mind the 20-operator budget, EA-40). Or let scree… |
| high | EA-32 | confirmed | Act item misstates the paper's comparison and is confirmatory by the rubric; nothing anchors a 'reversal' call | In triage-rubric.md, require a named anchor (a label section with its country, or a guideline statement) before an item can be called a 'reversal/contradicts current practice'; with no anchor, the item goes to Read. The bold claim must be the paper's own pr… |
| high | EA-33 | confirmed | Publication-date windowing lost 11 in-window records, including an auto-Act guideline and a QT systematic review | Apply S04-W1-1 (datetype edat, retstart pagination), together with the text-word floor arms of EA-30 so edat does not widen the indexing-lag gap. Add a regression eval using a record whose pubdate is year-only. |
| high | EA-34 | confirmed | Shipped example trials_term ANDs every word and matches none of the pivotal pediatric trials; the search_trials parameter it fills is unspecified | Define trials_term as an Essie boolean expression with OR-grouped synonyms. Name the parameter it goes into (condition, or advanced_query). Fix the example. At init, run each drafted term once without dates and show the match count in the confirm pass. |
| medium | EA-31 | owner_fork | The default floor excludes the observational and pharmacovigilance tiers the rubric defines, and regulator channels are not swept, so the safety-signal Act path is unreachable while the all-clear line claims nothing practice-changing appeared | Either add a harm arm to the sweep (domain query AND adverse effects[sh] / safety[ti] with cohort, case-control or pharmacovigilance terms, screened at the regulator-grade bar) plus a regulator check via firecrawl; or scope honestly: rewrite the rubric to d… |
| medium | EA-35 | confirmed | Registry event rules cannot be enforced through the MCP tool (no why_stopped, no result values, no posting dates), 'newly completed' has no defining date, registry items have no floor, and the stated coupling to pubmed-research-note is false | Define each event by a queryable field and detect it with search_trials advanced_query: results = AREA[ResultsFirstPostDate]RANGE in window; the completed-no-results event needs one definition — the probes proposed two: 'completed-unpublished' = CompletionD… |
| medium | EA-36 | confirmed | Registry-watch lines carry no intervention, comparator, condition, result or link, and bypass triage | Render each registry line as '<drug> vs <comparator> in <condition>, phase, n — <event> on <qualifying in-window date>; primary endpoint met/not met or a short quote of why_stopped; [NCT…](https://clinicaltrials.gov/study/NCT…)'. Triage registry events with… |
| medium | EA-37 | owner_fork | Guidelines are 'auto-Act, whatever it says', contradicting the 'Act ONLY if…' population test and the mandatory-number rule; consensus statements are never retrieved at floor high | Make guidelines Act-eligible rather than auto-Act: Act only when from a body on a configurable 'followed guidance' list (e.g. NICE, CANMAT, APA, WFSBP, RCPsychT, Thai national bodies) or when it changes a named recommendation that passes the practice test; … |
| medium | EA-38 | confirmed | Item grammar '<claim with its one number — design, n>' lets n alone pass, does not tie the estimate to its own n, design and significance, and has no slot for CI or a validity flag; 4 of 8 items are mis-paired | Change the template to '<finding in words, including direction or "no difference"> — <estimate, CI> from <design, k and n FOR THAT ESTIMATE>'. Act items must carry the effect size with CI (or absolute rates per arm) plus design and n, and may take one valid… |
| medium | EA-40 | confirmed | Domain-query drafting has no recipe: no operator budget against the MCP's 20-operator cap, no INVALID_QUERY rule, no breadth rules and no recall check | Add a drafting recipe to config-schema.md: MeSH plus tiab synonyms, named drugs and conditions, a domain query ≤15 operators (the floor uses 5), and split broad domains into several queries. Have init count operators and run a recall preview (draft over the… |
| medium | EA-41 | confirmed | Init requires interactive elicitation and a user confirm ('same turn'), with no headless path and no stateful unattended mode, although the README advertises cron and Routine runs; a scoped run with no config is undefined | No config and no interactive channel: write nothing, stop with one line ('run /digest interactively once to set the watchlist') and print the drafted config, or seed from the argument and mark it 'unconfirmed'. Change 'same turn' to 'after the confirm'. Def… |
| medium | EA-42 | confirmed | The rule 'screen titles/abstracts, fetch metadata only for keepers' cannot be followed, because search returns PMIDs only | Rewrite: 'call get_article_metadata in batches of ≤20 for every hit (after pagination); screen on title, abstract and article_types; keep only DOI, PMID and a one-line claim in working notes'. Revisit the 200 cap with this cost in view. |
| medium | EA-43 | confirmed | Whether last_swept advances after a registry-down, errored or truncated run is undefined | State per-engine and per-domain rules: advance a domain only when both engines completed for it. On registry-down, write the digest but keep last_swept (dedup absorbs the PubMed repeats), or track a separate registry_last_swept. |
| medium | EA-45 | confirmed | The trigger eval cannot tell this skill from the synced namesake, and its prompt is one the spec routes to that namesake | Anchor the grader to the plugin namespace ('psych-paper-digest:psych-paper-digest', later 'evidence:lit-watch'). Use a watchlist-shaped prompt. Add the spec §4.3 near-miss as a negative case graded against this plugin. |
| medium | EA-46 | confirmed | No grader covers the state contract or triage inflation, and the plan wrongly calls them ungradeable | Add: a file-regex grader on the config showing today's date after the write; a fixture of confirmatory positives only, asserting the all-clear line and no items under '## Act'; a mock PubMed error asserting no digest-*.md and an unchanged config; an llm rub… |
| low | EA-39 | confirmed | The Act handoff passes only the question — no triggering DOI and no ready-to-paste prompt — so pubmed-research-note's same publication-type floor can drop the study that triggered it | End each Act item with a copy-paste prompt carrying the question plus 'seed: doi:…/PMID', marked must-include. pubmed-research-note treats seeded identifiers as mandatory load-bearing candidates. |
| low | EA-44 | confirmed | The ISO date format is rejected by search_articles, and the eval grader and mocks cannot notice | State the format ('date_from/date_to as YYYY/MM/DD; convert last_swept'). Tighten the grader to '"date_from"\s*:\s*"\d{4}/\d{2}/\d{2}"' and check it equals the scaffold date minus 3 days. Let mocks reject malformed arguments. |
| low | EA-47 | confirmed | Digest header counts are undefined, the Suppressed line gives a false reason, and the web fallback URL silently truncates at 20 records | Define every header count ('trials N'). Change the Suppressed template to '<domain>: N suppressed (x below floor, y off-domain, z mechanistic)'. Add retmax and retstart to the fallback URL alongside the planned MCP pagination. |
| low | EA-48 | confirmed | Template produces dense, repetitive prose for a second-language reader | Render the Act item as a bullet with sub-bullets (Finding / Why it matters / Question → pubmed-research-note). Drop the domain word when only one domain was swept. Expand non-standard abbreviations once. Keep the question to one question of at most ~20 words. |
| low | EA-49 | confirmed | The dedup ledger relies on PMIDs the digest is forbidden to carry, and suppressed items are recounted in each overlap | State dedup as DOI + NCT only, or add a hidden id footer (HTML comment) carrying every screened PMID. Exclude records whose entry date falls before the previous window's end from the counts. |
| low | EA-50 | confirmed | No rule for Act items beyond the cap of 5 or for empty sections, yet the grader requires all four headings | 'Items beyond 5 move to Read, tagged Act-grade (cap)'. Give every section a one-line empty state, e.g. 'No registry events this window.' |
