#### inventory-alignment-family.json :: alignment gates (intent-lock + misread-capture, decision-interview, plan-critique, learn-hub intent-lock clone)
  OBS: Boundary carve-outs are reciprocal in the three gate descriptions: intent-lock names decision-interview, plan-critique and misread-capture; decision-interview names intent-lock, misread-capture and plan-critique; plan-critique names intent-lock ×2 and decision-interview. But the discriminators are session STATES (is the goal locked? does a plan artifact exist? was work delivered?), not phrases, and phrase-level collisions remain. (a) At task start, 'ask me everything at once' / 'what do you need
  OBS: ROUTING.md, which CLAUDE.md makes the mandatory first stop, cannot see any Not-for clause: route.py:54-75 builds the cue from the 'Use when' clause truncated at 200 chars. decision-interview's boundary survives only because 'mid-task' was front-loaded into its Use-when clause (MEMORY.md 2026-07-28). intent-lock's row (ROUTING.md:12) loses its unprompted-offer condition, its Not-for and its second Thai trigger.
  OBS: Family vocabulary has drifted where behavior is meant to be shared. Silence: intent-lock:190 'Silence is a stop', decision-interview:81 'silence is a stop, not a license to wait', plan-critique:50 'silence and an abandoned picker are not a stop verdict'. Question cap: 3 (intent-lock) vs 4 (decision-interview, plan-critique). Picker type names: `single_select`/`multi_select`/`rank_priorities` (intent-lock) vs 'multiSelect' (the siblings). 'Ledger' names four different things: the misread file, in
  OBS: Shared mechanics are copy-pasted three times with small wording drift: the admission threshold ('materially different work … more than roughly a fifth'), the destructive/irreversible/outward-facing always-ask, the autonomous fallback, picker mechanics, stop sovereignty, push-once, and the two-empty-rounds guard. Plugins cannot reference files outside their own directory, so a shared reference is impossible without merging the gates into one plugin (e.g. one 'alignment' plugin with intent-lock, d
  OBS: None of the three gates names Claude Code's actual picker tool (AskUserQuestion: 1–4 questions, 2–4 options, `multiSelect`, automatic 'Other'). intent-lock is written against the claude.ai tool: `rank_priorities` does not exist, and its manual escape duplicates the automatic 'Other'. All three forbid prose questions, yet the picker is absent in subagents (this workflow agent's tool list has no AskUserQuestion) and headless runs, and it failed in remote sessions (MEMORY.md: 'transport error', 'pe
  OBS: The 'mandatory Step 0 intent-lock' cannot be guaranteed to execute. It is mandatory with failure conditions in pubmed-research-note (SKILL.md:310), comprehensive-review (:209), concept-animation (:168), ml-concept-lab (:241) and firecrawl Path C (:359-361), and in learn-hub's pubmed/comprehensive clones. Yet: no plugin.json declares the dependency; micky-psych-tools has no .claude/ settings and learn-hub's settings.json enables no plugins; intent-lock is absent from this cloud session's availabl
  OBS: The misread ledger cannot compound by construction. It is runtime-written state inside the plugin tree, and marketplace installs execute from a cache copy that is replaced on update and never committed. It is forked across two repos. It holds personal data in an MIT-distributed plugin. Its only git change is 2026-07-11, although MEMORY.md logs many later runs where prior #1 fired. A rewrite should move it to a user- or project-scope path outside the plugin, with a deterministic append script (fi
  OBS: All three gate descriptions sit at 1012–1020 of 1024 chars (4, 11 and 12 chars of headroom; 1053–1082 UTF-8 bytes). Evals across the group are descriptive only (`assertions: []` in all 19 cases), decision-interview has none, no runner exists, and there are no should-trigger/should-not-trigger query sets for description optimization. Every boundary edit made so far (0.4.1, 0.4.2, 0.1.1) was therefore verified by review, not by evals.
  OBS: Body sizes: intent-lock ~6.2k tokens / 245 lines (heavy rhetoric plus version-history narration), plan-critique ~2.5k tokens / 70 very long lines, decision-interview ~1.9k tokens / 87 lines, misread-capture ~1.0k tokens / 65 lines. intent-lock is the one loaded as Step 0 by five-plus other skills, so its size and its contradictions multiply across the pipeline.
== micky-psych-tools/plugins/intent-lock [plugin (2 skills, no commands/agents/hooks/mcp) v0.4.2 (plugin.json == marketplace.json == MEMORY.md versions table; CHANGELOG has 0.4.2, 0.4.0, 0.3.0, 0.2.0, 0.1.0 — 0.4.1 missing)] intent-lock(desc=1020,lines=245,fm=name,description); misread-capture(desc=789,lines=65,fm=name,description)
  intent-lock-1 [H] intent-lock: the silent output contract is contradicted by many leftover visible-output instructions. Instead of deleting them, line 22 adds a blanket 'reinterpret these verbs' patch, so the text says the opposite of what it means in at least 11 places. || EV: SKILL.md:16 'Exactly three things reach the user, and nothing else' and :22 'Where a phase below says *write, show, open with, state, publish,* or *emit* anythi
  intent-lock-2 [H] intent-lock: the mandatory picker contract cannot always be executed in Claude Code. It names claude.ai picker types, has no fallback when the picker tool is missing or errors, and relies on 'silence' and an 'abandoned picker', which Claude Code cannot observe: AskUserQuestion blocks until answered or rejected, and a rejection i || EV: SKILL.md:136 'Every Phase 1 question goes through the option-picker. No exceptions, no prose questions'; :160-162 '`single_select` … `multi_select` … `rank_prio
  intent-lock-3 [H] The misread ledger cannot compound as designed. It is runtime-written state inside the plugin install directory (a cache copy when installed from a marketplace, replaced on update and never committed), and it is forked into two repos. Git shows a single change ever (2026-07-11), although MEMORY.md records many later runs where p || EV: misread-capture/SKILL.md:39 'Append to `${CLAUDE_PLUGIN_ROOT}/skills/intent-lock/references/misreads.md`'; `git log -- …/misreads.md` → only 71d7b14 2026-07-11.
  intent-lock-4 [H] misread-capture: the load-bearing `Prior:` line has no eliciting question. The two permitted questions ask for the want and the tell, not a check to run next time. So the Prior must either be drafted by Claude (forbidden) or come from a forbidden third question. The body also licenses extra questions that contradict its own 'two || EV: SKILL.md:26 'Two questions. Never more'; :30-31 lists them (want; when you knew); :35 'Do not ask a third question'; but :33 'ask once more for the want behind 
  intent-lock-5 [H] Cross-plugin interface drift: callers print and expect preface fields intent-lock removed in 0.3.0 (Reframed, Skipped), and a 'reframe' step intent-lock forbids. || EV: intent-lock SKILL.md:226 'There is no `Reframed:` line, because there is no reframe.' vs pubmed-research-note SKILL.md:264 '> Reframed: <the reframe, if it was 
  intent-lock-6 [M] intent-lock: internal contradiction on what the Phase 3 preface contains and where [UNTESTED] sits. || EV: SKILL.md:207 'Its residue reaches them as the `Skipped:` line of the preface' and :183 '[UNTESTED] … sits in the preface … on its own line' vs :220-223 'Preface
  intent-lock-7 [M] intent-lock: the degenerate-loop guard is logically inconsistent with the survival-probe text and drifts from README/CHANGELOG. A prediction line that 'dies twice' is a revision, so a guard that requires 'prediction unrevised' can never catch it. The 'every hypothesis killed' disjunct also contradicts 'A killed set is informatio || EV: SKILL.md:179 'A prediction that dies twice on the same line is not a prediction, it is a wish, and the guard will catch it.' vs :193 'two consecutive rounds tha
  intent-lock-8 [M] intent-lock: 'craft my prompt' is a trigger, but emitting a prompt is a failure condition — and ledger prior #2 says to check whether the user wants the prompt. When the prompt IS the deliverable, the skill cannot deliver it, violating its own 'execute as written' rule. || EV: SKILL.md:3 trigger '"craft my prompt"' and 'never emit a portable prompt artifact'; :247 'A prompt, spec, or instruction was emitted as an artifact for the user
  intent-lock-9 [M] The misread ledger's contents contradict both skills' rules. Priors are written in third person ('he'), i.e. not in the user's words. Entries are newest-first, while misread-capture says newest last. Retirement-on-no-answer is a silent drop, which the ledger header forbids. || EV: misreads.md:34 'Entries are written by the user, never by Claude' vs :46/:47/:58/:65 'When he asks…', 'when he says…'; misread-capture SKILL.md:53 models the sa
  intent-lock-10 [M] misread-capture description promises a 'review or edit priors' mode with no procedure behind it, and it duplicates its prohibition clause. || EV: SKILL.md:3 'Also use when the user asks to record, review, or edit their misread priors' — the body has only capture, entry grammar and retirement (:24-69). :3 
  intent-lock-11 [M] intent-lock body carries version-history narration, which is time-sensitive content that belongs in the CHANGELOG. It inflates a 6.2k-token body that every mandatory Step 0 caller loads (21 lines are longer than 300 chars). || EV: SKILL.md:24 '`GOAL UNIFIED` is now a private green light, not an announcement'; :95 'and now it does not get *ink* either'; :99 'The round cap is gone.'; :101 '
  intent-lock-12 [M] intent-lock description: body procedure and internal jargon spend budget, leaving 4 chars under the cap. This blocks any future trigger or boundary edit. || EV: SKILL.md:3 (1020 chars): 'Never propose a better version of the request, never emit a portable prompt artifact, never publish the convergence gates, never ask p
  intent-lock-13 [L] failure-conditions.md has entries left stale by the 0.3.0 silent-run change, plus one unmeasurable condition. || EV: failure-conditions.md:11 'The prediction was withheld until the gate' (under the silent contract it is ALWAYS withheld); :12 'A round opened without a revised p
  intent-lock-14 [L] Ambiguous stated-default surfacing: Phase 0 says each sub-threshold item gets a stated `Default: X. Say so if wrong.`, while the output contract lists the defaults as internal and Phase 3 allows a single Assumed line. || EV: SKILL.md:73-77 'Everything below the line gets a stated default … `Default: X. Say so if wrong.`'; :168 repeats it; vs :22 'the defaults list … never print it' 
  intent-lock-15 [L] CHANGELOG skips 0.4.1; README install uses a placeholder owner; README design commitments are stale. || EV: CHANGELOG.md jumps '## 0.4.2' → '## 0.4.0' while MEMORY.md:402 records 0.4.1; README.md:49 '<owner>'; README.md:27 'Every round opens with the actual first outp
  intent-lock-16 [L] Evals are descriptive only: `assertions: []` in all 13 cases across both skills, there is no runner, and there is no should/should-not trigger query set, even though the description sits 4 chars from the cap. || EV: intent-lock/evals/evals.json ids 1-8 and misread-capture/evals/evals.json ids 1-5 all have `"assertions": []`; scripts/validate.py only checks 'evals.json is va
== micky-psych-tools/plugins/decision-interview [plugin (1 skill + 1 command) v0.1.1 (plugin.json == marketplace.json == MEMORY.md; no CHANGELOG exists to document 0.1.0 → 0.1.1)] decision-interview(desc=1013,lines=87,fm=name,description)
  decision-interview-1 [M] No evals, README, or CHANGELOG. It is the only gate in the family without them, so the 0.1.0→0.1.1 boundary change (the reciprocal plan-critique carve-out) is documented only in MEMORY.md and triggering is unregression-tested. || EV: `find plugins/decision-interview -type f` → only .claude-plugin/plugin.json, commands/resolve-decisions.md, skills/decision-interview/SKILL.md.
  decision-interview-2 [M] The description's unprompted trigger is looser than the body's rule (it drops the materiality threshold), inviting interviews over sub-threshold decisions — the exact failure the body warns against. || EV: SKILL.md:3 'unprompted, the moment two or more decisions accumulate or a single decision blocks the work now' vs :15 'the moment a second above-threshold decisi
  decision-interview-3 [M] The fallback trigger conditions cannot be detected, and there is no path for 'picker tool unavailable or errored'. 'Unattended' and 'abandoned round' are asserted but never operationalised. || EV: SKILL.md:81 'An unattended session, an interrupted picker, an abandoned round: silence is a stop, not a license to wait.' No mention of a tool error or a subage
  decision-interview-4 [L] Family vocabulary contradiction on silence: here silence 'is a stop', while plan-critique says silence is 'not a stop verdict' — for the same routed behavior (the autonomous fallback). || EV: decision-interview SKILL.md:81 'silence is a stop, not a license to wait' vs plan-critique SKILL.md:50 'silence and an abandoned picker are not a stop verdict —
  decision-interview-5 [L] Question cap is 4 per round here but 3 in intent-lock, where the lower cap is argued from user attention ('beyond three questions the user answers the easy ones and abandons the rest'). The family has no shared rationale. || EV: decision-interview SKILL.md:54 'up to four questions per call (the tool's ceiling)' vs intent-lock SKILL.md:99 'At most 3 questions per round … beyond three que
  decision-interview-6 [L] The description sits 11 chars from the 1024 cap and exceeds 1024 in UTF-8 bytes (1053). validate.py counts chars, but any byte-counting consumer would reject it. || EV: Measured: desc chars 1013, bytes 1053; scripts/validate.py:108 `n = len(fm.get("description", ""))`.
== micky-psych-tools/plugins/plan-critique [plugin (1 skill + 1 command + 1 reference) v0.1.0 (plugin.json == marketplace.json == MEMORY.md; CHANGELOG 0.1.0 dated 2026-08-06)] plan-critique(desc=1012,lines=70,fm=name,description)
  plan-critique-1 [M] Command and skill disagree on what to do when no plan exists. || EV: commands/critique-plan.md:10-11 'If no plan exists, say so and ask for one — never invent a plan to critique.' vs SKILL.md:24 '**No plan, only an intention** … 
  plan-critique-2 [M] The drafting hand-off to intent-lock is not reachable by routing. intent-lock's description has no plan-drafting trigger and excludes 'already-precise requests', and ROUTING.md has no 'draft a plan' row. Yet plan-critique eval 3 expects this route. || EV: plan-critique evals.json id 3: 'The request routes through intent-lock for alignment, the plan gets drafted'; intent-lock SKILL.md:3 triggers are 'interview me'
  plan-critique-3 [M] The load-bearing termination and stop rules are packed into single mega-bullets, which makes them hard to follow and audit. || EV: SKILL.md:50 is one 807-char bullet holding the three end conditions, stop sovereignty, silence routing, the two-empty-rounds guard, the destructive carve-out an
  plan-critique-4 [L] Silence semantics are worded opposite to both siblings (same behavior, contradictory words). || EV: SKILL.md:50 'silence and an abandoned picker are not a stop verdict' vs decision-interview SKILL.md:81 'silence is a stop' and intent-lock SKILL.md:190 'Silence
  plan-critique-5 [L] The description spends ~170 chars on the lens enumeration (already in the body and reference), pushing 'Use when' to char 543 and leaving 12 chars under the cap. Lens names also drift from the reference. || EV: SKILL.md:3 '…runs nine adversarial lenses (goal-fit, gaps, sequencing, feasibility, risk, assumptions, verifiability, simplicity, alternatives)…'; critique-lens
  plan-critique-6 [L] No misread-capture carve-out: a user rejecting the delivered revised plan ('this isn't what I wanted') has no stated route from this skill. || EV: SKILL.md:3 Not-for lists executing, drafting, pre-build ambiguity, mid-task decisions, and code/prose only; SKILL.md:68-74 '## Not for' is the same.
  plan-critique-7 [L] 'review my plan' collides with ledger Active prior #1, which fires intent-lock on any 'review' request, so the two gates may double-run. || EV: SKILL.md:3 trigger '"review my plan"' vs intent-lock references/misreads.md:46 'When he asks to "research" or "review" a topic, run intent-lock first'.
  plan-critique-8 [L] Evals cannot catch regressions: assertions are empty, and the riskiest branches have no cases. || EV: evals.json ids 1-6 all have `"assertions": []`; no case for the yardstick round (SKILL.md:30), the autonomous fallback (:64-66) or the always-fork override (:42
== learn-hub/plugins/intent-lock [plugin clone (fork of micky-psych-tools/plugins/intent-lock, registered in learn-hub/.claude-plugin/marketplace.json as 'learn-hub-local') v0.4.2 (plugin.json identical to upstream; learn-hub marketplace entries carry no version field)] intent-lock(desc=1020,lines=245,fm=name,description); misread-capture(desc=789,lines=65,fm=name,description)
  intent-lock (learn-hub clone)-1 [H] The fork splits the single compounding ledger into two. Priors captured in learn-hub sessions are invisible to micky-psych-tools sessions and vice versa, defeating the design premise 'There is exactly one ledger file'. || EV: micky-psych-tools/plugins/intent-lock/README.md:44 'There is exactly one ledger file, at `skills/intent-lock/references/misreads.md`'; the file exists at both /
  intent-lock (learn-hub clone)-2 [M] The 'mandatory Step 0' is not guaranteed in learn-hub either. The learn-hub-local marketplace is not enabled by project settings, and the repo uses three incompatible gating models at once: a mandatory vendored intent-lock, a conditional 'if it is available', and an inlined bespoke gate. || EV: learn-hub/.claude/settings.json contains only 'hooks'; learn-hub README.md:3-5 'It is the mandatory Step 0 gate for `comprehensive-review` and `pubmed-research-
  intent-lock (learn-hub clone)-3 [M] No sync or parity mechanism for vendored copies. The 'unmodified' claim is true today only by coincidence (upstream has not moved since f91b3b0), and the vendored consumers already carry the Reframed/Skipped interface drift. || EV: README.md:3 '@ `f91b3b0`, **unmodified**'; learn-hub/plugins/pubmed-research-note/skills/pubmed-research-note/SKILL.md:264-265 '> Reframed: …' / '> Skipped: …' 
  intent-lock (learn-hub clone)-4 [L] intent-lock's Phase 3 hand-off target (decision-interview) is not vendored into learn-hub, so the instruction points at a skill that does not exist in that marketplace. || EV: intent-lock SKILL.md:228 'Those belong to the `decision-interview` skill'; learn-hub/.claude-plugin/marketplace.json lists source-to-vault, vault-vectors, vault
#### inventory-infra.json :: Infrastructure plugins and marketplace tooling — /home/user/micky-psych-tools (plugin-creator, vault-keeper, firecrawl, scripts/validate.py, route.py,
  OBS: Coupling to the repo: plugin-creator (all three commands) and vault-keeper are published as installable plugins, but both only work when cwd is the micky-psych-tools root. Their tools (scripts/validate.py, route.py, bump.py) sit outside the plugin directories and are not copied on install. I verified this by installing into a scratch CLAUDE_CONFIG_DIR: the cache holds only .claude-plugin, commands, skills, README and CHANGELOG. The walk-up vault resolution also matches learn-hub's own .claude-pl
  OBS: Validation is split between two tools, and neither is complete. validate.py covers house rules: name == directory, description length 200-1024, version parity, and command/agent description presence. `claude plugin validate --strict` (v2.1.280) covers real YAML parsing, unknown manifest fields, hooks JSON and event names, duplicate plugin names, author presence and a version-drift warning. It passes a stdio MCP server that validate.py rejects. Running the CLI on the marketplace root checks only 
  OBS: Description saturation: 10 of 17 skill descriptions in the marketplace are at least 990 characters (cap 1024), totalling about 15,950 characters (about 4k tokens) always in context. The house 200-character floor, plus plugin-creator's 'expand with more trigger phrasings', pushes descriptions toward keyword stuffing and procedural detail. Whether Claude Code's skill-listing character budget truncates this set is unverified and should be checked before the rewrite.
  OBS: Duplicated information: plugin summaries live on six surfaces (plugin.json, marketplace.json, README.md, the CLAUDE.md Plugins section, MEMORY.md 'Plugins at a glance', ROUTING.md). The measured drift is 9 of 14 plugin.json descriptions differing from the catalog, gridgeist missing from CLAUDE.md, vault-keeper's CHANGELOG two versions behind, and 'Four jobs' against 'Five jobs'. ROUTING.md is about 5k tokens, and all 29 of its Quick-index cues are verbatim substrings of descriptions Claude alrea
  OBS: Mandated context cost per session: CLAUDE.md is about 3.5k tokens (69% plugin list), MEMORY.md is mandated first read at about 27k tokens (108 KB), and ROUTING.md is mandated per request at about 5k tokens.
  OBS: Evals: the three plugins ship 20 eval cases in total (vault-keeper 6, empty-vault 6, firecrawl 8). All use {skill_name, evals:[{id,name,prompt,expected_output,files:[],assertions:[]}]} with empty assertions, and nothing runs them. plugin-creator and refine-plugin have no evals at all, although the plugin ships an evals template for others.
  OBS: Undeclared dependencies: the CLI 2.1.280 validator accepts a plugin.json `dependencies` field (no unknown-field warning, while 'bogusField' gets one), yet no plugin declares its real dependencies. Examples are intent-lock and vault-keeper, which about 12 producers rely on. The cross-repo contract (psych vault → learn-hub digest-report, ingest-infographic, ingest-animation) is the most fragile link: learn-hub moved digest-report into plugins/ and added asset receivers on 2026-08-20, and empty-vau
  OBS: Deterministic logic written as prose that belongs in scripts: vault index rebuild and orphan/reachability report, links-mirror sync, collision suffixing, kebab-topic-to-MOC mapping, CHANGELOG-entry-on-bump, leftover-placeholder checks, and ROUTING freshness. Existing scripts have tooling bugs: the validate.py crash path, route.py writing on `--help`, bump.py's ASCII escaping and missing trailing newline, and the frontmatter parser duplicated between validate.py and route.py.
  OBS: A vendored guide copied 'verbatim' goes stale: comparing the firecrawl body with the vendor's live SKILL.md (fetched 2026-09-23) found at least 8 material changes, including the build-skills install step, `ask` → `doctor`, the CLI skill rename to `firecrawl`, and keyless REST scope. A rewrite should keep the vendor text as a dated reference, or fetch it at runtime, behind a slim routing body.
  OBS: Frontmatter portability: plugins/intent-lock/skills/intent-lock/SKILL.md (outside this group) fails PyYAML ('mapping values are not allowed here', line 2 col 574) but passes both validate.py and the CLI. Any rewrite tooling that uses a strict YAML parser will reject it, so quote or fold descriptions consistently.
  OBS: Side effects of this scan, none in the repo: probes were run on copies and fixtures in the scratchpad, and a scratch CLAUDE_CONFIG_DIR (scratchpad/cfg) was used for the install check. `git status` in micky-psych-tools is clean.
== micky-psych-tools/plugins/plugin-creator [plugin v0.3.0] plugin-creator(desc=767,lines=93,fm=name,description); refine-plugin(desc=883,lines=79,fm=name,description)
  plugin-creator-1 [H] The plugin is distributed as an installable plugin, but every step depends on cwd being the marketplace repo root and on repo scripts that are not shipped. There is no precondition check. Run from another repo, it writes plugins/<name>/ there or fails when it calls scripts/validate.py. || EV: SKILL.md:9-10 'it writes into plugins/<name>/, registers the catalog entry, and validates'. The guard (SKILL.md:47-51) checks only the name format and collision
  plugin-creator-2 [M] The scaffold produces a plugin that fails the sibling audit. It writes no README.md, no CHANGELOG.md and no evals, but refine-plugin's checklist requires all three. The evals.json template is never referenced. || EV: SKILL.md:57-59 'Always: .claude-plugin/plugin.json. Per chosen component: skills/<skill>/SKILL.md, commands/<cmd>.md, agents/<agent>.md, hooks/hooks.json, .mcp.
  plugin-creator-3 [M] The meta-plugin breaks its own description rule. Both skill descriptions are imperative, and refine-plugin uses second person. || EV: authoring-rules.md:33 '**Third person, action-first** — "Scaffolds a…", not "This skill will…"'. Compare plugin-creator SKILL.md:3 'description: Scaffold a new 
  plugin-creator-4 [M] The negative scope misroutes edits of existing plugins to manual editing plus bump.py instead of the sibling refine-plugin skill and /refine-plugin command. || EV: SKILL.md:3 'Not for editing or versioning an existing plugin (hand-edit the files, then run scripts/bump.py)'. SKILL.md:20 '**Don't:** modify an existing plugin
  plugin-creator-5 [M] refine-plugin bumps the version without writing a CHANGELOG entry, although its own checklist requires 'one changelog entry per released version'. This is how per-plugin changelogs have gone stale. || EV: refine-plugin SKILL.md:58-68 (propose level → run bump.py → done; no changelog step). audit-checklist.md:67-68 'one changelog entry per released version'. Resul
  plugin-creator-6 [M] The {{PLACEHOLDER}} syntax in the templates is not valid YAML because it parses as a flow mapping. A placeholder left unfilled or half-filled breaks the frontmatter, and validate.py has no check for leftover placeholders. || EV: templates/SKILL.md:2 'name: {{SKILL_NAME}}', :8 'description: {{one-line, …}}', templates/command.md:3 'argument-hint: [{{OPTIONAL_ARG_HINT}}]'. PyYAML safe_loa
  plugin-creator-7 [M] The hooks template gives no guidance to use ${CLAUDE_PLUGIN_ROOT} for script paths. A relative script path breaks after install, because hooks run in the user's project cwd. || EV: templates/hooks.json:9 '"command": "{{SHELL_COMMAND or path to a script the hook runs}}"'. ${CLAUDE_PLUGIN_ROOT} is mentioned nowhere in authoring-rules.md or S
  plugin-creator-8 [M] The MCP rules forbid stdio servers, which Claude Code accepts. The plugin.json template also lacks the mcpServers key that authoring-rules says must be present, so a scaffolded .mcp.json is never checked by validate.py. || EV: authoring-rules.md:49-52 'Every server entry must have both type and url'. validate.py:93 makes a {command,args} server FAIL (reproduced on a scratch copy), whi
  plugin-creator-9 [L] The procedure contradicts the reference on hooks descriptions. || EV: SKILL.md:65 'Commands and hooks get a plain one-line description.' vs authoring-rules.md:45 'Hooks have no description field and are not checked here.'
  plugin-creator-10 [L] The length floor encourages padding: descriptions under 200 characters must be 'expanded with more trigger phrasings' instead of being made more precise. || EV: SKILL.md:63-64 'if the draft is under 200, expand with more trigger phrasings before writing.'
  plugin-creator-11 [L] The plugin's own manifest breaks its own checklist on keyword count, and it drifts from the catalog entry. || EV: plugin.json:9-19 lists 9 keywords; audit-checklist.md:60 'Keywords are discoverable — 3–6'. The catalog entry has 5 keywords and a different description.
  plugin-creator-12 [L] The scaffold workflow skips the repo-level bookkeeping that CLAUDE.md requires: the root README plugin list, the CLAUDE.md plugin list and the MEMORY.md versions table. Those lists have drifted as a result. || EV: CLAUDE.md:58 '7. Update MEMORY.md (versions table, milestones) when releasing.' SKILL.md has no such step. gridgeist is in marketplace.json, README.md:94 and ME
  plugin-creator-13 [L] The authoring standards leave out current best practice: skill name limits (lowercase-hyphen, 64 characters max, matching the directory), the SKILL.md body-size and progressive-disclosure guidance, keeping references one level deep, allowed-tools and disable-model-invocation, ${CLAUDE_PLUGIN_ROOT}, and plugin.json `dependencies` || EV: authoring-rules.md covers only names, versions, the catalog entry, description length and recipe, command/agent description presence, MCP url/type and folder la
  plugin-creator-14 [L] The /route command reads ROUTING.md and then says to regenerate it first, and it writes to the repo to answer a read-only question. || EV: commands/route.md:6 'Read `ROUTING.md` at the marketplace repo root' then :9 'Before consulting the table, always run `python scripts/route.py` first'.
== micky-psych-tools/plugins/vault-keeper [plugin v0.4.0] vault-keeper(desc=910,lines=114,fm=name,description); empty-vault(desc=1003,lines=111,fm=name,description)
  vault-keeper-1 [H] Resolving the vault root through the plugin root is wrong for an installed plugin. The skill claims `${CLAUDE_PLUGIN_ROOT}/../../vault` is equivalent to the repo vault, but after install it points into Claude's plugin cache. That is the kind of rogue vault the skill forbids. || EV: SKILL.md:29 and vault-layout.md:7 'From inside a plugin, `${CLAUDE_PLUGIN_ROOT}/../../vault` is equivalent.' The installed layout, verified in a scratch CLAUDE_
  vault-keeper-2 [H] The walk-up root detection matches any marketplace repo. From a learn-hub cwd it resolves learn-hub/vault, the Learn app's own vault in a different format, and would write MOCs/, notes/ and artifacts/ into it. The marketplace name is never checked, and nothing says what to do when no marketplace.json is found. || EV: SKILL.md:28 'walk up from cwd to the directory containing `.claude-plugin/marketplace.json`, then append `vault/`'. /home/user/learn-hub/.claude-plugin/marketpl
  vault-keeper-3 [H] The learn-hub root detection in empty-vault is stale. The marker directory it requires no longer exists, so a present sibling checkout fails detection. || EV: empty-vault SKILL.md:36-37 'the directory containing `.claude/skills/digest-report/` (and `vault/` + `scripts/sync-vault.mjs`)'. `ls /home/user/learn-hub/.claud
  vault-keeper-4 [H] empty-vault deletes assets that learn-hub now has receivers for and expects to be handed. The learn-hub skills wait for a handshake before the source asset is deleted, and empty-vault never performs it. || EV: empty-vault SKILL.md:55-57 'assets (rendered HTML, PNG — binaries) → cannot become Learn notes. They are *removed with their topic*'. learn-hub/.claude/skills/i
  vault-keeper-5 [M] The instruction to 'invoke the Learn repo's digest-report skill' is load-bearing but has no mechanism. In a micky-psych-tools session that skill is not loaded unless the learn-hub-local plugin is installed, and its sync needs learn-hub env that may be absent. || EV: empty-vault SKILL.md:63-64 'invoke the Learn repo's **`digest-report`** skill with the absolute file path plus context'. digest-report is not in this session's 
  vault-keeper-6 [M] The job count is inconsistent across surfaces: the skill says four, while the manifest, README and catalog say five. || EV: SKILL.md:10 'Four jobs: init …' and SKILL.md:70 '## The four jobs'. plugin.json:4 'Five jobs: init, save, index, query, empty'. README.md:22 '## The five jobs'.
  vault-keeper-7 [M] The 'Obsidian-style' vault produces wikilinks Obsidian cannot resolve. Links are written by frontmatter title, but files are named kebab-case or '<Topic> MOC.md' and no aliases are written, while Obsidian resolves links by filename or alias. || EV: SKILL.md:66 'Bodies link with Obsidian `[[wikilinks]]` by note title'. vault-layout.md:17-18 'MOC files are `<Topic> MOC.md` … Artifacts kebab-case'. vault/inde
  vault-keeper-8 [M] The index job is deterministic but written as prose, and it contradicts itself on links. The skill says a dangling link is fine but also tells Claude to 'fix … broken [[links]]', with no rule for telling the two apart. || EV: SKILL.md:66-67 'A link to a note that does not exist yet is fine — it marks a note worth writing, not an error' vs SKILL.md:101 'fix stale/broken `[[links]]`'. 
  vault-keeper-9 [M] The plugin CHANGELOG is two releases behind the version. || EV: CHANGELOG.md's newest entry is '## 0.2.0 — 2026-07-10'; plugin.json:3 has '"version": "0.4.0"'. The 0.3.0 asset type and the 0.4.0 empty-vault release appear on
  vault-keeper-10 [L] The README accepts a producer target type ('MOC') that the save procedure never handles. Save only decides between note and artifact. || EV: README.md:49 '**target type** (`note` / `artifact` / `asset` / `MOC`)' vs SKILL.md:79 '1. Decide `note` vs `artifact` (rule above).'
  vault-keeper-11 [L] empty-vault hands vault notes to digest-report as supplementary input, but digest-report's input contract lists only research-notes/ reports and vault artifacts. || EV: empty-vault SKILL.md:52-54 'notes → handed along with their MOC's artifacts as supplementary source material'. learn-hub digest-report SKILL.md:18-29 lists 'Two
  vault-keeper-12 [L] A Windows absolute path is hardcoded as the fallback location for the learn-hub repo. || EV: empty-vault SKILL.md:39 '(`C:\Users\User\Desktop\Learn` on the owner's machine)'.
  vault-keeper-13 [L] The reference file adds little because the body restates it, which undercuts the 'single source of truth' claim. || EV: SKILL.md:41-50 restates the tree, naming, collision rule and index determinism from vault-layout.md:10-32.
  vault-keeper-14 [L] The manifest is inconsistent with its sibling plugins, and bump.py has escaped its non-ASCII characters. || EV: plugin.json has no license; author is 'Thanawat Suharit' where plugin-creator and firecrawl use 'Thanawat Suharit (Micky)'; description contains '—'; there is n
  vault-keeper-15 [L] The evals cannot be graded: every assertions array is empty and no runner exists. Eval 3 relies on a kebab-to-MOC topic mapping the skill never specifies. || EV: evals.json in both skills: "assertions": [] for all 12 cases. empty-vault eval 3 prompt '/empty-vault panic-disorder' vs MOC file naming '<Topic> MOC.md'.
== micky-psych-tools/plugins/firecrawl [plugin v0.2.0] firecrawl(desc=1009,lines=367,fm=name,description)
  firecrawl-1 [H] The vendor guide described as kept verbatim is outdated in load-bearing places. Claude would skip a required install step, call a renamed diagnostic command, and try keyless REST calls the vendor no longer supports. There is no fetched-on date or version marker, so the drift cannot be detected. || EV: SKILL.md:146-147 'The build skills are already installed from the same command above. No separate install needed.' The vendor (2026-09-23) says 'Install build s
  firecrawl-2 [M] The install command the skill mandates adds a vendor skill also named `firecrawl`, with overlapping triggers, and the vendor's new research index overlaps the biomedical boundary this skill forbids. || EV: SKILL.md:33 `npx -y firecrawl-cli@latest init --all --browser`. The vendor SKILL.md frontmatter has 'name: firecrawl', and the vendor Path A says '`firecrawl` f
  firecrawl-3 [M] The skill writes the API key and scratch output into cwd without making sure they are gitignored. In this repo, .env is not ignored and README tells the user to `git add -A`, so the key can be committed despite the hygiene rule. || EV: SKILL.md:270 'echo "FIRECRAWL_API_KEY=fc-..." >> .env' and SKILL.md:55-57 'mkdir -p .firecrawl … -o .firecrawl/install-check.md'. .gitignore is '__pycache__/\n*
  firecrawl-4 [M] The body is large (367 lines, about 4.2k tokens) and loads in full on every trigger. Most of it is vendor material this marketplace rarely needs: the Path B app integration, the PKCE polling flow and WorkOS ID-JAG. The marketplace-specific behavior is the last 45 lines. The vendor guide should move to a reference file (or be fet || EV: SKILL.md: vendor sections at lines 19-338; 'Marketplace integration' and 'Credentials hygiene' at lines 340-384. Measured 16,859 body characters.
  firecrawl-5 [M] The docs call the body verbatim, but the changelog lists two local adaptations and the skill appends marketplace sections. No source version or date is recorded. || EV: README.md:9 'kept verbatim so it stays a faithful map' and CLAUDE.md:112 'kept verbatim', vs CHANGELOG.md 0.1.0 'Two local adaptations: the frontmatter descript
  firecrawl-6 [M] Some instructions cannot be executed as written: a browser flow in a headless container, and raw HTTP inside a fence labeled bash. || EV: SKILL.md:33 '--browser' ('It also opens browser auth'). SKILL.md:255-260 opens ```bash, then 'POST https://www.firecrawl.dev/api/auth/cli/status' and 'Content-T
  firecrawl-7 [L] The skill and its eval disagree on when to verify the install. The skill always verifies, which writes a file and spends a request; the eval verifies only when the CLI state is unknown. || EV: SKILL.md:52 'Before doing real work, verify the install:' vs evals.json id 1 'verifies with firecrawl --status only when the CLI state is unknown'.
  firecrawl-8 [L] Cross-plugin dependencies (intent-lock, vault-keeper) are undeclared, although CLI 2.1.280 recognizes a plugin.json `dependencies` field. || EV: plugin.json has no `dependencies`. A probe plugin.json containing 'dependencies' raised no unknown-field warning under `claude plugin validate --strict`, while 
  firecrawl-9 [L] The description's broad trigger and category are questionable: 'search the web' can capture ordinary web searches, and a web-data engine is filed under 'productivity'. || EV: SKILL.md:7 '"search the web"'. marketplace.json firecrawl entry has '"category": "productivity"'.
  firecrawl-10 [L] The evals cannot be graded (assertions are empty), and eval 6 locks in the outdated keyless-REST behavior. || EV: evals.json id 6 expected_output '(or the REST endpoints with no Authorization header)'.
== micky-psych-tools/scripts/validate.py [script vNone] 
  validate.py-1 [H] The script crashes with a traceback instead of reporting FAIL on malformed input, and evals success is printed without being counted by check(). || EV: validate.py:72-73 runs check(os.path.isfile(man_path)) and then an unconditional json.load(open(man_path)). The same pattern appears at :50-51 and :90-92. :111-
  validate.py-2 [M] The docstring and the README both overstate what the script covers relative to the CLI. The CLI does checks this script lacks (YAML parsing, hooks, unknown fields, duplicate plugin names, author), and it does check version parity, contrary to the README. || EV: validate.py:4 'Runs the checks `claude plugin validate` runs, plus…'. README.md:57-59 'The validator enforces things the CLI does not: … version parity'. Scratc
  validate.py-3 [M] The frontmatter parser is hand-rolled. It counts the quotes in a quoted description, needs LF line endings, cannot detect invalid YAML, and is copied verbatim into route.py. || EV: validate.py:41 keeps header.group(2).strip() with the quotes, so a quoted 1023-character description was reported as '1025 chars (hard cap 1024)' FAIL (reproduc
  validate.py-4 [M] The script rejects valid stdio MCP servers, ignores an auto-discovered .mcp.json that plugin.json does not declare, and never looks at hooks/hooks.json. || EV: validate.py:93 requires all('url' in s and 'type' in s …); a {command,args} server gets 'FAIL every mcp server has type and url' while `claude plugin validate -
  validate.py-5 [L] Checks that are mechanical and cheap are missing: leftover {{placeholders}}, the evals schema (skill_name matches, unique ids), CHANGELOG entry for the current version, README presence, plugin directories not in the catalog, ROUTING.md freshness, skill name format and length, and existence of cross-plugin or file references. || EV: None of these appear anywhere in validate.py (141 lines). CLAUDE.md:61-64 lists validate.py and route.py as the full health check.
  validate.py-6 [L] The 200-character minimum is a house heuristic but is reported as a hard failure. || EV: validate.py:110 check(n >= 200, 'min 200 for reliable triggering').
== micky-psych-tools/scripts/route.py + ROUTING.md [script + generated artifact vNone] 
  route.py / ROUTING.md-1 [M] ROUTING.md is a lossy, token-heavy copy of what Claude already routes on. Every cue is a verbatim substring of a skill or command description already in context, half are truncated, and all negative scope is dropped. CLAUDE.md still requires consulting it before any request. || EV: 20,197 bytes, 2,697 words, 168 lines ≈ 5,049 tokens (Quick index 6,356 B; Plugins section 13,410 B). 29 of 29 quick-index cues are verbatim substrings of descri
  route.py / ROUTING.md-2 [M] route.py ignores command-line arguments, so `--help` regenerates the file, a write triggered by a help flag. || EV: route.py:114-115 main() reads the catalog and writes OUT with no argv parsing. `python3 scripts/route.py --help` (run on a scratch copy) printed 'ROUTING.md reg
  route.py / ROUTING.md-3 [M] ROUTING.md embeds plugin versions, but bump.py never reruns route.py, and the documented workflow runs the router before the bump. Following CLAUDE.md therefore leaves pre-bump versions in ROUTING.md. || EV: route.py:145 f"### {p['name']}… _v{p['version']}_". bump.py:57 calls only validate.py. CLAUDE.md:55-57 has '4. Regenerate the router … 5. Bump with scripts/bump
  route.py / ROUTING.md-4 [L] Cue extraction is regex-based and fragile, and the constraints it places on descriptions are recorded only in the script's docstring. || EV: route.py:64-66 'description authors must phrase exclusions as "NOT for:", never "Do not use when…" … an abbreviation like "e.g. " … truncates early.'
  route.py / ROUTING.md-5 [L] The routing surface over-reaches: when nothing matches, it tells Claude to build a new plugin. || EV: CLAUDE.md:15 'If nothing fits, run `/new-plugin` to build one.' commands/route.md:24 'suggest `/new-plugin`'.
== micky-psych-tools/scripts/bump.py [script vNone] 
  bump.py-1 [M] It rewrites JSON with ASCII escapes and no trailing newline, which causes noisy diffs and hurts readability. || EV: bump.py:53 json.dump(man, open(man_path,'w'), indent=2) uses the ensure_ascii default and no newline. Seven plugin.json files now contain \u escapes (for exampl
  bump.py-2 [M] It updates only two of the version-bearing surfaces. It never touches the plugin CHANGELOG, the MEMORY.md versions table, ROUTING.md, or the catalog's own top-level version, which CLAUDE.md says must never be edited by hand but which no tool manages. || EV: bump.py:47-57 writes only man['version'] and entry['version']. marketplace.json:3 '"version": "1.16.0"'. CLAUDE.md:42 'NEVER edit a version by hand'. MEMORY.md 
  bump.py-3 [L] Its stated reason for version parity conflicts with what the CLI now reports: at install time plugin.json wins and the catalog entry version is ignored. Keeping them equal is still good hygiene. || EV: bump.py:4-8 'Claude Code compares the catalog version against what's installed, sees no change, and silently offers no update.' The CLI warning says 'At install
  bump.py-4 [L] There is no dry run and no rollback: files are written before validation, and a failing validation leaves them bumped. || EV: bump.py:53-57 writes, then subprocess.call(validate.py).
== micky-psych-tools/.claude-plugin/marketplace.json [catalog v1.16.0] 
  marketplace.json-1 [L] The plugin.json manifests and the catalog entries have drifted in description and keywords, and the author and license fields are inconsistent. This matters because ROUTING.md and the audit checklist read the catalog. || EV: 9 of 14 plugins have a plugin.json description different from the catalog's (intent-lock, plugin-creator, comprehensive-review, clinical-infographic, concept-an
  marketplace.json-2 [L] Optional metadata is missing: owner.email, $schema, and per-entry author, homepage, repository and license (gridgeist has these in its plugin.json only). There is no trailing newline. || EV: Top-level keys are ['name','version','description','owner','plugins']; owner is {'name': 'Thanawat Suharit (Micky)'}. Entry keys are name, source, version, desc
  marketplace.json-3 [L] The catalog's top-level version is bumped by hand, since no tool manages it. || EV: marketplace.json:3 '"version": "1.16.0"'; bump.py never touches mkt['version'].
== micky-psych-tools/README.md [doc vNone] 
  README.md (root)-1 [M] The recommended CLI validation command lacks --strict. Version drift and other warnings would pass with exit 0, and pointing the CLI at the marketplace never validates plugin contents. || EV: README.md:54 'claude plugin validate .claude-plugin/marketplace.json'. A scratch probe with an entry/plugin.json version mismatch returned '√ Validation passed 
  README.md (root)-2 [M] The README states that the validator enforces version parity 'the CLI does not'. That is false for CLI 2.1.280. || EV: README.md:57-59 vs the CLI warning 'plugins[0].version: Entry declares version "9.9.9" but plugins/a/.claude-plugin/plugin.json says "0.1.0"'.
  README.md (root)-3 [L] It says a GitHub-sourced marketplace must be public, but MEMORY.md records a private origin that is installed and in use. Private git repos work through existing git credentials. (This is likely stale; I did not verify it against the docs.) || EV: README.md:14-15 'a GitHub-sourced marketplace must be a **public** repo' vs MEMORY.md:10 'Remote: origin → https://github.com/safetymickky-ui/micky-psych-tools 
  README.md (root)-4 [L] It tells the user to run `git add -A` while .gitignore does not exclude .env, which the firecrawl skill writes the API key into. || EV: README.md:18; .gitignore holds only '__pycache__/ *.pyc .DS_Store'.
== micky-psych-tools/CLAUDE.md [doc (always-loaded project instructions) vNone] 
  CLAUDE.md (micky-psych-tools)-1 [M] Always-loaded instructions mandate a large amount of extra reading: MEMORY.md first (about 27k tokens) and ROUTING.md before any request (about 5k tokens). Roughly two thirds of CLAUDE.md itself is plugin summaries duplicated elsewhere. || EV: CLAUDE.md:7 'Read `MEMORY.md` first'; MEMORY.md is 108,178 bytes and 1,153 lines. CLAUDE.md:12-14 is the route-first mandate. The '## Plugins' section (lines 66
  CLAUDE.md (micky-psych-tools)-2 [M] The plugin list is stale: it leaves out gridgeist, which is in the catalog, the README and MEMORY.md. || EV: grep 'gridgeist' CLAUDE.md → no hits. marketplace.json:138 '"name": "gridgeist"', README.md:94, MEMORY.md:27.
  CLAUDE.md (micky-psych-tools)-3 [L] The workflow order leaves ROUTING.md with stale versions, because route runs before bump. The health check also omits `claude plugin validate --strict` on each plugin, which catches YAML, hooks and unknown-field errors that validate.py cannot. || EV: CLAUDE.md:54-57 (3 validate, 4 route, 5 bump); CLAUDE.md:61-64 lists only validate.py and route.py.
  CLAUDE.md (micky-psych-tools)-4 [L] Stale facts about firecrawl: the vendor now installs two segments by default, and the build segment needs `firecrawl setup build`. || EV: CLAUDE.md:112 'kept verbatim (one install command, three vendor skill segments, six usage paths A–F)'.
  CLAUDE.md (micky-psych-tools)-5 [L] The layout section omits components that the refine audit requires or that plugins ship: README.md, CHANGELOG.md, skills/<skill>/evals/, references/ and hooks/. || EV: CLAUDE.md:21-36 layout vs audit-checklist.md:67-69 and the evals/ and references/ directories present in the plugins.
#### inventory-learnhub-plugins.json :: learn-hub local marketplace plugins (catalog + digest-report, source-to-vault, vault-atomizer, vault-vectors, pk-plasma-animation)
  OBS: INSTALLATION: nothing loads these plugins. /home/user/learn-hub/.claude/settings.json has only hooks (SessionStart + two PreToolUse Bash gates), no extraKnownMarketplaces or enabledPlugins; .claude/settings.local.json does not exist; .claude/hooks/session-start.sh and launch.json contain no plugin/marketplace setup; README.md and CLAUDE.md contain no install instructions. The only install docs are `/plugin marketplace add .` + `/plugin install <x>@learn-hub-local` in the READMEs of the three clo
  OBS: The digest-report move made the pipeline hub unavailable by default: docs/digest-report-plugin-plan.md:67 says 'Delete `.claude/skills/digest-report/` in the same change' (done; `ls .claude/skills/digest-report` fails), yet always-loaded project skills route to it by name (ingest-infographic SKILL.md:45 'Run **`digest-report`** on the source report', ingest-animation SKILL.md:55), as do the cloned pubmed-research-note SKILL.md:280 and comprehensive-review SKILL.md:172. CLAUDE.md:355 likewise nam
  OBS: Plugins are repo-bound, not portable: none ships a script; every deterministic step calls learn-hub scripts by repo-relative path (scripts/split-note.mjs, voice-qc.mjs, vector-report.mjs, embed-missing.mjs, scripts/animations/pk-plasma/*.mjs, .claude/skills/atomize-book/scripts/measure-depth.py) and reads research-notes/, vault/, .env.local. Packaging them as plugins buys namespacing only, at the price of the install step above. A rewrite must choose one: (a) register the marketplace in project 
  OBS: source-to-vault is a superseded architecture. It was designed 2026-06-24 (docs/superpowers/plans/2026-06-24-source-to-vault-plugin.md, decision #1 'Direct to Supabase — skip markdown-file layer'; its tech stack still names the deprecated project tyxnedapxscpmytanloz). It writes production rows with no /vault file and bypasses the embed, mermaid prebake, image bake, duplication gate, repetition gate, links FK filtering and cache revalidation. Its triggers also collide with pdf-pipeline, atomize-b
  OBS: The 'atomize' collision from polish-plan P0-2 (docs/plugin-polish-plan.md:70-95) is still open. vault-atomizer triggers on 'atomize the vault' / 'break this chapter into atomic notes' and owns `/atomize`; atomize-book triggers on 'atomize this book'; pubmed-research-note uses 'atomize' as its digest gate word. atomize-book's NOT-for clause still sends 'PubMed/research-cited notes' to pubmed-research-note instead of digest-report. No ROUTING.md or router exists (P2-2).
  OBS: Report-format contract drift across the report→vault pipeline. The producers (pubmed-research-note SKILL.md:211-224, comprehensive-review SKILL.md:130-138) now forbid inline citations and emit a topic→DOI `## Sources` block. The consumer (digest-report SKILL.md:88-90, 115-116) requires keeping inline author-year+PMID citations and never mentions `## Sources`. pk-plasma-animation's research brief (pubmed-research-plan.md:91) prescribes `<title> (<year>)` Sources lines, which pubmed-research-note 
  OBS: The cross-repo contract with micky-psych-tools vault-keeper/empty-vault is broken on its side. It locates learn-hub as 'the directory containing `.claude/skills/digest-report/`' (empty-vault SKILL.md:36-37), which no longer exists, with a Windows fallback `C:\Users\User\Desktop\Learn` (:39). It deletes rendered HTML assets as non-transferable (:55-57), while learn-hub's ingest-infographic and ingest-animation descriptions expect empty-vault to hand those assets over. It also passes 'notes ... as
  OBS: Operational restatements of CLAUDE.md gotchas have drifted. Several skills re-describe sync mechanics instead of deferring to sync-vault. Examples: digest-report says '~40 min' for a whole-vault re-embed, but sync:apply is incremental (scripts/lib/sync-cache.mjs, apply-sync.mjs:234). digest-report uses PowerShell `Get-CimInstance` in a Linux cloud environment. vault-atomizer says vectors:missing is 'not optional' although apply-sync embeds changed notes (apply-sync.mjs:366-393, 558-559). vault-a
  OBS: Time-sensitive counts are stale everywhere. Measured now: 4,532 `type: note` files (grep -rl '^type: note' vault | wc -l), and `node scripts/split-note.mjs --list` reports '1681 split candidate(s) of 4532 notes' with a worst of 10,943 words (actinf-continuous-time-dynamical-systems). The skills say 2,979 notes / 291 candidates / 7,893 worst (vault-atomizer:9-10), ~2,400 notes / 2,434 present (vault-vectors:82, 137, 152), and ~350 notes missed by the glob (vault-vectors:142).
  OBS: No quality infrastructure exists. There are no evals in any of the 5 units. The validator proposed in polish-plan P1-1 (scripts/lib/plugin-manifest.mjs + test) does not exist. There is no bump script. The version question P1-2 (docs/plugin-polish-plan.md:140-149) is undecided: the catalog is versionless and all five plugin.json files are pinned at 0.1.0. `claude plugin validate` passes every plugin; the marketplace passes with one warning (no marketplace description).
  OBS: The MCP config is duplicated. pk-plasma-animation, pubmed-research-note and comprehensive-review each declare identical `pubmed` + `clinical-trials` HTTP servers in .mcp.json. This session also already has the claude.ai PubMed and Clinical_Trials connectors (mcp__PubMed__*, mcp__Clinical_Trials__*), so installing the plugins yields duplicate tool sets under different names.
  OBS: Cross-group note: .claude/skills/ingest-article/SKILL.md has a single-line description of 1,137 characters, over the 1,024 hard cap (measured with python regex on the frontmatter line). The group reviewing project skills should verify it.
  OBS: The PDF-reading premise differs between skills. source-to-vault reads PDFs with the Read tool's `pages` rendering (SKILL.md:43-46). pdf-pipeline SKILL.md:60-63 says that rendering needs poppler, which 'usually can't be installed here'. In this container `which pdftoppm pdftotext` returns nothing and `import pypdf` fails, so neither path works out of the box.
== learn-hub//home/user/learn-hub/.claude-plugin/marketplace.json [marketplace-catalog vnone — entries carry no version field (deliberate per docs/digest-report-plugin-plan.md:33,71; plugin.json versions are all 0.1.0)] 
  learn-hub-local-1 [H] The catalog is never registered or enabled for a session, so none of its plugins load by default. Always-loaded project skills and CLAUDE.md nevertheless route work to these plugins. || EV: .claude/settings.json contains only a "hooks" key; `cat .claude/settings.local.json` → no such file; grep for extraKnownMarketplaces/enabledPlugins/learn-hub-lo
  learn-hub-local-2 [M] Name collisions with micky-psych-tools: intent-lock, pubmed-research-note and comprehensive-review are cloned under identical names in a second marketplace. || EV: marketplace.json lists "intent-lock", "pubmed-research-note", "comprehensive-review"; /home/user/micky-psych-tools/plugins contains intent-lock, pubmed-research
  learn-hub-local-3 [M] Versioning is undecided. With versionless entries and every plugin.json pinned at 0.1.0 with no bump script, an installed copy is not refreshed when the source changes. micky-psych-tools CLAUDE.md names exactly this failure: 'Claude Code sees no version change and silently offers no update'. || EV: docs/plugin-polish-plan.md:140-149 'P1-2. Decide the version field question deliberately' (still open); all five target plugin.json files have "version": "0.1.0
  learn-hub-local-4 [L] No marketplace-level description (validator warning). || EV: `claude plugin validate .` → '‼ Found 1 warning: description: No marketplace description provided'.
  learn-hub-local-5 [L] The validator from polish-plan P1-1 was never built, so nothing tests name/dir/catalog agreement or the description length cap. || EV: `ls scripts/lib/plugin-manifest*` → No such file; docs/plugin-polish-plan.md:119-138.
  learn-hub-local-6 [L] Catalog descriptions drift from plugin.json descriptions (two sources of truth). || EV: source-to-vault catalog vs plugin.json:4 wording quoted in duplication_and_drift.
== learn-hub//home/user/learn-hub/plugins/digest-report [plugin v0.1.0] digest-report(desc=991,lines=203,fm=name,description)
  digest-report-1 [H] The input contract contradicts the current producers. The skill requires preserving inline author-year/PMID citations and never mentions the `## Sources` block that pubmed-research-note and comprehensive-review now emit instead. Per-claim provenance from current-format reports therefore has nowhere to go; the notes keep only the || EV: SKILL.md:88-90 'keep inline citations (author-year + PMID/DOI/NCT) next to the claims they support'; SKILL.md:115-116 'Attribution already lives in `sources:` +
  digest-report-2 [H] The skill is not loaded in a default session: its plugin is not enabled, and the .claude/skills copy was deleted when it was packaged. Every route that names it (ingest-infographic, ingest-animation, both report plugins, empty-vault) dead-ends. || EV: docs/digest-report-plugin-plan.md:67 'Delete `.claude/skills/digest-report/` in the same change'; `ls .claude/skills/digest-report` → No such file; .claude/sett
  digest-report-3 [M] Stale sync-cost claim that contradicts sync-vault's incremental content-hash cache. || EV: SKILL.md:165-167 'the apply pipeline re-parses and re-embeds the **whole vault** (~40 min at current size)'; .claude/skills/sync-vault/SKILL.md 'It is increment
  digest-report-4 [M] The README and the skill disagree on how to run voice QC. The README form fails with a usage error. || EV: README.md:38-39 'the produced notes must pass `npm run voice:qc -- --files …`'; scripts/voice-qc.mjs:36-39 `if (!worktree && !base) { console.error('usage: … (-
  digest-report-5 [M] Voice QC cannot run as written. `--base` needs the new files committed, but no step commits them. `--worktree` uses `git diff --name-only`, which omits untracked (brand-new) files, so it silently checks nothing for a fresh digest. The narration grep is deterministic prose that belongs in a script (scripts/lib/voice-qc.mjs alread || EV: SKILL.md:140-141 'it needs the files committed and a base ref'; no commit step between §5 and §7; scripts/voice-qc.mjs:43-44 `git('diff', '--name-only', '--', '
  digest-report-6 [M] The 'deterministic slug' rule is not what real digests produced, so the 'same id every run' guarantee is fiction. Idempotency rests entirely on step 2's DB provenance query. || EV: SKILL.md:130-132 'topic `id` = the report's kebab basename … note `id` = `<topic-slug>-<slugified note title>`'; actual digest of trazodone-novel-and-latest-evi
  digest-report-7 [M] 'Digest the pending reports' is defined two conflicting ways: the skill digests every un-landed report, while the command's no-arg form only surveys and forbids starting. || EV: SKILL.md:22-24 '"digest the pending reports" means: scan the directory, check each basename against DB provenance (step 2), and digest what has not landed'; com
  digest-report-8 [M] Windows-only process check in a skill that runs in the Linux cloud container. || EV: SKILL.md:173-176 'verify the node process actually died (`Get-CimInstance Win32_Process -Filter "Name='node.exe'"` filtered on `apply-sync`)'.
  digest-report-9 [L] The topic field list omits `source:` (book|research|review|digest, migration 0022), which decides the /books shelf. A review digested without it derives to 'research'. || EV: SKILL.md:72-75 lists id/type/title/domain/description/icon/color/order/status/created only; CLAUDE.md topic format has `source: book # optional — book | researc
  digest-report-10 [L] Missing authoring and safety rules that bite digests: escaping literal `$` for money (remark-math), fetch-first before sync, and the SessionStart-readiness check that vault-atomizer carries. || EV: No `\$` rule anywhere in SKILL.md although §4 invites `$...$` KaTeX (SKILL.md:92); vault-atomizer SKILL.md:42-50 and :160-167 contain fetch-first and hook check
  digest-report-11 [L] The provenance SQL is given without naming the tool or project ref. Cloud sessions need the Supabase MCP with project_id juvoohejxuuvwolmgoep. || EV: SKILL.md:44-48 'run the provenance query against the live DB' (no project); contrast vault-vectors SKILL.md:95 'project **`juvoohejxuuvwolmgoep`**'.
  digest-report-12 [L] The description sits near the hard cap and includes stale app-surface text ('graph'). || EV: Measured desc_chars=991 (cap 1024); SKILL.md:3 '…so the notes surface in the app (reader, graph, mastery, review)'; CLAUDE.md Phase 4 'a knowledge-graph view sh
== learn-hub//home/user/learn-hub/plugins/source-to-vault [plugin v0.1.0] ingest-source(desc=252,lines=157,fm=name,description)
  source-to-vault-1 [H] It writes production rows with no /vault source. That violates the repo's core pipeline (vault → sync), and the result is the documented 'DB rows can outlive their /vault source, and sync:apply can never re-derive them' trap: rows that restore and vault walks cannot see. || EV: SKILL.md:8-10 'Turn one source file … into atomic `topics` + `notes` + `links` rows in the `learn-hub` Supabase project'; SKILL.md:140-141 'Apply by passing the
  source-to-vault-2 [H] A no-arg run batch-ingests everything in Book/, the shared inbox where pdf-pipeline lands uploads and where source EPUBs of already-atomized books live. With the create-only auto-apply gate, a whole re-import under filename-derived slugs lands in production with no confirmation. || EV: SKILL.md:27-28 'With no path, process every `.pdf`/`.epub` under `Book/`'; commands/ingest.md:11; SKILL.md:128-129 'If everything is **create-only** … apply imm
  source-to-vault-3 [H] Routing collision with no negative scope: its triggers match atomize-book, pdf-pipeline and ingest-article requests. || EV: SKILL.md:3 'Use for "/ingest", "ingest this PDF/EPUB", "import notes from <file>", "turn this book into notes", or when a .pdf/.epub path is dropped'; atomize-b
  source-to-vault-4 [H] The note upsert omits every sync-owned derived column (embedding, diagrams, images). Ingested notes are invisible to similar_notes() and Explore, have no prebaked mermaid, no baked figures, and never pass the duplication or repetition gates. || EV: SKILL.md:116 column list `(id,topic_id,title,summary,body_md,"order",difficulty,tags,aliases,sources,estimated_min,created,updated)`; CLAUDE.md data model notes
  source-to-vault-5 [M] The links writes ignore the links.to_note FK (migration 0020) and the chapter-reference rule. A `[[topic-id]]` or any unknown wikilink target makes the insert fail. It also tells Claude to turn topic prerequisites into rows of a note→note table. || EV: SKILL.md:71-72 'use `[[note-id]]` in bodies … `prerequisites` on the topic become `prereq` edges'; SKILL.md:117-119 'insert into public.links (from_note,to_note
  source-to-vault-6 [M] It never busts the app cache, yet claims the content is visible immediately. getVaultContent/getNoteContent are unstable_cache'd under tag `notes` for up to 1h. || EV: SKILL.md:152-153 'The content now surfaces in the app (Home, Topic, Note, Graph, Search) with no further steps.'; CLAUDE.md 'Note content is cached — bust it af
  source-to-vault-7 [M] It reads a whole book into one context (20-page windows 'no page cap') with no chapter slicing, loss measurement, figure gate, depth gate, voice rule or order-band check. Every lesson atomize-book encodes is absent. || EV: SKILL.md:43-46 'Use the Read tool with the `pages` parameter in 20-page windows … until the document ends — **no page cap**'.
  source-to-vault-8 [L] Stale app surface list: the Graph page was removed and /search now redirects to /books. || EV: SKILL.md:153 '(Home, Topic, Note, Graph, Search)'; CLAUDE.md Phase 4 'a knowledge-graph view shipped here and was later removed'; '/search page (which 301-redir
  source-to-vault-9 [L] The schema pointer is stale: it cites the init migration as the column spec. || EV: SKILL.md:19 'Tables/columns: see `supabase/migrations/0001_init.sql`'; supabase/migrations runs 0001…0023.
  source-to-vault-10 [L] Three names for one thing (plugin source-to-vault, skill ingest-source, command /ingest). There is no README, CHANGELOG, keywords or evals. || EV: plugin.json:2 "source-to-vault"; SKILL.md:2 'name: ingest-source'; commands/ingest.md; `find plugins/source-to-vault -type f` → 3 files.
== learn-hub//home/user/learn-hub/plugins/vault-atomizer [plugin v0.1.0] vault-atomizer(desc=643,lines=207,fm=name,description)
  vault-atomizer-1 [M] The survey criterion now flags over a third of the vault, and it overlaps the depth gate's own target. atomize-book's depth floor demands ≥1,200 prose words with ≥3 sections, while this skill flags ≥1,500 words with ≥3 sections for splitting, so compliant notes become split candidates. The contract conflict (P1-6) is acknowledge || EV: `node scripts/split-note.mjs --list --limit 3` → '1681 split candidate(s) of 4532 notes (≥1500 words, ≥3 sections)'; SKILL.md:99-103 '`atomize-book`'s depth gat
  vault-atomizer-2 [M] Stale time-sensitive statistics in the opening argument. || EV: SKILL.md:9-10 'today **291 of 2,979 notes** are ≥1,500 words with ≥3 sections, and the worst is **7,893 words across 14 sections**'; measured now: 1,681 of 4,53
  vault-atomizer-3 [M] The sync runs in the foreground, chained with &&. That contradicts the repo rule (background job writing a log; a foreground run times out mid-embed and a fresh clone has no cache) and the sibling skills. || EV: SKILL.md:157 `npm run sync:apply && npm run revalidate && npm run vectors:missing`; digest-report SKILL.md:167-170 'as a **background job writing to a log file*
  vault-atomizer-4 [L] False premise that children arrive without vectors. apply-sync embeds every changed note it writes; vectors:missing covers only DB rows with no vault file. || EV: SKILL.md:169-171 '`vectors:missing` is not optional: children reach the DB with `embedding is null`'; scripts/apply-sync.mjs:366-393 embeds changed notes and :5
  vault-atomizer-5 [L] A stale UI reference: the graph view was removed. || EV: SKILL.md:105 '**`summary` is load-bearing** — it is the graph hover text and the hub's listing line.'
  vault-atomizer-6 [L] Garbled safety claim. || EV: SKILL.md:197-199 'If a split turns out wrong, the children are removable and the hub still holds the original text in its children' (the hub does not hold the m
  vault-atomizer-7 [L] 'Look at it in the app' is unactionable without a mechanism. Cloud sessions need the verify skill to mint a session. || EV: SKILL.md:196 'Do one, sync, look at it in the app, then decide.'
  vault-atomizer-8 [L] No README, CHANGELOG or evals. || EV: `find plugins/vault-atomizer -type f` → plugin.json, commands/atomize.md, skills/vault-atomizer/SKILL.md.
== learn-hub//home/user/learn-hub/plugins/vault-vectors [plugin v0.1.0] vault-vectors(desc=484,lines=149,fm=name,description)
  vault-vectors-1 [M] The command contradicts the skill on syncing: the command's default path syncs, while the skill says syncing is outside its remit and must be asked first. || EV: commands/vectors.md:10 'No flags — the **fast path**. Sync anything new, embed only the notes that have no vector'; SKILL.md:54-57 'They need `npm run sync:appl
  vault-vectors-2 [L] The command defines a --no-report flag the skill never handles. vectors:check always writes the report. || EV: commands/vectors.md:15 '`--no-report` — do the embedding work only; skip the corpus report.'; no mention of --no-report in SKILL.md; SKILL.md:50 'Writes `docs/n
  vault-vectors-3 [L] The 'read-only' check writes a git-tracked doc, dirtying the tree on every run. || EV: SKILL.md:50 'Read-only, ~20 s. Writes `docs/note-vector-report.md`'; `git ls-files docs/note-vector-report.md` → tracked.
  vault-vectors-4 [L] Stale corpus sizes and timings. || EV: SKILL.md:82 'At ~2,400 notes this takes **~15 minutes**'; :137 '2,400 notes x 384 floats'; :152 '"0 created, all 2,434 already present"'; :142 '~350 notes'; mea
  vault-vectors-5 [L] The timing promise conflicts with the skill. || EV: commands/vectors.md:11 'Normally under a minute.' vs SKILL.md:73 'First run downloads the ~130 MB model; expect a slow start'.
  vault-vectors-6 [L] The step 1 instruction to 'merge origin/master' is an unguarded git write on the user's branch. || EV: SKILL.md:42 'If you are behind, merge `origin/master` before going further.'
== learn-hub//home/user/learn-hub/plugins/pk-plasma-animation [plugin v0.1.0] pk-plasma-animation(desc=965,lines=129,fm=name,description)
  pk-plasma-animation-1 [M] The filing step asks Claude to hand-build a 'targeted upsert' that mirrors apply-sync's animation path, including the headless thumb bake and the audits. No script exists for it. This contradicts the README's 'filed through the normal sync' and bypasses auditAnimationLayout / auditAnimationSelfContained unless re-implemented by  || EV: SKILL.md:112-114 'in a fresh clone with no sync cache a full run re-processes every note, so prefer a targeted upsert of the one row that mirrors `apply-sync.mj
  pk-plasma-animation-2 [M] The research brief contradicts the skill it is handed to, with no precedence rule. The brief prescribes titles and years in `## Sources`; pubmed-research-note forbids both and counts them as a failure. The reference report follows the brief (titles, years, PubMed URLs). || EV: references/pubmed-research-plan.md:3 'The brief handed to `pubmed-research-note`'; :91 '## Sources  one line per source: `<technology> ★ · <title> (<year>) — <D
  pk-plasma-animation-3 [M] Step 1 hard-depends on sibling plugins that are not guaranteed to be installed, and gives no fallback if pubmed-research-note is absent. || EV: SKILL.md:51-54 'Run the `pubmed-research-note` skill (its engines, citation discipline and `research-notes/` filing)'; .claude/settings.json has no enabledPlugi
  pk-plasma-animation-4 [L] Eager, unconditional loading of every reference, including on the rebuild path. || EV: SKILL.md:15-22 'Read, before anything else:' + three references + 'The reference build: `scripts/animations/mph-plasma/` and `research-notes/methylphenidate-for
  pk-plasma-animation-5 [L] The 'web engine' named for labels is undefined in this repo. || EV: references/pubmed-research-plan.md:36-38 'every marketed product's FDA label … Fetch with the web engine'; no firecrawl plugin in .claude-plugin/marketplace.jso
  pk-plasma-animation-6 [L] The app check has no mechanism: auth is required, and the verify skill is not named. || EV: SKILL.md:117 '`npm run revalidate`; open `/animation/<id>` in the app at a phone and a desktop width.'
  pk-plasma-animation-7 [L] A file name the reference build does not use. The reference directory has pk-engine.test.mjs, not report-fidelity.test.mjs. || EV: SKILL.md:88-90 '`report-fidelity.test.mjs` — pin … exactly as `scripts/animations/mph-plasma/pk-engine.test.mjs` does'; `ls scripts/animations/mph-plasma` → for
  pk-plasma-animation-8 [L] Duplicate MCP server declarations, identical to two sibling plugins and to the session's built-in connectors. || EV: .mcp.json:3-10 `pubmed` / `clinical-trials` URLs identical to plugins/pubmed-research-note/.mcp.json and plugins/comprehensive-review/.mcp.json; session exposes
  pk-plasma-animation-9 [L] Author metadata drifts from siblings ('Thanawat Suharit (Micky)' plus a license field, versus 'Thanawat Suharit' and no license elsewhere). || EV: plugin.json:6 "name": "Thanawat Suharit (Micky)", :8 "license": "MIT"; digest-report plugin.json:5 { "name": "Thanawat Suharit" }.
#### inventory-learnhub-project-skills.json :: learn-hub project skills — /home/user/learn-hub/.claude/skills (atomize-book, ingest-article, ingest-slides, ingest-infographic, ingest-animation, pdf
  OBS: DESCRIPTION BUDGET (measured with a YAML parser, chars of the description value): atomize-book 1075, ingest-article 1137, ingest-slides 1150, ingest-infographic 933, ingest-animation 963, pdf-pipeline 934, sync-vault 252, vault-coverage 659, check-repetition 387, verify 236. Total for these 10 = 7,726 chars (~1.9k tokens) always loaded; with concept-animation (1000) the directory's total is 8,726 (~2.2k tokens). Three exceed the 1024-char spec cap: atomize-book, ingest-article, ingest-slides. Fo
  OBS: ALWAYS-LOADED CONTEXT: learn-hub/CLAUDE.md is 3,126 lines / 268,390 chars (~67k tokens). Its Gotchas section alone runs lines 817–2997, with 136 top-level gotchas, and 58 of those headings are pipeline/import topics (figures, extraction, sync, order, wikilinks, similarity). That material belongs in the import skills' references, loaded on demand. CLAUDE.md names these skills: sync-vault 13×, atomize-book 8×, ingest-article 6×, verify 4×, ingest-infographic 1×, vault-coverage 1×, the rest 0×.
  OBS: GOTCHA DRIFT BETWEEN CLAUDE.md AND THE SKILLS. It runs in both directions. (a) CLAUDE.md is newer and the skill is stale: CLAUDE.md:1743 says 'sync:apply is incremental via a content-hash cache', but atomize-book SKILL.md:1116-1117 and 1265-1267 and ingest-article SKILL.md:314-316 still say it re-embeds the whole vault from scratch (~40 min). CLAUDE.md:2588 says body [[topic-id]] is a supported chapter reference and forbids repointing it at a note, but atomize-book SKILL.md:1027-1029 says it 404
  OBS: SHARED SYNC/REVALIDATE/VERIFY TAIL: restated in 8 places (atomize-book §9, ingest-article §7, ingest-slides §7, ingest-infographic §4-5, ingest-animation §4-5, pdf-pipeline §4-6 plus preflight-and-apply.md and surface-checklist.md, sync-vault, check-repetition 'Apply fixes'). The copies give four mutually contradictory apply rules: (1) 'sync.sql ≲200KB → MCP execute_sql, else sync:apply' (sync-vault:32-34, pdf-pipeline:136-141, surface-checklist:13); this can never be met, because pdf-pipeline:1
  OBS: ROUTING OVERLAP between pdf-pipeline, ingest-article, ingest-slides and atomize-book. pdf-pipeline is the declared front door for any PDF. It routes article→ingest-article, book→atomize-book, and report/extract→'atomize-book (mini)'. It explicitly sends SLIDE DECKS to atomize-book mini (SKILL.md:98; routing.md:42,60) and never mentions ingest-slides, which claims slide decks→ARTICLES. So two skills give opposite answers for the same input. ingest-article triggers on any 'drops a PDF and says ing
  OBS: WINDOWS vs CLOUD PATHS. Windows-local: ingest-article hardcodes `C:\Users\User\Desktop\Learn\Raw Article PDF` (description plus SKILL.md:49). vault-coverage hardcodes `BOOK_ROOT="C:/Users/User/Desktop/Learn"` (SKILL.md:40). extract_pdf.py:75 carries a cp1252 console workaround. The CLAUDE.md sync gotcha uses PowerShell `Get-CimInstance`. docs/handoff-vault-embeddings.md:43 confirms `C:/Users/User/Desktop/Learn` is the main checkout, so the ingest-article inbox is INSIDE the repo, and .gitignore 
  OBS: PDF TOOLING IS ABSENT in the cloud environment: `import fitz`, `import pypdf` and `import pdfplumber` all raise ModuleNotFoundError, and pdftotext/pdftoppm are not on PATH. There is no requirements file, and the SessionStart hook does not install them. Every PDF path in the group depends on PyMuPDF with no install step: extract-figures.py --pdf, atomize-book §1 get_toc, ingest-article's fitz snippets, and both ingest-slides scripts. render_slides.py:15 even asserts 'already available in this rep
  OBS: SCRIPT COUPLING: ingest-article calls `.claude/skills/atomize-book/scripts/{extract-figures,check-figures,measure-loss}.py` by path (SKILL.md:80,256,290). ingest-slides and ingest-article read each other's and pdf-pipeline's reference files by relative path (ingest-slides:28-29,123; ingest-article:62). Rename or plugin-ize atomize-book and the article pipeline breaks. The shared figure/loss tooling needs a neutral home. The repo already has a deterministic validator that no skill uses: scripts/c
  OBS: CROSS-REPO HANDOFF DRIFT: ingest-infographic and ingest-animation both advertise a handshake 'the micky-psych-tools empty-vault skill waits for before it deletes the source asset'. empty-vault never calls either one: micky-psych-tools/plugins/vault-keeper/skills/empty-vault/SKILL.md:55-57 says assets 'cannot become Learn notes. They are removed with their topic'. empty-vault also finds the Learn repo by looking for `.claude/skills/digest-report/`, but digest-report now lives at learn-hub/plugins
  OBS: STALE COUNTS: the vault now has 4,532 type:note files and 851 _topic.md files (measured). The skills still cite '2,566 notes' (atomize-book:1117), '2,835 files vs 2,746 notes today' (atomize-book:1142-1143), '~2,700 notes' (ingest-article:315), '~2,400 notes' (sync-vault:113), '4 seed files' (ingest-article:144; 36 articles exist) and '5.8 MB dump' (pdf-pipeline:150). coverage-sources.json maps 26 books against 70 distinct `book:` strings in the vault.
  OBS: TESTS/EVALS: no unit has trigger or behaviour evals. atomize-book's Python helpers do have 354 stdlib unittest cases, all passing (`python3 -m unittest discover -s .claude/skills/atomize-book/scripts -p 'test_*.py'` → 'Ran 354 tests ... OK'), but `npm test` does not run them.
  OBS: COMMANDS: none of these units ships a commands/ directory. As project skills they are invoked as /<skill-name>, so there are no thin-wrapper or command-contradiction issues. Skills do cross-reference each other as slash commands (pdf-pipeline: `/ingest-article`, `/atomize-book`, `/sync-vault`).
== learn-hub//home/user/learn-hub/.claude/skills/atomize-book [project-skill vunversioned project skill (last SKILL.md commit 2026-09-20, 89fcb73)] atomize-book(desc=1075,lines=1256,fm=name,description)
  atomize-book-1 [H] The skill contradicts itself, its own qc-gate.py and CLAUDE.md on whether a body [[topic-id]] wikilink is valid. It says both that it is not a 404 and that it 404s, and it tells agents to repoint these links at a note, which CLAUDE.md explicitly forbids. || EV: SKILL.md:295-296 'It is not a 404 (a topic target routes to `/topic/<id>` by design)' vs SKILL.md:1027-1029 'it renders as `/note/<topic-id>`, which 404s'; SKIL
  atomize-book-2 [H] Stale sync economics. The skill says sync:apply re-embeds the whole vault from scratch, but it is incremental via a content-hash cache. Step 9 also tells Claude to run `npm run sync` and then, a few lines later, to kill it. || EV: SKILL.md:1116-1117 '`sync` and `sync:apply` each re-embed the WHOLE vault. At 2,566 notes that is ~40 minutes'; SKILL.md:1265-1267 'both re-parse the vault, re-
  atomize-book-3 [H] Four drafting rules that CLAUDE.md says must live in the drafting spec are absent from the skill and both references: literal `\$` money escaping, detection of equation-as-PNG EPUBs, the stadium-node `([...])` ban, and adding the new book to the LIVE_GROUPS shelf fixture. Each covers a documented silent failure that every gate p || EV: grep for '\$', 'EQ:', 'stadium', 'LIVE_GROUPS', 'book-category' in atomize-book/*.md returns 0 hits; CLAUDE.md:2489 'the book-level rule belongs in the drafting
  atomize-book-4 [H] Over-long body: 1,256 lines, ~21.3k tokens loaded on every trigger. Roughly 60% is reference-grade material (figure pipeline §5.0–5c ~7.3k tok, measurement interpretation §6 ~2.9k, traps ~2.2k) that could load on demand, and much of it is historical narrative ('Measured on ...'). The 'Hard-won traps' section largely restates the || EV: Measured: body 85,280 chars / 13,466 words. Traps duplicating body: SKILL.md:1181-1186 (thin notes) vs 267-278; 1187-1192 (order) vs 3b 209-241; 1193-1211 (numb
  atomize-book-5 [M] The QC <details> bash 'equivalent' still builds its id set from every .md file, including vault/articles. That reintroduces the article-id false pass qc-gate.py was fixed for (resolving ids by type). It duplicates a script that exists, and it is more wrong than the script. || EV: SKILL.md:1051 `find vault -name '*.md' -exec grep -h "^id:" {} + ... > /tmp/allids.txt`; CLAUDE.md gotcha 'An ARTICLE id is not a wikilink target either ... qc-
  atomize-book-6 [M] The prefix-collision check uses a one-level glob that silently skips the nested books. That is the exact failure the same step documents two paragraphs later. || EV: SKILL.md:80 'Check it's free: `grep -rh "^id:" vault/*/_topic.md | sort`' vs SKILL.md:90-91 'its one-level `vault/*/_topic.md` glob silently skipped the nested 
  atomize-book-7 [M] Mermaid validation guidance contradicts ingest-article and ignores the repo's deterministic checker. atomize-book mandates the validator MCP, ingest-article forbids it, and scripts/check-mermaid.mjs (the authoritative renderer-based check) is referenced by no skill. || EV: SKILL.md:443-444 '**Validate every diagram** with the mermaid validator MCP'; drafting-agent.md:172 'Validate each via the mermaid validator MCP'; ingest-articl
  atomize-book-8 [M] note-format.md holds up a TOPIC id as the example of an existing note to wikilink. That is exactly the topic-as-note bridge error the skill warns about. || EV: note-format.md:263-265 'wikilink the existing note (e.g. `aim-neurodegenerative`, `cp-methods-neural-networks`)'; `grep -rl '^id: aim-neurodegenerative$' vault`
  atomize-book-9 [M] Scale guidance contradicts the depth contract. The opener sizes a 16-chapter book at ~20 notes, while the depth rules (≥1200 prose words per note, count notes from concepts) produce far more. || EV: SKILL.md:27 'a 16-chapter book → ~9 topics, ~20 notes, ~20 subagents' vs SKILL.md:267-277 and note-format.md:205 '≥ 1200 prose words'
  atomize-book-10 [M] Deterministic checks are written as prose or ad-hoc shell instead of scripts. Examples: the Calibre figure-markup sanity check, the topic-id bridge-list resolution, the section-heading dump for split chapters (redundant with check-manifest.py), the PDF bookmark TOC dump, duplicated page-run detection, and the five-times-repeated || EV: SKILL.md:406-407 (grep -c '<img' vs caption grep), 301-302 (find/sed topic ids), 327 (`grep -n '^#\|^[A-Z][a-z].*$'`), 129-134 (fitz get_toc snippet); CLAUDE.md
  atomize-book-11 [M] The PDF route is not executable in the cloud environment. PyMuPDF is required but not installed, and the skill gives no install step. || EV: SKILL.md:129-131 `import fitz`; extract-figures.py --pdf; `python3 -c 'import fitz'` → ModuleNotFoundError: No module named 'fitz'
  atomize-book-12 [L] Section numbering is out of order (5.0, 5.1, 5.2, 5d, 5a, 5b, 5c), which makes the cross-references ('see 5a', '§5b') hard to follow. || EV: headings at SKILL.md:340 (5.0), 424 (5.1), 493 (5.2), 571 (5d), 614 (5a), 681 (5b), 738 (5c)
  atomize-book-13 [L] The description is 1,075 chars, over the 1024-char cap, and includes a trigger that belongs to sync-vault. || EV: SKILL.md:3-17; SKILL.md:15 '"re-sync the vault"'
  atomize-book-14 [L] Time-sensitive counts are stated as current facts. || EV: SKILL.md:1142-1143 '2,835 files vs 2,746 notes today'; measured today: 4,532 type:note files, 851 _topic.md
== learn-hub//home/user/learn-hub/.claude/skills/ingest-article [project-skill vunversioned project skill (last commit 2026-08-21, 5eea16f)] ingest-article(desc=1137,lines=374,fm=name,description)
  ingest-article-1 [H] The watched-folder inbox hardcodes a Windows absolute path, then operates on it with repo-relative paths. The folder is inside the repo but not gitignored. A bare invocation ('invokes the skill with no explicit PDF') triggers ingest-and-delete of every PDF there. || EV: SKILL.md:3 and :49 '`C:\Users\User\Desktop\Learn\Raw Article PDF` is the drop folder'; :51 '(`Glob` `Raw Article PDF/*.pdf`)'; :348 '`rm "Raw Article PDF/<file>
  ingest-article-2 [H] The documented semantic-floor command cannot run. measure-source-cover.mjs only accepts `ch*.txt` files (the article dump is source.txt), and it recurses --notes with readdirSync, which throws on a file path. || EV: figures-and-loss.md:455-458 `npm run book:source-cover -- --chapters "scripts/.out/articles/<slug>" --notes "vault/articles/<slug>.md"`; scripts/measure-source-
  ingest-article-3 [M] The reference contradicts its own prose and SKILL.md on required measure-loss flags, and keeps a manual recomputation recipe the flags have superseded. || EV: figures-and-loss.md:354-360 command has only `--top 150 --keep-frontmatter --figures --json`, while :370 says '**`--strip-references` is REQUIRED here**' and :3
  ingest-article-4 [M] Stale sync-cost claim: sync:apply is incremental. || EV: SKILL.md:314-316 'Each command independently re-parses, re-prebakes every mermaid fence and re-embeds the WHOLE vault — ~40 minutes at 2,700 notes'; CLAUDE.md:1
  ingest-article-5 [M] Its mermaid-validation rule contradicts atomize-book and pdf-pipeline. It reinvents, as an inline node snippet, what scripts/check-mermaid.mjs already does. || EV: SKILL.md:174 'Validate mermaid with the repo's OWN renderer, not the validator MCP' plus the snippet at 181-191; atomize-book SKILL.md:443-444 says use the MCP;
  ingest-article-6 [M] The no-.env.local fallback hand-extracts SQL from the whole-vault dump and writes diagrams `{}`. That bypasses the sync:apply path and the 'omit, never write {}' bake-column rule. On a re-ingest of an existing article it would wipe baked SVGs. || EV: SKILL.md:337-341 'slimming the prebaked-SVG `diagrams` blob to `{}`'; pdf-pipeline preflight-and-apply.md:71-74; CLAUDE.md gotcha 'A failed BAKE must omit its c
  ingest-article-7 [M] Loads roughly 18-19k tokens every run: the ~6.7k body plus ~12k of mandatory references, including a 32.5k-char figures file that largely duplicates atomize-book. || EV: SKILL.md:28 'read all three before authoring'; measured sizes 26,626 + 7,303 + 32,508 + 8,502 chars
  ingest-article-8 [M] Cross-skill script coupling by path. The PDF paths also need PyMuPDF, which the cloud env lacks. || EV: SKILL.md:80 `python .claude/skills/atomize-book/scripts/extract-figures.py`; SKILL.md:112-115 `import fitz`; `python3 -c 'import fitz'` → ModuleNotFoundError
  ingest-article-9 [L] Stale count of seed files. || EV: SKILL.md:144 'See the 4 seed files in `/vault/articles/`'; `ls vault/articles | wc -l` → 36
  ingest-article-10 [L] Description over the cap, invalid strict YAML, and a trigger collision. || EV: desc 1137 chars; YAML error at col 445 'Loss is MEASURED, not assumed: every figure'; trigger '"bullet-reconstruct this"' also matches anthropic-skills:bullet-r
== learn-hub//home/user/learn-hub/.claude/skills/ingest-slides [project-skill vunversioned project skill (last commit 2026-08-20)] ingest-slides(desc=1150,lines=124,fm=name,description)
  ingest-slides-1 [H] The script paths are wrong as written. From the repo root, `python scripts/extract_pdf.py` hits learn-hub/scripts/, which has no such files. || EV: SKILL.md:62 `python scripts/extract_pdf.py "<folder-or-pdf>"`, :71 `python scripts/render_slides.py`; `ls scripts/extract_pdf.py scripts/render_slides.py` in th
  ingest-slides-2 [H] Routing conflict with the declared PDF front door: pdf-pipeline sends slide decks to atomize-book (mini) and does not know this skill exists. || EV: pdf-pipeline SKILL.md:98 '...or slide deck ... → report/extract → atomize-book (mini)'; pdf-pipeline routing.md:60 '**A slide deck** → usually **report/extract*
  ingest-slides-3 [M] The script docstring claims PyMuPDF is 'already available'. It is not in the cloud env, and no extractor engine is installed, so both scripts fail there. || EV: render_slides.py:15 'Requires PyMuPDF (`import fitz`), already available in this repo.'; `python3 -c 'import fitz'` / `import pdfplumber` → ModuleNotFoundError;
  ingest-slides-4 [M] The sync tail contradicts ingest-article, and the cross-reference to ingest-article points at the wrong step. || EV: SKILL.md:114 'Same three-command tail as `ingest-article` — ... 1. `npm run sync`' vs ingest-article SKILL.md:313 'Skip the `npm run sync` dry run'; SKILL.md:12
  ingest-slides-5 [M] It claims to reuse ingest-article 'wholesale' but applies none of its gates: no figure manifest or check-figures, and no measure-loss. A slide's chart or table can therefore drop silently. || EV: SKILL.md:22-24 'It reuses `ingest-article`'s article schema and its sync/verify tail wholesale'; no check-figures/measure-loss steps anywhere in SKILL.md
  ingest-slides-6 [L] Help text drifted from the code: the default output dir is named `_grill_text` in help but `_slide_text` in code. The render/text dumps default into the user's deck folder, and neither PPT/ nor the dumps are gitignored. || EV: extract_pdf.py:85 'default: <folder>/_grill_text' vs :95 `os.path.join(base or ".", "_slide_text")`; render_slides.py:58 default `<pdf-dir>/_slides`
  ingest-slides-7 [L] The description exceeds 1024 chars. || EV: desc 1150 chars
== learn-hub//home/user/learn-hub/.claude/skills/ingest-infographic [project-skill vunversioned project skill (last commit 2026-08-20)] ingest-infographic(desc=933,lines=148,fm=name,description)
  ingest-infographic-1 [H] The description and the handshake section build on a producer and caller contract that no longer exists. empty-vault removes assets and never invokes this skill, so the trigger is dead and the 'handshake' has no listener. || EV: SKILL.md:3 'or when the micky-psych-tools empty-vault skill hands over an infographic asset'; SKILL.md:137-138 'This is the handshake the `empty-vault` skill (m
  ingest-infographic-2 [H] Wrong claim about the sync. `npm run sync` writes no rows, so a 'targeted upsert' is not equivalent to it. || EV: SKILL.md:116-118 'a targeted upsert is equivalent to a full `npm run sync` (both idempotent by `id`)'; ingest-article SKILL.md:306 '`npm run sync` alone does NO
  ingest-infographic-3 [M] Internal contradiction on the permitted edits. || EV: SKILL.md:17-18 'The only edits allowed are the mechanical **light-lock** normalisation in step 3' vs SKILL.md:79 'Two **mechanical** normalisations are permitte
  ingest-infographic-4 [M] Stale app route: the Infographics gallery at /infographic now redirects to the merged Visualizations gallery. || EV: SKILL.md:11 'the app's **Infographics** gallery (`/infographic`)', :133 'cached `/infographic` gallery projection'; CLAUDE.md:615 '`/infographic` 301-redirects 
  ingest-infographic-5 [M] A missing topic sends Claude to a skill that is not guaranteed loaded. || EV: SKILL.md:45 'Run **`digest-report`** on the source report FIRST'; digest-report exists only at learn-hub/plugins/digest-report and is absent from this session's
  ingest-infographic-6 [L] Invalid strict YAML frontmatter. || EV: YAML ScannerError at col 296: '...files a `type: infographic` sidec...'
== learn-hub//home/user/learn-hub/.claude/skills/ingest-animation [project-skill vunversioned project skill (last commit 2026-08-20)] ingest-animation(desc=963,lines=135,fm=name,description)
  ingest-animation-1 [H] Same dead producer and caller contract as ingest-infographic: empty-vault never hands animations over and waits for no handshake. || EV: SKILL.md:3 'or when the micky-psych-tools empty-vault skill hands over an animation asset'; SKILL.md:123-125 'the handshake the `empty-vault` skill ... waits fo
  ingest-animation-2 [M] The sanity check misses the two animation defects CLAUDE.md documents as universal: the stage-collapse layout (100dvh) and the doctype/color-scheme quirks mode. 'Make no other change' conflicts with CLAUDE.md's 'fix at source when each is next touched'. || EV: SKILL.md:88-98 checks only self-contained and reduced-motion, then 'Make no other change to the document.'; CLAUDE.md gotchas 'A fixed `height:100dvh` cannot ma
  ingest-animation-3 [M] A missing topic requires a skill that is not guaranteed to be installed. || EV: SKILL.md:54-55 'run `digest-report` on the source report'
  ingest-animation-4 [L] Invalid strict YAML frontmatter. || EV: YAML ScannerError at col 320: '...files a `type: animation` sidecar...'
== learn-hub//home/user/learn-hub/.claude/skills/pdf-pipeline [project-skill vunversioned project skill (last commit 2026-08-21, 5eea16f)] pdf-pipeline(desc=934,lines=192,fm=name,description)
  pdf-pipeline-1 [H] Slide decks are routed to atomize-book (mini), contradicting ingest-slides, which owns them and turns them into articles. The router does not mention ingest-slides. || EV: SKILL.md:98 'A long report, guideline, review, thesis chapter, or slide deck ... → **report/extract** → `atomize-book` (mini)'; routing.md:42 and :60 '**A slide
  pdf-pipeline-2 [H] The apply-path rule cannot be met and contradicts the sub-skills. It says run `npm run sync` and apply through MCP if sync.sql is ≲200KB, while the same file says the dump is 5.8 MB, and atomize-book/ingest-article say not to run the sync dry run at all. || EV: SKILL.md:138-141 '≲ ~200 KB → apply via Supabase MCP execute_sql'; SKILL.md:150 'you don't need the whole 5.8 MB dump'; surface-checklist.md:13; ingest-article 
  pdf-pipeline-3 [M] Wrong vault path for books: atomize-book writes flat `vault/<topic-id>/`, and CLAUDE.md treats the nested layout as the trap. || EV: SKILL.md:36 '/atomize-book → /vault/<book>/<chapter>/*.md'; routing.md:13 same; atomize-book note-format.md:12 '`vault/<topic-id>/_topic.md`'; CLAUDE.md:2697 'T
  pdf-pipeline-4 [M] Verification steps target UI that no longer exists or has changed. || EV: SKILL.md:179-180 'the **graph** (new book/chapter hubs + edges)' vs CLAUDE.md:813 'a knowledge-graph view shipped here and was later removed'; surface-checklist
  pdf-pipeline-5 [M] The preflight premise is stale for cloud sessions. || EV: SKILL.md:59 'A remote/web session starts with **no `node_modules`, no `.env.local`**'; CLAUDE.md 'The SessionStart hook ... installs deps ... and materializes t
  pdf-pipeline-6 [M] surface-checklist restricts mermaid types that atomize-book allows, and tells Claude to re-run `npm run sync`, which does not write the DB, to fix a blank diagram. || EV: surface-checklist.md:42-43 '(`flowchart`/`mindmap` only ...) and re-run `npm run sync`' vs atomize-book SKILL.md:446-448 allowing sequenceDiagram/stateDiagram-v
  pdf-pipeline-7 [L] The id-prefix check in routing.md uses a one-level glob that misses nested books. || EV: routing.md:82 '`grep -rh "^id:" vault/*/_topic.md`'
  pdf-pipeline-8 [L] Routing is written as prose tables, although a deterministic pre-classifier (DOI/abstract/TOC/ISBN/page-count/outline probe) could print a suggested route and leave only edge cases to judgement. || EV: routing.md:20-45 signal checklist; SKILL.md:94-98
== learn-hub//home/user/learn-hub/.claude/skills/sync-vault [project-skill vunversioned project skill (last commit 2026-08-20)] sync-vault(desc=252,lines=155,fm=name,description)
  sync-vault-1 [H] Recommends the nested `vault/<book-slug>/<chapter-topic-id>/` layout that CLAUDE.md documents as the trap that silently broke five tools. atomize-book writes the flat layout. || EV: SKILL.md:126-132 'The recommended layout groups a book's chapters under one folder ... vault/<book-slug>/<chapter-topic-id>/_topic.md'; CLAUDE.md:2697 'The vaul
  sync-vault-2 [H] The default step path (1–3: npm run sync, read sync.sql, apply through MCP) only applies to a vault ≲200KB, which this vault (4,532 notes) never is. Step 1 triggers a full re-render and re-embed with no warning, and none of the operational traps for running the sync is present. || EV: SKILL.md:32-34 '`sync.sql` ≲ ~200 KB → MCP path'; SKILL.md:65-72 step 1 `npm run sync`; no mention of background/log/EXIT=0/`| tail`/PUPPETEER/`git fetch` anywh
  sync-vault-3 [M] The verification step compares whole-table counts against parser counts, which cannot match: the project is shared, and DB rows outlive their vault files. || EV: SKILL.md:81-88 'Confirm the counts match what the parser reported'; CLAUDE.md:2894 '13 live `notes` rows with no `/vault` file'; atomize-book SKILL.md:1153-1154
  sync-vault-4 [M] The deletion guidance ignores the purge:hidden workflow and the progress/activity cascade. Deleting a note row via execute_sql destroys mastery history unless merge_note_history runs first. || EV: SKILL.md:157-158 'To remove content, delete the row explicitly via `execute_sql`'; CLAUDE.md 'A merge deletes study history unless `merge_note_history` runs FIR
  sync-vault-5 [L] Stale inventory: it lists 3 tables although the sync writes 6 types, and the note count is out of date. || EV: SKILL.md:18 'Tables: `topics`, `notes`, `links`'; SKILL.md:113 're-embeds ~2,400 notes'
== learn-hub//home/user/learn-hub/.claude/skills/vault-coverage [project-skill vunversioned project skill (last commit 2026-08-20)] vault-coverage(desc=659,lines=122,fm=name,description)
  vault-coverage-1 [M] The source map is badly behind the vault. Most books (44 of 70) will report 'not in the source map', and no import skill says to add entries. || EV: coverage-sources.json `sources` length 26; `find vault -name _topic.md -exec grep -h '^book:'` distinct values = 70; SKILL.md:31-33 'A new book that is not in t
  vault-coverage-2 [M] A hardcoded Windows path in the primary command, and the skill cannot run in the cloud environment, where no sources exist. || EV: SKILL.md:40 `BOOK_ROOT="C:/Users/User/Desktop/Learn" node scripts/scrape/coverage-baseline.mjs`; `ls Book 'raw book'` → no such file
  vault-coverage-3 [L] Time-sensitive 'last run' status presented as current. || EV: SKILL.md:69 'Last run: **all four passed**'; findings dated 2026-07-28 at 2290 notes
== learn-hub//home/user/learn-hub/.claude/skills/check-repetition [project-skill vunversioned project skill (last commit 2026-08-20)] check-repetition(desc=387,lines=97,fm=name,description)
  check-repetition-1 [L] Missing negative scope against sibling redundancy tools, so routing between check-repetition, similarity:detect and atomize-book §7b is left to chance. || EV: description has no NOT clause; atomize-book SKILL.md:956-980 uses measure-redundancy.py + detect-duplicates.mjs and never references `repetition:check`
  check-repetition-2 [L] The revalidation mechanism is inconsistent with the rest of the group. || EV: SKILL.md:88 '`POST /api/revalidate` with `{ secret, tag: "notes" }`' vs `npm run revalidate` in sync-vault/atomize-book/ingest-article
== learn-hub//home/user/learn-hub/.claude/skills/verify [project-skill vunversioned project skill (last commit 2026-09-20, 51d96bc)] verify(desc=236,lines=60,fm=name,description)
  verify-1 [M] It references working scripts that do not exist in the skill. They lived in an ephemeral session scratchpad, so every run must re-implement magic-link minting, cookie chunking and the puppeteer setup from prose. That is deterministic code that should be bundled as scripts. || EV: SKILL.md:41-43 'Full working scripts from the last run: see the session scratchpad pattern below'; skill directory contains only SKILL.md
  verify-2 [L] The description has no trigger phrases or negative scope, and the body hardcodes cloud-container paths. || EV: desc 236 chars; SKILL.md:42 `/home/user/learn-hub/node_modules`, :53 `/opt/pw-browsers/chromium`
#### inventory-renderers.json :: HTML renderers / visual output plugins (clinical-infographic, concept-animation, ml-concept-lab, code-explainer, gridgeist, + learn-hub concept-animat
  OBS: SHARED TAIL BOILERPLATE, MEASURED: the four in-house renderers (CI, CA, ML, CE) use the same section skeleton: Prime directive, Step 0 gate, Step 1-3, Step 2.5/3.5 Render & verify, Step 3/4 'Where output goes', Handoffs, Close, Failure conditions. The shared tail sections (render/verify + output + handoffs + close + failure conditions) take 67/177 body lines in CI (38%), 63/152 in CA (41%), 79/225 in ML (35%) and 56/162 in CE (35%). That is 265 lines of near-duplicate procedure in total. Exactly
  OBS: RULE RECURRENCE MATRIX (CI / CA / ML / CE / GG / learn-hub CA). (R1) Self-contained, meaning no external CSS/JS/fonts/images/CDN: stated in CI SKILL:109-110,182-183, design-system:196-204 and README:47; CA SKILL:122-123,195 and grammar:55-56; ML SKILL:176-179,275-276; CE SKILL:138-139,207, contract:123-125 and shell:110; learn-hub CA. That is 5 of 6 units, 2–4 times each. (R2) prefers-reduced-motion is mandatory in CA, ML, CE and learn-hub CA; GG mentions it; not applicable to CI. (R3) Colour ne
  OBS: THE LAYOUT RULE IS DUPLICATED, AND BOTH COPIES CARRY A BUG THAT WAS FIXED ONLY DOWNSTREAM. CA grammar:46 and ML build-contract:140 both prescribe `.wrap{height:100dvh}` — never min-height'. learn-hub reversed that rule on 2026-08-21: the stage collapsed on every phone for all 25 animations. The fix exists only in the learn-hub clone, and it never flowed back upstream. I ran learn-hub's own sync audit (`auditAnimationLayout` in learn-hub scripts/lib/animation-responsive.mjs) against the shipped e
  OBS: THE REAL SOURCE OF TRUTH IS EXECUTABLE CODE IN ANOTHER REPO. learn-hub ships `auditAnimationSelfContained`, `auditAnimationLayout`, `auditInfographicResponsive`, `src/lib/animation-layout.ts` (a render-time guard with floor `max(220px,min(38dvh,280px))`) and `scripts/animations/pk-plasma/verify.mjs` (whose header says 'the concept-animation skill's Step 3.5, made executable'). No plugin in this group ships a single script: none of the five has a `scripts/` directory. Every verification step is p
  OBS: THE FILING PIPELINE IS INCOHERENT ACROSS THE TWO REPOS. CI, CA and ML file HTML into micky `vault/assets/` via vault-keeper. vault-keeper's own empty-vault skill (plugins/vault-keeper/skills/empty-vault/SKILL.md:54-56) says assets 'cannot become Learn notes. They are *removed with their topic*'. But learn-hub's ingest-infographic and ingest-animation descriptions say they fire 'when the micky-psych-tools empty-vault skill hands over an infographic/animation asset'. Meanwhile the learn-hub concep
  OBS: POLICY IS INCONSISTENT ACROSS SIBLINGS. The Step 0 gate is none in CI, mandatory intent-lock in CA and ML, conditional in CE, and inlined in the learn-hub clone. Vault filing is the default in CI, CA and ML and opt-in in CE. Category is 'research' for CI, 'education' for CA, ML and CE, and 'design' for GG.
  OBS: THE BOUNDARY GRAPH IS ASYMMETRIC AND TRIGGERS OVERLAP. ML's Handoffs name CA, CE, dataviz, gridgeist and firecrawl. CA names only CI and dataviz, and never mentions ML. CE never mentions ML. CA's scope includes 'algorithms, statistical ideas' and triggers on 'make an animation of X'; ML's description explicitly lists 'animate this algorithm' and 'show me how X works' as triggers, so both skills compete for algorithm animations. On clinical concepts, ML's description routes them to clinical-infog
  OBS: DESCRIPTIONS ARE CROWDED AGAINST THE CAP (measured characters): CI 991, CA 1022 (1074 bytes as UTF-8), ML 997, CE 863, GG 708, learn-hub CA 1000. Four of the six are within 33 characters of the 1024 hard cap, which contradicts CLAUDE.md's 'aim well under'. Every description is third person except the learn-hub clone ('Author …', imperative). Every description puts its what-signal in the first 200 characters and has trigger phrases (Thai phrases in CI, CA, ML and CE) and a NOT-for clause.
  OBS: EVALS ARE THIN. Only CI (4 cases) and CE (6 cases) have evals/evals.json, in skill-creator format. Every case has `"assertions": []` and `"files": []`, so nothing is machine-gradable and there are no fixtures. There are no should-trigger / should-not-trigger query sets. CI eval #1 depends on a vault artifact (PPGL) that no longer exists. CA, ML and GG have no evals.
  OBS: DEPENDENCIES ARE NOT GUARANTEED. plugin.json has no dependency mechanism, so intent-lock, vault-keeper, comprehensive-review, pubmed-research-note and firecrawl are separate installs from the same marketplace. dataviz is not in this marketplace at all; it is an org/built-in skill. The browser tooling is assumed: Chromium exists only at /opt/pw-browsers. Playwright is installed globally only, so an ESM `import 'playwright'` from a working dir fails with ERR_MODULE_NOT_FOUND (measured). There is n
  OBS: REPO-LEVEL VALIDATION DOES NOT CATCH BROKEN LINKS. validate.py prints 'all checks passed' while CE's SKILL.md and README reference files that were never committed. It only checks name/description/version parity. It does not check intra-skill links, CHANGELOG vs version, or README file lists.
  OBS: GRIDGEIST IS AN OUTLIER IN THIS GROUP. It is a vendored third-party frontend-design skill, not an HTML renderer. It has no command, README, CHANGELOG or evals. It carries an OpenAI Codex metadata file (skills/gridgeist/agents/openai.yaml) and the assets that only that file uses. CLAUDE.md's Plugins section omits it entirely, although MEMORY.md and ROUTING.md list it.
== micky-psych-tools/plugins/clinical-infographic [plugin v0.2.1 (plugin.json == marketplace.json; validate.py passes). CHANGELOG top entry is 0.2.0.] clinical-infographic(desc=991,lines=177,fm=name,description)
  clinical-infographic-1 [H] The template and the worked example ship a prefers-color-scheme:dark block. learn-hub, the downstream renderer, documents this as a bug: infographics are light-locked paper, and a partial dark block strands ink-on-tint text as invisible under an OS dark theme. learn-hub CLAUDE.md claims this skill enforces the light-lock. It doe || EV: references/infographic-template.html:100-104 '@media (prefers-color-scheme:dark){ /* Screen dark mode only; print stays light for legibility. */ :root { --bg:#1
  clinical-infographic-2 [H] The mechanism strip scaffold cramps on phones. Its items are `flex:1 1 0; min-width:0`, and there is no mobile stacking rule. learn-hub's own sync audit flags both the template and the example. || EV: references/infographic-template.html:74-75 '.mech .node { flex:1 1 0; … min-width:0; }', and line 93, the only mobile rule: '@media (max-width:720px){ .columns{
  clinical-infographic-3 [M] The palette claims 'AA-contrast', but white header text fails WCAG AA on two of the three column accents. The header is 15px bold, which is below the large-text threshold. || EV: design-system.md:121-123 'A documented, print-safe, AA-contrast starting palette'. Template:40 '.col-head { color:#fff; … font-size:15px; font-weight:700 }'. Me
  clinical-infographic-4 [M] The template violates the design system's own legibility minimum. || EV: design-system.md:220 'Legible minimums: no clinical text below ~12px at print size' vs template:56 '.stat .k { font-size:11px', :73 '.viz .cap { … font-size:10.
  clinical-infographic-5 [M] The render/verify step is vague and partly non-executable. It names no browser, script or viewport set. The optional OCR has no tool in the environment. The check is written as prose that a deterministic script could perform (external-reference scan, mobile-strip audit, dark-block scan). || EV: SKILL.md:126-130 '1. Rasterise it — render the HTML to an image (a headless-browser screenshot) and *look* … 2. Optionally OCR the render'. `which tesseract` re
  clinical-infographic-6 [M] Vault filing leads to a dead end. The asset is filed to vault/assets, but vault-keeper's empty-vault deletes assets rather than handing them to learn-hub's ingest-infographic, which expects that handover. || EV: SKILL.md:143-146 'hand it to the vault-keeper skill to place in vault/assets/'. plugins/vault-keeper/skills/empty-vault/SKILL.md:54-56 'assets (rendered HTML, P
  clinical-infographic-7 [L] The CHANGELOG is stale: the plugin is at 0.2.1, but the CHANGELOG's newest entry is 0.2.0. || EV: plugin.json:3 '"version": "0.2.1"'. CHANGELOG.md:3 '## 0.2.0 — 2026-07-11' is the top entry. MEMORY.md:986 records the 0.2.1 release (interconnect wave).
  clinical-infographic-8 [L] The examples README links a source report that no longer exists, and eval 1 depends on it. || EV: examples/README.md:6 'rendered from the sourced report `vault/artifacts/ppgl-perioperative-management.md`'. `ls vault/artifacts` shows only burnout-syndrome-com
  clinical-infographic-9 [L] lessons-learned.md is a time-bound retrospective that is loaded with a vague instruction. It duplicates fixes already in the SKILL and design-system, and costs ~1.4k tokens whenever it is read. || EV: SKILL.md:50-51 'The worked retrospective in references/lessons-learned.md explains *why* … — read it once.' lessons-learned.md:1-2 'A worked retrospective from 
  clinical-infographic-10 [L] The SKILL never points at the worked example or the preview PNG. The only pointer is in examples/README.md. || EV: `grep -n examples SKILL.md` finds no hits. examples/ppgl-perioperative-management.html (481 lines) sits at the plugin root.
== micky-psych-tools/plugins/concept-animation [plugin v0.1.1 (plugin.json == marketplace.json). There is no CHANGELOG or README.] concept-animation(desc=1022,lines=152,fm=name,description)
  concept-animation-1 [H] The layout contract prescribes the rule that learn-hub measured collapsing every animation's stage on phones. The shipped example fails learn-hub's sync-time layout audit. || EV: animation-grammar.md:46 '`.wrap{ height:100dvh; display:flex; flex-direction:column; }` — **never** `min-height:100vh`' and :48 'the stage wrapper takes the lef
  concept-animation-2 [H] The fit check is the one learn-hub proved cannot fail on the shape it guards: a single laptop viewport, asking only whether the page scrolls. || EV: SKILL.md:145-150 'embed the HTML at a common laptop height (e.g. 1366×768) and confirm the document does not scroll'. learn-hub CLAUDE.md: 'The animation skill'
  concept-animation-3 [M] The viewer geometry it cites is stale. || EV: SKILL.md:146 and grammar:42 say '(the Learn hub uses an iframe of `height: calc(100svh - 6rem)`)'. The actual code at learn-hub src/components/animation/animati
  concept-animation-4 [M] The example is a doctype-less fragment with no color-scheme, so it renders in quirks mode inside the app iframe and gets an opaque white canvas at desktop widths. The grammar has no rule against this; learn-hub's grammar does. || EV: examples/tms-electromagnetic-induction.html:1 opens with '<!--' then '<style>', with no <!DOCTYPE>. `grep color-scheme` finds 0 hits. learn-hub grammar: 'A full
  concept-animation-5 [M] The description sits at the hard limit (1022 of 1024 characters). Any edit will breach it, which directly contradicts the repo's 'aim well under' rule. || EV: measured len(description) = 1022 characters, 1074 UTF-8 bytes. CLAUDE.md: 'hard limit 1024 chars; aim well under.'
  concept-animation-6 [M] Scope overlaps with ml-concept-lab, and there is no reciprocal boundary. The description claims 'algorithms, statistical ideas', but neither the NOT-for clause nor the Handoffs mention ml-concept-lab. ML's description triggers on 'animate this algorithm'. || EV: SKILL.md:6 'scene by scene: mechanisms, pathways, processes, algorithms, statistical ideas'. `grep ml-concept-lab SKILL.md` finds 0 hits. ml-concept-lab SKILL.m
  concept-animation-7 [M] Vault filing leads to a dead end. The asset goes to vault/assets, but empty-vault deletes assets instead of handing them to learn-hub ingest-animation. || EV: SKILL.md:157-158 'hand it to vault-keeper to place in vault/assets/'. empty-vault SKILL.md:54-56 'assets … are *removed with their topic*'. learn-hub ingest-ani
  concept-animation-8 [L] The render/verify step names no tool or script. Per-scene screenshots, the self-contained scan and the reduced-motion presence check are deterministic tasks left as prose. || EV: SKILL.md:139-141 'Render the HTML in a headless browser and screenshot each scene's hold plus the final frame'. The plugin has no scripts/ directory.
  concept-animation-9 [L] The worked example is orphaned. It is not referenced from SKILL.md, and there is no README. || EV: `grep -n examples skills/concept-animation/SKILL.md` finds nothing. The plugin root has no README.md or CHANGELOG.md.
  concept-animation-10 [L] There are no evals, so the trigger boundary with ml-concept-lab and clinical-infographic is untested. || EV: skills/concept-animation/ contains only SKILL.md and references/.
== micky-psych-tools/plugins/ml-concept-lab [plugin v0.1.0 (plugin.json == marketplace.json). There is no CHANGELOG and no plugin README (examples/README.md only).] ml-concept-lab(desc=997,lines=225,fm=name,description)
  ml-concept-lab-1 [H] The layout rule is the stage-collapse bug. The example fails learn-hub's layout audit, and a real ML output filed to learn-hub reproduced the bug. || EV: build-contract.md:140 '`.wrap { height: 100dvh; display: flex; flex-direction: column }` — **never** `min-height`.' and :141 'The stage takes the remainder: `fl
  ml-concept-lab-2 [H] The copy-ready verify recipe fails as written in this environment: the bare ESM playwright import does not resolve, because playwright is only installed globally. The recipe also verifies a single viewport, which is the known-insufficient fit check. || EV: build-contract.md:195-197 '// verify.mjs — node verify.mjs ./lab.html\nimport { chromium } from 'playwright';'. `npm ls -g` shows playwright@1.56.1. Running an 
  ml-concept-lab-3 [M] The contract prescribes a relative-error gradient check, which the plugin's own worked example found produces false failures at the optimum. The fix (an absolute difference with a noise floor) never reached the contract. || EV: build-contract.md:103 '| Anything with gradients | Analytic ∇ vs central finite difference, max relative error < 1e-5 |' and :96 chip example '✓ ∇ vs finite-dif
  ml-concept-lab-4 [M] The clinical-concept routing contradicts itself. The description sends clinical concepts to clinical-infographic; the Handoffs and CLAUDE.md send them to concept-animation. || EV: SKILL.md:13-14 'NOT for: … clinical concepts (clinical-infographic)'. SKILL.md:237-239 'concept-animation — the watch-only sibling … and clinical concepts are i
  ml-concept-lab-5 [M] The worked-example path is wrong relative to the skill directory. The file lives at the plugin root, so 'copy its shape' points at nothing unless it is resolved via ${CLAUDE_PLUGIN_ROOT}. || EV: SKILL.md:117-118 '`examples/learning-rate-and-conditioning.html` is a worked lab built to both; copy its shape'. The actual location is plugins/ml-concept-lab/e
  ml-concept-lab-6 [M] The body is over-long, and it includes a 60-line JS verify script embedded as prose in a reference; a deterministic check like this belongs in scripts/. The body is ~3.5k tokens plus ~7.3k tokens of references. || EV: SKILL.md body 225 lines (~3533 tok). build-contract.md:195-242 is a full verify.mjs in a code fence. The plugin has no scripts/ directory.
  ml-concept-lab-7 [L] The trigger phrases overlap with concept-animation and are over-broad. || EV: SKILL.md:9-10 '"show me how X works", "animate this algorithm"'. concept-animation's description claims 'algorithms, statistical ideas' and 'make an animation o
  ml-concept-lab-8 [L] The example is a doctype-less fragment, so it renders in quirks mode in learn-hub's iframe, and it declares no color-scheme. || EV: examples/learning-rate-and-conditioning.html:1-8 opens with '<!--' … '<style>', with no <!DOCTYPE>.
  ml-concept-lab-9 [L] There are no evals, no CHANGELOG and no plugin README. || EV: The plugin root has .claude-plugin, commands, examples and skills only; skills/ml-concept-lab has only SKILL.md and references/.
== micky-psych-tools/plugins/code-explainer [plugin v0.1.0 (plugin.json == marketplace.json). There is no CHANGELOG.] code-explainer(desc=863,lines=162,fm=name,description)
  code-explainer-1 [H] The SKILL's primary starting artifact does not exist and never did. The README also lists a missing template and a missing worked example. validate.py still passes. || EV: SKILL.md:67-68 '[references/explainer-template.html](references/explainer-template.html) is the working skeleton — start from it rather than from a blank file.'
  code-explainer-2 [M] The byte-for-byte fidelity check is prose. It is a deterministic comparison (the code pane's textContent vs the source bytes, plus escaping) that should be a script. || EV: explanation-contract.md:145-146 '5. Re-read the code in the rendered page against the source, character by character, for escaping and truncation damage.' SKILL
  code-explainer-3 [L] The theme is named inconsistently: 'Dark+' in the description, marketplace entry and evals; 'Dark Modern' in the README and shell spec, which uses Dark Modern values. || EV: SKILL.md:5 '(Dark+ theme, line numbers, syntax highlighting)'. README.md:4 '(Dark Modern, line numbers, syntax highlighting)'. vscode-shell.md:8 'Theme is **Dar
  code-explainer-4 [L] The contract asks for explanation cards to be <button> elements while they contain headings and paragraphs, which is invalid HTML. Button content is limited to phrasing content. || EV: explanation-contract.md:22 'Cards are real `<button>`s (or `tabindex="0"` with a key handler)'. The cards carry a heading plus prose per SKILL Step 2 item 5 and
  code-explainer-5 [L] The fit rule relies on a single laptop viewport and conflicts slightly with the stacked mobile layout. The shell says the page never scrolls, but a stacked sub-900px layout will scroll. || EV: vscode-shell.md:35-36 'panes scroll internally — the page itself never scrolls' vs explanation-contract.md:130-131 'Below ~900px the panes stack — code first, e
  code-explainer-6 [L] The boundary with ml-concept-lab is asymmetric, and there is no CHANGELOG. || EV: `grep ml-concept-lab skills/code-explainer/SKILL.md` finds 0 hits. ml-concept-lab SKILL.md:240-243 defines the split. The plugin root has no CHANGELOG.md.
== micky-psych-tools/plugins/gridgeist [plugin (vendored third-party, MIT, upstream github.com/ohmiler/gridgeist) v0.1.0 (plugin.json == marketplace.json). The upstream commit or version is not recorded, so drift from upstream cannot be checked.] gridgeist(desc=708,lines=70,fm=name,description)
  gridgeist-1 [L] The plugin ships a Codex-only metadata file and three image assets that nothing in Claude Code reads. This is dead weight and could confuse maintainers into thinking gridgeist has an agent. || EV: skills/gridgeist/agents/openai.yaml:1-9 'interface: display_name: "Gridgeist" … icon_small: "./assets/gridgeist-small.svg" … default_prompt: "Use $gridgeist to 
  gridgeist-2 [L] It is omitted from the CLAUDE.md plugin inventory, which claims to describe every plugin. || EV: CLAUDE.md '## Plugins' lists pubmed-research-note … decision-interview; `grep -n -i gridgeist CLAUDE.md` finds nothing. MEMORY.md:615 'Added gridgeist 0.1.0 (ni
  gridgeist-3 [L] There are no evals, no README, no CHANGELOG and no command, and the vendored upstream revision is not recorded, so a future sync cannot be diffed. || EV: The plugin root has .claude-plugin, LICENSE and skills only. MEMORY.md:615-618 says 'vendored from upstream `ohmiler` under MIT' but gives no commit or tag.
  gridgeist-4 [L] The marketplace description is minimal and imperative, and its routing signal is weaker than the skill description's. || EV: .claude-plugin/marketplace.json gridgeist description (132 chars): 'Design, redesign, and review distinctive web interfaces with a strong grid, precise typograp
== learn-hub/.claude/skills/concept-animation [project skill (clone of micky-psych-tools concept-animation, diverged) vunversioned (project skill). Last commit 2026-09-05 (bececa3), vs the micky plugin's last change on 2026-07-20.] concept-animation(desc=1000,lines=203,fm=name,description)
  concept-animation (learn-hub copy)-1 [H] The frontmatter is invalid under strict YAML. The unquoted description contains ': ', so PyYAML (and any strict validator, e.g. skill-creator's) fails to parse it. The sibling ingest-animation and ingest-infographic have the same problem. || EV: SKILL.md:3 'description: Author a self-contained HTML animation … Files a `type: animation` sidecar + sibling HTML …'. yaml.safe_load raises 'ScannerError: mapp
  concept-animation (learn-hub copy)-2 [M] The grammar's stage floor is the bare min() form that learn-hub's own CLAUDE.md says is wrong. It gives a 116px stage in landscape. The app guard uses max(220px, …). || EV: references/animation-grammar.md:58 '`flex:1 1 0; min-height:220px; min-height:min(38dvh,280px); display:flex;`' (the second declaration overrides the first). le
  concept-animation (learn-hub copy)-3 [M] The verify step lists 4 viewports and omits landscape phone, which learn-hub CLAUDE.md and the executable pk-plasma verify script both require. The executable check that already exists is not referenced. || EV: SKILL.md:129 'at **360×780, 390×844, 430×932 and 1440×900**'. scripts/animations/pk-plasma/verify.mjs:25 'export const VIEWPORTS = [[360, 780], [390, 844], [430
  concept-animation (learn-hub copy)-4 [M] It calls the example 'complete, compliant', but the example violates the grammar it accompanies: it is a doctype-less fragment with no color-scheme, and it caps .wrap at 1040px instead of 1152px. || EV: SKILL.md:42-43 'examples/tms-electromagnetic-induction.html is a complete, compliant reference build.' The example's line 1 is '<!--' then '<style>' (no <!DOCTY
  concept-animation (learn-hub copy)-5 [M] The sync instruction contradicts learn-hub's documented gotcha. `npm run sync` is the dry SQL emitter, and the two sync pipelines should never both be run. || EV: SKILL.md:171 'Then run the **sync-vault** skill (`npm run sync` → apply, or `npm run sync:apply` for a large vault)'. learn-hub CLAUDE.md: '`sync:apply` is incr
  concept-animation (learn-hub copy)-6 [L] The builder path is stale; the referenced file does not exist. || EV: SKILL.md:94 '(`scripts/animations/mph-plasma/build.mjs` is the pattern)'. `ls scripts/animations/mph-plasma/` shows drug/formulations/params/presets/scenes and 
  concept-animation (learn-hub copy)-7 [L] It duplicates ingest-animation's filing/verify procedure instead of delegating to it, so the two copies of the sidecar contract can drift. || EV: concept-animation/SKILL.md:155-161 (sidecar YAML 'type: animation … html: <slug>.html') and :181 'select id, topic_id, source, length(html) as html_len' vs inge
  concept-animation (learn-hub copy)-8 [L] There is a name and trigger collision with the micky plugin when both are installed. Both trigger on 'animate this concept' and '/animate', and they file to different destinations (vault-keeper vault/assets vs /vault plus Supabase). || EV: learn-hub SKILL.md:3 triggers '"animate this concept" … "/animate"'. micky plugins/concept-animation/skills/concept-animation/SKILL.md:7-8 has the same triggers
#### inventory-research-writers.json :: literature-writing plugins (pubmed-research-note, comprehensive-review, psych-paper-digest + learn-hub forks of the first two)
  OBS: Measured description lengths sit at the 1024-char hard cap: comprehensive-review 1020 (99.6%), pubmed-research-note 1007 (98%), psych-paper-digest 939 (92%); forks 991 and 1011. validate.py counts len() chars, so there is no headroom for a routing fix without cutting. In all three, the first ~200 chars carry only WHAT; the trigger phrases (WHEN) start around char 250, and ROUTING.md truncates at ~200. Each spends 70-170 chars on engine lists/output mechanics that belong in the body.
  OBS: No unit ships a script. Deterministic work is prose: window math, 90-day cap, JSON config read/write, last_swept advance, digest filename collision, DOI/PMID/NCT dedup grep, Sources-line linting (no authors/journal/year/PMID), 'every section has a number'. Several failure conditions police exactly these.
  OBS: Evals: 34 evals across the 3 upstream skills (17/5/12). All have 'assertions': [] and 'files': [], and there is no should-trigger/should-not-trigger query set for tuning descriptions. Several evals assume uncontrollable world states (PubMed down; a window holding only confirmatory trials). Both forks copied their evals byte-for-byte, so 3 of the forks' evals now fail by construction (vault-keeper handoffs).
  OBS: All 5 units bundle a byte-identical .mcp.json (md5 1b35ee7c...) with the same two HTTP servers and hardcode plugin-scoped prefixes plus 'no ToolSearch'. Installing all three upstream plugins registers the same servers three times. The instructions also ignore an equivalent claude.ai connector (this session exposes mcp__PubMed__* and mcp__Clinical_Trials__*). A shared engine layer or runtime tool resolution would remove this duplication.
  OBS: Shared contracts are copy-pasted rather than referenced: depth contract, citation discipline, Sources grammar, firecrawl policy, PubMed fallback. Drift already exists: the NCT Sources-line grammar has 3 variants across the group, and the preface block has 2 variants (Assumed/Reframed/Skipped vs Assumed/Skipped).
  OBS: Upstream drift against intent-lock 0.4.2, installed in both repos. intent-lock now forbids reframes and says it emits 'no Reframed line' and 'no preface block'. pubmed-research-note still mandates a Reframed line and a 'reframe governs' rule. Eval 13 still expects intent-lock's visible 'cold read / ledger / rounds / gates'.
  OBS: intent-lock and vault-keeper are mandatory default steps in the two writers, but no unit says what to do when that plugin is not installed. The marketplace installs plugins individually, so nothing guarantees these dependencies are present.
  OBS: Name collision: an unrelated claude.ai skill, anthropic-skills:psych-paper-digest (a daily 8-12 paper, 10-domain digest), shares the name and trigger vocabulary of this repo's watchlist psych-paper-digest. pubmed-research-note's NOT-for clause describes the claude.ai version.
  OBS: Forks: `git diff --stat f91b3b0 HEAD` is empty for both upstream dirs, so upstream is frozen at the fork point. The forks changed only the filing path, but each fork copies the whole plugin, including the version, CHANGELOG and evals. The two forks now disagree on the digest gate: pubmed gates the digest on 'atomize', while comprehensive-review digests and syncs to live Supabase by default. digest-report's own description sides with the atomize gate. Upstream vault-keeper's empty-vault already h
  OBS: Version-history narration is embedded in operational text across the group ('the old version', 'as of this version', 'replaces the old four frames', 'past reviews have run ~20 to 65+'). It costs always-loaded tokens and goes stale.
  OBS: CHANGELOG gaps: pubmed-research-note lacks 1.3.0 and 1.4.0, and psych-paper-digest lacks 0.1.1. These releases are recorded only in MEMORY.md.
  OBS: Token budget: pubmed-research-note's always-loaded body is ~4.7k tokens, plus ~5.9k tokens of references it requires reading every run (decision-brief, report-craft, tool-catalog). Progressive disclosure therefore saves little, and much of that load is repetition: the opt-out rule appears 4x and the depth contract 4x.
== micky-psych-tools//home/user/micky-psych-tools/plugins/pubmed-research-note [plugin (1 skill, 0 commands, 0 agents, 0 hooks, bundled .mcp.json) v1.7.0] pubmed-research-note(desc=1007,lines=304,fm=name,description)
  pubmed-research-note-1 [H] Preface contract is stale against the installed intent-lock 0.4.2: the skill mandates a 'Reframed:' line and a reframe that 'governs' the brief, but intent-lock now forbids reframing and emits no Reframed line or preface block. || EV: SKILL.md:263 '> Reframed: <the reframe, if it was accepted>'; references/intent-lock-pairing.md:86-90 'intent-lock's Phase 0 reframe operates on the request ...
  pubmed-research-note-2 [H] Decision-brief slot arithmetic is self-contradictory and does not match what intent-lock is said to fix: brief has 6 slots, text says 'first four' + 'last three' (=7), and intent-lock-pairing's four intent-lock slots include a 'scope boundary' that is not a brief slot while omitting slots the brief says the interview fixes. || EV: decision-brief.md:11-12 'fixes the first four slots ... You derive the last three from them' with six headings at L16,31,50,71,78,90; intent-lock-pairing.md:49-
  pubmed-research-note-3 [M] Atomic-note template contradicts the SKILL's Sources rule and the vault-keeper handoff contract: says the report puts PMIDs in Sources (SKILL forbids PMIDs), and maps `type: concept|mechanism|moc|drug|disorder` onto vault-keeper's 'target type' slot, whose legal values are note/artifact/asset/MOC. || EV: atomic-note-template.md:23 'The report keeps its prose clean and pushes PMIDs into `## Sources`' vs SKILL.md:219-221 'No authors, no journal, no publication yea
  pubmed-research-note-4 [M] Eval 6 contradicts the default Show step: it expects a two-line chat response with contents 'NOT restated inline', while SKILL makes rendering the full report inline mandatory; eval 6 also omits the default vault filing. || EV: evals.json:48 'Chat response is two lines ... The report file is presented; its contents are NOT restated inline beyond the default show step.' vs SKILL.md:155-
  pubmed-research-note-5 [M] Hardcoded plugin-scoped MCP prefixes with an explicit 'No ToolSearch step' ignore an equivalent connector that may be the only live path; three plugins also register the same two servers, tripling connections. || EV: tool-catalog.md:25-28 'call the tools directly with the stable prefix `mcp__plugin_pubmed-research-note_pubmed__<tool>` ... No ToolSearch step.'; this session e
  pubmed-research-note-6 [M] No failure path if intent-lock or vault-keeper is not installed, although both are mandatory default steps ('ALWAYS runs intent-lock FIRST', 'Run all three every time'). || EV: SKILL.md:82 '**Every research request routes through `intent-lock` FIRST — always'; SKILL.md:150-151 'Run all three every time unless the user opts out'; SKILL.
  pubmed-research-note-7 [M] CHANGELOG is missing the 1.3.0 and 1.4.0 releases (both recorded only in MEMORY.md), yet the 1.6.0 entry cites '1.3.0 behavior'. || EV: CHANGELOG.md jumps '## 1.5.0 — 2026-07-11' (L42) to '## 1.2.0 — 2026-07-10' (L67); CHANGELOG.md:40 '(1.3.0 behavior)'; MEMORY.md:1106 'pubmed-research-note **1.
  pubmed-research-note-8 [L] NCT Sources-line grammar is specified two different ways inside the same skill. || EV: SKILL.md:221-222 'Registry entries: `NCT NNNNNNNN — topic, status, n, readout`' vs tool-catalog.md:100 '`NCT NNNNNNNN — status, n, endpoint, readout`'.
  pubmed-research-note-9 [L] Version-history narration and time-relative wording inside operational instructions (Claude does not need the old version's story to act). || EV: SKILL.md:32 'as of this version, **no fixed report shape either**'; SKILL.md:61 'This is the half of the deal the old version underweighted'; SKILL.md:293 'The 
  pubmed-research-note-10 [L] Report file has no naming/slug convention and no schema for .pubmed-research-note.json; 'write into the working directory' is vague for a default-every-run step. || EV: SKILL.md:153-154 'write the report into the working directory (or `report_dir` from `.pubmed-research-note.json` if present).' - no filename rule (contrast psyc
  pubmed-research-note-11 [L] Skill names a vault path despite forbidding it ('never invent a vault path'). || EV: SKILL.md:170-172 'atomic notes (distilled, linkable one-idea notes in `notes/`) ... Never invent a vault path.'
  pubmed-research-note-12 [L] Heavy repetition inflates the always-loaded body: opt-out rule 4x, depth contract 2x in SKILL plus 2 refs, drift tells duplicated in report-craft. || EV: 'explicit opt-out' occurs 4x in SKILL.md; SKILL.md:291-320 'Signs the report has drifted' duplicates report-craft.md:118-140 'Signs you've drifted'.
  pubmed-research-note-13 [L] Evals are prose-only: 17 evals, 0 assertions, 0 fixtures; several (e.g. 11 omega-3 effect shrinkage) depend on live literature and cannot be graded deterministically. || EV: evals.json every entry '"assertions": []' and '"files": []'.
  pubmed-research-note-14 [L] No slash command, unlike both sibling literature plugins (/comprehensive-review, /digest). || EV: find plugins/pubmed-research-note -type f lists no commands/ directory.
== micky-psych-tools//home/user/micky-psych-tools/plugins/comprehensive-review [plugin (1 skill, 1 command, 0 agents, 0 hooks, bundled .mcp.json) v0.3.0] comprehensive-review(desc=1020,lines=205,fm=name,description)
  comprehensive-review-1 [M] 'No section without a number' rule conflicts with the skill's own instructions to write thin domains and empty modalities as number-free lines, and with 'Definition' being the only exception. || EV: SKILL.md:132-133 '**No section may exist without a number in it** (Definition & nosology is the one permitted exception'; SKILL.md:35-36 'a domain the literatur
  comprehensive-review-2 [M] No engine-failure policy: unlike both siblings there is no 'PubMed unreachable = fatal' rule, no E-utilities/CT.gov API fallback, and no instruction for when the bundled servers are absent. || EV: SKILL.md:103-124 (Step 2) covers PubMed/CT.gov/firecrawl usage only; contrast psych-paper-digest SKILL.md:74-76 and pubmed-research-note tool-catalog.md:59-61.
  comprehensive-review-3 [M] 'Source floors' wording contradicts 'capped by relevance, never by count' and the review-arc column actually lists evidence TYPES, making the term ambiguous for a count-sensitive reader. || EV: SKILL.md:108 'Per-domain search recipes and source floors are in references/review-arc.md' vs SKILL.md:57 'Sources are capped by relevance, never by count'; rev
  comprehensive-review-4 [M] Mandatory dependency on intent-lock and vault-keeper with no absent-plugin fallback. || EV: SKILL.md:64 '**Every review request routes through `intent-lock` FIRST — always'; SKILL.md:169-173 'File it via the **vault-keeper** skill ... Skip only on an e
  comprehensive-review-5 [L] Citation contract cites nonexistent 'house rules'. || EV: SKILL.md:128 'Inherited from the house rules, binding here too:' - no file or section by that name in the plugin.
  comprehensive-review-6 [L] Output file location and name are unspecified, yet the file is 'the deliverable'. || EV: SKILL.md:168 '**Write** the finished review as a single md file.'; SKILL.md:196 'The file is the deliverable'.
  comprehensive-review-7 [L] Intent-lock locks 'length', but nothing downstream uses a length target and the depth rules forbid cutting content, so a locked length has no mechanism. || EV: SKILL.md:67-68 'It locks ... **emphasis** ... **length**, and **explicit exclusions**'; SKILL.md:160 'a long section is **subdivided, not amputated**: never cut
  comprehensive-review-8 [L] Registry recipe uses a non-existent status filter; the CT.gov MCP status enum has no 'completed-no-results' value, so the recipe requires per-trial get_trial_details that the text does not state. || EV: review-arc.md:23 'ClinicalTrials.gov: condition = `<disorder>`, recruiting + completed-no-results'; mcp__Clinical_Trials__search_trials status enum = NOT_YET_RE
  comprehensive-review-9 [L] Description consumes the full 1024-char budget (1020 chars), including a non-routing NOT-for item. || EV: measured len(description)=1020; SKILL.md:15 'writing into vault/ directly (vault-keeper owns paths).'
  comprehensive-review-10 [L] History narration in operational text and in the reference. || EV: SKILL.md:99 'The one structural rule kept from the old contract'; review-arc.md:51-53 'past reviews have run from ~20 to 65+ ... Two disciplines replace the old
  comprehensive-review-11 [L] Evals: only 5, prose-only, no assertions or fixtures, no Thai-trigger eval, no no-filesystem eval, no opt-out-of-intent-lock eval. || EV: evals.json 5 entries, each '"assertions": []'; description L10 includes Thai trigger 'รีวิวทั้งโรค' with no eval covering it.
  comprehensive-review-12 [L] CHANGELOG has no history before 0.3.0. || EV: CHANGELOG.md:24 '## 0.2.0 and earlier ... Pre-changelog releases'.
== micky-psych-tools//home/user/micky-psych-tools/plugins/psych-paper-digest [plugin (1 skill, 1 command, 0 agents, 0 hooks, bundled .mcp.json) v0.1.1] psych-paper-digest(desc=939,lines=162,fm=name,description)
  psych-paper-digest-1 [H] MCP sweep path windows on the default publication date (pdat) while the stated rationale and the web fallback use entry date (edat); items indexed in-window with older or future-dated issue dates are silently missed on the primary path. || EV: sweep-recipes.md:39-40 '`search_articles` args: `query`, `date_from`, `date_to`, `sort: pub_date`, `max_results: 50`' (no datetype); mcp__PubMed__search_article
  psych-paper-digest-2 [H] Silent truncation: each domain query is capped at max_results 50 with no pagination, so anything beyond 50 is neither screened nor counted, violating the skill's own 'No silent drops' and making the 'screened N' header wrong. || EV: sweep-recipes.md:39-40 '`max_results: 50`'; sweep-recipes.md:92-93 'Everything dropped is counted ... No silent drops: screened and kept counts both appear in t
  psych-paper-digest-3 [M] ClinicalTrials.gov recipe misstates the MCP tool: search_trials DOES date-filter via advanced_query AREA[...]RANGE[...], and 'has-results' is not a status value. || EV: sweep-recipes.md:65-68 'filter by status: completed, terminated, has-results ... the search tool cannot date-filter directly'; mcp__Clinical_Trials__search_tria
  psych-paper-digest-4 [M] Watchlist-health rule is unexecutable: '0 kept items across 3 consecutive sweeps' needs per-sweep history, but the config stores only last_swept and dedup reads only the newest digest. || EV: config-schema.md:55-56 'A domain returning **0 kept items across 3 consecutive sweeps** → suggest widening'; config-schema.md:7-20 schema has no history fields;
  psych-paper-digest-5 [M] Dedup against only the newest digest breaks after scoped runs: a later full sweep dedups against a single-domain digest, so other domains' items from the overlap window reappear as new. || EV: sweep-recipes.md:80-82 'glob `digest-*.md` in the digest directory, take the newest, grep its DOIs/PMIDs/NCTs'; SKILL.md:45-46 scoped runs write their own diges
  psych-paper-digest-6 [M] Name collision with a different installed skill (anthropic-skills:psych-paper-digest, a daily 8-12 paper 10-domain digest) with overlapping triggers; routing between them is undefined, and pubmed-research-note's NOT-for describes the other one. || EV: Session skill list: 'anthropic-skills:psych-paper-digest: Daily psychiatry literature digest — searches for and summarizes 8-12 of the latest psychiatry papers 
  psych-paper-digest-7 [M] Init vault discovery is tied to the marketplace repo layout and fails silently elsewhere. || EV: config-schema.md:42-43 '(walk up to the directory holding `.claude-plugin/marketplace.json`, look in `vault/MOCs/`)'.
  psych-paper-digest-8 [L] Stale statement about Claude Code scheduling (Routines/cron triggers now exist). || EV: README.md:48 'Claude Code has no built-in cron for skills; schedule the trigger outside'.
  psych-paper-digest-9 [L] CHANGELOG missing the 0.1.1 entry that plugin.json/marketplace/MEMORY record. || EV: plugin.json:3 '"version": "0.1.1"'; CHANGELOG.md:3 only '## 0.1.0 — 2026-07-10'; MEMORY.md:221 'Collateral: **psych-paper-digest → 0.1.1**'.
  psych-paper-digest-10 [L] Ambiguous collision filename rule (leading space) and undefined 'newest' ordering for the dedup glob. || EV: SKILL.md:94 'Write `digest-YYYY-MM-DD.md` (collision → ` -2`, never overwrite)'; sweep-recipes.md:81 'take the newest' (mtime vs lexical unspecified; '-2' suffi
  psych-paper-digest-11 [L] Triage-rubric is the core judgment but is never explicitly required reading. || EV: SKILL.md:80-81 'Three tiers, full criteria and examples in [references/triage-rubric.md]' - no 'read before triage'.
  psych-paper-digest-12 [L] Deterministic mechanics (window math, 90-day cap, JSON config read/write, last_swept advance, filename collision, DOI/PMID/NCT dedup grep) are written as prose instead of a script; they are exactly the error-prone parts the failure list polices. || EV: SKILL.md:50-58, 72-73, 92-97; failure conditions L170-171, L176.
  psych-paper-digest-13 [L] Evals 6, 7, 9 cannot be run as written because they require controlled literature/engine states; no assertions anywhere. || EV: evals.json:48 'The window holds only small confirmatory positive trials'; L72 'PubMed is unreachable via both'; all '"assertions": []'.
== learn-hub//home/user/learn-hub/plugins/pubmed-research-note [plugin fork (1 skill, 0 commands, bundled .mcp.json) in learn-hub-local marketplace v1.7.0] pubmed-research-note(desc=991,lines=311,fm=name,description)
  pubmed-research-note (learn-hub fork)-1 [H] Evals were not forked: evals 7 and 16 require handing off to vault-keeper, which does not exist in learn-hub and which the fork's SKILL forbids, so they fail by construction; eval 8 expects deference to psych-paper-digest, which the fork's description no longer mentions. || EV: evals.json:56 'then HANDS OFF to vault-keeper rather than acting on the vault itself'; evals.json:128 'The skill hands the finished report to vault-keeper as an
  pubmed-research-note (learn-hub fork)-2 [M] Output pipeline now writes the report twice / to two places with no stated relationship: step 1 writes to the working dir or report_dir, step 3 saves research-notes/<slug>.md; README still advertises report_dir. || EV: fork SKILL.md:153-154 'write the report into the working directory (or `report_dir` ...)'; fork SKILL.md:158-159 'save the finished report as `research-notes/<k
  pubmed-research-note (learn-hub fork)-3 [M] Dangling cross-references introduced by the fork: a 'step 4' that is not listed and an 'Atomic notes' section that does not exist (heading is 'Atomize — opt-in, word-gated'). || EV: fork SKILL.md:164-165 'the **`digest-report` skill** — which is step 4, and is gated on the word *atomize* (see **Atomic notes** below)'; fork SKILL.md:272 '## 
  pubmed-research-note (learn-hub fork)-4 [M] Stale upstream wording left in place contradicts the fork's wiring: report is still 'filed to the vault' 'as an artifact' and atomic notes live in `notes/`. || EV: fork SKILL.md:26 'The report is shown inline and filed to the vault'; fork SKILL.md:171-172 'Filing the report as an artifact is **not** the same as **atomize**
  pubmed-research-note (learn-hub fork)-5 [M] Fork diverges in behaviour from the sibling comprehensive-review fork: here digest is gated on 'atomize', there digest+Supabase sync is the default. || EV: fork SKILL.md:164 'gated on the word *atomize*' vs learn-hub comprehensive-review SKILL.md:166-176 'Two steps, both defaults ... **Digest** it via the **`digest
  pubmed-research-note (learn-hub fork)-6 [M] Version and CHANGELOG not forked: two behaviourally different artifacts share name 'pubmed-research-note' and version 1.7.0, and the fork's CHANGELOG describes vault-keeper filing it no longer does. || EV: fork plugin.json:3 '"version": "1.7.0"' == upstream; `diff` of CHANGELOG.md reports identical; learn-hub .claude-plugin/marketplace.json entries have no version
  pubmed-research-note (learn-hub fork)-7 [L] firecrawl plugin is referenced as an engine but is not in the learn-hub marketplace. || EV: fork SKILL.md (unchanged Firecrawl bullet) 'Via the `firecrawl` plugin (`firecrawl search` / `firecrawl scrape`)'; learn-hub/.claude-plugin/marketplace.json lis
  pubmed-research-note (learn-hub fork)-8 [L] atomic-note-template.md is kept but its own banner tells Claude to ignore its frontmatter, paths and MOC wiring - ~1.3k tokens of mostly-disclaimed content loaded on atomize. || EV: fork atomic-note-template.md:3-10 '⚠ learn-hub fork — this file describes the UPSTREAM ... note shape, which is NOT this repo's format ... ignore its frontmatte
  pubmed-research-note (learn-hub fork)-9 [L] All upstream defects carry over unchanged (Reframed drift vs intent-lock 0.4.2, decision-brief 4+3 slot arithmetic, NCT grammar mismatch, PMIDs-in-Sources contradiction, hardcoded prefixes). || EV: diff -ru shows decision-brief.md, intent-lock-pairing.md, tool-catalog.md, report-craft.md identical; learn-hub/plugins/intent-lock/skills/intent-lock/SKILL.md:
== learn-hub//home/user/learn-hub/plugins/comprehensive-review [plugin fork (1 skill, 1 command, bundled .mcp.json) in learn-hub-local marketplace v0.3.0] comprehensive-review(desc=1011,lines=216,fm=name,description)
  comprehensive-review (learn-hub fork)-1 [H] Digest + Supabase sync is made a default step, contradicting digest-report's own trigger contract, the sibling pubmed fork's atomize gate, and learn-hub's practice of gating DB writes; every review run would write to the shared live database unless the user says 'don't digest this'. || EV: fork SKILL.md:166-176 'Two steps, both defaults ... **Digest** it via the **`digest-report` skill**, which ... then syncs to Supabase ... Skip only on an explic
  comprehensive-review (learn-hub fork)-2 [M] Self-contradiction about what the deliverable is. || EV: fork SKILL.md:180-181 'A report left in `research-notes/` is a draft, not a deliverable' vs fork SKILL.md:205-206 'The file is the deliverable — never restate t
  comprehensive-review (learn-hub fork)-3 [M] Evals not forked: eval 1 requires vault-keeper filing under a MOC, impossible in learn-hub. || EV: evals.json:8 'files it to the vault via vault-keeper under the disorder's MOC' (byte-identical to upstream); fork SKILL.md:178 'There is no `vault-keeper` and n
  comprehensive-review (learn-hub fork)-4 [L] Inconsistent handling of psych-paper-digest: removed from the description's NOT-for but kept in Handoffs and README, although the plugin is not in learn-hub. || EV: fork SKILL.md:199 '**psych-paper-digest** — not chained'; fork README.md:43 'Watchlist surveillance belongs to **psych-paper-digest**.'; learn-hub/.claude-plugi
  comprehensive-review (learn-hub fork)-5 [L] Failure condition and README use stale/garbled filing language after the rewire. || EV: fork SKILL.md:229 'The finished review was not filed to the vault, absent an explicit opt-out'; fork README.md:10-11 'The deliverable is an md file filed to the
  comprehensive-review (learn-hub fork)-6 [L] Version/CHANGELOG not forked; firecrawl dependency absent in learn-hub; all upstream defects (number rule vs thin domains, no engine-failure policy, 'source floors', 'house rules') carry over. || EV: fork plugin.json '"version": "0.3.0"' == upstream; CHANGELOG identical; review-arc.md identical; learn-hub marketplace lists no firecrawl.