#### SKL — Official Agent Skills authoring
[SKL-01] (confirmed|commands-vs-skills|official-docs) Custom slash commands are merged into skills: a .claude/commands/x.md file and a skills/x/SKILL.md both create /x and behave the same. Skills add a supporting-files directory, invocation-control frontmatter and model auto-loading.
   IMPL: A thin commands/<cmd>.md that only forwards to a skill duplicates it. The skill is already invocable as /plugin:skill and takes argument-hint and $ARGUMENTS, so fold the command into the skill.
   SRC: https://code.claude.com/docs/en/skills
[SKL-02] (confirmed|commands-vs-skills|official-docs) In plugins, the commands/ directory is now described as 'skills as flat Markdown files', and new plugins should use skills/. Commands count toward the plugin's projected skill token cost.
   IMPL: The rewrite should retire plugins/*/commands/*.md (e.g. /new-plugin, /digest, /infographic) in favour of skills/ entries. Each extra command is another listing entry competing for the description budget.
   SRC: https://code.claude.com/docs/en/plugins
[SKL-03] (confirmed|frontmatter|official-docs) In Claude Code every frontmatter field is optional and only description is recommended. Field names must match the documented table exactly, and an unrecognized field is silently ignored with no error.
   IMPL: validate.py should whitelist field names (name, description, when_to_use, argument-hint, arguments, disable-model-invocation, user-invocable, allowed-tools, disallowed-tools, model, effort, context, agent, background, hooks, paths, shell, metadata, license, compatibility), because a typo such as allowed_tools silently does nothing.
   SRC: https://code.claude.com/docs/en/skills
[SKL-04] (confirmed|frontmatter|official-docs) Outside Claude Code (claude.ai uploads, the Skills API, package_skill.py), only the six Agent Skills spec fields are allowed, and any other key is a hard error. Claude Code-only body features such as !`cmd` injection do not work there.
   IMPL: If any repo skill will also be enabled on the claude.ai account (for Cowork, cloud sessions or routines), keep its frontmatter to the six spec fields. Otherwise, mark it as Claude Code-only in the plan.
   SRC: https://code.claude.com/docs/en/skills
[SKL-05] (confirmed|frontmatter|official-docs) The platform (API and claude.ai) docs treat name and description as REQUIRED. name: at most 64 chars, only lowercase letters, digits and hyphens, no XML tags, and must not contain the reserved words 'anthropic' or 'claude'. description: non-empty, at most 1,024 chars, no XML tags. This contradicts Claude Code's 'all fields optional' (SKL-03). The difference is by surface, not by date.
   IMPL: Write both name and description on every skill, even though Claude Code tolerates omission, and have validate.py enforce the stricter platform limits so the skills stay portable.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview
[SKL-06] (confirmed|naming|open-standard) The open Agent Skills spec requires name to be 1–64 chars of lowercase a-z, 0-9 and hyphens. It must not start or end with a hyphen, must not contain '--', and must equal the parent directory name.
   IMPL: This matches the repo's existing rule (skill name == directory name). Add the no-leading/trailing-hyphen and no '--' checks to validate.py.
   SRC: https://agentskills.io/specification
[SKL-07] (confirmed|description|official-docs) Claude Code appends when_to_use to description in the skill listing and truncates the combined text at 1,536 characters. The key use case therefore goes first. This per-skill cap is separate from the 1,024-char spec limit on description itself.
   IMPL: Descriptions that pack trigger phrases and NOT-for boundaries into ~1,000 chars could move the trigger phrases into when_to_use (Claude Code-only), so the portable description stays short. Either way, lead with the core use case.
   SRC: https://code.claude.com/docs/en/skills
[SKL-08] (confirmed|discovery-budget|official-docs) The skill listing (names plus descriptions) is capped at 1% of the model's context window by default (skillListingBudgetFraction 0.01; per-skill skillListingMaxDescChars defaults to 1536). On overflow, the descriptions of the least-invoked skills are dropped first, but their names stay. /doctor estimates the listing cost and /skill-doctor reports per-skill cost and usage.
   IMPL: Roughly 28 skills across the two repos, most with ~900–1,150-char descriptions, plus synced claude.ai skills, can overflow the budget. Rarely used skills would then lose their descriptions and stop auto-triggering. Measure with /doctor and /skill-doctor, and set shorter description targets in the plan.
   SRC: https://code.claude.com/docs/en/skills
[SKL-09] (confirmed|description|official-docs) Platform best practices say descriptions must be written in third person ('Processes Excel files…'), not first person ('I can help…') or second person ('You can use this…'), because the description is injected into the system prompt.
   IMPL: Audit descriptions for 'you'/'I' phrasing. Most repo descriptions already use a third-person verb ('Interrogates…', 'Extracts…'), which should become the standard.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-10] (confirmed|description|open-standard) The agentskills.io guide (linked from the Claude Code skills page) instead recommends imperative phrasing addressed to the agent ('Use this skill when…'), a focus on user intent rather than implementation, and being 'pushy'. This is in tension with SKL-09's third-person rule. The two pages carry no dates, so which is newer is unknown. A compatible reading is: open with a third-person 'what' sentence, then add an imperative 'Use when…' clause.
   IMPL: Pick one house pattern and enforce it in the rubric: a third-person capability sentence, then a 'Use when …' trigger clause, then a short 'Not for …' boundary.
   SRC: https://agentskills.io/skill-creation/optimizing-descriptions
[SKL-11] (confirmed|description|official-repo) Anthropic's skill-creator (the source of the repo's evals.json format) says Claude tends to UNDER-trigger skills, so descriptions should be slightly 'pushy' and list the contexts in which to use the skill, even when the user does not name the domain.
   IMPL: The existing trigger-phrase-heavy descriptions follow this advice. Keep that pushiness but control its length (SKL-07/08), and verify it with trigger evals rather than by adding phrases.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[SKL-12] (confirmed|description|official-docs) A description must state both what the skill does and when to use it, with specific key terms, because Claude picks from potentially 100+ skills using the description alone. Vague descriptions ('Helps with documents') are anti-patterns.
   IMPL: Add a rubric item checking that each description has a what-clause and a when-clause containing concrete nouns the user would type (e.g. 'PubMed', '.epub', 'infographic').
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-13] (confirmed|description|open-standard) The recommended description length is a few sentences to a short paragraph, which is enough to cover scope without bloating context across many skills. The spec's 1,024 chars is a hard ceiling, not a target, and descriptions tend to grow during optimization.
   IMPL: Repo descriptions cluster at 990–1,150 chars. Set a soft target (e.g. ≤600 chars) and a hard fail above 1,024 in validate.py. atomize-book (1,075) and ingest-slides (1,150) already exceed the spec limit.
   SRC: https://agentskills.io/skill-creation/optimizing-descriptions
[SKL-14] (partially|evaluation|open-standard) Description triggering should be tested with about 20 realistic queries: 8–10 that should trigger and 8–10 that should not, the latter being near-miss negatives that share keywords. Run each query 3 times with a 0.5 trigger-rate threshold, split queries 60/40 into train and validation, iterate about 5 times, and choose the description by its validation score. Simple one-step queries may not trigger any skill, so they make poor test cases.
   CORRECTED: Test triggering with about 20 realistic queries (8–10 should-trigger, 8–10 near-miss should-not-trigger). Run each 3 times with a 0.5 trigger-rate threshold, keep a fixed 60/40 train/validation split, iterate about 5 times, and pick the description with the best validation pass rate. Agents often skip skills for simple one-step tasks they can handle alone, so the most useful should-trigger queries need the skill but don't obviously name it. Still mix single-step and multi-step queries.
   IMPL: For sibling skills that overlap (intent-lock / decision-interview / plan-critique; concept-animation / ml-concept-lab; comprehensive-review / pubmed-research-note), write near-miss trigger evals that pit each against the others.
   SRC: https://agentskills.io/skill-creation/optimizing-descriptions
   EVID: The numbers are verbatim: "Aim for about 20 queries: 8-10 that should trigger and 8-10 that shouldn't", near-miss negatives, "3 is a reasonable starting point", "0.5 is a reasonable default", Train ~60% / Validation ~40%, "Five iterations is usually enough", "Select the best iteration by its validation pass rate". The last sentence overreaches. The page says a one-step request "may not trigger a PDF skill even if the description matches perfectly", but it also says to "Include single-step tasks alongside multi-step workflows". It does not call them poor test cases.
[SKL-15] (confirmed|size|open-standard) Keep the SKILL.md body under 500 lines and about 5,000 tokens: only the core instructions needed on every run. Metadata costs about 100 tokens per skill and is always loaded. Bundled files cost nothing until they are read.
   IMPL: atomize-book's SKILL.md is 1,274 lines and must be split into references/. Add a line check and a rough token check (e.g. chars/4 ≤ 5,000) to validate.py.
   SRC: https://agentskills.io/skill-creation/best-practices
[SKL-16] (confirmed|progressive-disclosure|official-docs) Reference files should sit one level deep from SKILL.md. When a file is only reachable through another referenced file, Claude may preview it partially (e.g. with head -100) and miss content.
   IMPL: validate.py can parse markdown links in references/*.md and flag any reference file that links to another reference file SKILL.md does not link to directly.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-17] (confirmed|progressive-disclosure|official-docs) Long reference files should open with a table of contents. The two Anthropic sources disagree on the threshold: platform best practices say over 100 lines, skill-creator says over 300 lines.
   IMPL: Adopt the stricter 100-line threshold (skill-creator states '>300 lines') and have validate.py require a '## Contents' block near the top of any references/*.md longer than 100 lines.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-18] (confirmed|progressive-disclosure|open-standard) Each pointer to a bundled file should say WHEN to load it (the trigger condition), not just that it exists.
   IMPL: Rewrite 'see references/…' lines as conditional loads, e.g. 'Before rendering, read references/design-system.md'.
   SRC: https://agentskills.io/skill-creation/best-practices
[SKL-19] (confirmed|progressive-disclosure|anthropic-blog) Contexts that are mutually exclusive or rarely used together belong in separate files, so a task loads only the path it needs. Code can serve as both an executable tool and documentation, but the skill must make clear which role applies.
   IMPL: Split mode-specific branches (e.g. firecrawl paths A–F, vault-keeper's five jobs, atomize-book's EPUB/PDF paths) into per-mode reference files.
   SRC: https://www.anthropic.com/engineering/equipping-agents-for-the-real-world-with-agent-skills
[SKL-20] (confirmed|writing-style|official-docs) Conciseness is the default assumption: Claude is already very smart, so include only context it lacks. For each piece of content, cut it unless the agent would get the task wrong without it.
   IMPL: Rewrite passes should strip general explanation (what a PDF, PubMed or an RCT is) and keep project-specific conventions, gotchas and exact commands.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-21] (confirmed|writing-style|official-docs) In Claude Code, a loaded skill stays in context across turns, so every line is a recurring token cost. The Claude Code docs say to state what to do rather than narrate how or why.
   IMPL: Long rationale paragraphs in SKILL.md bodies (history of why a rule exists) should move to references/ or ADRs. The body keeps the rule itself.
   SRC: https://code.claude.com/docs/en/skills
[SKL-22] (partially|writing-style|open-standard) Sources conflict on emphasis versus explanation. skill-creator and agentskills.io say reasoning-based instructions beat rigid ALWAYS/NEVER directives, and treat all-caps MUST as a yellow flag. Platform best practices, in contrast, show Claude A proposing 'stronger language such as "MUST filter"' when a rule is missed. The Claude Code docs (SKL-21) favour stating what to do over narrating why.
   CORRECTED: skill-creator and agentskills.io prefer explaining the reason over rigid ALWAYS/NEVER directives. Only skill-creator calls all-caps ALWAYS/NEVER a 'yellow flag'. The platform page only mentions that a helper Claude might suggest stronger wording such as 'MUST filter' for the author to review, so it does not really endorse emphasis. The real tension is with the Claude Code docs, which say to state what to do rather than narrate how or why. A reconciliation: keep the why to one short clause.
   IMPL: House rule for the rewrite: give each rule a one-clause reason ('X, because Y'). Reserve MUST/NEVER for truly load-bearing constraints (never fabricate clinical facts, move→verify→delete order), since many emphasised lines dilute each other.
   SRC: https://agentskills.io/skill-creation/evaluating-skills
   EVID: agentskills evaluating-skills: "Reasoning-based instructions ... work better than rigid directives (\"ALWAYS do X, NEVER do Y\")" is verbatim. The 'yellow flag' wording is only in skill-creator: "If you find yourself writing ALWAYS or NEVER in all caps... that's a yellow flag", plus "in lieu of heavy-handed musty MUSTs". agentskills does not use that phrase. The platform page shows Claude A might suggest "stronger language such as \"MUST filter\"", but as a suggestion for the author to review (step 4 is literally "Review Claude A's suggestions"), not an endorsement. The sharper tension is with
[SKL-23] (confirmed|writing-style|official-docs) Match the degree of freedom to how fragile the task is. Use high freedom (text heuristics) when many approaches are valid, medium (pseudocode or templates) when a preferred pattern exists, and low (exact scripts, 'do not modify the command') for fragile, sequence-critical operations.
   IMPL: Label sections by freedom level: sync, bump, delete and vault-drain steps are low-freedom exact commands, while report shaping and chapter structure are high freedom.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-24] (confirmed|anti-patterns|official-docs) Avoid presenting several equal options. Give one default with a narrow escape hatch.
   IMPL: Where a skill lists several engines or tools as peers (e.g. five search engines in pubmed-research-note), state the default engine and the specific condition for each alternative.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-25] (confirmed|anti-patterns|official-docs) Do not put time-sensitive information in skills (e.g. 'before August 2025 use…'). Put the current method in the body and collapse deprecated approaches into an 'Old patterns' section.
   IMPL: Dated incident notes ('found 2026-08-…', version-history asides) belong in CHANGELOG/MEMORY or an Old-patterns details block, not in the instructions Claude follows.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-26] (confirmed|anti-patterns|official-docs) Use one term consistently throughout a skill, for example always 'field' and never a mix of field/box/element/control.
   IMPL: Fix one glossary per repo (e.g. 'vault' vs 'hub', 'report' vs 'note' vs 'artifact', 'asset') and apply it across skills, since cross-plugin handoffs depend on shared terms.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-27] (confirmed|anti-patterns|official-docs) Always use forward slashes in skill file paths. Claude Code also rejects plugin component paths that contain a backslash on macOS and Linux.
   IMPL: The owner works on Windows (e.g. ingest-article hardcodes C:\Users\User\Desktop\...). Move machine-specific paths out of descriptions and into config or env variables, and make validate.py flag backslash paths.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-28] (confirmed|workflows|official-docs) For complex multi-step tasks, give sequential steps plus a checklist that Claude copies into its response and ticks off, so it does not skip validation steps.
   IMPL: Pipelines such as atomize-book, ingest-article, empty-vault and comprehensive-review should each open with a copyable progress checklist in place of prose step descriptions.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-29] (confirmed|workflows|official-docs) Build in feedback loops (run validator → fix → repeat, and proceed only after it passes). For batch, destructive or high-stakes operations, use plan-validate-execute: write a structured plan file, validate it with a script against the source of truth, then execute.
   IMPL: Existing gates (validate.py, qc-gate.py, check-figures.py) fit this pattern. Make each skill say 'only proceed when X passes', and make vault-drain and sync operations write a plan file that is validated before execution.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-30] (confirmed|workflows|open-standard) A 'Gotchas' list is often the highest-value content in a skill: concrete, environment-specific corrections. It should live in SKILL.md, where the agent sees it before hitting the situation. Each correction the user has to make should be added to it.
   IMPL: learn-hub's CLAUDE.md holds a very large gotchas corpus that loads every session. Move skill-specific gotchas (e.g. figure-extraction and sync traps) into the owning skill's SKILL.md, or into a clearly triggered reference file.
   SRC: https://agentskills.io/skill-creation/best-practices
[SKL-31] (confirmed|scripts|official-docs) Scripts should solve errors themselves rather than defer them to Claude, and constants must be justified in comments (no 'voodoo constants'). The instructions must say whether Claude should EXECUTE a script (the common case, where only its output enters context) or READ it as reference.
   IMPL: Every scripts/ reference in a SKILL.md should read 'Run `…`' or 'See `…` for the algorithm'. Logic that agents currently re-derive in prose (e.g. manual char counts, YAML checks) belongs in a tested script.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-32] (confirmed|scripts|open-standard) Scripts built for agents must be non-interactive (no TTY prompts) and document themselves via --help. They should send structured data to stdout and diagnostics to stderr, return helpful errors and meaningful exit codes, be idempotent, offer --dry-run for destructive actions, and keep output size bounded.
   IMPL: Audit bump.py, route.py, validate.py and the learn-hub sync/purge scripts against this checklist. Most already dry-run by default, but add JSON output and --help where they are missing.
   SRC: https://agentskills.io/skill-creation/using-scripts
[SKL-33] (confirmed|substitutions|official-docs) ${CLAUDE_SKILL_DIR} is the skill's own directory, ${CLAUDE_PLUGIN_ROOT} the plugin root, ${CLAUDE_PLUGIN_DATA} a persistent data directory, and ${CLAUDE_PROJECT_DIR} the project root. They are substituted in the body and in Bash allowed-tools rules, so the same variable in both places lets a bundled script run without a permission prompt.
   IMPL: Replace relative or cwd-dependent script paths in plugin skills with ${CLAUDE_SKILL_DIR}/scripts/… or ${CLAUDE_PLUGIN_ROOT}/…, paired with a matching allowed-tools: Bash(${CLAUDE_SKILL_DIR}/scripts/x.py *).
   SRC: https://code.claude.com/docs/en/skills
[SKL-34] (confirmed|substitutions|official-docs) Argument placeholders are $ARGUMENTS, $ARGUMENTS[N] / $N (0-based, shell-quoted) and named $name from the `arguments` field. If no placeholder receives the arguments, Claude Code appends 'ARGUMENTS: <input>'. A literal $ before a digit or placeholder name must be escaped as \$.
   IMPL: When folding commands into skills, use argument-hint plus $ARGUMENTS, and grep skill bodies for unescaped $<digit> in prose (dosing and price examples) that would otherwise be substituted.
   SRC: https://code.claude.com/docs/en/skills
[SKL-35] (partially|dynamic-context|official-docs) !`cmd` (inline) and ```! (block) run shell commands before the skill is sent to Claude, and their output replaces the placeholder. Any non-zero exit aborts the WHOLE invocation (append `|| true` where non-zero is expected). Commands run in the session's cwd, are checked against permission rules (anything short of allow aborts outside auto mode), and never run for skills synced from claude.ai.
   CORRECTED: !`cmd` (inline, only at line start or after whitespace) and ```! blocks run before the skill reaches Claude, and their output replaces the placeholder. A failed command aborts the whole invocation. With bash, any non-zero exit fails, except that exit code 1 from search/comparison commands (e.g. grep) counts as a normal result. Append `|| true` to other commands that may exit non-zero, such as check scripts. Commands run in the session's current cwd (use ${CLAUDE_SKILL_DIR}/${CLAUDE_PROJECT_DIR} in paths). They are checked against permission rules, and outside auto mode anything short of allow aborts. They are not run for claude.ai-synced skills in local (non-cloud) sessions.
   IMPL: Use injection for cheap live state (e.g. !`git status --short`, current version from plugin.json, the vault index), always with absolute ${CLAUDE_*} paths and `|| true`. Never use it for anything that should work when the skill is synced to claude.ai.
   SRC: https://code.claude.com/docs/en/skills
   EVID: skills.md confirms that injected commands run before the content is sent, that output replaces the placeholder, and that "A failed command aborts the entire skill invocation". It also confirms they run in the session shell's cwd, that anything short of allow aborts outside auto mode, and that they never run for claude.ai-synced skills on your machine (v2.1.228+). But 'any non-zero exit aborts' overstates it. The page says: "One carveout applies: Claude Code treats exit code 1 from search and comparison commands as a normal result and injects their output. Exit codes of 2 or higher fail even fo
[SKL-36] (confirmed|invocation-control|official-docs) disable-model-invocation: true makes a skill user-only. Its description is removed from Claude's context, it cannot be preloaded into subagents or fired by scheduled tasks, and it is meant for side-effect workflows. user-invocable: false hides a skill from the / menu for background knowledge that Claude alone should apply.
   IMPL: Candidates are empty-vault, release/bump flows and purge-style operations. Caveat: other plugins delegate to vault-keeper through the Skill tool, and disable-model-invocation blocks exactly that, so split destructive jobs into their own user-only skill.
   SRC: https://code.claude.com/docs/en/skills
[SKL-37] (confirmed|invocation-control|official-docs) allowed-tools only pre-approves the listed tools for the turn that invokes the skill; the grant clears on the next user message. It does NOT restrict which tools exist. disallowed-tools removes tools while the skill is active. Workspace trust does not gate allowed-tools.
   IMPL: Do not rely on allowed-tools as a safety fence. Use disallowed-tools (e.g. keep AskUserQuestion out of autonomous fallbacks, or Write out of read-only skills) or hooks, and keep grants narrow, since any repo checkout can self-grant them.
   SRC: https://code.claude.com/docs/en/skills
[SKL-38] (confirmed|invocation-control|official-docs) context: fork runs the skill as the prompt of a new subagent (agent: Explore, Plan, general-purpose or a custom type) that does NOT see the conversation history. It runs in the background by default (background: false waits for it) and only makes sense for skills that contain an explicit task.
   IMPL: Self-contained heavy pipelines (literature sweeps, coverage reports) are fork candidates. Interview or gate skills (intent-lock, decision-interview, plan-critique) must NOT fork, because they need the conversation and the user.
   SRC: https://code.claude.com/docs/en/skills
[SKL-39] (confirmed|frontmatter|official-docs) model and effort frontmatter override the session's model and effort level while the skill is active. The model override lasts for the current turn only and is not saved. With context: fork, model sets the subagent's model.
   IMPL: Consider effort: high on adjudication-heavy skills (comprehensive-review, pubmed-research-note) instead of prose pleas for thoroughness. Leave model unset unless measured, since values outside availableModels are ignored.
   SRC: https://code.claude.com/docs/en/skills
[SKL-40] (confirmed|lifecycle|official-docs) An invoked skill's rendered content enters the conversation once and is not re-read on later turns. After auto-compaction, only the first 5,000 tokens of each skill's latest invocation are re-attached, within a combined 25,000-token budget filled from the most recently invoked skill.
   IMPL: Put standing rules (fidelity contracts, never-fabricate, handoff targets) in the first ~5k tokens of each SKILL.md and write them as rules that hold throughout the session. Anything past that point can disappear after compaction.
   SRC: https://code.claude.com/docs/en/skills
[SKL-41] (confirmed|frontmatter|official-docs) Skills can declare hooks in frontmatter using the settings hook format. A skill hook is registered when the skill is invoked and keeps running for the rest of the session, unless once: true removes it after its first successful run. The Claude Code docs recommend hooks for enforcing behavior deterministically.
   IMPL: Rules that are currently only prose ('run validate.py before commit', 'never write vault files directly') could become skill-scoped PreToolUse or Stop hooks. Note that they persist beyond the skill's own turn.
   SRC: https://code.claude.com/docs/en/hooks
[SKL-42] (confirmed|frontmatter|official-docs) The paths frontmatter field takes glob patterns. Claude then auto-loads the skill only when working with files that match them.
   IMPL: learn-hub project skills tied to file areas (e.g. sync-vault on vault/**, check-repetition on vault notes) could use paths to cut false triggers. Test first, because paths also narrows auto-loading for chat-only requests.
   SRC: https://code.claude.com/docs/en/skills
[SKL-43] (partially|evaluation|official-docs) Evaluation-driven development: create evaluations BEFORE writing extensive docs. Find gaps by running Claude without the skill, build three scenarios, measure a no-skill baseline, write minimal instructions and iterate. The platform page adds that no built-in runner exists; that statement predates or ignores Claude Code's `claude plugin eval` (SKL-46).
   CORRECTED: Evaluation-driven development: create evaluations before writing extensive docs. Run Claude without the skill to find gaps, build at least three scenarios, measure a no-skill baseline, write minimal instructions, and iterate. The platform page says there is no built-in runner for its JSON eval format. For Claude Code plugins, `claude plugin eval` (v2.1.269+) and the skill-creator plugin now fill that gap. Their formats differ from the platform's example.
   IMPL: Each rewritten skill should ship with at least 3 evals and a recorded with/without baseline before its body is expanded. A skill with no measurable delta over baseline is a cut candidate.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   EVID: The five steps are verbatim on the platform page: "Create evaluations BEFORE writing extensive documentation"; Identify gaps (run without a Skill), "Build three scenarios", Establish baseline, Write minimal instructions, Iterate. It also says "There is not currently a built-in way to run these evaluations." That note is about the API/platform surface. Calling it something that 'predates or ignores' claude plugin eval is the researcher's speculation: the page carries no date, and plugin eval is a Claude Code-only command (v2.1.269+). skill-creator's eval loop also exists.
[SKL-44] (confirmed|evaluation|official-docs) Claude Code's guidance measures triggering and output quality separately, running each realistic prompt in a FRESH session with and without the skill, because leftover authoring context masks gaps in the written instructions.
   IMPL: The rewrite plan must not accept 'it worked in the authoring session' as evidence. Verification runs happen in isolated subagents or new sessions.
   SRC: https://code.claude.com/docs/en/skills
[SKL-45] (confirmed|evaluation|official-docs) Iterate with two Claude instances. Claude A (the expert) helps write and refine the skill; Claude B (fresh, skill loaded) uses it on real tasks. Observations from B go back to A. Also watch how Claude navigates the skill: unexpected read order, missed links, overused files, and bundled files it never opens.
   IMPL: The rewrite workflow should read transcripts of skill runs. A references/ file that no transcript ever opens is either unneeded or poorly signposted (SKL-18).
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-46] (confirmed|evaluation|official-docs) Claude Code (v2.1.269+) has `claude plugin eval`. Cases live in <plugin>/evals/ as case.yaml or prompt.md plus graders/*.md. Each case runs 3 times, a no-plugin baseline is compared by default, and the run exits non-zero below a threshold, so it can gate CI. A `tool_used: Skill` grader measures triggering. Its format is explicitly separate from skill-creator's evals/evals.json.
   IMPL: The repo's per-skill evals/evals.json (skill-creator format) cannot be run by `claude plugin eval`. The plan must either keep skill-creator for iteration and add plugin-level case dirs for CI, or convert. The two eval dirs would sit at different levels (plugin/evals/ vs skills/<s>/evals/).
   SRC: https://code.claude.com/docs/en/plugin-evals
[SKL-47] (confirmed|evaluation|official-repo) The two published evals.json schemas disagree on the assertion field name. skill-creator's schema uses `expectations`; agentskills.io's 'Evaluating skill output quality' uses `assertions` for the same list of verifiable statements.
   IMPL: validate.py should pin one field name, `expectations` if skill-creator's scripts are the runner, and reject the other so evals do not silently go ungraded.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/references/schemas.md
[SKL-48] (confirmed|evaluation|open-standard) Write assertions after seeing the first outputs. Make them objectively checkable, not vague ('output is good') and not brittle exact-phrase matches. Require concrete evidence for each PASS, and remove assertions that pass both with and without the skill, since they show no skill value.
   IMPL: Review existing evals.json expectations for ones the model satisfies without the skill (e.g. 'produces markdown'). Replace them with repo-specific checks such as a handoff to vault-keeper, a marked verdict line, or a safety banner being present.
   SRC: https://agentskills.io/skill-creation/evaluating-skills
[SKL-49] (partially|validation|official-cli) `claude plugin validate <dir>` checks skills, agents and commands in a directory, including SKILL.md frontmatter that fails to parse. --strict treats warnings (unrecognized fields, missing metadata) as errors for CI. In this session, `claude plugin validate --strict` passed on the micky-psych-tools marketplace and on learn-hub/.claude/skills.
   CORRECTED: `claude plugin validate <dir>` (v2.1.233+ for non-manifest runs) reports SKILL.md/agent/command frontmatter that fails to parse. --strict turns warnings such as unrecognized fields into exit 1 for CI. A run from the marketplace root checks only marketplace.json and never opens skill files, so CI must validate each plugins/<name>/ directory (and .claude/skills) separately. In this session, --strict passed on the marketplace manifest, on all 14 micky-psych-tools plugin directories, and on learn-hub/.claude/skills.
   IMPL: Call `claude plugin validate --strict` from validate.py (or CI) in addition to the custom checks. Keep the custom checks for the rules the official validator does not cover: version parity, description length, line and token budgets, and the evals schema.
   SRC: https://code.claude.com/docs/en/skills
   EVID: The quote is verbatim in skills.md (v2.1.233+), and --strict "Treat[s] warnings as errors and exit 1... such as unrecognized fields". But plugin-marketplaces says: "From a marketplace directory, Claude Code doesn't open the plugins' skill, agent, command, or hook files." So a marketplace-root pass says nothing about SKILL.md frontmatter. I re-ran it locally (claude 2.1.280): `claude plugin validate --strict .` in micky-psych-tools gives 'Validating marketplace manifest... Validation passed', only the manifest. I also ran it on each of the 14 plugins/*/ dirs, and every one passed. learn-hub/.cl
[SKL-50] (confirmed|validation|official-docs) If SKILL.md frontmatter YAML is malformed, Claude Code still loads the body but with empty metadata, so /name works while Claude cannot match the description and auto-triggering silently breaks.
   IMPL: Frontmatter parse errors are a silent triggering failure. validate.py should parse YAML strictly and fail on errors rather than warn.
   SRC: https://code.claude.com/docs/en/skills
[SKL-51] (confirmed|validation|local-repo) Local check: strict PyYAML (as used by skill-creator's quick_validate.py and package_skill.py) FAILS to parse the unquoted `description:` of several repo skills, because an inner ': ' sequence is read as a mapping. Affected: micky-psych-tools intent-lock, learn-hub ingest-article, concept-animation, ingest-infographic and ingest-animation. Claude Code's own loader and `claude plugin validate --strict` accepted them.
   IMPL: Write every description as a quoted string or a folded block scalar (`description: >-`) so it parses under strict YAML (portability to claude.ai and the API). Add a yaml.safe_load check to validate.py.
   SRC: local: python3 -c "yaml.safe_load(frontmatter)" over plugins/*/skills/*/SKILL.md and learn-hub/.claude/skills/*/SKILL.md
[SKL-52] (partially|size|local-repo) Local measurement: learn-hub atomize-book SKILL.md is 1,274 lines with a 1,075-char description, and ingest-slides has a 1,150-char description. Both exceed the spec's 1,024-char description limit, and atomize-book is about 2.5× the 500-line body guidance. The next largest are firecrawl (384 lines) and ingest-article (378 lines).
   CORRECTED: Three learn-hub skills exceed the spec's 1,024-char description limit: atomize-book (1,075), ingest-slides (1,150) and ingest-article (about 1,137, measured leniently because strict YAML won't parse it). Several others sit at 1,000–1,022 with no headroom. atomize-book's SKILL.md is 1,274 lines, about 2.5× the 500-line guidance. The next largest are firecrawl (384) and ingest-article (378).
   IMPL: Priority targets for the progressive-disclosure split and description trimming are atomize-book first, then ingest-slides, firecrawl and ingest-article.
   SRC: local: wc -l + yaml description length over /home/user/{micky-psych-tools/plugins/*/skills/*,learn-hub/.claude/skills/*}/SKILL.md
   EVID: Re-measured: atomize-book is 1,274 lines with a 1,075-char description; ingest-slides has a 1,150-char description; firecrawl is 384 lines and ingest-article 378. All correct. The list of over-limit descriptions is incomplete because PyYAML could not parse five files. Measured with a lenient regex, learn-hub ingest-article's description is about 1,137 characters, also over 1,024. intent-lock (1,020), learn-hub concept-animation (~1,000), comprehensive-review (1,020) and micky concept-animation (1,022) sit just under.
[SKL-53] (confirmed|mcp|official-docs) Skills should refer to MCP tools by fully qualified name. In Claude Code, a tool from a plugin-bundled MCP server is named mcp__plugin_<plugin>_<server>__<tool>, and that full form is required in allowed-tools, permission rules and hook matchers. Bare-server patterns never match it.
   IMPL: Skills that call PubMed, ClinicalTrials or Firecrawl should name the exact tools (e.g. mcp__PubMed__search_articles for a connector, mcp__plugin_firecrawl_<server>__… if bundled). The platform page's 'ServerName:tool_name' format is the API-side convention, not Claude Code's.
   SRC: https://code.claude.com/docs/en/mcp
[SKL-54] (confirmed|naming|official-docs) Skill names should follow a consistent pattern; gerund form (processing-pdfs) is suggested, noun phrases and action-oriented names are acceptable, and vague or generic names (helper, utils, data) should be avoided.
   IMPL: The repo's noun-phrase names are 'acceptable alternatives'. Keep them, since renaming changes /commands and routing, but require new skills to follow one pattern.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[SKL-55] (partially|naming|official-docs) When names collide, a synced claude.ai skill loses the bare /name to any local, plugin or command entry and stays reachable only as /anthropic-skills:<name>. Both entries still appear in Claude's listing. Name comparison ignores case, spacing and dash variants.
   CORRECTED: When a claude.ai-synced skill's name collides with any other command (built-in, bundled skill, local skill, .claude/commands file, plugin skill or MCP prompt), /<name> runs the other one and the synced skill stays reachable only as /anthropic-skills:<name> (v2.1.269+). Name comparison ignores case, spacing, invisible characters and dash/fullwidth variants. The docs do not say how both appear in Claude's model-facing listing.
   IMPL: This account syncs a claude.ai 'psych-paper-digest' skill (daily 8–12-paper digest) alongside the repo plugin of the same name (watchlist triage). Two descriptions for overlapping intents compete in the listing, so rename one or sharpen both 'Not for' boundaries.
   SRC: https://code.claude.com/docs/en/skills
   EVID: skills.md confirms: "When another command uses that short name, /<name> runs the other command, and the synced skill runs only as /anthropic-skills:<name>." The other command can be a built-in, a bundled skill, a local skill, a .claude/commands file, a plugin skill or an MCP prompt. Name comparison "ignores case, spacing, and invisible characters, and treats ... dash variants as their plain equivalents" (v2.1.228+). But 'Both entries still appear in Claude's listing' appears nowhere in skills.md or commands.md. The page only says /skills and /context group synced skills under 'claude.ai sync'.
[SKL-56] (confirmed|scope|open-standard) Scope each skill as one coherent unit of work, like a well-designed function. Too-narrow skills force several to load and risk conflicting instructions; too-broad skills are hard to trigger precisely. Overly comprehensive skills can also hurt, because the agent follows instructions that don't apply.
   IMPL: Use this as the test for merge and split decisions, e.g. whether vault-keeper's five jobs form one unit, and whether intent-lock, decision-interview and plan-critique should stay three skills with sharply disjoint triggers.
   SRC: https://agentskills.io/skill-creation/best-practices
[SKL-57] (confirmed|security|official-docs) Treat skills like installed software: audit every bundled file (SKILL.md, scripts, assets) for unexpected network calls, file access or tool use. Skills that fetch external URLs carry particular risk because fetched content can contain instructions.
   IMPL: Skills that ingest web or PDF content (firecrawl, ingest-article, pubmed-research-note) should state explicitly that fetched text is data, never instructions, and keep allowed-tools narrow.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/overview
--- missed_practices
MP-SKL-1: Use the platform's pre-share 'Checklist for effective Skills' as a ready-made rubric. It covers core quality, code/scripts and testing, including: at least three evaluations, tested with Haiku, Sonnet and Opus, and required packages listed and verified. It maps directly onto a per-plugin rewrite scorecard.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   Q: Before sharing a Skill, verify: ... [ ] SKILL.md body is under 500 lines ... [ ] Required packages listed in instructions and verified as available ... [ ] At least three evaluations created [ ] Tested with Haiku, Sonnet, and Opus
MP-SKL-2: Test a skill against every model it will run on. Effectiveness depends on the model: Haiku may need more guidance, while Opus may be hurt by over-explanation.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   Q: Skills act as additions to models, so effectiveness depends on the underlying model. Test your Skill with all the models you plan to use it with.
MP-SKL-3: Because the skill file is not re-read on later turns, write guidance that must hold for a whole task as standing instructions, not one-time steps. This matters for long multi-phase skills like atomize-book or comprehensive-review.
   SRC: https://code.claude.com/docs/en/skills
   Q: Claude Code does not re-read the skill file on later turns, so write guidance that should apply throughout a task as standing instructions rather than one-time steps.
MP-SKL-4: In a plugin skill, the frontmatter `name` becomes the last segment of the command (/plugin:name). In a personal or project skill it is only a display label and the directory name sets the command. So a name/directory mismatch silently renames a plugin skill's command, and name == directory should be enforced for plugin skills.
   SRC: https://code.claude.com/docs/en/skills
   Q: In a personal or project skill, `name` sets only the display label shown in skill listings, and the command still comes from the directory name. In a plugin skill, `name` sets the last segment of the command and the plugin prefix stays in place.
MP-SKL-5: `claude plugin validate` run at the marketplace root does not check any SKILL.md. CI must validate each plugin directory (and a `skills`-named dir for plugin-root SKILL.md files), or frontmatter parse errors go undetected.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces
   Q: From a marketplace directory, Claude Code doesn't open the plugins' skill, agent, command, or hook files. To find errors in those files, see Validate a plugin or a directory without a manifest.
MP-SKL-6: When output format matters, give a concrete template or input/output examples rather than describing the format in prose. Keep short templates inline and move long or conditional ones to assets/.
   SRC: https://agentskills.io/skill-creation/best-practices
   Q: When you need the agent to produce output in a specific format, provide a template. This is more reliable than describing the format in prose, because agents pattern-match well against concrete structures.
MP-SKL-7: Ground a skill in real expertise and real traces: complete the task hands-on, capture the corrections you made, and read execution traces (not just outputs) to find vague instructions, instructions that don't apply, and menus without defaults. Don't generate skills from generic LLM knowledge.
   SRC: https://agentskills.io/skill-creation/best-practices
   Q: A common pitfall in skill creation is asking an LLM to generate a skill without providing domain-specific context — relying solely on the LLM's general training knowledge. The result is vague, generic procedures
MP-SKL-8: Procedures that have grown inside CLAUDE.md belong in a skill, because a skill's body loads only on use while CLAUDE.md is always in context. This is directly relevant to learn-hub's very large CLAUDE.md, whose many procedural gotchas are paid on every turn.
   SRC: https://code.claude.com/docs/en/skills
   Q: Create a skill when you keep pasting the same instructions, checklist, or multi-step procedure into chat, or when a section of CLAUDE.md has grown into a procedure rather than a fact. Unlike CLAUDE.md content, a skill's body loads only when it's used, so long reference material costs almost nothing 
--- open_questions
OQ: Which description style is newer or authoritative: third person (platform best-practices, SKL-09) or imperative 'Use this skill when…' (agentskills.io, SKL-10)? Neither page carries a date, and GitHub history for agentskills/agentskills was not reachable from this session.
OQ: Is Claude Code's tolerance of unquoted ': ' inside description (SKL-51) a guaranteed behaviour or an incidental parser leniency? The docs only promise that malformed YAML loads with empty metadata.
OQ: The listing budget is '1% of the model's context window', but the override variable SLASH_COMMAND_TOOL_CHAR_BUDGET is a character count. Is the default budget counted in tokens or characters? Run /doctor in a session that loads both repos to measure real overflow.
OQ: Do thin plugin commands/ entries and their target skills each take a separate listing slot (double-counting against the budget)? `claude plugin details` was not usable on an unloaded local plugin path, so this was not measured.
OQ: Is `claude plugin eval` generally available on this account? Its docs have troubleshooting entries for 'plugin eval is currently in early access' and 'currently unavailable'. Should CI adopt it alongside the skill-creator evals.json format?
OQ: How should destructive delegated skills (e.g. vault-keeper's empty job) be restricted? disable-model-invocation blocks Claude's Skill-tool delegation from other plugins, so a split into separate user-only skills may be needed.
OQ: Should repo skills that are also enabled on claude.ai (synced) restrict themselves to the six spec frontmatter fields, or be marked Claude Code-only? That depends on which surfaces the owner actually uses them from.
#### PLG — Official Claude Code plugin + marketplace structure
[PLG-01] (confirmed|manifest-schema|official-docs) plugin.json is optional; when present, `name` (kebab-case) is the only required field. Components are auto-discovered from default locations without a manifest.
   IMPL: validate.py should not demand fields beyond name as errors; keep description/author/keywords as house-style warnings, not hard failures.
   SRC: https://code.claude.com/docs/en/plugins-reference#required-fields
[PLG-02] (partially|manifest-schema|official-docs) Unknown top-level plugin.json fields are ignored at runtime and reported by `claude plugin validate` only as warnings; wrong-typed known fields are load errors. `--strict` turns warnings into exit 1 and is the recommended CI mode.
   CORRECTED: Unknown top-level plugin.json fields are ignored at runtime and only warned by `claude plugin validate`; a wrong-typed value on most recognized fields is a load error (except `experimental` and `metadata`, which are ignored with a warning). `--strict` turns warnings into exit 1 and the docs recommend it for CI.
   IMPL: Health check should run `claude plugin validate --strict` (per plugin and on the marketplace) before/alongside the custom validate.py, so typos in manifests fail CI.
   SRC: https://code.claude.com/docs/en/plugins-reference#plugin-validate
   EVID: L513-533: unrecognized fields are ignored and reported as warnings; --strict row (L1334) says "Use in CI to catch issues the runtime tolerates". But the wrong-type rule is "Most fields: the plugin fails to load" with an explicit exception: "`experimental` and `metadata`: Claude Code ignores a non-object value, and `claude plugin validate` reports a warning."
[PLG-03] (confirmed|manifest-schema|official-docs) Type errors on recognized fields break loading (e.g. keywords as a string), while `experimental` and `metadata` with non-object values are only warnings.
   IMPL: Any generator in plugin-creator that writes manifests must emit arrays for keywords/paths; schema-check with the official validator rather than re-implementing types in validate.py.
   SRC: https://code.claude.com/docs/en/plugins-reference#unrecognized-fields
[PLG-04] (confirmed|manifest-schema|official-docs) The full manifest schema includes displayName, version, description, author{name,email,url}, homepage, repository, license, keywords, metadata (free-form, unread), defaultEnabled, component path fields (skills, commands, agents, workflows, hooks, mcpServers, outputStyles, lspServers), experimental.{themes,monitors,evals}, userConfig, channels, dependencies, and `$schema` for editor validation (ignored at load).
   IMPL: Add `$schema` to every plugin.json/marketplace.json for editor checking; put house-only data (e.g. routing hints) under `metadata` rather than inventing top-level keys.
   SRC: https://code.claude.com/docs/en/plugins-reference#metadata-fields
[PLG-05] (confirmed|manifest-schema|official-cli) `claude plugin init` (CLI 2.1.280) scaffolds a skills-dir plugin with `$schema: https://anthropic.com/claude-code/plugin.schema.json`, version 0.1.0, a root SKILL.md with `"skills": ["./"]`, and optional skills/agents/hooks folders via --with; its skill template tells authors to put trigger phrases in the description.
   IMPL: plugin-creator templates can mirror the official scaffold ($schema line, skills/ layout) instead of a bespoke skeleton; use the CLI scaffold as the reference shape.
   SRC: claude plugin init demo-plugin --with skills agents hooks (run with HOME=$(mktemp -d))
[PLG-06] (confirmed|versioning|official-docs) Version precedence: plugin.json `version` always wins over the marketplace entry `version`, silently. Official docs explicitly advise NOT setting version in both places.
   IMPL: The CLAUDE.md hard rule 'Version lives in two files' and bump.py's dual-write contradict current guidance; plan to keep version only in plugin.json (or omit both) and drop per-entry versions from marketplace.json.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#version-resolution-and-release-channels
[PLG-07] (confirmed|versioning|official-docs) Resolution order when version is unset: plugin.json → marketplace entry → git commit SHA (for github/url/git-subdir and relative-path sources in a git-hosted marketplace) → archive digest → 'unknown'.
   IMPL: For a single-owner, actively edited marketplace hosted on GitHub, omitting version entirely gives automatic commit-SHA updates with zero bump ceremony.
   SRC: https://code.claude.com/docs/en/plugins-reference#version-management
[PLG-08] (confirmed|versioning|official-docs) Explicit versions pin updates: commits without a bump are invisible to /plugin update. Commit-SHA versioning (omit version in both files) is the documented choice for internal plugins under active development.
   IMPL: Decide per repo: explicit semver (keep bump.py, single-file) or SHA versioning (retire bump.py). Either way, never both files.
   SRC: https://code.claude.com/docs/en/plugins-reference#version-management
[PLG-09] (confirmed|versioning|official-docs) Plugins loaded in place from a marketplace added as a local directory ignore version pinning: edits apply at next session start or /reload-plugins with no bump.
   IMPL: The owner's own dev loop (local-dir marketplace or --plugin-dir) never needs bumps; versions only matter for GitHub-sourced installs, which weakens the case for mandatory per-change bumping.
   SRC: https://code.claude.com/docs/en/plugins-reference#plugin-caching-and-file-resolution
[PLG-10] (confirmed|validation|official-docs) Validating a marketplace directory checks marketplace.json schema, duplicate names, path traversal, each local plugin's plugin.json, and warns on entry/plugin.json version mismatch — but it does NOT open skill, agent, command or hook files.
   IMPL: CI must also run `claude plugin validate --strict plugins/<name>` for every plugin to catch broken SKILL.md/agent frontmatter; the marketplace-level run alone is insufficient.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#marketplace-validation-errors
[PLG-11] (confirmed|validation|official-docs) A per-plugin validate run checks plugin.json, hooks/hooks.json, and frontmatter of skills/agents/commands; exit codes are 0 pass, 1 fail, 2 run error; --json emits a machine-readable report (v2.1.259+).
   IMPL: validate.py can shell out to `claude plugin validate --json --strict` and only add house rules (description length, routing, eval presence) on top, instead of duplicating schema logic.
   SRC: https://code.claude.com/docs/en/plugins-reference#plugin-validate
[PLG-12] (confirmed|validation|official-cli) All 14 micky-psych-tools plugins and all 8 learn-hub plugins currently pass `claude plugin validate --strict`; the learn-hub marketplace fails strict (exit 1) only because it lacks a top-level description.
   IMPL: Baseline is clean; add a description to learn-hub's marketplace.json and make strict validation a gate so the rewrite cannot regress it.
   SRC: claude plugin validate --strict /home/user/learn-hub (and each plugins/* dir in both repos)
[PLG-13] (confirmed|layout|official-docs) Only plugin.json belongs in .claude-plugin/; every component directory (skills/, commands/, agents/, hooks/, output-styles/, monitors/, bin/) and .mcp.json/.lsp.json/settings.json sit at the plugin root.
   IMPL: Current layout already complies; keep references/ and evals/ inside skill folders, not under .claude-plugin/.
   SRC: https://code.claude.com/docs/en/plugins#plugin-structure-overview
[PLG-14] (confirmed|layout|official-docs) A CLAUDE.md at a plugin root is not loaded into context, and a plugin validate run warns about it; instructions must ship as skills (or agents/hooks).
   IMPL: Any per-plugin guidance that must reach the model belongs in SKILL.md/references; READMEs and CHANGELOGs are for humans only.
   SRC: https://code.claude.com/docs/en/plugins-reference#standard-plugin-layout
[PLG-15] (confirmed|components|official-docs) commands/ is the older flat-file format; docs say to use skills/ for new plugins, and custom commands have been merged into skills (both produce slash commands and behave the same).
   IMPL: Thin commands (/infographic, /animate, /digest, /route …) are candidates to fold into the owning skill's frontmatter (argument-hint, arguments) rather than being separate files.
   SRC: https://code.claude.com/docs/en/plugins#plugin-structure-overview
[PLG-16] (confirmed|components|official-docs) Skills docs state commands were merged into skills; `.claude/commands/deploy.md` and `.claude/skills/deploy/SKILL.md` both create /deploy, with skills adding supporting files and invocation control.
   IMPL: A command that only says 'invoke skill X with $ARGUMENTS' duplicates what the skill's own /plugin:skill entry already provides.
   SRC: https://code.claude.com/docs/en/skills
[PLG-17] (confirmed|namespacing|official-docs) Plugin skills are always namespaced /plugin-name:skill; frontmatter `name` sets the last segment, and the bare /name also works unless another command already uses it.
   IMPL: Command names used in docs/ROUTING.md should be written in the namespaced form (/clinical-infographic:clinical-infographic or the chosen skill name) since bare names can be shadowed.
   SRC: https://code.claude.com/docs/en/skills#how-a-skill-gets-its-command-name
[PLG-18] (confirmed|components|official-docs) Invocation control for user-triggered workflows is done in skill frontmatter: disable-model-invocation (manual /name only), user-invocable:false (model-only), argument-hint, and named `arguments`.
   IMPL: Replace thin commands with argument-hint/arguments on the skill; use disable-model-invocation for destructive flows like empty-vault if they should never auto-fire.
   SRC: https://code.claude.com/docs/en/skills#frontmatter-reference
[PLG-19] (confirmed|components|official-docs) Claude Code truncates the combined skill `description` + `when_to_use` at 1,536 characters in the skill listing.
   IMPL: The repo's 1024-char description cap is a stricter house rule; if when_to_use is adopted, validate.py must cap the combined length, and key triggers must come first.
   SRC: https://code.claude.com/docs/en/skills#frontmatter-reference
[PLG-20] (confirmed|components|official-docs) Path fields behave differently: `skills` adds to the default skills/ scan, while commands, agents, workflows, outputStyles, experimental.themes and experimental.monitors replace their default directory.
   IMPL: Rely on default directories and omit path fields; declaring `commands` would silently stop scanning commands/.
   SRC: https://code.claude.com/docs/en/plugins-reference#path-behavior-rules
[PLG-21] (confirmed|components|official-docs) All component paths must be relative, start with ./ and use forward slashes; backslash paths are rejected on macOS/Linux.
   IMPL: Existing `"mcpServers": "./.mcp.json"` is correct; validate.py can reject any manifest path lacking ./ or containing backslashes.
   SRC: https://code.claude.com/docs/en/plugins-reference#path-behavior-rules
[PLG-22] (confirmed|caching|official-docs) Marketplace plugins are copied into ~/.claude/plugins/cache (unless loaded in place); components or scripts that reference files outside the plugin directory fail because those files are not copied.
   IMPL: Skills that reach for the repo-root vault/ or sibling plugins via relative paths only work when loaded in place; the rewrite must make each plugin self-contained.
   SRC: https://code.claude.com/docs/en/plugins-reference#path-traversal-limitations
[PLG-23] (confirmed|caching|official-docs) Symlinks inside a plugin that point elsewhere in the same marketplace are dereferenced (content copied into the cache); symlinks outside the marketplace are skipped. --plugin-dir/local-path installs keep only intra-plugin symlinks.
   IMPL: Shared references (e.g. authoring rules, report templates) can live once and be symlinked into consuming plugins instead of copy-pasted, as long as they stay inside the marketplace repo.
   SRC: https://code.claude.com/docs/en/plugins-reference#share-files-within-a-marketplace-with-symlinks
[PLG-24] (confirmed|env-vars|official-docs) ${CLAUDE_PLUGIN_ROOT} changes on every update for copied plugins and must not hold state; ${CLAUDE_PLUGIN_DATA} (~/.claude/plugins/data/{id}/) persists across updates and is deleted on final uninstall.
   IMPL: Mutable state such as intent-lock's misreads ledger or psych-paper-digest's last_swept must move to ${CLAUDE_PLUGIN_DATA} (or the project dir), not live under the plugin tree.
   SRC: https://code.claude.com/docs/en/plugins-reference#environment-variables
[PLG-25] (confirmed|env-vars|official-docs) CLAUDE_PLUGIN_ROOT/DATA are exported to hooks and MCP/LSP processes but NOT to the Bash tool; in skill and agent content the placeholders are substituted inline at load time.
   IMPL: SKILL.md bodies must write the literal ${CLAUDE_PLUGIN_ROOT}/${CLAUDE_SKILL_DIR} placeholder (substituted at load) rather than telling Claude to `echo $CLAUDE_PLUGIN_ROOT` in Bash.
   SRC: https://code.claude.com/docs/en/plugins-reference#environment-variables
[PLG-26] (confirmed|env-vars|official-docs) ${CLAUDE_SKILL_DIR} resolves to the skill's own subdirectory inside the plugin (not the plugin root), for referencing bundled references/ and scripts/ regardless of cwd.
   IMPL: Long SKILL.md files that point at references/*.md should use ${CLAUDE_SKILL_DIR}/references/... so progressive-disclosure reads work from any working directory.
   SRC: https://code.claude.com/docs/en/skills#available-string-substitutions
[PLG-27] (confirmed|components|official-docs) A plugin-root settings.json supports only the `agent` and `subagentStatusLine` keys; unknown keys are silently ignored.
   IMPL: Plugins cannot ship permissions/env defaults via settings.json; config like the firecrawl key must use userConfig or environment.
   SRC: https://code.claude.com/docs/en/plugins#ship-default-settings-with-your-plugin
[PLG-28] (confirmed|components|official-docs) bin/ executables are put on the Bash tool PATH while the plugin is enabled, but claude.ai organization distribution rejects plugins with a top-level bin/.
   IMPL: If helper scripts are added, keep them in scripts/ referenced via ${CLAUDE_PLUGIN_ROOT} to stay compatible with claude.ai sync (needed for cloud sessions).
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#keep-executables-out-of-the-top-level-bin-directory
[PLG-29] (confirmed|agents|official-docs) Plugin agents honor name, description, model, effort, maxTurns, tools, disallowedTools, skills, memory, background, omitClaudeMd, isolation (worktree only), color, experimental; hooks, mcpServers and permissionMode are ignored for security, and initialPrompt is unsupported.
   IMPL: If any plugin adds subagents (e.g. parallel study adjudicators), declare tools/skills in frontmatter and do not rely on per-agent MCP or hooks.
   SRC: https://code.claude.com/docs/en/plugins-reference#plugin-agent-frontmatter
[PLG-30] (confirmed|agents|official-docs) Plugin agents are scanned recursively and namespaced plugin:subfolder:name; agent `name` values cannot contain ':'.
   IMPL: Refer to plugin agents by scoped name in skills and hook matchers (anchor as ^plugin:agent$).
   SRC: https://code.claude.com/docs/en/plugins-reference#agents
[PLG-31] (confirmed|hooks|official-docs) Plugin hooks live in hooks/hooks.json (or inline in plugin.json), support command/http/mcp_tool/prompt/agent types, and should reference bundled scripts via ${CLAUDE_PLUGIN_ROOT}, preferably in exec form (`args`) to avoid quoting problems.
   IMPL: If gates (e.g. intent-lock enforcement, pre-save checks) move into hooks, use exec-form commands and scripts under the plugin, never repo-relative paths.
   SRC: https://code.claude.com/docs/en/plugins-reference#environment-variables
[PLG-32] (confirmed|hooks|official-docs) Hooks that target a plugin's own bundled MCP server must use the scoped tool name; matchers on the bare server key never fire.
   IMPL: Any hook or allowed-tools list referencing bundled PubMed/CT.gov tools must use mcp__plugin_<plugin>_<server>__* names.
   SRC: https://code.claude.com/docs/en/plugins-reference#hooks
[PLG-33] (confirmed|mcp|official-docs) Plugin MCP servers (in .mcp.json at plugin root or inline) start automatically when enabled; their tools are named mcp__plugin_<plugin>_<server>__<tool>, and that full name is what permission rules, skill allowed-tools and agent tools must use.
   IMPL: Three plugins bundling the same pubmed/clinical-trials servers yield three differently named tool sets; consolidate the MCP declaration into one plugin (dependency) or reference tools generically in skill text.
   SRC: https://code.claude.com/docs/en/mcp#plugin-provided-mcp-servers
[PLG-34] (confirmed|mcp|local-repo) pubmed-research-note, comprehensive-review and psych-paper-digest each declare identical HTTP MCP servers (pubmed, clinical-trials) in their own .mcp.json.
   IMPL: Candidate for a shared 'evidence-engines' dependency plugin (see PLG-37) or a single owner, avoiding duplicate servers and inconsistent tool names.
   SRC: /home/user/micky-psych-tools/plugins/{pubmed-research-note,comprehensive-review,psych-paper-digest}/.mcp.json
[PLG-35] (confirmed|userConfig|official-docs) userConfig declares values prompted at enable time (string/number/boolean/directory/file; sensitive values go to secure storage); values substitute as ${user_config.KEY} in MCP/LSP configs and exec-form hooks and are exported to hooks as CLAUDE_PLUGIN_OPTION_<KEY>. Project-level pluginConfigs are ignored.
   IMPL: Firecrawl's FIRECRAWL_API_KEY and vault paths could be declared as userConfig (sensitive for the key) instead of prose instructions about environment setup.
   SRC: https://code.claude.com/docs/en/plugins-reference#user-configuration
[PLG-36] (confirmed|manifest-schema|official-docs) defaultEnabled:false ships a plugin installed-but-disabled, recommended for plugins that add cost or connect to external services; the marketplace entry's value overrides plugin.json.
   IMPL: Consider for firecrawl (paid external API) so installing the marketplace doesn't enable it silently.
   SRC: https://code.claude.com/docs/en/plugins-reference#default-enablement
[PLG-37] (confirmed|dependencies|official-docs) plugin.json `dependencies` (bare names or {name, version semver range, marketplace}) are resolved and auto-installed; a manifest of only dependencies acts as a bundle.
   IMPL: Encode the pipeline's prose couplings (pubmed-research-note/comprehensive-review → intent-lock, vault-keeper; empty-vault → learn-hub) as declared dependencies instead of 'run plugin X first' text.
   SRC: https://code.claude.com/docs/en/plugin-dependencies
[PLG-38] (confirmed|dependencies|official-docs) Cross-marketplace dependencies are refused unless the root marketplace lists the target in allowCrossMarketplaceDependenciesOn.
   IMPL: learn-hub-local plugins could depend on micky-psych-tools plugins (e.g. intent-lock) by adding `allowCrossMarketplaceDependenciesOn: ["micky-psych-tools"]` instead of vendoring copies.
   SRC: https://code.claude.com/docs/en/plugin-dependencies#depend-on-a-plugin-from-another-marketplace
[PLG-39] (confirmed|release|official-docs) Release tags use `{plugin-name}--v{version}` matching plugin.json; version-constrained dependencies resolve against these tags (for relative-path plugins, on the marketplace repo).
   IMPL: If explicit versions are kept, add `claude plugin tag` to the release step so cross-repo consumers can pin ~x.y ranges.
   SRC: https://code.claude.com/docs/en/plugin-dependencies#tag-plugin-releases-for-version-resolution
[PLG-40] (confirmed|release|official-cli) `claude plugin tag` validates the plugin, checks plugin.json and any enclosing marketplace entry agree on version, requires a clean tree under the plugin dir, refuses existing tags, and supports --dry-run/--push.
   IMPL: bump.py can shrink to 'edit plugin.json version → claude plugin tag --dry-run'; the agreement check makes the dual-file rule unnecessary when the entry omits version.
   SRC: claude plugin tag --help (Claude Code 2.1.280)
[PLG-41] (confirmed|marketplace-schema|official-docs) marketplace.json requires name (kebab-case, not reserved), owner{name}, plugins[]; each entry needs name + source; optional top-level description, version, metadata.pluginRoot, allowCrossMarketplaceDependenciesOn, renames. Missing description is a validator warning.
   IMPL: Entries can shrink to name/source/description/category/keywords; learn-hub needs a top-level description to pass --strict.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#marketplace-schema
[PLG-42] (confirmed|marketplace-schema|official-docs) Entry display fields (description, keywords, author, etc.) override plugin.json values in listings; relative-path sources resolve from the marketplace root and must start with ./.
   IMPL: Description text duplicated in plugin.json and marketplace entry will drift (it already differs in both repos); pick one home, ideally the entry for listing copy and plugin.json minimal.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#relative-paths
[PLG-43] (confirmed|marketplace-schema|official-docs) strict (default true) makes plugin.json the component authority with marketplace entries only supplementing; strict:false makes the entry the entire definition and conflicts with a plugin.json that declares components.
   IMPL: Keep default strict mode; don't declare components in marketplace entries.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#strict-mode
[PLG-44] (confirmed|marketplace-schema|official-docs) A plugin's name is its stable identifier; renames/removals should be recorded in an append-only top-level `renames` map so existing installs migrate (validate checks for cycles). displayName changes UI labels without breaking installs.
   IMPL: If the rewrite merges or renames plugins (e.g. folding decision-interview/plan-critique/intent-lock), add renames entries rather than just deleting catalog lines.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#rename-or-remove-a-plugin
[PLG-45] (confirmed|consumption|official-docs) A project repo consumes another marketplace by committing extraKnownMarketplaces (and enabledPlugins keyed plugin@marketplace) in .claude/settings.json; applied only after folder trust; project enablement overrides user settings.
   IMPL: learn-hub can reference micky-psych-tools (github source) instead of keeping forked copies under learn-hub/plugins/.
   SRC: https://code.claude.com/docs/en/settings-reference#extraknownmarketplaces
[PLG-46] (confirmed|consumption|official-docs) Enabling an external-source plugin (GitHub/npm) in project settings does not install it for others; each user must run claude plugin install. Third-party marketplaces default to autoUpdate false.
   IMPL: Consumption via settings needs a one-time install step documented, and autoUpdate:true on the extraKnownMarketplaces entry if learn-hub should track micky-psych-tools automatically.
   SRC: https://code.claude.com/docs/en/settings-reference#enabledplugins
[PLG-47] (partially|consumption|official-docs) Cloud sessions do not install plugins that a repo enables via enabledPlugins/extraKnownMarketplaces; plugins must be enabled on the claude.ai account (synced plugins). The repo's .claude/skills, agents, commands, hooks and .mcp.json do load because they are part of the clone.
   CORRECTED: Cloud sessions do not install plugins a repo enables via enabledPlugins/extraKnownMarketplaces; enable them on the claude.ai account (synced plugins) instead. The repo's .claude/skills, agents and commands load from the clone; its .claude/settings.json hooks/permissions and .mcp.json load only in a single-repository session (multi-repo sessions start above the clones and don't read them).
   IMPL: learn-hub runs heavily in cloud sessions: switching from vendored copies to marketplace consumption only works there if the plugins are synced via claude.ai (or kept as committed .claude/skills); the plan must pick per surface.
   SRC: https://code.claude.com/docs/en/cloud-environments#what-carries-over-from-your-setup
   EVID: cloud-environments.md carry-over table: repo-enabled plugins/marketplaces "No ... Enable the plugin for your claude.ai account instead, so Claude Code loads it as a synced plugin"; .claude/skills, agents, commands "Yes". But .claude/settings.json hooks and .mcp.json are "Yes, in a session with one repository" — "A session with several repositories ... starts above the clones and doesn't read them".
[PLG-48] (confirmed|consumption|official-docs) git-subdir plugin sources let one marketplace list a plugin living in a subdirectory of another repo (sparse clone), pinnable by ref/sha.
   IMPL: learn-hub-local's marketplace could list intent-lock/pubmed-research-note/comprehensive-review via git-subdir pointing at micky-psych-tools/plugins/<name> instead of copying them.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#git-subdirectories
[PLG-49] (confirmed|consumption|official-cli) `claude plugin marketplace add` accepts --scope user|project|local (where the declaration is written) and --sparse paths for monorepo checkouts.
   IMPL: Use `claude plugin marketplace add safetymickky-ui/micky-psych-tools --scope project --sparse .claude-plugin plugins` from learn-hub to write the consumption config rather than hand-editing.
   SRC: claude plugin marketplace add --help (Claude Code 2.1.280)
[PLG-50] (confirmed|distribution|official-docs) Folders under a skills directory with .claude-plugin/plugin.json load as <name>@skills-dir with no marketplace/install; project-scope ones need workspace trust, load only from the primary working directory, and background monitors don't load.
   IMPL: learn-hub's project skills could be grouped into @skills-dir plugins under .claude/skills if namespacing/bundling is wanted, without a marketplace install step.
   SRC: https://code.claude.com/docs/en/plugins-reference#skills-directory-plugins
[PLG-51] (confirmed|testing|official-docs) --plugin-dir loads a plugin (or folder of plugins, v2.1.265+) for a session; a --plugin-dir copy takes precedence over a same-named installed plugin; /reload-plugins picks up edits.
   IMPL: Dev/test loop for the rewrite: `claude --plugin-dir ./plugins` from the repo root, no bumps or reinstalls.
   SRC: https://code.claude.com/docs/en/plugins#test-your-plugins-locally
[PLG-52] (confirmed|testing|official-cli) `claude plugin details` reports always-on vs on-invoke token cost per component; for an uninstalled plugin it works via --plugin-dir. pubmed-research-note measures ~432 tokens always-on and ~6.4k on invoke, with 2 bundled MCP servers not counted.
   IMPL: Add a token-budget report (plugin details per plugin) to the rewrite's acceptance checks so long SKILL.md bodies are measured, not guessed.
   SRC: claude --plugin-dir ./plugins/pubmed-research-note plugin details pubmed-research-note (Claude Code 2.1.280)
[PLG-53] (confirmed|evals|official-docs) `claude plugin eval` (v2.1.269+) runs cases from <plugin>/evals/<case>/prompt.md|case.yaml + graders/*.md with a no-plugin baseline; this format is distinct from skill-creator's evals/evals.json, and experimental.evals relocates the directory.
   IMPL: Existing skill-level evals/evals.json (skill-creator format) do not run under claude plugin eval; the plan must choose to keep, convert, or add plugin-root evals/ cases (no path collision since current evals sit under skills/<skill>/).
   SRC: https://code.claude.com/docs/en/plugin-evals
[PLG-54] (confirmed|evals|official-docs) The documented first diagnostic for a skill is a `tool_used: Skill` grader; Δ near zero with it failing means the description isn't triggering on natural phrasing.
   IMPL: Description rewrites should be validated with a trigger eval per skill rather than by char-count heuristics alone.
   SRC: https://code.claude.com/docs/en/plugin-evals#create-your-first-eval-suite
[PLG-55] (confirmed|official-examples|official-repo) Anthropic's own marketplace mostly omits version for first-party relative-path plugins (38 of 52 relative entries have none; plugin-dev and skill-creator have no version in either file), and security-guidance shows the documented drift: plugin.json says 2.0.8 while its marketplace entry says 2.0.7.
   IMPL: Evidence that dual-file versioning drifts even for Anthropic; SHA versioning (omit version) is the prevailing first-party pattern for in-repo plugins.
   SRC: https://github.com/anthropics/claude-plugins-official/blob/main/plugins/security-guidance/.claude-plugin/plugin.json
[PLG-56] (partially|official-examples|official-repo) Well-built official plugins (feature-dev, pr-review-toolkit, hookify, plugin-dev) use a minimal plugin.json (name, description, author), a README.md and LICENSE per plugin, agents/ with name/description/tools/model/color frontmatter, and hooks.json with a top-level description and quoted ${CLAUDE_PLUGIN_ROOT} commands with timeouts.
   CORRECTED: Official exemplar plugins (feature-dev, pr-review-toolkit, hookify, plugin-dev) use a minimal plugin.json (name, description, author, no version), ship README.md and LICENSE, and define agents with name/description/model/color (tools only sometimes). hookify's hooks.json has a top-level description and double-quoted ${CLAUDE_PLUGIN_ROOT} shell-form commands with timeouts. They still use the older commands/ layout and shell-form hooks, so they are not a template for the docs' current skills/ + exec-form advice.
   IMPL: Target shape for rewritten plugins: minimal manifest, README + LICENSE, logic in skills/agents/hooks; note official repos still ship commands/ in older plugins, so docs (use skills/) outrank those examples.
   SRC: https://github.com/anthropics/claude-plugins-official/blob/main/plugins/hookify/hooks/hooks.json
   EVID: feature-dev, pr-review-toolkit, hookify, plugin-dev each have plugin.json with only name/description/author, plus README.md and LICENSE (HTTP 200). hookify/hooks/hooks.json has top-level description and quoted \"${CLAUDE_PLUGIN_ROOT}\" shell-form commands with "timeout": 10. But agent frontmatter is not uniform: pr-review-toolkit/agents/code-reviewer.md has name/description/model/color and NO tools field. These exemplars also still ship commands/ (feature-dev/commands/feature-dev.md, hookify/commands/hookify.md, pr-review-toolkit/commands/review-pr.md) and use shell-form hooks, which the curre
[PLG-57] (confirmed|local-findings|local-repo) intent-lock's misread-capture skill appends its ledger into the installed plugin tree via ${CLAUDE_PLUGIN_ROOT}, which docs say is ephemeral for copied plugins.
   IMPL: When installed from GitHub the ledger is lost on each update (old version dir swept ~14 days later); move it to ${CLAUDE_PLUGIN_DATA} or a project/vault path.
   SRC: /home/user/micky-psych-tools/plugins/intent-lock/skills/misread-capture/SKILL.md:39
[PLG-58] (partially|local-findings|local-repo) vault-keeper resolves the vault as a path above the plugin root, which only exists when loaded in place from the repo checkout.
   CORRECTED: vault-keeper resolves the vault mainly by walking up from cwd to the marketplace root. That works only when the session's cwd is inside the micky-psych-tools checkout. Its stated equivalent, ${CLAUDE_PLUGIN_ROOT}/../../vault, is true only when the plugin is loaded in place from the repo, not when copied into the plugin cache.
   IMPL: For cached installs this points into ~/.claude/plugins/cache; make the vault location a userConfig `directory` value or ${CLAUDE_PROJECT_DIR}-relative.
   SRC: /home/user/micky-psych-tools/plugins/vault-keeper/skills/vault-keeper/SKILL.md:29
   EVID: vault-keeper SKILL.md:27-29: the primary rule is "walk up from cwd to the directory containing `.claude-plugin/marketplace.json`, then append `vault/`". The quoted "${CLAUDE_PLUGIN_ROOT}/../../vault is equivalent" is a secondary form. That form only resolves to the repo vault when loaded in place; in a copied install it points into ~/.claude/plugins/cache/..., which docs say never contains files outside the plugin.
[PLG-59] (partially|local-findings|local-repo) learn-hub vendors forked copies of intent-lock, pubmed-research-note and comprehensive-review whose SKILL.md/plugin.json differ from micky-psych-tools while carrying the same version numbers (e.g. both 1.7.0), and bump.py's premise is the dual-file rule.
   CORRECTED: learn-hub vendors copies of intent-lock, pubmed-research-note and comprehensive-review under the same version numbers as micky-psych-tools (0.4.2, 1.7.0, 0.3.0). pubmed-research-note and comprehensive-review have diverged in SKILL.md/plugin.json; intent-lock differs only in README. micky-psych-tools' bump.py is built on keeping the version in both files, which the official docs advise against.
   IMPL: Same name + same version + different content across two marketplaces is ambiguous; either rename the learn-hub forks (e.g. learn-hub adapters) or consume upstream and move learn-hub-specific behavior into a small wrapper/dependency.
   SRC: diff -rq /home/user/learn-hub/plugins/pubmed-research-note /home/user/micky-psych-tools/plugins/pubmed-research-note
   EVID: diff -rq: pubmed-research-note and comprehensive-review differ in plugin.json, README and SKILL.md (plus a reference file / command) while both repos carry 1.7.0 and 0.3.0. intent-lock differs ONLY in README.md; its SKILL.md and plugin.json are identical, both 0.4.2. learn-hub's marketplace entries carry no version. bump.py docstring: "Version lives in two files that must never disagree" (its stated rationale conflicts with docs, which say plugin.json always wins and advise not setting both).
--- missed_practices
MP-PLG-1: `claude plugin validate` warns on any plugin.json without `version`, so `--strict` (the recommended CI mode) exits 1 for commit-SHA-versioned plugins. This happens even when the marketplace entry sets a version. Reproduced on 2.1.280 for both a plugin-dir and a marketplace-dir run. Picking commit-SHA versioning and strict CI therefore conflict, and the plan has to choose one.
   SRC: claude plugin validate --strict <plugin dir with no version> (Claude Code 2.1.280, reproduced locally)
   Q: version: No version specified. Consider adding a version following semver (e.g., "1.0.0")
MP-PLG-2: Plugin MCP servers and claude.ai connectors are deduplicated by endpoint, and only the highest-precedence definition connects (local > project > user > plugin > claude.ai connector). Three plugins bundling the same PubMed/CT.gov URLs, plus the account's PubMed connector, collapse to one connection. The callable tool prefix therefore depends on which definition wins, so skills should not hard-code one plugin's mcp__plugin_<p>_<s>__ name. A single shared owner of the servers is safer.
   SRC: https://code.claude.com/docs/en/mcp#scope-hierarchy-and-precedence
   Q: The three scopes match duplicates by name. Plugins and connectors match by endpoint, so one that points at the same URL or command as a server above is treated as a duplicate.
MP-PLG-3: Non-sensitive userConfig values substitute into skill and agent content, not just MCP/hook configs. A `directory`-typed userConfig field (e.g. vault path) is the documented way to give a copied plugin a stable external path, instead of ${CLAUDE_PLUGIN_ROOT}/../../vault or writing into the plugin tree.
   SRC: https://code.claude.com/docs/en/plugins-reference#user-configuration
   Q: Each value is available for substitution as `${user_config.KEY}` in MCP and LSP server configs and hook commands. Non-sensitive values can also be substituted in skill and agent content.
MP-PLG-4: A plugin-directory validate run does not check a SKILL.md at the plugin root. The `plugin init` scaffold (`"skills": ["./"]`, root SKILL.md) gets no frontmatter validation unless the plugin sits in a directory named `skills` and that directory is validated separately. Prefer skills/<name>/SKILL.md so CI covers every skill.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces#check-a-plugin-whose-skill-is-its-root-skillmd
   Q: When you run `claude plugin validate` against a plugin directory, Claude Code doesn't check a `SKILL.md` at the plugin root.
MP-PLG-5: The skill listing has a total budget of 1% of the context window. When it overflows, descriptions of the least-invoked skills are shortened first. Many plugins with long descriptions (this marketplace plus learn-hub's) compete for that budget, so the key trigger use case must come first in each description. Check the cost with /doctor or /skill-doctor.
   SRC: https://code.claude.com/docs/en/skills#skill-descriptions-are-cut-short
   Q: The budget scales at 1% of the model's context window. When the listing overflows, Claude Code drops descriptions starting with the skills you invoke least, so the skills you use most keep their full text.
MP-PLG-6: Keep each SKILL.md under 500 lines and move detailed material into referenced supporting files (progressive disclosure). This applies to the long research/review skills.
   SRC: https://code.claude.com/docs/en/skills
   Q: Keep `SKILL.md` under 500 lines. Move detailed reference material to separate files.
MP-PLG-7: `claude plugin eval` never starts a plugin's real MCP servers unless asked. MCP-dependent skills (pubmed-research-note, comprehensive-review, psych-paper-digest) need per-tool mock files under evals/mocks/<server>/<tool>.md, or `--allow-real-servers`, to be evaluated.
   SRC: https://code.claude.com/docs/en/plugin-evals#mock-mcp-servers
   Q: A run never starts your plugin's real MCP servers unless you ask. Claude Code registers a stand-in under each server's own name. Tools with a mock file answer from it and are allowed without an `--allow-tools` grant, and a tool with no mock file isn't available to Claude.
MP-PLG-8: CLAUDE_CODE_PLUGIN_DIRS (v2.1.280+) loads plugins by absolute path wherever the --plugin-dir flag can't be passed. Project and local settings cannot set it, but an environment-level variable (e.g. a cloud environment's env vars) can. This is an in-place loading route alongside claude.ai-synced plugins.
   SRC: https://code.claude.com/docs/en/plugins#test-your-plugins-locally
   Q: To load plugins in a session where you can't add the flag, list their absolute paths in the `CLAUDE_CODE_PLUGIN_DIRS` environment variable instead.
--- open_questions
OQ: Is safetymickky-ui/micky-psych-tools private? That decides whether learn-hub can consume it via extraKnownMarketplaces/git-subdir with plain git credentials and whether claude.ai account sync is needed for cloud sessions.
OQ: Docs say project-enabled 'external source' plugins are not auto-installed; it is not explicit whether a relative-path plugin inside a GitHub-hosted marketplace declared via extraKnownMarketplaces counts as external — needs a live test outside cloud.
OQ: Do project-scope @skills-dir plugins load in Claude Code cloud sessions (they require workspace trust; cloud table only confirms plain .claude/skills load)?
OQ: Should the rewrite adopt SHA versioning (omit version everywhere) or keep explicit semver in plugin.json only plus `claude plugin tag`? Depends on whether any consumer pins version ranges.
OQ: claude plugin eval requires v2.1.269+ and incurs real model cost; decide whether to migrate skill-creator evals/evals.json to plugin-eval cases or run both.
OQ: `claude plugin details <path>` does not accept a path; it needed `claude --plugin-dir <path> plugin details <name>` — confirm this is the intended invocation for CI token budgeting.
#### EVL — Testing and evaluating skills/plugins
[EVL-01] (confirmed|eval-systems|official-docs) Claude Code now has two separate, non-interchangeable eval systems: `claude plugin eval` (plugin-level, prompt.md/case.yaml + graders/*.md, CI-gateable) and skill-creator's per-skill `evals/evals.json` loop (in-conversation iteration). A suite written for one does not run under the other.
   IMPL: The repo's skills/<skill>/evals/evals.json files are skill-creator format only; a rewrite must pick a canonical system (or keep both deliberately) rather than assume one feeds the other.
   SRC: https://code.claude.com/docs/en/skills.md
[EVL-02] (confirmed|plugin-eval-layout|official-cli) `claude plugin eval` discovers cases as `<eval dir>/**/case.yaml` or `prompt.md` (+ `graders/*.md`), where the eval dir is `evals/` below the plugin root unless `--eval-dir` or the manifest's `experimental.evals` says otherwise. Installed CLI is 2.1.280; the feature needs >= 2.1.269.
   IMPL: Plugin-level suites belong at plugins/<name>/evals/<case>/prompt.md, not at plugins/<name>/skills/<skill>/evals/evals.json.
   SRC: claude plugin eval --help (Claude Code 2.1.280)
[EVL-03] (confirmed|repo-mapping|official-docs) If no `<case>/prompt.md` or `<case>/case.yaml` exists under the eval dir in effect, the run reports 'No eval cases found' and exits 1. So running `claude plugin eval` against any plugin in this repo today finds zero cases: every existing eval lives in skills/<skill>/evals/evals.json, which is neither in the default evals/ dir nor a recognized case file.
   IMPL: Adding `claude plugin eval` to CI before migrating cases would fail every plugin with exit 1 ('no cases were found' is an exit-1 condition).
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-04] (partially|repo-mapping|local-repo) KEY MAPPING (repo evals.json -> claude plugin eval): the repo's 15 evals.json files (11 in micky-psych-tools, 4 duplicated in learn-hub/plugins; 118 evals in total) all use the keys {id, name, prompt, expected_output, files, assertions}. The mapping is: `name` -> case directory `evals/<skill>/<name>/`; `prompt` -> prompt.md body; `expected_output` -> the human-only frontmatter field `expected_outcome` (note the different name) and/or the rubric body of `graders/criteria.md` (type: llm); each `assertions[]` string -> one `graders/<slug>.md` (regex / tool_used / tool_order / file_exists / llm); `files[]` -> fixtures inside the case dir exposed via case.yaml `context.add_dirs` (or a `scaffold_script`); `skill_name` -> the `input_match` of a `tool_used: Skill` grader; `id` -> no counterpart (drop). 'negative-*' cases -> `tool_used: Skill` with `min: 0`, `max: 0`, `arm: both`. A mechanical converter is feasible because every file has an identical key set.
   CORRECTED: Only the skeleton converts mechanically. Mapping: `name` → a case dir (grouped under a non-case dir such as evals/<skill>/<name>/), `prompt` → the prompt.md body, and `expected_output` → the human-only `expected_outcome` field (never scored). Graders cannot be generated mechanically, because every assertions[] is empty. Each case needs hand-authored graders, e.g. an llm rubric distilled from expected_output plus a `tool_used: Skill` grader whose input_match allows the optional `plugin:` prefix. For negative cases, the only possible check is `tool_used: Skill` with min 0 / max 0 / arm: both on this plugin's skill; a positive 'routed to <other plugin>' check is impossible in an isolated run. `files` would need to become a fixture directory (add_dirs) or a scaffold_script. The count is 15 files and 118 evals excluding the plugin-creator template, or 16 and 120 including it.
   IMPL: Write a small scripts/evals_to_cases.py (or fold it into plugin-creator) that emits evals/<skill>/<name>/{prompt.md,graders/*.md}; keep evals.json only if the skill-creator loop is still wanted.
   SRC: /home/user/micky-psych-tools/plugins/intent-lock/skills/intent-lock/evals/evals.json
   EVID: Counts check out once the template is excluded. There are 16 evals.json files: 11 real suites in micky-psych-tools, the plugin-creator template (2 placeholder evals), and 4 files in learn-hub/plugins that are byte-identical copies (checked with cmp). Excluding the template that is 15 files and 118 evals. Every eval has exactly the keys {id, name, prompt, expected_output, files, assertions} under a top-level {skill_name, evals}. The overreach is the conversion claim: all 120 `assertions` arrays and all `files` arrays are empty. So no assertion strings exist to turn into graders/*.md, and `conte
[EVL-05] (confirmed|plugin-eval-layout|official-docs) prompt.md frontmatter is strict: an unknown key is an error. The human-only 'what good looks like' field is `expected_outcome`, not skill-creator's `expected_output`, so copying evals.json keys into frontmatter verbatim breaks case loading.
   IMPL: A converter must rename expected_output -> expected_outcome and must not emit id/files/assertions as frontmatter keys.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-06] (confirmed|plugin-eval-layout|official-docs) Cases can be grouped by nesting them under a directory that isn't itself a case, and filtered with `--case <glob>` and `--tag`. A non-default eval dir can be set in plugin.json as `experimental.evals` (a relative path of plain directory names; absolute paths and `..` are rejected).
   IMPL: For multi-skill plugins (intent-lock, vault-keeper, plugin-creator) use evals/<skill-name>/<case>/ plus `tags: [<skill-name>, trigger|negative|output]` rather than pointing experimental.evals into skills/*/evals.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-07] (confirmed|authoring-tooling|official-cli) `claude plugin eval init --bare <name>` writes a blank template without running anything: prompt.md with `max_turns: 10` and `allowed_tools: [Read, Glob, Grep, Skill]`, and graders/criteria.md with `type: llm`, `weight: 1`. Without `--bare`, init runs an interview that reads the plugin, proposes should/shouldn't-trigger prompts, designs graders, pilots them once, and writes one case dir per prompt (needs a terminal; CI must use --bare).
   IMPL: plugin-creator's references/templates/evals.json should be replaced (or joined) by a prompt.md + graders/ template matching the --bare output, so /new-plugin scaffolds runnable cases.
   SRC: claude plugin eval init --help (Claude Code 2.1.280); verified by running --bare in a scratch dummy plugin
[EVL-08] (confirmed|graders|official-docs) There are exactly six grader types: regex, tool_used, tool_order, file_exists (free, computed from transcript/files), and llm, baseline (judge-model calls). There are no custom-code graders.
   IMPL: Python checks like validate.py cannot be graders; every deterministic assertion must be expressible as a regex over a file/last_message/trace, a tool-call count/order, or a created-file glob.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-09] (confirmed|graders|official-docs) To check a build/test/script outcome inside a run, the documented pattern is to have the prompt ask Claude to run it and write the outcome to a file, grade that file, and assert the command ran with a `tool_used` grader whose `input_match` names the command.
   IMPL: Skills whose correctness is enforced by a bundled checker (plugin-creator -> validate.py, atomize-book QC scripts) can be graded by having the case run the checker and regex its written result, with --allow-tools "Bash(python3 scripts/validate.py*)".
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-10] (confirmed|graders|official-docs) Recommended grader design is one grader on the result (final message or produced file) plus one on the process (tool_used or tool_order), so a case tells you both whether the answer was right and whether the plugin produced it.
   IMPL: Each converted case should get at least a skill-fired/tool-order grader plus an outcome grader; the current evals.json encode both in one prose expected_output that must be split.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-11] (confirmed|graders|official-docs) LLM judges vary more on longer text, so long outputs such as generated files should be graded with regex over the file contents; keep llm graders for short outputs, with rubrics written as concrete PASS/FAIL conditions. An llm grader passes on at least two of three judge votes; the default judge is a small fast model (haiku), overridable with --judge-model.
   IMPL: Long-report skills (pubmed-research-note, comprehensive-review, psych-paper-digest) should be graded by regex over the written .md for required markers (verdict block, per-study sections, citation/PMID patterns), not by a haiku rubric over the whole report.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-12] (confirmed|graders|official-docs) Grader targets: `last_message` (default), `trace` (JSONL; regex sees every message, but an llm judge sees only the first 12 and last 12), `files` (list of created paths, not contents), `{source: file, path}` (contents of one file), and `mock_calls`. Regex over the trace must match JSON-escaped quotes (\").
   IMPL: Process assertions for multi-phase skills (intent-lock's silent Phase 0 then picker rounds, empty-vault's move -> verify -> delete) should be tool_order/regex-on-trace, never an llm judge over a long trace.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-13] (confirmed|graders|official-docs) With `{source: file}`, PNG/JPEG/GIF/WebP files are shown to an llm judge as images; other binaries (.pptx, PDF) are refused and must be rendered to an image or written out as text. `files` lists only created paths, and `file_exists` counts only files created during the run.
   IMPL: HTML-producing skills (clinical-infographic, concept-animation, code-explainer, ml-concept-lab) should be graded with file_exists on *.html plus regex over the HTML for contract markers (prefers-reduced-motion block, no external src/href, mandatory 'medications to avoid' banner), not a visual judge.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-14] (confirmed|baseline-ablation|official-docs) By default each case runs in two arms, with the plugin and with no plugin, and reports WITH, W/OUT and Δ. A case scoring 1.0 in both arms means the plugin isn't what made it pass. `--ablation none` runs one arm, roughly halving cost.
   IMPL: Use Δ as the release signal for encoded-workflow skills (intent-lock, plan-critique, decision-interview); a Δ≈0 case is either redundant or its graders don't discriminate.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-15] (confirmed|baseline-ablation|official-docs) In a two-arm run, every `tool_used` grader on `Skill`, and any grader marked `arm: with-only`, is excluded from the score in both arms and shown only as a with-arm 'plugin-fired indicator'. The exception is when every grader in a case is of that kind: they are then scored normally. `arm: both` forces scoring in both arms, which is the right setting for a must-not-invoke check (`min: 0`, `max: 0`).
   IMPL: The repo's ~17 'negative-*' evals become tool_used Skill min:0 max:0 arm:both; positive trigger cases need an outcome grader too, or their Δ comes only from the fallback rule and is inflated (the without-arm can never fire the skill).
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-16] (partially|trigger-eval|official-docs) The canonical trigger check is a `tool_used` grader on `Skill` whose `input_match` regex names the skill. It also matches the namespaced `plugin-name:skill-name` form.
   CORRECTED: The canonical trigger check is a `tool_used` grader on `Skill` with an input_match regex naming the skill. It counts the namespaced `plugin-name:skill-name` form only if the regex allows an optional prefix, as the doc's example does: '"skill"\s*:\s*"(?:[\w-]+:)?<skill-name>"'. Any converter must emit that form.
   IMPL: Use input_match '"skill"\s*:\s*"(?:[\w-]+:)?<skill>"' in every case; for plugins with two skills (intent-lock + misread-capture) this also asserts the right sibling fired.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
   EVID: The quoted sentence describes the doc's example grader, whose regex builds in an optional namespace prefix: `input_match: '"skill"\s*:\s*"(?:[\w-]+:)?your-skill-name"'`. Namespaced invocations are matched because of that `(?:[\w-]+:)?` group, not automatically. A regex that only names the bare skill would miss 'plugin:skill'.
[EVL-17] (confirmed|trigger-eval|official-docs) The most common first finding is Δ near zero with the `tool_used: Skill` grader failing, which means the description doesn't trigger on natural phrasing. The fix is to edit the description and re-run. If the Skill grader passes but Δ is negative, suspect the judge (re-run with --judge-model sonnet and tighten the rubric) before the plugin.
   IMPL: Description edits (the repo's 200-1024 char rule) should be justified by before/after trigger-grader results, not by char counts alone.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-18] (confirmed|scoring|official-docs) Each case runs 3 times per arm by default (runs: 1-50). A run's score is the (weighted) fraction of graders passed, and the case score is the mean over runs. The default --threshold is 1.0, so the command exits 1 if any case is below perfect.
   IMPL: A CI gate needs an explicit --threshold (for example 0.8) and grader `weight`s that make the safety-critical checks dominate (for example clinical-infographic's avoid-banner, empty-vault's no-delete-before-verify).
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-19] (confirmed|isolation|official-docs) Runs are isolated: a throwaway home dir, working dir and config, with only the target plugin loaded. User settings, hooks, CLAUDE.md files, personal MCP servers, other installed plugins, memory and skills are all absent, and no project .claude/ or .mcp.json above the sandbox is read.
   IMPL: micky-psych-tools' routing layer (CLAUDE.md 'route first' + ROUTING.md) and learn-hub's CLAUDE.md conventions are invisible during evals, so trigger results measure the SKILL.md description alone, and any convention a skill silently relies on from CLAUDE.md must move into the skill/references.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-20] (confirmed|isolation|official-docs) A case's `plugins` field lists the plugin directories under test, relative to the case directory, and defaults to the nearest enclosing plugin. The docs show only a single-path example (`plugins: ["../.."]`) for when auto-detection fails.
   IMPL: Cross-plugin routing negatives (intent-lock eval 8 -> plugin-creator; pubmed-research-note -> vault-keeper/intent-lock delegation) either need sibling plugin dirs listed in `plugins:` (unverified that multiple load) or must be reduced to 'this skill must not fire' checks.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-21] (confirmed|run-config|official-docs) Run limits: max_turns defaults to 10 (up to 200) and timeout_seconds to 300 (up to 3600). Hitting the cap is recorded as a run error and usually lowers the score, so set it generously.
   IMPL: Multi-phase skills (comprehensive-review, pubmed-research-note, atomize-book) need raised limits or cases scoped to one phase (for example 'Step 0 gate fires', 'report skeleton written') instead of whole-pipeline runs.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-22] (confirmed|isolation|official-docs) Tool access in runs: only listed read-only tools (Read, Glob, Grep, NotebookRead, Skill, Agent, TodoWrite, Task*) are granted. Bash/Write/Edit/WebFetch/WebSearch are removed unless granted with --allow-tools for the whole run. Granted Bash runs under the OS sandbox (home dir unreadable, network limited), and only allowlisted env vars and EVAL_* reach the child.
   IMPL: File-writing skills need `--allow-tools Write` to be testable; learn-hub skills that need Supabase keys from .env.local or the network (sync-vault, ingest-*) cannot run end to end in an eval, so test their deterministic cores with unit tests (EVL-41) and eval only the agentic decision steps.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-23] (confirmed|fixtures|official-docs) Each run starts in an empty workspace, and `@path` mentions in the prompt are not expanded. Fixtures come from case.yaml `context.add_dirs` (read-only dirs inside the case dir) or `context.scaffold_script` (bash that runs outside the sandbox and only with --scaffold). `context.history_file` resumes a saved .jsonl conversation.
   IMPL: evals.json `files` (empty today in all 118 evals) map to evals/<skill>/<case>/resources/ + `context.add_dirs: [resources]`; misread-capture's post-delivery complaint cases need a `history_file` transcript to be realistic.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-24] (confirmed|mcp-mocks|official-docs) MCP servers declared by the plugin are never started by default. Mocks go in `evals/mocks/<server>/<tool>.md`, where <server> is the server name in the plugin's MCP config; tools without a mock file are unavailable. An `expect:` block validates the tool input and aborts the run with score 0 on violation. `type: agent` mocks answer via the judge model, and their recordings can be adopted into `mocks/.replay/` and committed for repeatable CI. `_tools.json` supplies the real tool schemas.
   IMPL: pubmed-research-note, comprehensive-review and psych-paper-digest each declare MCP servers `pubmed` and `clinical-trials` in .mcp.json, so their suites need evals/mocks/pubmed/search_articles.md etc. (fixed PMID fixtures). `expect:` can assert psych-paper-digest's windowed date filters; `mock_calls` graders can assert the right searches were made.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-25] (confirmed|ci|official-docs) CI recipe: `claude plugin eval . --trust-plugin --json results.json --threshold 0.8 --model <pinned> --judge-model <pinned> --no-publish --max-cost-usd N`. Exit codes: 0 all pass; 1 below threshold, load error, no cases, or untrusted dir; 2 partial (cost ceiling or auth); 130/143 interrupted/terminated. Pin both models so a model rollout isn't mistaken for a plugin regression.
   IMPL: Add an opt-in `scripts/eval.sh <plugin>` wrapper with pinned models and a cost ceiling next to validate.py; keep it out of the every-commit health check because it spends model usage.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-26] (partially|results|official-docs) Each run writes `<eval dir>/results/<timestamp>/{aggregate-result.json, report.html}`, and results/ should be gitignored. aggregate-result.json is `schemaVersion: 1` with camelCase fields (aggregates.overallScore, casesPassed, meanDelta, cases[].aggregates.delta, partial). On a claude.ai subscription the HTML report is published as a private artifact by default unless --no-publish is passed. The Artifact tool is off inside runs.
   CORRECTED: Each path-target run writes <eval dir>/results/<timestamp>/{aggregate-result.json, report.html}; gitignore results/. A terminal run on a claude.ai subscription with artifacts enabled publishes the report as a private artifact unless --no-publish is passed. API-key runs stay local, and runs started by a Claude Code session stay local unless --publish-report is passed. The Artifact tool is off inside runs.
   IMPL: micky-psych-tools/.gitignore currently holds only __pycache__/, *.pyc, .DS_Store, so add `plugins/*/evals/results/`. Skills that end by publishing an artifact can only be graded on what they write before that step.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
   EVID: The results layout, the `.gitignore` note, `schemaVersion: 1` with camelCase fields (aggregates.overallScore, casesPassed, meanDelta, cases[].aggregates.delta, partial) and 'The Artifact tool is off' are all verbatim. The publish claim overstates the default. The doc publishes only 'If you're signed in with a claude.ai subscription and artifacts are available for your account'. It also says 'A run that a Claude Code session started, such as when you ask Claude to run the suite for you, also stays local... Add `--publish-report`'. For a named installed-plugin target, results go to ./evals/resul
[EVL-27] (confirmed|cost|official-docs) Evals cost real model usage: about cases × runs agent runs per arm, doubled for the baseline, plus three judge calls per llm/baseline grader per run. For cheap iteration, run one case, one run, one arm; for every-change CI suites, use only free graders and `--ablation none` where Δ isn't needed.
   IMPL: Converting all 118 existing evals at the defaults (3 runs × 2 arms) means ~700 agent runs per full pass, so tag a small `smoke` subset per plugin (regex/tool_used only) and reserve full two-arm runs for releases/bumps.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
[EVL-28] (confirmed|skill-creator-format|official-repo) skill-creator's evals.json schema: {skill_name, evals: [{id, prompt, expected_output, files, expectations|assertions}]}. The field name for checks is inconsistent across Anthropic sources: skill-creator's references/schemas.md and its aggregate_benchmark.py (which reads grading.json) use `expectations`, while skill-creator SKILL.md and agentskills.io use `assertions` (agentskills' grading.json uses `assertion_results`).
   IMPL: The repo uses `assertions` (the agentskills/SKILL.md spelling). If the skill-creator scripts are used, the grading.json the grader writes must still use `expectations` with text/passed/evidence, or the viewer shows empty values.
   SRC: /mnt/skills/examples/skill-creator/references/schemas.md
[EVL-29] (partially|repo-mapping|official-repo) In the skill-creator loop, prompts are written first and assertions are drafted while the first runs execute, after seeing real outputs. In the repo, all 118 evals across 15 evals.json files have `"assertions": []` and `"files": []`, and validate.py only checks that evals.json parses as JSON. So no eval in either repo has ever been graded or run through the loop.
   CORRECTED: All 118 real evals (120 counting the plugin-creator template, which stamps the same empty pattern into every new plugin) have `assertions: []` and `files: []`, and validate.py only checks that evals.json parses. No grading, benchmark or feedback artifacts exist in either repo, so no eval has demonstrably been graded. Whether any was informally run cannot be established.
   IMPL: The existing evals are prompt inventories, not tests. The rewrite plan should budget one real run per skill to produce assertions/graders before claiming eval coverage, and validate.py should fail an evals.json whose assertions are all empty (or cases with no graders).
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
   EVID: The SKILL.md quote is verbatim ('You'll draft assertions in the next step while the runs are in progress'). In the repos, a JSON scan shows 0 of 120 evals (16 files, including the plugin-creator template) with non-empty assertions or files. validate.py lines 111-114 only `json.load` the file. There are no *-workspace dirs and no grading.json, benchmark.json, feedback.json or eval_metadata.json anywhere. That shows no eval was ever graded with assertions. It doesn't prove none was ever run, because skill-creator workspaces are sibling dirs that may never have been kept.
[EVL-30] (confirmed|baseline-ablation|official-repo) skill-creator run protocol: for each test case, spawn the with-skill and baseline subagents in the same turn. For a new skill the baseline is no skill; when improving an existing skill, snapshot the old version first and use it as the baseline (old_skill/). Results go in <skill>-workspace/iteration-N/eval-<name>/.
   IMPL: For the rewrite itself, the right baseline is the CURRENT SKILL.md (old-version snapshot), not no-skill, so a rewrite can be shown not to regress before it replaces the long existing skills.
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-31] (confirmed|benchmark|official-repo) skill-creator benchmark: aggregate_benchmark.py produces benchmark.json/.md with pass_rate, time_seconds and tokens per configuration (mean ± stddev) plus the delta. Token and duration data must be captured from each subagent's completion notification immediately, because it isn't persisted anywhere else.
   IMPL: Long skills should be judged on the pass-rate delta against the token/time overhead they add; a skill that doubles tokens for a small pass-rate gain is a candidate for trimming.
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-32] (confirmed|benchmark|open-standard) Analyze results beyond the aggregates: remove or replace assertions that always pass in both configurations (non-discriminating); investigate ones that always fail in both; treat high stddev as either flaky evals or ambiguous instructions; read transcripts for time/token outliers.
   IMPL: When converting expected_output prose into graders, drop checks the base model already satisfies (for example 'responds in Thai'), which would inflate WITH scores without showing the skill's value.
   SRC: https://agentskills.io/skill-creation/evaluating-skills.md
[EVL-33] (confirmed|graders|official-repo) Assertion quality: good assertions are programmatically verifiable, specific and observable, or countable. Weak ones are vague ('output is good') or brittle exact-phrase checks. Graders must require concrete evidence for a PASS, and should also critique the evals, since a passing grade on a weak assertion creates false confidence.
   IMPL: Regex graders should target structural markers the skill contract defines (headings, marker blocks, file names), not exact prose; the repo's expected_output strings mix several checks and need splitting into one discriminating grader each.
   SRC: /mnt/skills/examples/skill-creator/agents/grader.md
[EVL-34] (confirmed|human-review|official-repo) Subjective qualities (style, visual design) are better caught by human review than forced into pass/fail assertions. skill-creator's eval viewer (eval-viewer/generate_review.py) shows the outputs and benchmark and collects per-case feedback into feedback.json; `--static <path>` writes a standalone HTML for headless/remote environments.
   IMPL: Visual skills (clinical-infographic, concept-animation, ml-concept-lab, code-explainer) should combine regex contract graders with a human-review pass, since no plugin-eval grader can judge an HTML page's rendered design.
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-35] (confirmed|version-comparison|official-docs) For 'is the new version better?', skill-creator offers a blind A/B: a comparator agent judges two outputs without knowing which version produced which, and an analyzer explains why the winner won.
   IMPL: Use blind A/B (old SKILL.md vs rewritten SKILL.md) as the acceptance gate for each rewritten skill whose value is holistic (report quality) rather than assertion-shaped.
   SRC: https://code.claude.com/docs/en/skills.md
[EVL-36] (confirmed|trigger-eval|official-repo) Trigger/description evals: about 20 realistic queries (8-10 should-trigger, 8-10 should-not). The most valuable negatives are near misses that share keywords but need something else, not obviously irrelevant prompts. Queries should carry concrete detail (paths, context, casual phrasing).
   IMPL: This repo's plugins overlap heavily (intent-lock / decision-interview / plan-critique; pubmed-research-note / comprehensive-review / psych-paper-digest; concept-animation / ml-concept-lab). Each needs near-miss negatives drawn from its siblings' territory, which the current evals only partly have (0 negatives in 4 of 11 files).
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-37] (confirmed|trigger-eval|official-repo) Claude consults a skill only for tasks it can't easily handle on its own. Simple one-step queries may not trigger a well-matching skill, so trigger-eval queries must be substantive, multi-step or specialized.
   IMPL: Positive trigger cases for gate skills (intent-lock, decision-interview) must present genuinely ambiguous, costly builds; trivial negatives like 'rename usrId to userId' pass for free and discriminate little.
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-38] (confirmed|trigger-eval|official-repo) The description-optimization loop (skill-creator scripts/run_loop.py) splits the query set 60% train / 40% held-out, runs each query 3 times with a 0.5 trigger-rate threshold, proposes description edits from train failures only (up to 5 iterations), and selects `best_description` by held-out score to avoid overfitting.
   IMPL: Description rewrites should be run through this loop (or an equivalent held-out check), not tuned by adding the exact keywords of failed prompts, which agentskills.io calls overfitting.
   SRC: /mnt/skills/examples/skill-creator/SKILL.md
[EVL-39] (confirmed|trigger-eval|official-repo) skill-creator's trigger runner doesn't install the plugin. It writes a temporary command file into the project's .claude/commands/ so the description shows up in the skill listing, then runs `claude -p` with the raw query. It therefore tests the description in the user's live environment, alongside their other skills, whereas plugin eval tests it in isolation.
   IMPL: The two tools answer different trigger questions: plugin eval shows whether the description alone can win, and the skill-creator loop shows whether it wins against the owner's real installed marketplace of ~13 sibling plugins. The latter is the realistic routing test for this repo.
   SRC: /mnt/skills/examples/skill-creator/scripts/run_eval.py
[EVL-40] (confirmed|post-deploy-telemetry|official-docs) The skill listing is budgeted at 1% of the context window, and each entry's description + when_to_use is capped at 1,536 chars. When the listing overflows, descriptions of the least-invoked skills are shortened first. /doctor estimates the listing's cost, and /skill-doctor (>= 2.1.252) reports each skill's context cost and usage and flags skills that have never been invoked.
   IMPL: Isolated plugin evals can't reproduce listing-budget truncation, so use /skill-doctor on the owner's real setup as production telemetry (which of the ~20 skills are never invoked) to prioritise the rewrite. /skill-doctor is not available over Remote Control.
   SRC: https://code.claude.com/docs/en/skills.md
[EVL-41] (confirmed|deterministic-tests|local-repo) Deterministic logic bundled with a skill should be verified by scripts or unit tests rather than LLM judgment. skill-creator says to write and run a script for programmatically checkable assertions. learn-hub already unit-tests atomize-book's Python helpers with stdlib unittest, outside `npm test`.
   IMPL: Keep a three-layer test stack: (1) static validation, (2) unit tests for scripts/ inside skills, (3) behavioural evals. Wire layer 2 into the health check explicitly, since npm test and validate.py don't run it.
   SRC: /home/user/learn-hub/CLAUDE.md
[EVL-42] (confirmed|static-validation|official-docs) `claude plugin validate <path>` statically checks plugin.json, hooks/hooks.json and the frontmatter of skills/agents/commands in default dirs. `--strict` turns warnings into errors (for CI) and `--json` emits a machine-readable report. Run on a marketplace dir, it also validates each local plugin's plugin.json and warns when the marketplace entry's version doesn't match plugin.json. The docs present this as the syntax/schema check, as opposed to behaviour evals.
   IMPL: scripts/validate.py duplicates part of this (manifest/catalog version equality, frontmatter). Run `claude plugin validate . --strict` plus a per-plugin run in the health check, and keep validate.py only for repo-specific rules (200-1024 char descriptions, name==dir, ROUTING.md freshness, evals coverage).
   SRC: https://code.claude.com/docs/en/plugin-marketplaces.md
[EVL-43] (confirmed|methodology|official-docs) Official measurement advice is to check triggering and output quality separately, in fresh sessions, against a baseline with the skill disabled. Leftover authoring context masks gaps in the written instructions.
   IMPL: Rewrite verification must not be done in the same session that rewrote the skill; use plugin eval or skill-creator subagents (fresh context) for every rewritten skill.
   SRC: https://code.claude.com/docs/en/skills.md
[EVL-44] (confirmed|methodology|official-docs) Evaluation-driven development: run Claude without the skill on representative tasks, write about three evaluations targeting the observed gaps, measure the baseline, then write only the minimal instructions needed. The checklist also asks for testing with Haiku, Sonnet and Opus.
   IMPL: For each long SKILL.md, the rewrite should start from a without-skill baseline run and cut sections that close no observed gap. Use `--model` to run the smoke suite on at least two model tiers.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[EVL-45] (confirmed|doc-conflict|official-docs) Doc conflict: the platform best-practices page (undated) says there is no built-in way to run skill evaluations, while the newer Claude Code docs ship `claude plugin eval` (released v2.1.269, week of Sept 7-11 2026) and skill-creator's eval/benchmark modes (announced March 3, 2026). The newer Claude Code docs supersede the older statement for plugin-packaged skills.
   IMPL: Prefer the built-in `claude plugin eval` for plugins over growing a custom eval harness; the best-practices JSON shape (skills/query/files/expected_behavior) is illustrative only.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[EVL-46] (partially|eval-systems|official-docs) `claude plugin eval` requires a plugin manifest (plugin.json) or a skills-directory plugin (a folder under a skills dir containing .claude-plugin/plugin.json, loaded as <name>@skills-dir). Plain project skills in .claude/skills/<name>/SKILL.md without a manifest are not a valid target. The docs' alternative for them is the skill-creator loop or fresh-session with/without comparisons.
   CORRECTED: Documented targets are plugin dirs with a manifest and skills-directory plugins (<name>@skills-dir). CLI 2.1.280 also resolves a bare skill folder (SKILL.md, no manifest) as an unversioned plugin and can run evals for it, but this is undocumented and shouldn't be relied on. For plain .claude/skills skills, the documented route is skill-creator or manual fresh-session with/without comparisons. Every plugin in this repo has a manifest, so this doesn't block anything.
   IMPL: learn-hub's 11 project skills in .claude/skills (atomize-book, ingest-*, sync-vault, ...) can't be plugin-eval'd as they stand. Either move them into learn-hub/plugins/<name>/ (which already has a marketplace), add .claude-plugin/plugin.json to make them skills-dir plugins, or evaluate them with skill-creator.
   SRC: https://code.claude.com/docs/en/plugin-evals.md
   EVID: The docs' Requirements list only 'A plugin directory with a `plugin.json` or `.claude-plugin/plugin.json` manifest, or a skills-directory plugin'. Empirically, CLI 2.1.280 also accepts a bare skill folder (SKILL.md, no manifest). `claude plugin eval .` printed 'Plugin under test: "foo" (no version)' and set up two ablation arms, and `init --bare c1` scaffolded evals/ there. The init error text says 'not a plugin or skill folder'. So 'not a valid target' is false in practice, though it matches the docs.
[EVL-47] (partially|packaging|official-repo) skill-creator's packager excludes a skill-root `evals/` directory from the uploadable .skill bundle, so skill-level evals never ship to claude.ai/API. A plugin-root evals/ travels with the plugin: an installed plugin can be evaluated from its installed copy's eval directory.
   CORRECTED: skill-creator's package_skill.py excludes a skill-root evals/ from the .skill bundle uploaded to claude.ai. Inside a plugin, skills/<skill>/evals/ still ships with the plugin, but `claude plugin eval` never reads it. A plugin-root evals/ (or experimental.evals dir) travels with the plugin, and `claude plugin eval <name>` runs the installed copy's cases, writing results under ./evals/results/ in the current directory.
   IMPL: Keeping skills/<skill>/evals/ is harmless for packaging. Moving cases to plugins/<name>/evals/ lets `claude plugin eval <name>@micky-psych-tools` re-check an installed copy after a marketplace update.
   SRC: /mnt/skills/examples/skill-creator/scripts/package_skill.py
   EVID: package_skill.py has `ROOT_EXCLUDE_DIRS = {"evals"}` ('Directories excluded only at the skill root'), so evals/ is left out of the .skill zip built for upload. 'Never ship' overreaches. That applies only to package_skill.py bundles. A skill-level evals/ inside a Claude Code plugin is part of the plugin directory and is distributed to anyone who installs the plugin; it is just not runnable by `claude plugin eval`. The installed-copy half is confirmed ('An installed plugin by name... The cases in the installed copy's eval directory').
[EVL-48] (confirmed|methodology|anthropic-blog) Model and version drift is a reason to re-run evals: a skill that worked last month can behave differently after model or infrastructure changes. For capability-uplift skills, a base model that passes the evals without the skill signals the skill is no longer needed.
   IMPL: Re-run each plugin's suite after default-model changes. A persistently ~0 Δ on a long skill's cases is evidence for deleting or shrinking that skill in the rewrite.
   SRC: https://claude.com/blog/improving-skill-creator-test-measure-and-refine-agent-skills
--- missed_practices
MP-EVL-1: Match the eval goal to the skill type. For capability-uplift skills, evals detect when the base model has caught up. For encoded-preference (workflow) skills, which is most of this repo (intent-lock, vault-keeper, plan-critique, decision-interview), evals should verify fidelity to the owner's actual workflow, so graders belong on process order and hand-offs, not just the final answer.
   SRC: https://claude.com/blog/improving-skill-creator-test-measure-and-refine-agent-skills
   Q: Capability uplift skills may become less necessary as models improve. Evals tell you when that's happened. Encoded preference skills are more durable, but only as valuable as their fidelity to your actual workflow. Evals verify that fidelity.
MP-EVL-2: Evaluate progressive disclosure by watching which bundled files Claude reads. Unread reference files, repeated reads and unexpected read orders are signals to restructure the skill. In plugin eval this can be graded with `tool_used`/`tool_order` on Read with an input_match naming the reference file. It matters for this repo's reference-heavy skills.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   Q: Ignored content: If Claude never accesses a bundled file, it might be unnecessary or poorly signaled in the main instructions
MP-EVL-3: Complement synthetic evals with observation in real workflows: one Claude instance authors or refines the skill ('Claude A') while a fresh instance ('Claude B') uses it on actual tasks, and observed failures feed back into the skill.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   Q: Use the Skill in real workflows: Give Claude B (with the Skill loaded) actual tasks, not test scenarios
MP-EVL-4: Close the loop by giving failed assertions, human feedback, execution transcripts and the current SKILL.md to an LLM to propose edits. Instruct it to generalize rather than patch for the test cases, keep the skill lean, explain the why, and bundle repeated helper work into scripts/. This avoids overfitting a small suite.
   SRC: https://agentskills.io/skill-creation/evaluating-skills
   Q: Generalize from feedback. The skill will be used across many different prompts, not just the test cases. Fixes should address underlying issues broadly rather than adding narrow patches for specific examples.
MP-EVL-5: Define explicit stopping criteria for the iterate loop rather than iterating indefinitely.
   SRC: https://agentskills.io/skill-creation/evaluating-skills
   Q: Stop when you're satisfied with the results, feedback is consistently empty, or you're no longer seeing meaningful improvement between iterations.
MP-EVL-6: Before reading a score drop as a regression, check run errors for usage- or rate-limit messages. Limit-hit runs score about 0 but the suite is not marked partial.
   SRC: https://code.claude.com/docs/en/plugin-evals
   Q: The suite still finishes and isn't marked `partial`, so the result can look like a regression. Check the `NOTES` column or `cases[].arms.with[].error` in the JSON for the limit message before trusting the scores
MP-EVL-7: A passing eval suite is not a safety vetting. If the plugin ships hooks or runs real MCP servers, treat scores as advisory unless the suite ran in an isolated container or CI runner, because hooks and servers run outside the agent sandbox and can touch the files graders read.
   SRC: https://code.claude.com/docs/en/plugin-evals
   Q: When the plugin ships hooks you didn't write, or you start its real MCP servers, treat its scores as advisory unless you ran it in an isolated environment such as a container or CI runner
MP-EVL-8: Mine the discriminating assertions, the ones that pass with the skill and fail without, to learn which instructions or scripts carry the value. Treat stddev as meaningful only once each eval runs multiple times.
   SRC: https://agentskills.io/skill-creation/evaluating-skills
   Q: Study assertions that pass with the skill but fail without. This is where the skill is clearly adding value. Understand why — which instructions or scripts made the difference?
--- open_questions
OQ: Does `plugins:` in prompt.md/case.yaml accept several plugin directories so sibling plugins load together (needed to test cross-plugin routing and delegation such as pubmed-research-note -> intent-lock/vault-keeper)? The docs use the plural but show only a single-path example.
OQ: `experimental.evals` is typed string|array. With an array (for example one dir per skill), where do results/ go, and are case names deduplicated across dirs? Not documented.
OQ: The three MCP-using plugins declare http servers at pubmed.mcp.claude.com and hcls.mcp.claude.com. Would these authenticate inside an isolated eval run with --allow-real-servers, or are mocks the only practical path?
OQ: Which field name is canonical for skill-creator checks, `assertions` (SKILL.md, agentskills.io, this repo) or `expectations` (schemas.md, aggregate_benchmark.py)? The viewer and aggregation code read `expectations` in grading.json.
OQ: Can `claude plugin eval` be pointed at a marketplace root to run every plugin's suite in one command, or must CI loop over plugins/*? The docs only describe plugin, case-file and installed-plugin targets.
OQ: Budget: converting all 118 existing evals at the defaults (3 runs x 2 arms, plus llm-judge calls) is roughly 700 agent runs per full pass. What smoke-subset size and release cadence does the owner accept?
OQ: learn-hub/plugins duplicates 4 of micky-psych-tools' evals.json files (intent-lock, misread-capture, comprehensive-review, pubmed-research-note). Should eval suites live only at the source-of-truth copy?
#### COM — Community and exemplar practice
[COM-01] (confirmed|description-triggering|community) Superpowers' writing-skills rule: the description should state ONLY triggering conditions (start with 'Use when...') and must never summarize the skill's process/workflow, because agents follow the short description as a shortcut and skip the body.
   IMPL: Most repo descriptions (e.g. pubmed-research-note 'a quantified, adjudicated evidence report with a clearly marked verdict...') are workflow summaries; rewrite them to trigger conditions + near-miss boundaries and move the procedure summary into the body.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-02] (confirmed|description-triggering|community) Jesse Vincent reports that from Opus 4.5 onward Claude more often 'winged' a skill it claimed to use, which he attributed to descriptions explaining what the skill does; he rewrote descriptions to contain only when-to-use information.
   IMPL: Long 'what-it-does' descriptions near the 1024 cap risk Claude acting from the description alone; test the repo's gate skills (intent-lock, decision-interview) for 'announced but not read' behaviour in transcripts.
   SRC: https://blog.fsck.com/2025/12/18/superpowers-4/
[COM-03] (confirmed|description-triggering|anthropic-blog) Anthropic's Claude Code team (Jun 2026) says the description is scanned from the session skill listing to decide whether a skill exists for the request, so it should describe when to trigger, not summarize; include literal trigger words.
   IMPL: Converge with COM-01: descriptions should lead with user phrasings/situations (e.g. 'critique this plan', '/digest'), not a feature inventory.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-04] (confirmed|conflict-description|official-docs) CONFLICT: the official authoring best-practices page and the official skill-creator skill both say the description must include BOTH what the skill does and when to use it, which contradicts the trigger-only rule of superpowers (COM-01) and Anthropic's own Jun-2026 blog (COM-03). The official docs page was fetched today (live); the blog is dated 2026-06-03; superpowers v4 rule dates 2025-12-17. A defensible reconciliation: one short 'what' clause (capability noun phrase, no steps) + trigger conditions, never a workflow sequence.
   IMPL: The plan must pick a house rule and encode it in validate.py/plugin-creator templates; recommend 'one-clause capability + triggers + NOT-for boundaries, no step sequence' and verify with trigger evals rather than by argument.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
[COM-05] (confirmed|description-triggering|official-repo) anthropics/skills' skill-creator says Claude tends to undertrigger skills and recommends making descriptions a little 'pushy' (e.g. 'Make sure to use this skill whenever the user mentions ... even if they don't explicitly ask').
   IMPL: Pushiness must be balanced against overlap among sibling skills (comprehensive-review vs pubmed-research-note vs psych-paper-digest); only trigger evals with near-miss negatives can calibrate it.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-06] (confirmed|description-triggering|official-repo) Exemplar descriptions in anthropics/skills state explicit negative boundaries inside the description (docx: 'Do NOT use for PDFs...'; claude-api uses a TRIGGER/SKIP structure with an explicit SKIP clause that overrides triggers).
   IMPL: Keep the repo's 'NOT for ... (use X)' routing boundaries, but put them in the description itself (short) so sibling-skill disambiguation happens at listing time rather than depending on ROUTING.md.
   SRC: https://github.com/anthropics/skills/blob/main/skills/docx/SKILL.md
[COM-07] (confirmed|conflict-description|open-standard) CONFLICT on voice: the Agent Skills open-standard guide says frame the description imperatively ('Use this skill when...'), while the official Anthropic best-practices page says always write in third person and avoid second person ('You can use this to...'). Superpowers mixes both: third person but starting 'Use when...'.
   IMPL: Pick one voice and lint it; 'Use when <situation>...' (superpowers form) satisfies both the imperative-trigger advice and avoids first/second person pronouns flagged by the official page.
   SRC: https://agentskills.io/skill-creation/optimizing-descriptions
[COM-08] (confirmed|context-budget|community) Jesse Vincent reports users hitting Claude Code's hidden character limits on skill descriptions, causing skills to be hidden; he responded by consolidating skills. The current Claude Code docs confirm a listing budget of 1% of the context window that shortens/drops descriptions, starting with least-invoked skills.
   IMPL: With ~17 marketplace skills at ~700-1024 chars plus learn-hub's 11 project skills, total descriptions can crowd the listing; run /doctor and /skill-doctor and shorten descriptions rather than rely on the 1024 cap.
   SRC: https://blog.fsck.com/2025/12/18/superpowers-4/
[COM-09] (confirmed|context-budget|official-docs) Claude Code truncates the combined description + when_to_use text at 1,536 characters in the skill listing and advises putting the key use case first; the listing budget scales at 1% of context and can be raised via skillListingBudgetFraction or SLASH_COMMAND_TOOL_CHAR_BUDGET.
   IMPL: The repo's '1024 hard cap' is the spec limit, but the effective lever is front-loading: validate.py should also check that the first ~150 chars carry the primary trigger.
   SRC: https://code.claude.com/docs/en/skills
[COM-10] (confirmed|conflict-length|community) CONFLICT on body length: superpowers targets <500 WORDS for ordinary skills (<150-200 for always-loaded ones) and <500 characters for descriptions, whereas the Agent Skills spec and official docs allow up to 500 LINES / <5000 tokens for the SKILL.md body.
   IMPL: Repo SKILL.md bodies run 600-4,300 words (intent-lock 4,317 words); the official 500-line ceiling is met but the community word target is exceeded 3-8x — the plan should set a word budget (e.g. body <=1,500 words, overflow to references/).
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-11] (confirmed|structure-length|open-standard) The Agent Skills spec says keep SKILL.md under 500 lines and move detail to separate files; skill-creator treats these numbers as approximate, and an official exemplar (claude-api SKILL.md, 572 lines) exceeds it.
   IMPL: Treat 500 lines as a validate.py warning not an error; the stronger constraint is words-per-activation (COM-10).
   SRC: https://agentskills.io/specification
[COM-12] (confirmed|progressive-disclosure|open-standard) Progressive-disclosure pointers should say WHEN to load each reference file (a concrete condition), not just list the references directory.
   IMPL: Audit each SKILL.md's references/ pointers (e.g. plugin-creator, clinical-infographic fidelity contract) and rewrite them as 'Read X when Y' lines; a validate.py check can flag references/ files never named in SKILL.md.
   SRC: https://agentskills.io/skill-creation/best-practices
[COM-13] (confirmed|scripts-vs-prose|official-repo) Exemplar practice: bundled scripts are run as black boxes via --help rather than read into context (webapp-testing), and command flags are left to --help rather than documented in SKILL.md (superpowers; Simon Willison makes the same argument for CLI tools).
   IMPL: Where repo skills describe scripts' flags in prose (vault-keeper, psych-paper-digest sweeps), give the scripts a good --help and replace the prose with one invocation line.
   SRC: https://github.com/anthropics/skills/blob/main/skills/webapp-testing/SKILL.md
[COM-14] (confirmed|scripts-vs-prose|official-repo) skill-creator's rule for when to bundle a script: if test-run transcripts show subagents independently writing the same helper, bundle it in scripts/ and tell the skill to use it.
   IMPL: Use eval transcripts to decide which prose procedures (e.g. PubMed query assembly, HTML self-containment checks in concept-animation/ml-concept-lab) should become scripts.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-15] (confirmed|scripts-vs-prose|official-repo) Exemplars ship validators whose error output names the fix (pptx validate.py), so the model can loop validate-fix without reading validator source; Simon Willison highlights the slack-gif-creator's size-check function for the same self-correction loop.
   IMPL: Skills producing self-contained HTML (clinical-infographic, concept-animation, code-explainer, ml-concept-lab) should ship a checker script with fix-naming errors instead of long prose contracts.
   SRC: https://github.com/anthropics/skills/blob/main/skills/pptx/SKILL.md
[COM-16] (confirmed|mechanical-rules|community) Superpowers says not to spend skill prose on mechanical constraints that can be enforced by regex/validation — automate them and reserve documentation for judgment calls.
   IMPL: Rules like kebab-case names, version parity, description caps, reduced-motion blocks and 'no external refs' belong in validate.py / bundled checkers, and can be deleted from SKILL.md bodies.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-17] (partially|content-selection|official-repo) Newer exemplar docx/pptx skills are written as gotchas-only: they assume the model knows the library and list only footguns plus a task->approach table.
   CORRECTED: In the newer docx/pptx exemplars, the library-creation sections are written as footgun lists that assume the model knows the API, opening with a task→approach table. The skills also keep workflow, verification/QA and (for pptx) design-guidance sections, so they are not gotchas-only.
   IMPL: The firecrawl skill (Firecrawl's onboarding guide kept verbatim, 384 lines) is the opposite pattern; reduce it to a routing table + repo-specific gotchas and push vendor text to references/.
   SRC: https://github.com/anthropics/skills/blob/main/skills/docx/SKILL.md
   EVID: docx SKILL.md line 21 and pptx SKILL.md line 31 both say "The model knows the API; these are the footguns:", and each opens with a Task→Approach table. Neither skill is gotchas-only, though. docx also has 'Verify the output', 'Editing existing documents' and 'Comments' sections. pptx has a scripts table, 'Design Ideas' (palettes, typography, spacing), and required 'QA' sections that together run to about 238 lines.
[COM-18] (confirmed|content-selection|anthropic-blog) Anthropic's Claude Code team calls the Gotchas section the highest-signal content in a skill, built from observed failure points and updated over time; best skills start as a few lines and a single gotcha.
   IMPL: Each rewritten SKILL.md should have an explicit Gotchas section harvested from misread-capture logs and past corrections, instead of diffusing lessons through prose.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-19] (confirmed|content-selection|anthropic-blog) Don't state the obvious: content restating default Claude behaviour costs context without value; skills should push Claude out of its normal way of thinking.
   IMPL: Run a 'would Claude do this anyway?' pass over each skill; generic research-method or HTML-authoring advice is a deletion candidate, domain-specific clinical fidelity rules are not.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-20] (confirmed|rigid-vs-flexible|anthropic-blog) Anthropic (Jul 2026) found it had been overconstraining Claude via system prompt, CLAUDE.md and skills; it removed over 80% of Claude Code's system prompt for Claude 5-generation models with no measurable eval loss, and now advises skills avoid overconstraint except in highly important areas.
   IMPL: This is the newest guidance: default to goal + constraints + gotchas, and reserve hard rules for the genuinely high-stakes gates (clinical fact fidelity, destructive vault deletes).
   SRC: https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models
[COM-21] (confirmed|rigid-vs-flexible|anthropic-blog) Avoid railroading: state the goal and constraints rather than every step, because skills are reused across many situations.
   IMPL: Long fixed step lists (e.g. plan-critique's nine-lens pipeline, intent-lock's interrogation) should be reviewed for which steps are true invariants vs. defaults Claude can adapt.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-22] (confirmed|rigid-vs-flexible|community) Superpowers' 'Match the Form to the Failure': prohibitions + rationalization tables are only for discipline failures (agent knows the rule but skips it under pressure); wrong-shaped output needs a positive recipe/contract, omitted elements need a required template slot, and conditional behaviour needs a predicate — prohibitions measurably backfire on shaping problems, and nuance/exemption clauses degrade them.
   IMPL: Classify each 'never/always' rule in the repo's skills by failure type; e.g. report-shape rules in pubmed-research-note/comprehensive-review should become output contracts, not prohibition lists.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-23] (confirmed|anti-rationalization|community) For discipline-enforcing skills, superpowers closes loopholes explicitly: a rationalization table built from verbatim baseline excuses, a 'Red Flags - STOP' list, and an early foundational line that the letter of the rule is its spirit.
   IMPL: Appropriate only for the repo's gate skills (intent-lock Step 0, vault-keeper's move->verify->delete order); the tables must come from observed baseline excuses, not invented ones.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-24] (confirmed|anti-rationalization|community) Superpowers' persuasion-principles guide scopes emphasis by skill type: heavy authority/commitment language only for discipline skills, moderate for technique skills, and 'clarity only' (no persuasion) for reference skills.
   IMPL: Gives a checkable rubric: reference-type content (firecrawl, design systems, lens catalogs) should carry no MUST/NEVER capitals.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/persuasion-principles.md
[COM-25] (partially|conflict-emphasis|official-repo) CONFLICT on emphatic wording: skill-creator treats all-caps ALWAYS/NEVER and rigid structures as a yellow flag (explain the why instead); the official best-practices page suggests switching to stronger language like 'MUST filter'; superpowers uses 'YOU MUST'/EXTREMELY-IMPORTANT for discipline skills. Newest Anthropic guidance (Jul 2026, COM-20) sides with less constraint.
   CORRECTED: On emphatic wording, skill-creator treats all-caps ALWAYS/NEVER and rigid structure as a yellow flag (explain the why instead). superpowers deliberately uses 'YOU MUST' and EXTREMELY_IMPORTANT framing for discipline skills. The official best-practices page mentions 'MUST filter' only as a change Claude A might suggest during iteration, not as a recommendation. The newest Anthropic guidance (Jul 2026) leans toward less constraint except in highly important areas.
   IMPL: Adopt 'explain the why by default; caps only where a micro-test shows the softer wording fails' and add a validate.py warning counting all-caps MUST/NEVER per skill.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
   EVID: The skill-creator quote is verbatim (line 302), and superpowers does use 'YOU MUST' / 'ABSOLUTELY MUST' plus the <EXTREMELY_IMPORTANT> wrapper in its session-start hook. The official best-practices page does not itself recommend stronger language, though. It appears in an iteration walkthrough as something another Claude instance might propose: "Claude A might suggest reorganizing to make rules more prominent, using stronger language such as 'MUST filter' instead of 'always filter,' or restructuring the workflow section."
[COM-26] (partially|conflict-examples|anthropic-blog) CONFLICT on examples: Anthropic's Sep-2025 context-engineering post strongly advises curated few-shot examples, superpowers says one excellent example beats many; Anthropic's Jul-2026 post (newer) reports that examples constrain Claude 5-generation models and recommends expressive interface design instead.
   CORRECTED: The Jul-2026 post says examples constrain Claude 5-generation models' exploration, mainly for tool usage, and recommends expressive tool/script/file interfaces instead. That reverses the Sep-2025 'strongly advise' few-shot guidance for that context. The live best-practices page still recommends input/output example pairs where output style matters, so the newest guidance argues against gratuitous examples, not against all examples in skills.
   IMPL: Worked examples under examples/ (ml-concept-lab, code-explainer template) should be framed as references loaded on demand, not inline few-shot in SKILL.md; test whether they narrow output variety.
   SRC: https://claude.com/blog/the-new-rules-of-context-engineering-for-claude-5-generation-models
   EVID: Sep 29, 2025 post (anthropic.com/engineering/effective-context-engineering-for-ai-agents): "Providing examples, otherwise known as few-shot prompting, is a well known best practice that we continue to strongly advise." superpowers: "One excellent example beats many mediocre ones". The Jul 24, 2026 quote is verbatim, but it sits under 'Then: Give Claude examples / Now: Design interfaces' and is framed around tools: "The number one rule for tool usage was to give Claude examples ... Instead of using examples, think more about the design of your tools, scripts and files". The live official best-p
[COM-27] (confirmed|testing|community) Superpowers treats skill writing as TDD: run a baseline scenario with a subagent WITHOUT the skill, record failures verbatim, write the minimal skill addressing them, re-run WITH it, then close loopholes; this applies to edits too.
   IMPL: Each rewrite should start by running the existing evals/evals.json prompts against a no-skill baseline to prove which instructions actually change behaviour before cutting or adding.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-28] (partially|testing|community) Jesse Vincent found that subagent 'tests' posed as quizzes gave false perfect scores; realistic pressure scenarios that force a concrete choice (combining 3+ pressures like time, sunk cost, authority) were what exposed violations.
   CORRECTED: Jesse Vincent's Oct-2025 post reports that quiz-style subagent tests gave false perfect scores and that realistic pressure scenarios forcing a concrete choice exposed the violations. The later testing-skills-with-subagents.md in superpowers codifies combining 3+ pressures (time, sunk cost, exhaustion, authority).
   IMPL: Gate skills (intent-lock, decision-interview, vault-keeper empty-vault) need pressure-scenario evals (e.g. 'user is in a hurry, skip the interview'), not just happy-path prompts.
   SRC: https://blog.fsck.com/2025/10/09/superpowers/
   EVID: blog.fsck.com/2025/10/09/superpowers: "Claude told me that the subagents had gotten a perfect score ... Claude was quizzing the subagents like they were on a gameshow. This was less than useful. I asked to switch to realistic scenarios that put pressure on the agents". The scenarios it shows combine two pressures each ('Time Pressure + Confidence', 'Sunk Cost + Works Already') and end 'Choose and act.' The '3+ pressures' rule does not come from the blog. It is in superpowers' testing-skills-with-subagents.md: "**Best tests combine 3+ pressures.**"
[COM-29] (confirmed|testing|community) Before full scenarios, superpowers micro-tests wording: fresh-context single samples, a mandatory no-guidance control (if the control doesn't fail, don't write the guidance), 5+ reps per variant, and manual reading of every flagged match.
   IMPL: Provides a deletion test for the rewrite: any rule whose no-guidance control already behaves correctly can be cut.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-30] (confirmed|testing|official-repo) skill-creator runs each eval twice in parallel in the same turn — with the skill and a baseline (no skill for new skills; a snapshot of the old version when improving) — then grades assertions, aggregates a benchmark, and shows a human viewer before revising.
   IMPL: For a rewrite, the baseline is the current plugin version: snapshot each skill before editing and run old_skill vs new_skill on the existing evals.json.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-31] (confirmed|evals|official-repo) skill-creator's description optimizer uses ~20 realistic queries (8-10 should-trigger, 8-10 should-not), splits 60% train / 40% held-out test, runs each query 3 times for a trigger rate, and selects the best description by held-out score.
   IMPL: The repo has no should_trigger eval sets at all (grep finds none); add a trigger-eval file per skill so description rewrites are measured, not guessed.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-32] (confirmed|evals|official-repo) The most valuable negative trigger queries are near-misses that share keywords with the skill but need something else; obviously irrelevant negatives test nothing.
   IMPL: The repo's sibling families (comprehensive-review/pubmed-research-note/psych-paper-digest; concept-animation/ml-concept-lab; intent-lock/decision-interview/plan-critique) supply ready-made near-miss negatives for each other.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-33] (confirmed|evals|official-repo) Trigger evals must use substantive, multi-step queries: Claude only consults skills for tasks it can't easily do alone, so simple one-step prompts won't trigger even a perfectly matching description.
   IMPL: Write trigger queries with realistic clinical/project context (file paths, drug names, vault topics), not one-liners.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
[COM-34] (confirmed|evals|official-repo) Inconsistency inside skill-creator: SKILL.md refers to an 'assertions' field in evals.json, while references/schemas.md defines the field as 'expectations' (grading.json uses text/passed/evidence).
   IMPL: All 11 repo evals.json files use 'assertions' (plus a non-schema 'name' key); decide whether to align with schemas.md's 'expectations' and have validate.py enforce one schema.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/references/schemas.md
[COM-35] (confirmed|packaging-validation|official-repo) skill-creator's quick_validate.py (also used by package_skill.py) rejects frontmatter keys outside {name, description, license, allowed-tools, metadata, compatibility} and rejects angle brackets in descriptions; Claude Code docs confirm claude.ai upload/packaging fails hard on non-spec keys (e.g. argument-hint) while Claude Code itself accepts them.
   IMPL: If these skills should also run on claude.ai/Cowork, validate.py needs a 'portable' profile (spec-only keys, no '<'/'>' in descriptions); Claude Code-only keys stay allowed for the CC profile.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/scripts/quick_validate.py
[COM-36] (confirmed|cross-skill-references|community) Superpowers references other skills by fully-qualified name with explicit requirement markers and forbids @-file links, because @ force-loads the file into context immediately.
   IMPL: Delegations (e.g. 'vault writes go through vault-keeper', 'Act items hand off to pubmed-research-note') should be written as 'REQUIRED SUB-SKILL: vault-keeper:vault-keeper' style name references, never @paths.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-37] (confirmed|cross-skill-references|anthropic-blog) Anthropic notes skill-to-skill dependency management is not native to marketplaces yet; referencing another skill by name works if it is installed.
   IMPL: Cross-plugin handoffs (intent-lock gate, vault-keeper, firecrawl engine) need a stated fallback when the dependency plugin is not installed.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-38] (partially|shared-content|official-docs) Claude Code copies installed plugins to a cache, so a plugin cannot reference files outside its own directory (e.g. ../shared-utils); within-plugin cross-skill relative links (as superpowers does, ../using-superpowers/references/...) still work.
   CORRECTED: When a plugin is copied into the plugin cache (the normal case for git and other remote sources), it cannot reference files outside its own directory (e.g. ../shared-utils). Plugins loaded in place (relative-path sources in a marketplace added from a local directory, or link-mode command sources) are exempt, but relying on that breaks remote installs. Within-plugin relative links between skills still work.
   IMPL: Shared contracts (clinical fidelity rules, vault schema) cannot live in a repo-level shared/ folder referenced by path from several plugins; either duplicate with a sync check, or keep them in one plugin and delegate by skill name.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces
   EVID: code.claude.com/docs/en/plugin-marketplaces: "when users install a plugin, Claude Code copies the plugin directory to a cache location, unless the plugin loads in place. A `command` source in link mode loads in place, and so does a relative path source in a marketplace added from a local directory. Copied plugins can't reference files outside their directory using paths like `../shared-utils`". superpowers does link within the plugin (writing-skills → ../using-superpowers/references/codex-tools.md). The claim drops the 'unless the plugin loads in place' exception, which applies to a local-dire
[COM-39] (confirmed|shared-content|official-repo) The official exemplar repo chooses duplication for shared code: docx and pptx each carry a byte-identical scripts/office/soffice.py (and validate.py) so each skill stays self-contained.
   IMPL: Verified locally: md5 of skills/docx/scripts/office/soffice.py == skills/pptx/scripts/office/soffice.py (2508a6df). If the repo duplicates shared files across plugins, add a validate.py parity check (the learn-hub already uses byte-identical-twin + parity-test for mermaid-split/tag-canon).
   SRC: https://raw.githubusercontent.com/anthropics/skills/main/skills/pptx/SKILL.md
[COM-40] (confirmed|skill-scope|community) Superpowers consolidated several standalone skills into progressively-disclosed parts of broader skills (to save listing budget) and judged a 'what not to do' skill a mistake — skills should be about what to do.
   IMPL: Candidates for merge-into-references: intent-lock's misread-capture, plugin-creator's refine-plugin, vault-keeper's empty-vault — decide by trigger evals whether they need their own listing slot.
   SRC: https://blog.fsck.com/2025/12/18/superpowers-4/
[COM-41] (confirmed|skill-scope|anthropic-blog) Anthropic catalogued its internal skills into nine categories and found the best skills fit cleanly into one; skills straddling several confuse the agent. The open standard similarly warns against both too-narrow and too-broad scope.
   IMPL: Skills that combine research + authoring + filing (comprehensive-review, clinical-infographic) should keep one job and delegate the rest by name.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-42] (confirmed|commands|community) Superpowers deprecated (v5.0.0) then removed (v5.1.0, 2026-04-30) its slash commands because they were stubs that only told Claude to invoke the matching skill; earlier it had marked them disable-model-invocation to stop Claude invoking redirect commands. Claude Code docs now state custom commands are merged into skills.
   IMPL: The repo's thin commands ('Run the `plan-critique` skill now.') are exactly this pattern; since a skill directory already yields /<name>, consider deleting them or moving argument-hint/$ARGUMENTS handling into the skill frontmatter.
   SRC: https://github.com/obra/superpowers/blob/main/RELEASE-NOTES.md
[COM-43] (confirmed|routing|community) Superpowers routes via a bootstrap skill (using-superpowers) injected by a SessionStart hook on startup|clear|compact, which tells Claude to check skills before any response and sets process-skill-first priority; it also states user instructions (CLAUDE.md) outrank skills.
   IMPL: An alternative to the generated ROUTING.md + /route: a small injected router, or simply better descriptions; any injected router must stay tiny (superpowers targets <150-200 words for always-loaded content).
   SRC: https://github.com/obra/superpowers/blob/main/hooks/hooks.json
[COM-44] (confirmed|naming|community) Naming: superpowers prefers active, verb-first/gerund names that name the action or core insight; the official best-practices page likewise suggests gerund form and warns against vague names like helper/utils.
   IMPL: Renames are costly (routing, vault references, installs); treat as optional — the repo's names are already specific (not 'helper'), so keep them unless a rename also fixes a scope problem.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-45] (confirmed|structure|community) Flowcharts (DOT/ASCII) are for non-obvious decision points and loops where the agent might stop early, not for linear steps or reference; Jesse reports Claude follows DOT process definitions well.
   IMPL: Replace long prose routing branches (e.g. intent-lock's decision gate, empty-vault's move->verify->delete) with one small decision graph; keep linear procedures as numbered lists.
   SRC: https://blog.fsck.com/2025/12/18/superpowers-4/
[COM-46] (confirmed|scripts-vs-prose|community) Invoke bundled scripts through their interpreter in SKILL.md prose, never by bare path, because some plugin packagers strip executable bits.
   IMPL: Add a validate.py lint that flags bare `scripts/...` invocations without python3/node/bash in SKILL.md and commands.
   SRC: https://github.com/obra/superpowers/blob/main/skills/writing-skills/SKILL.md
[COM-47] (confirmed|state-and-measurement|anthropic-blog) Anthropic recommends persisting skill state/memory under ${CLAUDE_PLUGIN_DATA} (stable dir), storing setup in a config file and asking the user when it is missing, and measuring undertriggering by logging skill usage with a PreToolUse hook; Claude Code also ships /skill-doctor for per-skill cost and usage.
   IMPL: psych-paper-digest's .psych-paper-digest.json (config + last_swept) is a candidate to move to ${CLAUDE_PLUGIN_DATA}; add usage logging to find which of the 17 skills undertrigger.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
[COM-48] (confirmed|packaging|official-docs) The anthropics/skills marketplace defines plugins with strict:false entries that list skill directories from a shared source root and carry no per-plugin version; Claude Code docs say that with strict:false the marketplace entry is the plugin's entire definition, and a version set in either plugin.json or the entry pins updates to that string.
   IMPL: The repo's 'version lives in two files' drift hazard can be removed structurally (declare version in exactly one place, or rely on the next version-management source) rather than policed by bump.py.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces
[COM-49] (confirmed|testing|official-repo) Read execution transcripts, not only final outputs, to find instructions that cause wasted work; superpowers adds automated tests that skills trigger from naive prompts without naming them.
   IMPL: The eval loop for the rewrite should capture transcripts (claude -p JSONL) and check both 'skill was loaded' and 'no unproductive steps', as superpowers' tests/skill-triggering does.
   SRC: https://github.com/anthropics/skills/blob/main/skills/skill-creator/SKILL.md
--- missed_practices
MP-COM-1: For internal plugins under active development, omit `version` from BOTH plugin.json and the marketplace entry, so updates follow the git commit SHA. When both are set, plugin.json wins. This removes the two-file version-drift trap this repo guards with bump.py.
   SRC: https://code.claude.com/docs/en/plugins-reference
   Q: **Commit-SHA version** | Omit `version` from both `plugin.json` and the marketplace entry | Users get updates whenever the source's resolved commit changes | Internal or team plugins under active development
MP-COM-2: Run the built-in `claude plugin validate --strict` in CI or pre-commit. The runtime only warns on unrecognized fields, so without --strict a typo'd or non-spec field ships silently.
   SRC: https://code.claude.com/docs/en/plugins-reference
   Q: `claude plugin validate` reports unrecognized fields as warnings, not errors. (CLI --help: "--strict  Treat warnings as errors (exit 1). Use in CI to fail on unrecognized fields, missing metadata, and other issues that the runtime tolerates.")
MP-COM-3: Control who invokes each skill. Use `disable-model-invocation: true` for task/workflow skills the user should trigger with /name (optionally `context: fork` to run in a subagent). Use `user-invocable: false` for background-knowledge skills. Now that commands are merged into skills, this replaces separate redirect commands.
   SRC: https://code.claude.com/docs/en/skills
   Q: Task content gives Claude step-by-step instructions for a specific action, like deployments, commits, or code generation. These are often actions you want to invoke directly with `/skill-name` rather than letting Claude decide when to run them. Add `disable-model-invocation: true` to prevent Claude 
MP-COM-4: Remove 'think carefully' / 'think step by step' lines from saved instructions, including skills and CLAUDE.md. Current models already think before replying, and these lines only add latency.
   SRC: https://claude.dev/blog/getting-the-most-out-of-opus-5-5/
   Q: Remove “think carefully,” “think step by step,” and similar lines from your prompts and your saved instructions. Opus 5.5 always thinks before it replies, and it decides how much.
MP-COM-5: Provide defaults, not menus. When several approaches could work, name one default and mention alternatives briefly. Presenting equal options causes wasted exploration, which shows up in execution traces.
   SRC: https://agentskills.io/skill-creation/best-practices
   Q: When multiple tools or approaches could work, pick a default and mention alternatives briefly rather than presenting them as equal options.
MP-COM-6: Ground skills in real, project-specific expertise, such as hands-on task transcripts, incident reports, runbooks and past outputs. An LLM writing from general knowledge produces generic procedures.
   SRC: https://agentskills.io/skill-creation/best-practices
   Q: A common pitfall in skill creation is asking an LLM to generate a skill without providing domain-specific context — relying solely on the LLM's general training knowledge. The result is vague, generic procedures ("handle errors appropriately," "follow best practices for authentication") rather than 
MP-COM-7: Invest most in verification skills and verification steps inside skills (scripts, programmatic assertions on state). Internally at Anthropic these had the largest measured effect on output quality.
   SRC: https://claude.com/blog/lessons-from-building-claude-code-how-we-use-skills
   Q: Verification skills have had the most measurable impact on Claude’s output quality internally. It can be worth having an engineer spend a week just making your verification skills excellent.
MP-COM-8: Keep references one level deep from SKILL.md. Every reference file should be linked directly from SKILL.md, not chained through other references. Give reference files longer than 100 lines a table of contents so partial reads still show their scope.
   SRC: https://platform.claude.com/docs/en/agents-and-tools/agent-skills/best-practices
   Q: **Keep references one level deep from SKILL.md**. All reference files should link directly from SKILL.md to ensure Claude reads complete files when needed.
--- open_questions
OQ: Trigger-only descriptions (superpowers, Anthropic Jun-2026 blog) vs 'what + when' (official best-practices page, skill-creator): which triggers better on the current model (Opus 5.5) for this repo's overlapping sibling skills? Needs an A/B run of trigger evals per skill.
OQ: Does the combined skill listing (micky-psych-tools marketplace + learn-hub .claude/skills + user/anthropic-skills) already exceed the 1%-of-context listing budget so descriptions are being shortened or dropped? Check with /doctor and /skill-doctor in a real session.
OQ: Do superpowers' bulletproofing techniques (MUST, rationalization tables) still help on Claude 5-generation models given Anthropic's Jul-2026 finding that removing 80% of constraints cost nothing? Needs micro-tests with a no-guidance control for the repo's gate skills.
OQ: Which evals.json assertion key does the current skill-creator grader/aggregator actually read — 'assertions' (SKILL.md prose, used by all repo evals) or 'expectations' (references/schemas.md)?
OQ: Should cross-plugin shared contracts (vault format, clinical fidelity rules) be duplicated with a parity check or centralised in one plugin and reached by skill-name delegation, given copied plugins cannot read ../ paths?
OQ: Should the repo's thin slash commands be deleted in favour of skills-as-commands (argument-hint, disable-model-invocation in SKILL.md frontmatter), and would that break the generated ROUTING.md / /route workflow?
OQ: Are the skills meant to be portable to claude.ai/Cowork? If yes, frontmatter must be restricted to the six spec keys and descriptions must avoid angle brackets.
#### LOC — Local conventions, history and open items
[LOC-01] (confirmed|convention-enforced|local-repo) micky-psych-tools makes version parity a hard rule: plugin.json version must equal the marketplace entry version. validate.py:81-82 checks it and bump.py:51 writes both files.
   IMPL: The rewrite has to either keep this rule or replace it on purpose. LOC-02/03 show the current docs and CLI now point the other way.
   SRC: /home/user/micky-psych-tools/CLAUDE.md:40
[LOC-02] (confirmed|divergence|official-docs) Current official docs tell plugin authors NOT to put version in both plugin.json and the marketplace entry. They say plugin.json always wins. The repo rule forces the dual-write the docs warn against.
   IMPL: Keep version in plugin.json only, drop it from catalog entries, and retire bump.py's dual-write. The rationale in CLAUDE.md/README ('catalog version compared, silent no-update') is now inverted.
   SRC: https://code.claude.com/docs/en/plugin-marketplaces
[LOC-03] (confirmed|divergence|official-cli) The installed CLI already catches version drift. Test: a scratch copy with the entry set to 0.1.9 and plugin.json at 0.1.0. `claude plugin validate --strict .` warned and exited 1. `claude plugin tag` also validates that the two agree.
   IMPL: validate.py's parity check duplicates native `--strict`. README.md:57 ('The validator enforces things the CLI does not: ... version parity') is stale.
   SRC: claude plugin validate --strict . (Claude Code 2.1.280, scratch copy with drifted entry)
[LOC-04] (confirmed|convention-enforced|official-cli) Both validators pass today. validate.py prints 'all checks passed'. `claude plugin validate --strict` passes on the micky marketplace and on each of the 14 plugin dirs.
   IMPL: The rewrite starts from a mechanically clean baseline. Run `claude plugin validate --strict` alongside or instead of validate.py, and keep only the checks the CLI lacks (e.g. min description length).
   SRC: claude plugin validate --strict /home/user/micky-psych-tools/plugins/<each> (run 2026-09-23)
[LOC-05] (partially|convention-enforced|local-repo) validate.py hard-fails any skill description over 1024 chars and any under 200 chars. Both are house numbers: the 200 floor is a heuristic, and 1024 is stricter than the current runtime budget.
   CORRECTED: validate.py hard-fails a skill description over 1024 or under 200 chars. The 200 floor is a house heuristic. The 1024 cap matches the Agent Skills open-standard / Claude API limit ('Max 1024 characters'). It is stricter than Claude Code's own listing budget (1,536 chars for description + when_to_use combined), and Claude Code's validator does not enforce it.
   IMPL: Re-base the cap on the docs' 1,536 combined description+when_to_use limit (LOC-06), or keep 1024 as a deliberate style cap and label it that way.
   SRC: /home/user/micky-psych-tools/scripts/validate.py:109
   EVID: validate.py:109-110 hard-fails a description over 1024 chars or under 200 chars. The 200 floor is a house heuristic. 1024 is not purely a house number, though. The Agent Skills spec (agentskills.io/specification) says "Max 1024 characters", and the platform best-practices page says "Maximum 1,024 characters". Only Claude Code's own listing is looser (1,536 combined chars), and `claude plugin validate --strict` accepts descriptions over 1024 (learn-hub's 1,075–1,150-char descriptions pass).
[LOC-06] (confirmed|divergence|official-docs) Current docs cap the COMBINED description + when_to_use text at 1,536 chars in the skill listing. when_to_use is a separate frontmatter field that no skill in either repo uses. The repos' custom parsers (validate.py/route.py frontmatter()) ignore it.
   IMPL: Move the long trigger-phrase lists out of `description` into `when_to_use`, and teach validate.py/route.py to read the field or drop them for the CLI.
   SRC: https://code.claude.com/docs/en/skills
[LOC-07] (partially|inventory|local-repo) Description length is squeezed against the cap: 8 of 17 micky skills are at 1003-1022 chars (comprehensive-review 1020, concept-animation 1022, intent-lock 1020, decision-interview 1013, plan-critique 1012, firecrawl 1009, pubmed 1007, empty-vault 1003). MEMORY logs repeated 'retrimmed under the 1024 cap' edits.
   CORRECTED: 8 of 17 micky skill descriptions sit at 1003–1022 chars, within 25 chars of validate.py's 1024 cap. MEMORY records one explicit 'descriptions retrimmed under the 1024 cap' edit (the 2026-08-08 milestone).
   IMPL: The descriptions are overloaded: trigger lists plus boundary carve-outs plus pipeline wiring. A rewrite should split them (description = what + key use case; when_to_use = triggers; body = handoffs).
   SRC: python3 /home/user/micky-psych-tools/scripts/validate.py
   EVID: validate.py output confirms 8 of 17 descriptions between 1003 and 1022 chars: pubmed 1007, intent-lock 1020, empty-vault 1003, comprehensive-review 1020, firecrawl 1009, concept-animation 1022, decision-interview 1013, plan-critique 1012. MEMORY.md, however, has exactly one 'retrimmed under the 1024 cap' entry (line 220). No other trim or cap entries exist, so 'repeated' is not supported.
[LOC-08] (partially|convention-enforced|local-repo) route.py builds ROUTING.md from the 'Use when' clause of each description, truncated to a 200-char cue. That imposes wording rules on descriptions that exist only for the custom router: exclusions must be phrased 'NOT for:', and boundary words must sit in the first 200 chars. MEMORY records that Thai triggers and decision-interview's mid-task scoping were moved to survive this window.
   CORRECTED: route.py builds each ROUTING.md cue from the first 'Use when…' clause of a description. The clause ends at the first '. ' and is truncated to 200 chars. Descriptions must therefore avoid phrasing exclusions as '…use when…', and must put boundary words early in the Use-when clause, before its first sentence break. MEMORY records plan-critique's Thai triggers and decision-interview's mid-task scoping being moved to survive this window.
   IMPL: Custom-router constraints are shaping descriptions that Claude Code reads in full. A rewrite should decide whether ROUTING.md is still worth constraining authoring for.
   SRC: /home/user/micky-psych-tools/scripts/route.py:64
   EVID: route.py:64-66 contains the quote. use_when(desc, cap=200) takes the FIRST 'Use when/Use whenever/Use this skill when/Use the moment' clause and ends it at the first '. ' break. It truncates that clause to 200 chars, not the first 200 chars of the description. MEMORY:259 records moving plan-critique's Thai triggers 'inside route.py's 200-char cue window'. MEMORY:395 and commit 675a17b record decision-interview front-loading mid-task scoping for the same reason.
[LOC-09] (confirmed|convention-enforced|local-repo) CLAUDE.md orders the agent to consult the generated ROUTING.md (or /route) before acting on any request a plugin could serve. The generator itself admits the table only re-indexes the descriptions Claude already routes on natively.
   IMPL: ROUTING.md plus /route is a second routing layer on top of native skill discovery. Candidates: demote it to human documentation, or delete it and the 'route first' mandate.
   SRC: /home/user/micky-psych-tools/scripts/route.py:128
[LOC-10] (confirmed|convention-enforced|local-repo) ROUTING.md is currently fresh: regenerating it in a scratch copy gives a byte-identical file. Freshness depends only on the model following prose steps (plugin-creator/refine-plugin step 6, CLAUDE.md health check). There is no hook, no CI, and no .claude/ or .github/ directory in micky-psych-tools.
   IMPL: If the rewrite keeps validate/route, enforce them mechanically (a pre-commit hook or a project PreToolUse/Stop hook), not by prose.
   SRC: /home/user/micky-psych-tools/CLAUDE.md:61
[LOC-11] (confirmed|divergence|local-repo) validate.py requires every MCP server entry to have both `type` and `url`, so any stdio server (command/args) fails. Current plugin docs document stdio servers in plugin .mcp.json.
   IMPL: The custom validator is narrower than the platform. The plugin-creator authoring-rules.md 'MCP wiring' section repeats the same restriction.
   SRC: /home/user/micky-psych-tools/scripts/validate.py:93
[LOC-12] (confirmed|divergence|official-docs) Plugin .mcp.json officially supports stdio servers via command/args with ${CLAUDE_PLUGIN_ROOT} substitution.
   IMPL: Relax validate.py/authoring-rules to accept the command form, or defer MCP validation to `claude plugin validate`.
   SRC: https://code.claude.com/docs/en/plugins-reference
[LOC-13] (confirmed|inventory|local-repo) Identical PubMed + ClinicalTrials .mcp.json files (pubmed.mcp.claude.com, hcls.mcp.claude.com) ship in 3 micky plugins (pubmed-research-note, comprehensive-review, psych-paper-digest) and 3 learn-hub plugins (pubmed-research-note, comprehensive-review, pk-plasma-animation). micky recorded this as a deliberate choice. learn-hub polish-plan P1-3 flags it as an unverified possible conflict with the session connectors.
   IMPL: The rewrite should decide on one MCP owner, or verify empirically how the plugin servers interact with the account-level PubMed/Clinical_Trials connectors this session already carries.
   SRC: /home/user/micky-psych-tools/MEMORY.md:996
[LOC-14] (confirmed|convention-enforced|local-repo) Evals exist as skills/<skill>/evals/evals.json in skill-creator shape (skill_name + evals[]: id, name, prompt, expected_output, files, assertions) for 11 of 17 micky skills, 83 cases in total. Every case has an empty `assertions` array. validate.py only checks that the file parses as JSON.
   IMPL: The evals are prose expectations that nothing executes or grades. Migrating them to a runnable format (LOC-15) is new work, not a rename.
   SRC: /home/user/micky-psych-tools/scripts/validate.py:114
[LOC-15] (confirmed|divergence|official-cli) The installed CLI has a native plugin eval runner. It reads PLUGIN-level evals/<case>/prompt.md + graders/*.md (or case.yaml), adds a no-plugin baseline arm by default, and `claude plugin eval init --bare <name>` scaffolds prompt.md (max_turns, allowed_tools frontmatter) + graders/criteria.md (type: llm). Skill-level evals/evals.json is not in its search path.
   IMPL: Convert each skill's evals.json into plugin-level evals/<case>/prompt.md + graders. Trigger/negative-trigger cases can use `tool_used: Skill` graders under with/without ablation.
   SRC: claude plugin eval --help (Claude Code 2.1.280)
[LOC-16] (confirmed|inventory|local-repo) 6 of 17 micky skills have no evals at all: concept-animation, decision-interview, gridgeist, ml-concept-lab, plugin-creator, refine-plugin. That includes the meta-plugin that scaffolds evals.json for others.
   IMPL: Eval coverage has to be built, not just migrated, for about a third of the skills.
   SRC: /home/user/micky-psych-tools/plugins/*/skills/*/ (inventory: evals/evals.json presence)
[LOC-17] (partially|divergence|local-repo) All 12 micky commands are thin wrappers (12-25 lines) that just invoke the same-named skill with $ARGUMENTS handling. Each adds ~30-90 always-on tokens on top of its skill (claude plugin details).
   CORRECTED: 11 of 12 micky commands are thin wrappers (12–24 lines) that invoke their plugin's skill, often under a different name (e.g. /animate→concept-animation), with $ARGUMENTS handling. /route is a standalone procedure, not a skill wrapper. Each command adds ~30–90 always-on tokens.
   IMPL: Skills are already slash-invocable. Fold each wrapper's argument handling into the skill (argument-hint/arguments frontmatter) and delete commands/ unless a command needs different model-invocation behaviour.
   SRC: /home/user/micky-psych-tools/plugins/concept-animation/commands/animate.md:18
   EVID: There are 12 command files of 12-25 lines. The per-command always-on cost in `claude plugin details` is ~30-90 tokens. However, /route (route.md, 25 lines) is not a skill wrapper: it runs route.py and matches against ROUTING.md itself. Most commands are also not 'same-named': /animate→concept-animation, /resolve-decisions→decision-interview, /critique-plan→plan-critique, /explain-code→code-explainer, /visualize→ml-concept-lab, /digest→psych-paper-digest, /new-plugin→plugin-creator.
[LOC-18] (confirmed|divergence|official-docs) Current docs say custom commands were merged into skills, and the plugins reference says new plugins should use skills/.
   IMPL: The commands/ layout in CLAUDE.md, the plugin-creator templates (command.md) and the validate.py command checks all belong to the pre-merge model.
   SRC: https://code.claude.com/docs/en/skills
[LOC-19] (confirmed|inventory|official-cli) `claude plugin details` reports per-plugin cost. With all 14 micky plugins loaded the always-on total is ~6.6k tokens (e.g. vault-keeper ~773, plugin-creator ~721, intent-lock ~655). The largest on-invoke costs are intent-lock ~8.1k, pubmed-research-note ~6.4k and firecrawl ~6.2k. No plugin ships agents or hooks.
   IMPL: Use `claude plugin details` as the before/after token metric for the rewrite, especially for trimming long descriptions and removing command duplicates.
   SRC: claude --plugin-dir ./plugins/pubmed-research-note plugin details pubmed-research-note
[LOC-20] (confirmed|inventory|local-repo) No skill in either repo uses any frontmatter beyond name + description. That covers 17 micky skills and 20 learn-hub skills/plugin skills: no when_to_use, argument-hint, allowed-tools, disable-model-invocation, paths, model or effort.
   IMPL: Obvious rewrite levers: disable-model-invocation for destructive flows (empty-vault), argument-hint on skills that replace commands, and when_to_use for triggers.
   SRC: /home/user/micky-psych-tools/plugins/*/skills/*/SKILL.md (frontmatter audit via scripts/validate.py frontmatter())
[LOC-21] (confirmed|convention-enforced|local-repo) plugin-creator's authoring rules require third-person, action-first descriptions. The plugin's own skills break it: plugin-creator starts 'Scaffold…', refine-plugin starts 'Audit and refine…', and decision-interview starts with a noun phrase ('Mid-task decision gate').
   IMPL: The rewrite of plugin-creator must first fix its own descriptions to its stated rules (or the rules).
   SRC: /home/user/micky-psych-tools/plugins/plugin-creator/skills/plugin-creator/references/authoring-rules.md:33
[LOC-22] (partially|drift|local-repo) plugin-creator's description still says to hand-edit existing plugins and run bump.py. It predates its sibling refine-plugin skill, which now owns that job.
   CORRECTED: plugin-creator's description still tells users to hand-edit an existing plugin and run bump.py. That contradicts refine-plugin, which shipped alongside it in 0.1.0 and owns that job (audit → approved fixes → bump.py). The Not-for clause should point to /refine-plugin.
   IMPL: Stale carve-out. The Not-for should route to refine-plugin, and so should the body's Scope 'Don't' line (SKILL.md:20).
   SRC: /home/user/micky-psych-tools/plugins/plugin-creator/skills/plugin-creator/SKILL.md:3
   EVID: plugin-creator SKILL.md:3 contains the quote, and it conflicts with refine-plugin, which owns audit/refine/bump. The 'predates' part is wrong: plugin-creator CHANGELOG 0.1.0 says 'Initial lifecycle toolkit — two skills: plugin-creator … refine-plugin', so the two shipped together.
[LOC-23] (confirmed|drift|local-repo) The audit checklist requires README.md and CHANGELOG.md per plugin, but nothing enforces it. concept-animation, decision-interview, ml-concept-lab and gridgeist have no README. Those four plus code-explainer (5 of 14) have no CHANGELOG. gridgeist ships only a LICENSE (vendored MIT upstream).
   IMPL: Decide whether per-plugin README/CHANGELOG is a requirement (then check it mechanically) or drop it from the checklist.
   SRC: /home/user/micky-psych-tools/plugins/plugin-creator/skills/refine-plugin/references/audit-checklist.md:67
[LOC-24] (partially|drift|local-repo) CLAUDE.md points to domain docs that do not exist: no CONTEXT.md and no docs/adr/ at the repo root. Its Plugins section also lists 13 of the 14 plugins and omits gridgeist.
   CORRECTED: CLAUDE.md names CONTEXT.md and docs/adr/ as domain docs. Neither exists yet, which docs/agents/domain.md explicitly allows (they are created lazily). The CLAUDE.md Plugins section lists 13 of the 14 plugins and omits gridgeist.
   IMPL: The CLAUDE.md per-plugin catalog duplicates the marketplace descriptions and drifts. Generate it, or cut it to a pointer.
   SRC: /home/user/micky-psych-tools/CLAUDE.md:200
   EVID: CONTEXT.md and docs/adr/ are both absent, and CLAUDE.md's Plugins section lists 13 plugins without gridgeist (0 mentions). But docs/agents/domain.md, which CLAUDE.md points to, says: 'If any of these files don't exist, proceed silently. Don't flag their absence… The /domain-modeling skill… creates them lazily'. Their absence is the documented design, not a broken pointer.
[LOC-25] (confirmed|convention-enforced|local-repo) CLAUDE.md tells agents to read MEMORY.md first. MEMORY.md is 108 KB / 1153 lines (~27k tokens), and about 30 of its 51 milestones log vault-content filings or empty-vault runs, not plugin state.
   IMPL: Separate plugin living state (versions, open threads) from the content log before the rewrite adds to it. MEMORY's versions table duplicates plugin.json and could be generated.
   SRC: /home/user/micky-psych-tools/CLAUDE.md:7
[LOC-26] (refuted|open-item|local-repo) MEMORY's only open thread is stale. The improve-all-plugins branch no longer exists; `git branch -a` shows only master and the current branch.
   CORRECTED: MEMORY's only open thread (delete improve-all-plugins once merged) is still actionable, not stale. The branch still exists on origin at 57e470e, which MEMORY records as the commit merged to master, so it can now be deleted.
   IMPL: Close the thread. Status: DONE or moot. Confidence is medium because this checkout is shallow and may not show every local branch on the owner's machine.
   SRC: /home/user/micky-psych-tools/MEMORY.md:1148
   EVID: The branch still exists. `git ls-remote --heads origin` lists refs/heads/improve-all-plugins at 57e470eca5c2…, and the GitHub API shows its tip is 57e470e 'chore: release pubmed 1.2.0…', the same commit MEMORY:1134 records as 'Merged to master (57e470e)'. It is missing from `git branch -a` only because this is a shallow single-branch clone. The open thread is closable, since the branch tip equals the recorded merge commit, but the branch has not been deleted.
[LOC-27] (confirmed|open-item|local-repo) The 2026-07-10 improve-all-plugins plan still shows every step as '- [ ]', but all 11 tasks are DONE in the current files. validate.py has per-skill evals and command/agent description checks (T1). route.py has use_when() and route.md always regenerates (T2). vault-layout.md exists (T3). atomic-note-template defers placement to vault-keeper (T4). misread-capture uses the canonical ledger path (T5). intent-lock author is 'Micky' and failure-conditions.md exists (T6/T7). tool-catalog says Open Library is web-fallback only (T8). templates/evals.json exists and category is read at runtime (T9). CLAUDE.md layout lists route.py/ROUTING.md/vault (T10).
   IMPL: No carry-over work from this plan. Tick or archive it so the next planner doesn't re-audit it; the rewrite plan should supersede it.
   SRC: /home/user/micky-psych-tools/MEMORY.md:1128
[LOC-28] (confirmed|open-item|local-repo) learn-hub plugin-polish-plan.md is still headed 'proposed, not started' but is partly done. P2-1 (fork provenance banners) and P2-3 (research-notes/ as digest input) are DONE. P0-1 is OPEN: neither the pubmed-research-note nor the comprehensive-review SKILL.md contains the voice rule ('According to PubMed' / source-narration ban).
   IMPL: P0-1 is a content-corrupting open item. The rewrite of both report skills must add the generator-side voice rule and the citation mapping (P1-5, also OPEN — no 'parenthetical'/author-year rule in either skill).
   SRC: /home/user/learn-hub/docs/plugin-polish-plan.md:3
[LOC-29] (confirmed|open-item|local-repo) Polish-plan P0-3 is OPEN. learn-hub's forked evals still expect the vault-keeper handoff (2 hits in pubmed-research-note evals.json, 1 in comprehensive-review), and atomic-note-template.md still has 14 vault-keeper mentions. Nothing in learn-hub runs these evals.
   IMPL: Forked evals describe a pipeline that does not exist in learn-hub. Rewrite them in native eval format or delete them.
   SRC: /home/user/learn-hub/docs/plugin-polish-plan.md:95
[LOC-30] (confirmed|open-item|local-repo) Polish-plan P1-1 (vitest plugin-manifest validator) is OPEN: no scripts/lib/plugin-manifest.mjs exists. P1-2 (version question) is OPEN: all 8 learn-hub catalog entries are version-less and no decision is recorded. P1-4 is OPEN: learn-hub pubmed SKILL.md:153 still says to write into the working directory or `report_dir`. P2-2 (router) is OPEN: learn-hub has no ROUTING.md.
   IMPL: A shared validation story for both repos (native `claude plugin validate --strict` plus a thin custom layer) would close P1-1. Version-less plugin.json-only matches the docs (LOC-02).
   SRC: /home/user/learn-hub/docs/plugin-polish-plan.md:128
[LOC-31] (confirmed|open-item|local-repo) Polish-plan P0-2 (three skills claim 'atomize') is only PARTIAL. vault-atomizer's and digest-report's Not-for clauses name their siblings. atomize-book's Not-for mentions neither vault-atomizer nor digest-report, and pubmed-research-note still uses the bare word 'atomize' as its digest gate. P1-6 (depth floor vs split children) is PARTIAL: vault-atomizer warns and digest-report records its side, with no global decision.
   IMPL: The rewrite needs a trigger-collision test (native eval negative cases) across atomize-book / vault-atomizer / digest-report / pubmed-research-note.
   SRC: /home/user/learn-hub/docs/plugin-polish-plan.md:85
[LOC-32] (confirmed|open-item|local-repo) digest-report-plugin-plan.md is fully DONE (A-1..A-3 packaged at plugins/digest-report/ with the old .claude/skills copy deleted; B-1..B-7 content fixes present in SKILL.md: provenance SQL, background sync:apply, research-notes first, voice:qc, chapter-reference rule, stadium/colon rules, depth stance). Its header still says 'proposed, not started', and the plugin has no CHANGELOG.md of its own; the change is logged only in the repo CHANGELOG.
   IMPL: No carry-over work. Update the plan's status line. digest-report is the pipeline hub both repos depend on, so it belongs in the rewrite's cross-repo contract.
   SRC: /home/user/learn-hub/CHANGELOG.md:5443
[LOC-33] (confirmed|sync|local-repo) The learn-hub copies of intent-lock, pubmed-research-note and comprehensive-review were made by manual copy on 2026-08-11 from micky-psych-tools@f91b3b0, with a README banner. f91b3b0 is still micky's HEAD, so upstream has not moved since. The diffs are only the filing-path rewire (plugin.json description, README, SKILL.md 'Where output goes'/Close/Failure conditions, atomic-note-template, the CR command). There is no sync script, subtree or submodule.
   IMPL: Any rewrite in micky-psych-tools will silently diverge from the three learn-hub forks. The plan needs an explicit sync model: single source with a per-repo filing adapter, or a documented re-fork procedure.
   SRC: /home/user/learn-hub/plugins/intent-lock/README.md:3
[LOC-34] (confirmed|sync|local-repo) A fix has already been lost across the repo boundary. learn-hub's project skill .claude/skills/concept-animation (same name, a separate diverged copy with its own description and examples/) fixed the stage-collapse layout rule after measuring a collapse on 75/75 phone rows. micky's concept-animation plugin still prescribes the broken rule.
   IMPL: The rewrite must reconcile the two concept-animation copies (learn-hub:48 uses `min-height:100dvh` plus `flex:0 0 auto`) and give the same-named skill one owner.
   SRC: /home/user/micky-psych-tools/plugins/concept-animation/skills/concept-animation/references/animation-grammar.md:46
[LOC-35] (confirmed|sync|local-repo) micky's empty-vault finds the learn-hub checkout by looking for `.claude/skills/digest-report/`. That directory was deleted on 2026-08-11 when digest-report moved to plugins/digest-report/. empty-vault also still says assets cannot become Learn notes, although learn-hub's ingest-infographic/ingest-animation now accept assets from empty-vault.
   IMPL: The cross-repo handoff contract is broken on the micky side. Root detection should key on something stable (e.g. `.claude-plugin/marketplace.json` name `learn-hub-local` or `scripts/sync-vault.mjs`), and asset routing should name ingest-infographic/ingest-animation.
   SRC: /home/user/micky-psych-tools/plugins/vault-keeper/skills/empty-vault/SKILL.md:36
[LOC-36] (confirmed|divergence|local-repo) vault-keeper resolves the vault root by walking up to `.claude-plugin/marketplace.json`, or via `${CLAUDE_PLUGIN_ROOT}/../../vault`. That only holds when the plugin loads in place from this repo. learn-hub also has `.claude-plugin/marketplace.json` and `vault/` (the app's 817-entry corpus) at its root, so the walk-up from a learn-hub cwd lands on the wrong vault.
   IMPL: Replace the heuristic with an explicit, verified root (a config key, a marker file inside vault/, or userConfig). Medium confidence: the collision assumes vault-keeper runs with a learn-hub cwd.
   SRC: /home/user/micky-psych-tools/plugins/vault-keeper/skills/vault-keeper/references/vault-layout.md:7
[LOC-37] (confirmed|divergence|local-repo) misread-capture appends user state (the misread ledger) into the plugin's own directory via ${CLAUDE_PLUGIN_ROOT}. The learn-hub fork carries a second, independent misreads.md.
   IMPL: State written there is lost on update for a cached install (LOC-38), and the two repos now have split ledgers. Move it to ${CLAUDE_PLUGIN_DATA} or a user-scope path.
   SRC: /home/user/micky-psych-tools/plugins/intent-lock/skills/misread-capture/SKILL.md:39
[LOC-38] (confirmed|divergence|official-docs) Official docs say ${CLAUDE_PLUGIN_ROOT} of a copied (cached) plugin changes on update and must not hold state. ${CLAUDE_PLUGIN_DATA} is the persistent per-plugin directory.
   IMPL: Applies to misread-capture's ledger and to anything assuming the plugin sits inside the repo (vault-keeper's ../../vault).
   SRC: https://code.claude.com/docs/en/plugins-reference
[LOC-39] (confirmed|divergence|official-docs) Official docs: a plugin loaded in place from a local-directory marketplace picks up edits at the next session or /reload-plugins, with no version bump needed. The README recommends exactly that local registration and already says the bump can be skipped while iterating.
   IMPL: For the owner's local install, versions are informational. The two-file version ceremony protects only git-sourced installs, which weakens the case for bump.py's dual-write.
   SRC: https://code.claude.com/docs/en/plugins-reference
[LOC-40] (confirmed|divergence|local-repo) The micky README claims a GitHub-sourced marketplace must be public. MEMORY records the origin repo as private. Current docs support private repos through git credential helpers.
   IMPL: Fix the README. Docs: 'Claude Code supports installing plugins from private repositories' (code.claude.com/docs/en/plugin-marketplaces).
   SRC: /home/user/micky-psych-tools/README.md:15
[LOC-41] (confirmed|inventory|official-cli) learn-hub's marketplace (`learn-hub-local`, 8 plugins) follows none of micky's conventions: entries have no version, category or keywords, and there is no top-level description. It fails `claude plugin validate --strict` on that last point. .claude/settings.json only wires hooks, with no enabledPlugins or extraKnownMarketplaces.
   IMPL: The two repos need one shared manifest convention. learn-hub also needs settings-level marketplace/plugin enablement if its plugins should load in cloud sessions.
   SRC: claude plugin validate --strict /home/user/learn-hub (Claude Code 2.1.280)
[LOC-42] (confirmed|inventory|official-cli) In this cloud session no plugin from either marketplace is installed. Only learn-hub's .claude/skills project skills (atomize-book, concept-animation, ingest-*, sync-vault…) are live, so pubmed-research-note, intent-lock, digest-report etc. are not callable here.
   IMPL: Cross-plugin handoffs ('runs intent-lock first', 'hands to vault-keeper/digest-report') silently fail wherever the partner plugin is not installed. The rewrite should declare dependencies or degrade explicitly.
   SRC: claude plugin list (run 2026-09-23 in this session)
[LOC-43] (confirmed|inventory|official-docs) learn-hub's project skills break both the docs' size guidance and micky's cap. atomize-book/SKILL.md is 1,274 lines and ingest-article 378. The atomize-book (1075), ingest-article (1137) and ingest-slides (1150) descriptions exceed micky's 1024 cap but stay within the docs' 1,536 combined limit, and `claude plugin validate --strict .claude/skills` passes.
   IMPL: atomize-book (the most-churned skill: 6 of the last visible commits, through 2026-09-20) is the priority for progressive-disclosure restructuring. The two repos also disagree on the description cap.
   SRC: https://code.claude.com/docs/en/skills
[LOC-44] (confirmed|convention-enforced|local-repo) learn-hub's hooks follow a deliberate pattern: warn-only PreToolUse(Bash) gates that exit 0 on every path, with the matching logic in a unit-tested node module rather than the shell. Both gates run on every Bash call (matcher "Bash"). The SessionStart hook runs only in remote sessions.
   IMPL: A tested house model for any hook the rewrite adds (e.g. to enforce validate/route in micky). The cost is two node spawns per Bash call.
   SRC: /home/user/learn-hub/.claude/hooks/pre-sync-gate.sh:5
[LOC-45] (partially|tooling|local-repo) bump.py rewrites plugin.json with json.dump's defaults: non-ASCII is escaped and there is no trailing newline. micky's pubmed plugin.json now stores the em dash as — and lacks a final newline, while the learn-hub copy has a literal em dash. The two files differ byte-wise from formatting alone.
   CORRECTED: bump.py rewrites plugin.json with json.dump defaults: non-ASCII escaped (—), no trailing newline. The marketplace.json write uses ensure_ascii=False, so the two files it writes are formatted inconsistently. micky's pubmed plugin.json shows this, while learn-hub's fork has a literal em dash and a trailing newline. That formatting drift adds to the fork's real description change.
   IMPL: Minor, but it adds noise to fork diffs (LOC-33). If bump.py survives, pass ensure_ascii=False and write a trailing newline.
   SRC: /home/user/micky-psych-tools/scripts/bump.py:53
   EVID: bump.py:53 writes plugin.json with json.dump(indent=2) and default ensure_ascii=True, with no trailing newline (line 54 uses ensure_ascii=False for the catalog). micky's pubmed plugin.json has '—' and no final newline, while learn-hub's has a literal '—' and a newline. But the two files also differ in content: the description was rewired ('files it to the vault' vs 'saves it to research-notes/…'). The difference is not 'from formatting alone'.
[LOC-46] (confirmed|churn|local-repo) Plugin churn in micky-psych-tools stopped on 2026-08-08: 22 visible commits touching plugins/ between 2026-07-12 and 2026-08-08 (shallow clone), pubmed-research-note the most-edited with 5. learn-hub's skill churn continues: atomize-book 6 commits, concept-animation 4, ingest-article 3, the latest on 2026-09-20.
   IMPL: Rewrite effort should follow live churn: learn-hub's book/article/animation skills are actively evolving, while the micky plugins are stable. Both clones are shallow, so older history is not visible.
   SRC: git -C /home/user/micky-psych-tools log --oneline -40 -- plugins .claude/skills
--- missed_practices
MP-LOC-1: The description 1024-char cap is the cross-surface Agent Skills standard (and Claude API/claude.ai limit), not just a micky house rule. Skills meant to be portable, including learn-hub's 1,075–1,150-char ones, must stay at or under 1024 even though Claude Code tolerates up to 1,536 combined.
   SRC: https://agentskills.io/specification
   Q: `description` | Yes | Max 1024 characters. Non-empty. Describes what the skill does and when to use it.
MP-LOC-2: placeholder
   SRC: 
   Q: 
--- open_questions
OQ: How is micky-psych-tools actually installed on the owner's machine: a local-directory marketplace (loads in place, so version bumps and ${CLAUDE_PLUGIN_ROOT} writes behave like repo edits) or the private GitHub remote (cached copy, where misreads.md writes and the ../../vault resolution break)? MEMORY says 'user scope' but does not say which source.
OQ: Are learn-hub's 8 plugins installed anywhere? No settings.json enables them, and `claude plugin list` shows none in this cloud session. If they are not installed, the forked report skills and digest-report are unreachable as plugins.
OQ: What is the intended sync direction for the three forked plugins: upstream-first (micky is the source and learn-hub gets a filing adapter), or permanent forks? No mechanism exists today.
OQ: Which repo owns concept-animation? Two same-named, diverged copies exist (micky plugin vs learn-hub project skill with the layout fix).
OQ: Should ROUTING.md plus the 'route first' CLAUDE.md mandate survive a rewrite, given native skill discovery reads the same descriptions and the router constrains description wording?
OQ: Polish-plan P1-3 is unverified: do the plugin-bundled PubMed/ClinicalTrials MCP servers shadow or duplicate the account-level PubMed/Clinical_Trials connectors present in this session?
OQ: Both clones are shallow (micky 135 commits, learn-hub 373), so churn and fork history before about 2026-07-12 (micky) and 2026-08-20 (learn-hub plugins) are not visible.